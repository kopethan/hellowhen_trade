diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 244e4b1..610c3d4 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -3254,7 +3254,180 @@ html[data-theme="dark"] .onboarding-guide-primary {
   color: #050506;
 }
 
-@media (max-height: 699px) {
+@media (min-width: 860px) {
+  .utility-shell--full {
+    min-height: 100dvh;
+    padding: 24px;
+    background:
+      radial-gradient(circle at 12% 12%, color-mix(in srgb, var(--app-surface) 62%, transparent), transparent 34rem),
+      radial-gradient(circle at 88% 8%, color-mix(in srgb, var(--app-surface-soft) 58%, transparent), transparent 32rem),
+      var(--app-bg-muted);
+  }
+
+  .onboarding-guide-shell {
+    width: min(calc(100vw - 48px), 1120px);
+    min-height: min(820px, calc(100dvh - 48px));
+    max-height: calc(100dvh - 48px);
+    grid-template-rows: auto auto minmax(0, 1fr) auto;
+    border: 1px solid color-mix(in srgb, var(--app-border) 82%, transparent);
+    border-radius: 36px;
+    box-shadow: 0 30px 100px rgba(15, 23, 42, 0.18);
+  }
+
+  html[data-theme="dark"] .onboarding-guide-shell {
+    box-shadow: 0 30px 110px rgba(0, 0, 0, 0.42);
+  }
+
+  .onboarding-guide-topbar {
+    min-height: 84px;
+    padding: 24px 34px 8px;
+  }
+
+  .onboarding-guide-brand {
+    font-size: 22px;
+  }
+
+  .onboarding-guide-skip {
+    min-width: 76px;
+    border: 1px solid color-mix(in srgb, var(--app-border) 82%, transparent);
+    border-radius: 999px;
+    background: color-mix(in srgb, var(--app-surface) 70%, transparent);
+    padding: 0 14px;
+  }
+
+  .onboarding-guide-preference-bar {
+    justify-content: flex-end;
+    padding: 0 34px 6px;
+  }
+
+  .onboarding-guide-preference-pill {
+    min-height: 38px;
+    padding: 0 16px;
+    background: color-mix(in srgb, var(--app-surface) 78%, transparent);
+    box-shadow: 0 12px 34px rgba(15, 23, 42, 0.08);
+  }
+
+  .onboarding-guide-content {
+    grid-template-columns: minmax(360px, 560px) minmax(320px, 440px);
+    grid-template-areas:
+      "figure step"
+      "figure title"
+      "figure body"
+      "figure dots"
+      "figure muted";
+    column-gap: clamp(36px, 5vw, 76px);
+    align-content: center;
+    align-items: center;
+    justify-content: center;
+    justify-items: stretch;
+    overflow-y: auto;
+    padding: 24px 56px 26px;
+  }
+
+  .onboarding-guide-figure {
+    grid-area: figure;
+    width: min(100%, 560px, 58dvh);
+    gap: 16px;
+    justify-self: center;
+    align-content: center;
+  }
+
+  .onboarding-guide-figure figcaption {
+    font-size: 14px;
+  }
+
+  .onboarding-guide-step {
+    grid-area: step;
+    justify-self: start;
+    margin-top: 0;
+    border: 1px solid color-mix(in srgb, var(--app-border) 78%, transparent);
+    border-radius: 999px;
+    background: color-mix(in srgb, var(--app-surface) 74%, transparent);
+    padding: 8px 12px;
+    font-size: 13px;
+  }
+
+  .onboarding-guide-shell h1 {
+    grid-area: title;
+    max-width: 460px;
+    margin-top: 18px;
+    font-size: clamp(42px, 4.3vw, 58px);
+    text-align: left;
+  }
+
+  .onboarding-guide-body {
+    grid-area: body;
+    max-width: 450px;
+    margin-top: 18px;
+    font-size: 18px;
+    line-height: 1.52;
+    text-align: left;
+  }
+
+  .onboarding-guide-dots {
+    grid-area: dots;
+    justify-content: flex-start;
+    margin-top: 24px;
+  }
+
+  .onboarding-guide-step--muted {
+    grid-area: muted;
+    justify-self: start;
+    margin-top: 4px;
+    border: 0;
+    background: transparent;
+    padding: 0;
+  }
+
+  .onboarding-guide-actions {
+    justify-content: flex-end;
+    gap: 14px;
+    padding: 18px 34px 28px;
+  }
+
+  .onboarding-guide-actions button {
+    flex: 0 0 auto;
+    min-width: 164px;
+  }
+
+  .onboarding-guide-preferences-modal {
+    align-items: center;
+    padding: 24px;
+  }
+
+  .onboarding-guide-preferences-sheet {
+    width: min(100%, 520px);
+    border-radius: 32px;
+    padding: 24px;
+  }
+}
+
+@media (min-width: 860px) and (max-height: 720px) {
+  .utility-shell--full { padding: 12px; }
+  .onboarding-guide-shell {
+    min-height: calc(100dvh - 24px);
+    max-height: calc(100dvh - 24px);
+    border-radius: 28px;
+  }
+  .onboarding-guide-topbar { min-height: 66px; padding-top: 16px; }
+  .onboarding-guide-content { padding-top: 12px; padding-bottom: 12px; }
+  .onboarding-guide-figure { width: min(100%, 440px, 52dvh); }
+  .onboarding-guide-shell h1 { font-size: clamp(34px, 4vw, 48px); margin-top: 10px; }
+  .onboarding-guide-body { margin-top: 10px; font-size: 16px; line-height: 1.42; }
+  .onboarding-guide-dots { margin-top: 14px; }
+  .onboarding-guide-actions { padding-top: 12px; padding-bottom: 16px; }
+  .onboarding-guide-actions button { min-height: 46px; }
+}
+
+@media (max-width: 859px) {
+  .onboarding-guide-shell {
+    width: min(100vw, var(--app-shell-width));
+    min-height: 100dvh;
+    border-radius: 0;
+  }
+}
+
+@media (max-height: 699px) and (max-width: 859px) {
   .onboarding-guide-topbar { min-height: calc(56px + env(safe-area-inset-top)); }
   .onboarding-guide-preference-pill { min-height: 32px; }
   .onboarding-guide-figure { width: min(calc(100vw - 40px), 250px, 34dvh); }
