diff --git a/apps/api/src/modules/admin/admin.routes.ts b/apps/api/src/modules/admin/admin.routes.ts
index db1c17e..65805d2 100644
--- a/apps/api/src/modules/admin/admin.routes.ts
+++ b/apps/api/src/modules/admin/admin.routes.ts
@@ -753,6 +753,10 @@ async function findRelatedTradeIdForReport(targetType: string, targetId: string)
     const message = await prisma.proposalMessage.findUnique({ where: { id: targetId }, select: { proposal: { select: { tradeId: true } } } });
     return message?.proposal?.tradeId ?? null;
   }
+  if (targetType === 'public_message') {
+    const message = await prisma.tradePublicMessage.findUnique({ where: { id: targetId }, select: { tradeId: true } });
+    return message?.tradeId ?? null;
+  }
   if (targetType === 'media') {
     const media = await prisma.mediaAsset.findUnique({ where: { id: targetId }, select: { entityType: true, entityId: true } });
     return media?.entityType === 'trade' ? media.entityId : null;
@@ -763,7 +767,7 @@ async function findRelatedTradeIdForReport(targetType: string, targetId: string)
 function reportSupportCategory(reason: string, targetType: string) {
   if (reason === 'illegal_unsafe' || reason === 'harassment' || reason === 'scam') return 'safety_concern';
   if (targetType === 'media' || reason === 'inappropriate_image') return 'media_issue';
-  if (targetType === 'trade' || targetType === 'proposal' || targetType === 'message') return 'trade_issue';
+  if (targetType === 'trade' || targetType === 'proposal' || targetType === 'message' || targetType === 'public_message') return 'trade_issue';
   if (targetType === 'user' || targetType === 'profile' || reason === 'fake_profile') return 'account_issue';
   return 'general_feedback';
 }
diff --git a/apps/mobile/src/features/trade/TradePublicDiscussionScreen.tsx b/apps/mobile/src/features/trade/TradePublicDiscussionScreen.tsx
index f670542..ec5247d 100644
--- a/apps/mobile/src/features/trade/TradePublicDiscussionScreen.tsx
+++ b/apps/mobile/src/features/trade/TradePublicDiscussionScreen.tsx
@@ -153,13 +153,19 @@ export function TradePublicDiscussionScreen({ route, navigation }: Props) {
 
   function openOwnActions(message: TradePublicMessageItem) {
     if (message.status === 'deleted') return;
-    setActionTarget((current) => current?.messageId === message.id && current.mode === 'edit' ? null : { messageId: message.id, mode: 'edit' });
-    setEditDraft(message.body);
+    Alert.alert(t('trade.publicDiscussion.messageActions'), t('trade.publicDiscussion.messageMenuHintOwn'), [
+      { text: t('trade.publicDiscussion.editMessage'), onPress: () => { setActionTarget({ messageId: message.id, mode: 'edit' }); setEditDraft(message.body); } },
+      { text: t('trade.publicDiscussion.deleteMessage'), style: 'destructive', onPress: () => confirmDelete(message.id) },
+      { text: t('common.actions.cancel'), style: 'cancel' },
+    ]);
   }
 
   function openReport(message: TradePublicMessageItem) {
     if (message.status === 'deleted') return;
-    setActionTarget((current) => current?.messageId === message.id && current.mode === 'report' ? null : { messageId: message.id, mode: 'report' });
+    Alert.alert(t('trade.publicDiscussion.messageActions'), t('trade.publicDiscussion.messageMenuHintOther'), [
+      { text: t('trade.publicDiscussion.reportMessage'), style: 'destructive', onPress: () => setActionTarget({ messageId: message.id, mode: 'report' }) },
+      { text: t('common.actions.cancel'), style: 'cancel' },
+    ]);
   }
 
   const canSend = draft.trim().length > 0 && !sending;
@@ -176,6 +182,7 @@ export function TradePublicDiscussionScreen({ route, navigation }: Props) {
           <View style={[styles.introCard, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
             <SemanticBadge label={t('trade.publicDiscussion.heading')} tone="trade" size="sm" />
             <AppText style={[styles.introBody, { color: theme.color.muted }]}>{t('trade.publicDiscussion.body')}</AppText>
+            <AppText style={[styles.moderationHint, { color: theme.color.muted }]}>{t('trade.publicDiscussion.moderationHint')}</AppText>
           </View>
           {error ? <InfoNotice tone="danger" title={t('trade.detail.tradeError')} body={error} /> : null}
           {notice ? <InfoNotice tone="info" title={t('trade.publicDiscussion.heading')} body={notice} /> : null}
@@ -198,7 +205,6 @@ export function TradePublicDiscussionScreen({ route, navigation }: Props) {
               onCancelAction={() => { setActionTarget(null); setEditDraft(''); }}
               onOwnActions={() => openOwnActions(row.message)}
               onReport={() => openReport(row.message)}
-              onDelete={() => confirmDelete(row.message.id)}
             />
           ))}
         </ScrollView>
@@ -231,7 +237,7 @@ function EmptyDiscussion({ theme, t }: { theme: ThemeTokens; t: TFunction }) {
   );
 }
 
-function PublicMessageRow({ message, currentUserId, actionTarget, editDraft, sending, theme, t, language, onChangeEdit, onSaveEdit, onCancelAction, onOwnActions, onReport, onDelete }: { message: TradePublicMessageItem; currentUserId?: string; actionTarget: MessageActionTarget; editDraft: string; sending: boolean; theme: ThemeTokens; t: TFunction; language: SupportedLanguage; onChangeEdit: (value: string) => void; onSaveEdit: () => void; onCancelAction: () => void; onOwnActions: () => void; onReport: () => void; onDelete: () => void }) {
+function PublicMessageRow({ message, currentUserId, actionTarget, editDraft, sending, theme, t, language, onChangeEdit, onSaveEdit, onCancelAction, onOwnActions, onReport }: { message: TradePublicMessageItem; currentUserId?: string; actionTarget: MessageActionTarget; editDraft: string; sending: boolean; theme: ThemeTokens; t: TFunction; language: SupportedLanguage; onChangeEdit: (value: string) => void; onSaveEdit: () => void; onCancelAction: () => void; onOwnActions: () => void; onReport: () => void }) {
   const isOwn = message.authorId === currentUserId;
   const deleted = message.status === 'deleted' || Boolean(message.deletedAt);
   const actionMode = actionTarget?.mode ?? null;
@@ -251,7 +257,6 @@ function PublicMessageRow({ message, currentUserId, actionTarget, editDraft, sen
           <View style={styles.actionRow}>
             <SmallAction label={t('common.actions.save')} disabled={sending || editDraft.trim().length < 1} tone="primary" theme={theme} onPress={onSaveEdit} />
             <SmallAction label={t('common.actions.cancel')} disabled={sending} tone="ghost" theme={theme} onPress={onCancelAction} />
-            <SmallAction label={t('common.actions.remove')} disabled={sending} tone="danger" theme={theme} onPress={onDelete} />
           </View>
         </View>
       ) : (
@@ -277,6 +282,7 @@ const styles = StyleSheet.create({
   content: { paddingBottom: 18, gap: 12 },
   introCard: { borderRadius: 24, borderWidth: 1, padding: 16, gap: 9 },
   introBody: { lineHeight: 20, fontWeight: '700' },
+  moderationHint: { fontSize: 12, lineHeight: 18, fontWeight: '800' },
   emptyBox: { borderRadius: 22, borderWidth: 1, borderStyle: 'dashed', padding: 18, gap: 8 },
   emptyTitle: { fontSize: 20, lineHeight: 25, fontWeight: '900', letterSpacing: -0.3 },
   emptyBody: { lineHeight: 20, fontWeight: '700' },
diff --git a/apps/web/src/app/admin/reports/page.tsx b/apps/web/src/app/admin/reports/page.tsx
index 5c1232d..2a0e2fd 100644
--- a/apps/web/src/app/admin/reports/page.tsx
+++ b/apps/web/src/app/admin/reports/page.tsx
@@ -15,11 +15,11 @@ type ReportAction = AdminReportActionRequest['action'];
 type LastAction = { tone: NoticeTone; title: string; body: string; details?: string[] };
 
 const statusFilters: StatusFilter[] = ['pending', 'reviewing', 'resolved', 'dismissed', 'all'];
-const targetFilters: TargetFilter[] = ['all', 'user', 'profile', 'trade', 'need', 'offer', 'proposal', 'message', 'media', 'plan', 'plan_place'];
+const targetFilters: TargetFilter[] = ['all', 'user', 'profile', 'trade', 'need', 'offer', 'proposal', 'message', 'public_message', 'media', 'plan', 'plan_place'];
 const reasonFilters: ReasonFilter[] = ['all', 'spam', 'scam', 'harassment', 'illegal_unsafe', 'fake_profile', 'inappropriate_image', 'other'];
 const bulkReportActions: ReportAction[] = ['mark_reviewing', 'resolve', 'dismiss', 'reopen'];
 const noteRequiredReportActions: ReportAction[] = ['reopen', 'hide_target', 'restore_target', 'suspend_target_owner', 'unsuspend_target_owner', 'dismiss', 'resolve', 'escalate_to_support'];
-const visibilityTargetTypes = new Set(['trade', 'need', 'offer', 'media', 'plan', 'plan_place']);
+const visibilityTargetTypes = new Set(['trade', 'need', 'offer', 'public_message', 'media', 'plan', 'plan_place']);
 const adminNoteMinLength = 3;
 
 function labelize(value?: string | null) {
@@ -62,6 +62,7 @@ function hideTargetTitle(report: ReportDto) {
   if (report.targetType === 'need') return 'Need closed';
   if (report.targetType === 'offer') return 'Offer closed';
   if (report.targetType === 'media') return 'Media removed';
+  if (report.targetType === 'public_message') return 'Public message hidden';
   if (report.targetType === 'plan') return 'Plan hidden';
   if (report.targetType === 'plan_place') return 'Plan hidden after place report';
   return 'Target hidden';
@@ -72,6 +73,7 @@ function restoreTargetTitle(report: ReportDto) {
   if (report.targetType === 'need') return 'Need restored';
   if (report.targetType === 'offer') return 'Offer restored';
   if (report.targetType === 'media') return 'Media restored';
+  if (report.targetType === 'public_message') return 'Public message restored';
   if (report.targetType === 'plan') return 'Plan restored';
   if (report.targetType === 'plan_place') return 'Plan restored after place review';
   return 'Target restored';
@@ -96,6 +98,7 @@ function actionResultDetails(action: ReportAction, report: ReportDto, supportTic
     else if (report.targetType === 'need') details.push('This need is now closed and no longer active inventory.');
     else if (report.targetType === 'offer') details.push('This offer is now closed and no longer active inventory.');
     else if (report.targetType === 'media') details.push('This media is marked removed and should no longer be used publicly.');
+    else if (report.targetType === 'public_message') details.push('This public discussion message is hidden from the discussion without deleting audit history.');
     else if (report.targetType === 'plan') details.push('This hidden plan remains reversible and keeps audit history.');
     else if (report.targetType === 'plan_place') details.push('The parent plan was hidden because one of its places was reported.');
     else details.push('The target was removed from public surfaces without deleting audit history.');
@@ -105,6 +108,7 @@ function actionResultDetails(action: ReportAction, report: ReportDto, supportTic
     else if (report.targetType === 'need') details.push('This need is active again.');
     else if (report.targetType === 'offer') details.push('This offer is active again.');
     else if (report.targetType === 'media') details.push('This media is active again.');
+    else if (report.targetType === 'public_message') details.push('This public discussion message can appear in the discussion again.');
     else details.push('The target was restored without deleting previous audit history.');
   }
   if (action === 'suspend_target_owner') details.push('The owner account is restricted and active sessions were revoked.');
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 87da027..cd91f3c 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -610,6 +610,8 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-thread-action-card__cta { display: inline-flex; align-items: center; min-height: 32px; border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text-muted); padding: 0 10px; font-size: 11px; font-weight: 950; letter-spacing: .08em; text-transform: uppercase; }
 .trade-thread-page-hero > .button { justify-self: start; }
 .public-discussion-section { padding-bottom: calc(84px + env(safe-area-inset-bottom)); }
+.public-discussion-toolbar { display: flex; flex-wrap: wrap; align-items: center; gap: 8px 10px; margin-bottom: 8px; }
+.public-discussion-toolbar__hint { color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.4; }
 .public-message-list { display: grid; gap: 0; }
 .public-message-group { display: grid; gap: 8px; }
 .public-message-date { justify-self: center; margin: 10px 0; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text-muted); padding: 5px 10px; font-size: 11px; font-weight: 950; letter-spacing: .08em; text-transform: uppercase; }
diff --git a/apps/web/src/features/trade/TradePublicDiscussionClient.tsx b/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
index c7bb603..9aba0a6 100644
--- a/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
+++ b/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
@@ -81,7 +81,8 @@ export function TradePublicDiscussionClient({ tradeId }: { tradeId: string }) {
     try {
       const response = await api.trades.publicMessages(tradeId, { take: 80 });
       const nextMessages = normalizeMessages(response);
-      setMessages((current) => mergeMessages(current, nextMessages));
+      // Replace the visible list on refresh so messages hidden by moderation disappear locally.
+      setMessages(nextMessages);
       if (!options?.quiet) setNotice(null);
     } catch (cause) {
       if (!options?.quiet) setNotice({ tone: 'warning', body: getFriendlyApiErrorMessage(cause, t('trade.publicDiscussion.couldNotLoad')) });
@@ -197,6 +198,10 @@ export function TradePublicDiscussionClient({ tradeId }: { tradeId: string }) {
 
       <section className="trade-social-section public-discussion-section">
         {notice ? <p className={`notice-box ${notice.tone}`}>{notice.body}</p> : null}
+        <div className="public-discussion-toolbar">
+          <span className="semantic-badge info">{t('trade.publicDiscussion.messageCount', { count: messages.length })}</span>
+          <span className="public-discussion-toolbar__hint">{t('trade.publicDiscussion.moderationHint')}</span>
+        </div>
         {messages.length ? (
           <div className="public-message-list">
             {groupedMessages.map(({ message, showDate }) => {
@@ -246,7 +251,7 @@ export function TradePublicDiscussionClient({ tradeId }: { tradeId: string }) {
                             <button type="button" className="button secondary danger-text" onClick={() => void deleteMessage(message)}>{t('common.actions.remove')}</button>
                           </>
                         ) : (
-                          <ReportContentButton targetType="public_message" targetId={message.id} labelKey="report.publicMessage" helperKey="report.helper.publicMessage" buttonClassName="button secondary danger-text" />
+                          <ReportContentButton targetType="public_message" targetId={message.id} labelKey="trade.publicDiscussion.reportMessage" helperKey="report.helper.publicMessage" buttonClassName="button secondary danger-text" />
                         )}
                       </div>
                     ) : null}
diff --git a/packages/i18n/src/locales/en/trade.ts b/packages/i18n/src/locales/en/trade.ts
index 6ff88fe..efd3a95 100644
--- a/packages/i18n/src/locales/en/trade.ts
+++ b/packages/i18n/src/locales/en/trade.ts
@@ -157,6 +157,12 @@ export const trade = {
     title: 'Public discussion',
     heading: 'Public discussion',
     body: 'Messages here are visible to logged-in members who can view this trade. Keep private details inside a private proposal.',
+    moderationHint: 'Public messages are moderated. Use the message menu to edit, delete, or report.',
+    messageMenuHintOwn: 'Choose what to do with your message.',
+    messageMenuHintOther: 'Report public messages that look unsafe, spammy, or abusive.',
+    editMessage: 'Edit message',
+    deleteMessage: 'Delete message',
+    reportMessage: 'Report message',
     signedOutTitle: 'Sign in to view the public discussion',
     signedOutBody: 'Public discussion is available to logged-in members so reports and moderation stay accountable.',
     emptyTitle: 'No public messages yet',
diff --git a/packages/i18n/src/locales/fr/trade.ts b/packages/i18n/src/locales/fr/trade.ts
index 93368d0..1843f15 100644
--- a/packages/i18n/src/locales/fr/trade.ts
+++ b/packages/i18n/src/locales/fr/trade.ts
@@ -157,6 +157,12 @@ export const trade = {
     title: 'Discussion publique',
     heading: 'Discussion publique',
     body: 'Les messages ici sont visibles par les membres connectés qui peuvent voir cet échange. Gardez les détails privés dans une proposition privée.',
+    moderationHint: 'Les messages publics sont modérés. Utilisez le menu du message pour modifier, supprimer ou signaler.',
+    messageMenuHintOwn: 'Choisissez quoi faire avec votre message.',
+    messageMenuHintOther: 'Signalez les messages publics qui semblent dangereux, abusifs ou indésirables.',
+    editMessage: 'Modifier le message',
+    deleteMessage: 'Supprimer le message',
+    reportMessage: 'Signaler le message',
     signedOutTitle: 'Connectez-vous pour voir la discussion publique',
     signedOutBody: 'La discussion publique est réservée aux membres connectés afin de garder les signalements et la modération responsables.',
     emptyTitle: 'Aucun message public pour le moment',
