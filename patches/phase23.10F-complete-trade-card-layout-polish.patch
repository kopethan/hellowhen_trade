--- a/apps/web/src/features/trade/TradeStackDeck.tsx
+++ b/apps/web/src/features/trade/TradeStackDeck.tsx
@@ -35,6 +35,36 @@
   return metadata || fallback;
 }
 
+function compactCardMeta(metadata: string, fallback: string, maxParts = 2) {
+  const value = compactSideMeta(metadata, fallback);
+  return value
+    .split('·')
+    .map((part) => part.trim().replace(/[•·\s]+$/g, ''))
+    .filter(Boolean)
+    .slice(0, maxParts)
+    .join(' · ');
+}
+
+function formatExpiryCountdown(value?: string | null) {
+  if (!value) return null;
+  const expiresMs = new Date(value).getTime();
+  if (!Number.isFinite(expiresMs)) return null;
+
+  const secondsLeft = Math.max(0, Math.floor((expiresMs - Date.now()) / 1000));
+  if (secondsLeft <= 0) return null;
+
+  const days = Math.floor(secondsLeft / 86400);
+  const hours = Math.floor((secondsLeft % 86400) / 3600);
+  const minutes = Math.floor((secondsLeft % 3600) / 60);
+  const seconds = secondsLeft % 60;
+  const tone = days <= 0 ? 'urgent' : days <= 2 ? 'soon' : 'normal';
+
+  return {
+    value: `${String(Math.min(days, 999)).padStart(3, '0')}:${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`,
+    tone,
+  };
+}
+
 function compactJoin(values: Array<string | null | undefined>) {
   return values.filter((value): value is string => Boolean(value && value.trim())).join(' · ');
 }
@@ -83,6 +113,11 @@
   const summarySubtitle = summarySubtitleForTrade(trade, need, offer);
   const summaryChips = summaryChipsForTrade(trade, need, offer);
 
+  const needCardMeta = compactCardMeta(need.metadata, need.description);
+  const offerCardMeta = compactCardMeta(offer.metadata, offer.description);
+  const countdown = formatExpiryCountdown(trade.expiresAt);
+  const showTradeFooter = Boolean(countdown || exchange !== 'Need + Offer exchange');
+
   const summaryContent = postType === 'need_offer' ? (
     <div className="trade-stack-card trade-stack-card--summary trade-stack-card--mobile-parity">
       <div className="trade-stack-card__mobile-top">
@@ -92,21 +127,33 @@
 
       <div className="trade-stack-card__mobile-section trade-stack-card__mobile-section--need">
         <p>I need</p>
-        <h2>{need.title}</h2>
-        {compactSideMeta(need.metadata, need.description) ? <span>{compactSideMeta(need.metadata, need.description)}</span> : null}
+        <h2 title={need.title}>{need.title}</h2>
+        {needCardMeta ? <span title={compactSideMeta(need.metadata, need.description)}>{needCardMeta}</span> : null}
       </div>
 
       <div className="trade-stack-card__mobile-divider" aria-hidden="true">
+        <i />
         <span><WebIcon name="trade" size={17} decorative /></span>
+        <i />
       </div>
 
       <div className="trade-stack-card__mobile-section trade-stack-card__mobile-section--offer">
         <p>I offer</p>
-        <h2>{offer.title}</h2>
-        {compactSideMeta(offer.metadata, offer.description) ? <span>{compactSideMeta(offer.metadata, offer.description)}</span> : null}
+        <h2 title={offer.title}>{offer.title}</h2>
+        {offerCardMeta ? <span title={compactSideMeta(offer.metadata, offer.description)}>{offerCardMeta}</span> : null}
       </div>
 
-      {exchange !== 'Need + Offer exchange' ? <div className="trade-stack-card__mobile-money">{exchange}</div> : null}
+      {showTradeFooter ? (
+        <div className="trade-stack-card__mobile-footer">
+          {exchange !== 'Need + Offer exchange' ? <span className="trade-stack-card__mobile-money">{exchange}</span> : null}
+          {countdown ? (
+            <span className={`trade-stack-card__mobile-countdown is-${countdown.tone}`} aria-label={`Expires in ${countdown.value}`}>
+              <strong>{countdown.value}</strong>
+              <small>DDD:HH:MM:SS</small>
+            </span>
+          ) : null}
+        </div>
+      ) : null}
     </div>
   ) : (
     <TradePosterCard
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -1008,17 +1008,25 @@
 .inventory-card--skeleton .inventory-card__body > span:nth-child(3) { width: 84%; height: 13px; }
 .inventory-card--skeleton .inventory-card__body > span:nth-child(4) { width: 44%; height: 13px; }
 
-.trade-stack-card--mobile-parity { grid-template-rows: auto 1fr auto 1fr auto; gap: 0; padding: clamp(22px, 7vw, 32px); text-align: center; }
+.trade-stack-card--mobile-parity { grid-template-rows: auto minmax(0, 1fr) auto minmax(0, 1fr) auto; gap: 0; padding: clamp(22px, 7vw, 32px); text-align: center; }
 .trade-stack-card__mobile-top { display: flex; align-items: center; justify-content: space-between; gap: 12px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: .12em; text-transform: uppercase; }
 .trade-stack-card__mobile-top strong { color: var(--app-text); font-size: 12px; font-weight: 950; }
-.trade-stack-card__mobile-section { display: grid; align-content: center; justify-items: center; gap: 8px; min-height: 0; }
-.trade-stack-card__mobile-section p { margin: 0; color: var(--app-text-muted); font-size: 15px; font-weight: 950; }
-.trade-stack-card__mobile-section h2 { width: 100%; margin: 0; overflow-wrap: anywhere; font-size: clamp(28px, 8vw, 36px); line-height: .98; letter-spacing: -0.06em; }
-.trade-stack-card__mobile-section span { display: -webkit-box; max-width: 92%; overflow: hidden; color: var(--app-text-muted); font-size: 14px; font-weight: 850; line-height: 1.35; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
-.trade-stack-card__mobile-divider { position: relative; display: grid; place-items: center; min-height: 38px; color: var(--app-text-muted); }
-.trade-stack-card__mobile-divider::before { content: ''; position: absolute; right: 0; left: 0; top: 50%; height: 1px; background: color-mix(in srgb, var(--app-border) 72%, transparent); }
-.trade-stack-card__mobile-divider > span { position: relative; z-index: 1; display: grid; place-items: center; width: 28px; height: 28px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface); color: var(--app-text); box-shadow: 0 8px 20px rgba(15, 23, 42, 0.08); }
-.trade-stack-card__mobile-money { justify-self: center; align-self: end; display: inline-flex; align-items: center; min-height: 30px; border: 1px solid var(--money-border); border-radius: 999px; background: var(--money-bg); color: var(--money-text); padding: 0 12px; font-size: 12px; font-weight: 950; }
+.trade-stack-card__mobile-section { display: grid; align-content: center; justify-items: center; gap: 7px; min-height: 0; overflow: hidden; }
+.trade-stack-card__mobile-section p { margin: 0; font-size: 14px; font-weight: 950; letter-spacing: -0.02em; }
+.trade-stack-card__mobile-section--need p { color: color-mix(in srgb, #2563eb 76%, var(--app-text)); }
+.trade-stack-card__mobile-section--offer p { color: color-mix(in srgb, #059669 78%, var(--app-text)); }
+.trade-stack-card__mobile-section h2 { display: -webkit-box; width: 100%; max-width: 100%; margin: 0; overflow: hidden; overflow-wrap: anywhere; color: var(--app-text); font-size: clamp(26px, 7.25vw, 34px); line-height: .98; letter-spacing: -0.06em; text-overflow: ellipsis; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
+.trade-stack-card__mobile-section span { display: block; max-width: min(90%, 280px); overflow: hidden; color: var(--app-text-muted); font-size: 13px; font-weight: 850; line-height: 1.25; text-overflow: ellipsis; white-space: nowrap; }
+.trade-stack-card__mobile-divider { display: grid; grid-template-columns: minmax(64px, 1fr) auto minmax(64px, 1fr); align-items: center; gap: 12px; min-height: 34px; color: var(--app-text-muted); }
+.trade-stack-card__mobile-divider i { display: block; height: 2px; border-radius: 999px; background: color-mix(in srgb, var(--app-border-strong) 62%, transparent); }
+.trade-stack-card__mobile-divider > span { display: grid; place-items: center; width: 24px; height: 24px; color: color-mix(in srgb, var(--app-text-muted) 82%, var(--app-text)); }
+.trade-stack-card__mobile-footer { display: flex; align-items: flex-end; justify-content: center; gap: 10px; min-height: 28px; }
+.trade-stack-card__mobile-money { display: inline-flex; align-items: center; min-height: 28px; border: 1px solid var(--money-border); border-radius: 999px; background: var(--money-bg); color: var(--money-text); padding: 0 12px; font-size: 12px; font-weight: 950; }
+.trade-stack-card__mobile-countdown { display: grid; gap: 1px; justify-items: center; color: var(--app-text-muted); font-weight: 950; letter-spacing: .04em; line-height: 1; }
+.trade-stack-card__mobile-countdown strong { font-variant-numeric: tabular-nums; font-size: 13px; letter-spacing: .07em; }
+.trade-stack-card__mobile-countdown small { color: var(--app-text-muted); font-size: 8px; font-weight: 950; letter-spacing: .16em; }
+.trade-stack-card__mobile-countdown.is-soon strong { color: #b45309; }
+.trade-stack-card__mobile-countdown.is-urgent strong { color: #b91c1c; }
 
 /* Phase 22.4: web dark-mode surface polish */
 html[data-theme="dark"] .web-top-header,
@@ -1036,7 +1044,6 @@
   background: color-mix(in srgb, var(--app-surface) 92%, white 2%);
 }
 
-html[data-theme="dark"] .trade-stack-card__mobile-divider > span,
 html[data-theme="dark"] .inventory-empty-state__plus {
   background: color-mix(in srgb, var(--app-surface) 92%, white 3%);
 }
@@ -1057,7 +1064,6 @@
     background: color-mix(in srgb, var(--app-surface) 92%, white 2%);
   }
 
-  :root:not([data-theme="light"]) .trade-stack-card__mobile-divider > span,
   :root:not([data-theme="light"]) .inventory-empty-state__plus {
     background: color-mix(in srgb, var(--app-surface) 92%, white 3%);
   }
@@ -1070,13 +1076,15 @@
 
 @media (min-width: 760px) {
   .trade-stack-card--mobile-parity { padding: 30px; }
-  .trade-stack-card__mobile-section h2 { font-size: clamp(30px, 2.25vw, 38px); }
-  .trade-stack-card__mobile-section span { font-size: 15px; }
+  .trade-stack-card__mobile-section h2 { font-size: clamp(28px, 2.1vw, 36px); }
+  .trade-stack-card__mobile-section span { font-size: 14px; }
 }
 
 @media (max-width: 420px) {
   .trade-stack-card--mobile-parity { padding: 22px; }
-  .trade-stack-card__mobile-section h2 { font-size: 28px; }
+  .trade-stack-card__mobile-section h2 { font-size: 26px; }
+  .trade-stack-card__mobile-countdown strong { font-size: 12px; }
+  .trade-stack-card__mobile-countdown small { font-size: 7px; }
 }
 
 .payout-fee-box {
