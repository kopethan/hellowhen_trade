diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index abfa59e..b148fef 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -142,7 +142,8 @@ h3 { font-size: 17px; line-height: 1.2; letter-spacing: -0.02em; margin-bottom:
 .web-tab--active { background: var(--app-surface); color: var(--app-text); box-shadow: var(--app-control-shadow); }
 .web-bottom-tabs[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="plans"] { background: color-mix(in srgb, var(--plan-bg) 76%, var(--app-surface)); color: var(--plan-text); }
 .web-bottom-tabs[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="me"] { background: color-mix(in srgb, var(--app-surface) 88%, var(--app-text) 12%); color: var(--app-text); }
-.web-bottom-tabs[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="trade"] { background: var(--trade-solid); color: var(--trade-solid-text); }
+.web-bottom-tabs[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="trade"] { background: color-mix(in srgb, var(--trade-bg) 76%, var(--app-surface)); color: var(--trade-text); }
+.web-bottom-tabs[data-nav-mode="plans-me-trade"] .web-tab--active .web-tab__icon { color: currentColor; background-color: currentColor; }
 
 .web-scroll-top-button { position: fixed; z-index: 70; right: max(18px, calc((100vw - var(--app-shell-width)) / 2 + 18px)); bottom: calc(var(--app-tab-height) + env(safe-area-inset-bottom) + 18px); display: grid; place-items: center; width: 44px; height: 44px; border: 1px solid color-mix(in srgb, var(--app-border-strong) 78%, transparent); border-radius: 999px; background: color-mix(in srgb, var(--app-surface) 94%, transparent); color: var(--app-text); padding: 0; font-size: 22px; font-weight: 950; line-height: 1; box-shadow: var(--app-control-shadow); opacity: 0; pointer-events: none; transform: translateY(10px) scale(0.96); transition: opacity 160ms ease, transform 160ms ease; backdrop-filter: blur(18px); }
 .web-scroll-top-button.is-visible { opacity: 1; pointer-events: auto; transform: translateY(0) scale(1); }
@@ -1959,8 +1960,8 @@ html[data-theme="dark"] .trade-poster-card.has-fallback h2 {
 
 html[data-theme="dark"] .web-bottom-tabs--dock .web-tab--active .web-tab__icon,
 :root:not([data-theme="light"]) .web-bottom-tabs--dock .web-tab--active .web-tab__icon {
-  color: var(--app-text);
-  filter: drop-shadow(0 1px 0 rgba(0, 0, 0, 0.32));
+  color: currentColor;
+  filter: none;
 }
 
 html[data-theme="dark"] .web-bottom-tabs--dock .web-tab,
@@ -1993,13 +1994,16 @@ html[data-theme="dark"] .web-bottom-tabs--dock .web-tab--active,
   .web-app-shell--normal-dock .web-top-header--root { justify-content: flex-start; }
   .web-app-shell--normal-dock .web-top-header--local-only { box-shadow: 0 10px 34px rgba(15, 23, 42, 0.06); }
   .web-bottom-tabs { display: none; }
-  .web-bottom-tabs--dock { position: fixed; isolation: isolate; z-index: 100; bottom: calc(20px + env(safe-area-inset-bottom)); left: 50%; width: min(430px, calc(100vw - 40px)); min-height: var(--app-dock-height); display: grid; gap: 8px; padding: 8px; transform: translateX(-50%); border: 1px solid color-mix(in srgb, var(--app-border-strong) 78%, transparent); border-radius: 999px; background: color-mix(in srgb, var(--app-bg) 88%, transparent); box-shadow: 0 18px 55px rgba(15, 23, 42, 0.20), var(--app-control-shadow); backdrop-filter: blur(22px); }
-  .web-bottom-tabs--dock::before { content: ""; position: absolute; inset: -18px -28px -22px; z-index: -1; border-radius: 999px; background: radial-gradient(circle at 50% 100%, color-mix(in srgb, var(--app-text) 12%, transparent), transparent 68%); pointer-events: none; }
+  .web-bottom-tabs--dock { position: fixed; isolation: isolate; z-index: 100; bottom: calc(20px + env(safe-area-inset-bottom)); left: 50%; width: min(430px, calc(100vw - 40px)); min-height: var(--app-dock-height); display: grid; gap: 8px; padding: 8px; transform: translateX(-50%); border: 1px solid color-mix(in srgb, var(--app-border-strong) 64%, transparent); border-radius: 999px; background: color-mix(in srgb, var(--app-bg) 92%, transparent); box-shadow: 0 10px 30px rgba(15, 23, 42, 0.10); backdrop-filter: blur(22px); }
+  .web-bottom-tabs--dock::before { display: none; }
   .web-bottom-tabs--dock .web-tab { min-height: 52px; border: 1px solid transparent; border-radius: 999px; padding: 0 14px; color: color-mix(in srgb, var(--app-text-muted) 84%, var(--app-text)); font-size: 12px; }
   .web-bottom-tabs--dock .web-tab:hover { color: var(--app-text); background: color-mix(in srgb, var(--app-surface) 70%, transparent); transform: translateY(-1px); }
-  .web-bottom-tabs--dock .web-tab--active { border-color: color-mix(in srgb, var(--app-border-strong) 82%, transparent); background: var(--app-surface); color: var(--app-text); box-shadow: 0 12px 28px rgba(15, 23, 42, 0.12), var(--app-control-shadow); }
-  .web-bottom-tabs--dock .web-tab__icon { width: 26px; height: 24px; background-color: currentColor; opacity: 0.94; }
-  .web-bottom-tabs--dock .web-tab--active .web-tab__icon { background-color: currentColor; color: var(--app-text); opacity: 1; filter: drop-shadow(0 1px 0 color-mix(in srgb, var(--app-surface) 72%, transparent)); }
+  .web-bottom-tabs--dock .web-tab--active { border-color: color-mix(in srgb, var(--app-border-strong) 58%, transparent); background: var(--app-surface); color: var(--app-text); box-shadow: 0 8px 20px rgba(15, 23, 42, 0.08); }
+  .web-bottom-tabs--dock .web-tab__icon { width: 26px; height: 24px; background-color: currentColor; color: inherit; opacity: 0.94; }
+  .web-bottom-tabs--dock .web-tab--active .web-tab__icon { background-color: currentColor; color: currentColor; opacity: 1; filter: none; }
+  .web-bottom-tabs--dock[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="plans"] { background: color-mix(in srgb, var(--plan-bg) 78%, var(--app-surface)); color: var(--plan-text); }
+  .web-bottom-tabs--dock[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="me"] { background: color-mix(in srgb, var(--app-surface) 92%, var(--app-text) 8%); color: var(--app-text); }
+  .web-bottom-tabs--dock[data-nav-mode="plans-me-trade"] .web-tab--active[data-tab-key="trade"] { background: color-mix(in srgb, var(--trade-bg) 78%, var(--app-surface)); color: var(--trade-text); }
   .web-scroll-top-button { display: none; }
   @media (max-width: 980px) {
     .web-bottom-tabs--dock { bottom: calc(14px + env(safe-area-inset-bottom)); width: min(390px, calc(100vw - 28px)); }
