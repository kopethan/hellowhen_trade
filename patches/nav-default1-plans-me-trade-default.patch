diff --git a/.env.example b/.env.example
index 9e3729f..57ae0a9 100644
--- a/.env.example
+++ b/.env.example
@@ -434,12 +434,13 @@ PLANS_ENABLED=false
 PLANS_VISIBLE=false
 NEXT_PUBLIC_PLANS_ENABLED=false
 NEXT_PUBLIC_PLANS_VISIBLE=false
-# Optional nav experiment. Keep false until Plans / Me / Trade is ready for testing.
-NEXT_PUBLIC_MAIN_NAV_PLANS_ME_TRADE=false
+# Plans / Me / Trade is now the default nav whenever Plans are enabled and visible.
+# Set this to false only if you need to temporarily restore the legacy Trades / Needs / Offers / Account tabs.
+NEXT_PUBLIC_MAIN_NAV_PLANS_ME_TRADE=true
 EXPO_PUBLIC_FIRST_LAUNCH_GUARDS_ENABLED=false
 EXPO_PUBLIC_PLANS_ENABLED=false
 EXPO_PUBLIC_PLANS_VISIBLE=false
-EXPO_PUBLIC_MAIN_NAV_PLANS_ME_TRADE=false
+EXPO_PUBLIC_MAIN_NAV_PLANS_ME_TRADE=true
 
 # Google-confirmed offline place search. Keep disabled until API keys and quota limits are ready.
 GOOGLE_PLACES_ENABLED=false
diff --git a/apps/mobile/src/lib/betaFeatures.ts b/apps/mobile/src/lib/betaFeatures.ts
index 70c64dc..de2d57a 100644
--- a/apps/mobile/src/lib/betaFeatures.ts
+++ b/apps/mobile/src/lib/betaFeatures.ts
@@ -69,7 +69,7 @@ const moneyFeaturesVisible = !forceFirstLaunchSafeFlags && enabled(process.env.E
 const businessAccountsEnabled = !forceFirstLaunchSafeFlags && enabled(process.env.EXPO_PUBLIC_BUSINESS_ACCOUNTS_ENABLED);
 const plansEnabled = !forceFirstLaunchSafeFlags && enabled(process.env.EXPO_PUBLIC_PLANS_ENABLED);
 const plansVisible = plansEnabled && enabled(process.env.EXPO_PUBLIC_PLANS_VISIBLE);
-const mainNavPlansMeTrade = plansVisible && enabled(process.env.EXPO_PUBLIC_MAIN_NAV_PLANS_ME_TRADE);
+const mainNavPlansMeTrade = plansVisible && !disabled(process.env.EXPO_PUBLIC_MAIN_NAV_PLANS_ME_TRADE);
 const mobileMembershipVisible = !forceFirstLaunchSafeFlags && (
   enabled(process.env.EXPO_PUBLIC_MOBILE_MEMBERSHIP_VISIBLE)
   || (subscriptionsEnabled && plusSubscriptionFeatures.plusPublic)
diff --git a/apps/web/src/lib/betaFeatures.tsx b/apps/web/src/lib/betaFeatures.tsx
index 72ff328..19fea2f 100644
--- a/apps/web/src/lib/betaFeatures.tsx
+++ b/apps/web/src/lib/betaFeatures.tsx
@@ -71,7 +71,7 @@ const moneyFeaturesVisible = !forceFirstLaunchSafeFlags && enabled(process.env.N
 const businessAccountsEnabled = !forceFirstLaunchSafeFlags && enabled(process.env.NEXT_PUBLIC_BUSINESS_ACCOUNTS_ENABLED);
 const plansEnabled = !forceFirstLaunchSafeFlags && enabled(process.env.NEXT_PUBLIC_PLANS_ENABLED);
 const plansVisible = plansEnabled && enabled(process.env.NEXT_PUBLIC_PLANS_VISIBLE);
-const mainNavPlansMeTrade = plansVisible && enabled(process.env.NEXT_PUBLIC_MAIN_NAV_PLANS_ME_TRADE);
+const mainNavPlansMeTrade = plansVisible && !disabled(process.env.NEXT_PUBLIC_MAIN_NAV_PLANS_ME_TRADE);
 
 export const betaFeatures = {
   moneyProvider: forceFirstLaunchSafeFlags ? 'none' : rawMoneyProvider,
