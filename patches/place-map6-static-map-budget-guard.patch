diff --git a/.env.example b/.env.example
index a881484..9e3729f 100644
--- a/.env.example
+++ b/.env.example
@@ -453,6 +453,11 @@ GOOGLE_PLACES_REQUEST_TIMEOUT_MS=4500
 # Optional Place card static map previews. These render Google Static Maps as direct image URLs; do not download/re-host them.
 GOOGLE_STATIC_MAPS_ENABLED=false
 GOOGLE_STATIC_MAPS_API_KEY=
+# Keep anonymous static maps off until you are comfortable with public traffic. Logged-in users can still receive map URLs.
+GOOGLE_STATIC_MAPS_ANONYMOUS_ENABLED=false
+# Soft API-side exposure caps. These estimate Static Maps image loads and stop issuing URLs before the free-tier target.
+GOOGLE_STATIC_MAPS_DAILY_SOFT_LIMIT=250
+GOOGLE_STATIC_MAPS_MONTHLY_SOFT_LIMIT=9000
 # Default template family when a Place has no assigned template yet.
 GOOGLE_STATIC_MAPS_DEFAULT_TEMPLATE=clean_local
 # Optional Cloud Styling map IDs. Keep blank to fall back to simple URL styles.
diff --git a/apps/api/src/config/env.ts b/apps/api/src/config/env.ts
index 563f201..f09dbe2 100644
--- a/apps/api/src/config/env.ts
+++ b/apps/api/src/config/env.ts
@@ -142,6 +142,9 @@ export const env = {
   googleMapsServerApiKey: process.env.GOOGLE_MAPS_SERVER_API_KEY ?? process.env.GOOGLE_PLACES_SERVER_API_KEY ?? '',
   googleStaticMapsEnabled: enabled(process.env.GOOGLE_STATIC_MAPS_ENABLED),
   googleStaticMapsApiKey: process.env.GOOGLE_STATIC_MAPS_API_KEY ?? process.env.GOOGLE_MAPS_SERVER_API_KEY ?? process.env.GOOGLE_PLACES_SERVER_API_KEY ?? '',
+  googleStaticMapsAnonymousEnabled: enabled(process.env.GOOGLE_STATIC_MAPS_ANONYMOUS_ENABLED),
+  googleStaticMapsDailySoftLimit: boundedInt(process.env.GOOGLE_STATIC_MAPS_DAILY_SOFT_LIMIT, 250, 0, 1_000_000),
+  googleStaticMapsMonthlySoftLimit: boundedInt(process.env.GOOGLE_STATIC_MAPS_MONTHLY_SOFT_LIMIT, 9000, 0, 10_000_000),
   googleStaticMapsDefaultTemplate: process.env.GOOGLE_STATIC_MAPS_DEFAULT_TEMPLATE ?? 'clean_local',
   googleStaticMapsDefaultMapId: process.env.GOOGLE_STATIC_MAPS_DEFAULT_MAP_ID ?? '',
   googleStaticMapsLightMapId: process.env.GOOGLE_STATIC_MAPS_LIGHT_MAP_ID ?? process.env.GOOGLE_STATIC_MAPS_DEFAULT_MAP_ID ?? '',
diff --git a/apps/api/src/modules/places/placeStaticMap.ts b/apps/api/src/modules/places/placeStaticMap.ts
index 0250543..3681211 100644
--- a/apps/api/src/modules/places/placeStaticMap.ts
+++ b/apps/api/src/modules/places/placeStaticMap.ts
@@ -26,6 +26,25 @@ type StaticMappablePlace = {
   sourcePlace?: StaticMappablePlace | null;
 };
 
+type PlaceStaticMapBuildOptions = {
+  viewerId?: string | null;
+  estimatedRequestCount?: number;
+};
+
+type StaticMapBudgetWindow = {
+  key: string;
+  count: number;
+};
+
+export type PlaceStaticMapBudgetSnapshot = {
+  enabled: boolean;
+  anonymousEnabled: boolean;
+  dailySoftLimit: number;
+  monthlySoftLimit: number;
+  dailyIssued: number;
+  monthlyIssued: number;
+};
+
 export type PlaceStaticMapDto = {
   provider: 'google_static_maps';
   templateFamily: string;
@@ -38,6 +57,56 @@ export type PlaceStaticMapDto = {
   darkUrl: string;
 };
 
+let dailyBudgetWindow: StaticMapBudgetWindow = { key: '', count: 0 };
+let monthlyBudgetWindow: StaticMapBudgetWindow = { key: '', count: 0 };
+
+function utcDayKey(now = new Date()) {
+  return now.toISOString().slice(0, 10);
+}
+
+function utcMonthKey(now = new Date()) {
+  return now.toISOString().slice(0, 7);
+}
+
+function refreshBudgetWindows(now = new Date()) {
+  const dayKey = utcDayKey(now);
+  const monthKey = utcMonthKey(now);
+  if (dailyBudgetWindow.key !== dayKey) dailyBudgetWindow = { key: dayKey, count: 0 };
+  if (monthlyBudgetWindow.key !== monthKey) monthlyBudgetWindow = { key: monthKey, count: 0 };
+}
+
+function canUseAnonymousStaticMaps(options?: PlaceStaticMapBuildOptions | null) {
+  return Boolean(options?.viewerId) || env.googleStaticMapsAnonymousEnabled;
+}
+
+function reserveStaticMapBudget(options?: PlaceStaticMapBuildOptions | null) {
+  if (!canUseAnonymousStaticMaps(options)) return false;
+
+  refreshBudgetWindows();
+  const estimate = Math.max(1, Math.trunc(options?.estimatedRequestCount ?? 1));
+  const dailyLimit = env.googleStaticMapsDailySoftLimit;
+  const monthlyLimit = env.googleStaticMapsMonthlySoftLimit;
+  if (dailyLimit <= 0 || monthlyLimit <= 0) return false;
+  if (dailyBudgetWindow.count + estimate > dailyLimit) return false;
+  if (monthlyBudgetWindow.count + estimate > monthlyLimit) return false;
+
+  dailyBudgetWindow.count += estimate;
+  monthlyBudgetWindow.count += estimate;
+  return true;
+}
+
+export function getPlaceStaticMapBudgetSnapshot(now = new Date()): PlaceStaticMapBudgetSnapshot {
+  refreshBudgetWindows(now);
+  return {
+    enabled: env.googleStaticMapsEnabled,
+    anonymousEnabled: env.googleStaticMapsAnonymousEnabled,
+    dailySoftLimit: env.googleStaticMapsDailySoftLimit,
+    monthlySoftLimit: env.googleStaticMapsMonthlySoftLimit,
+    dailyIssued: dailyBudgetWindow.count,
+    monthlyIssued: monthlyBudgetWindow.count,
+  };
+}
+
 function normalizedText(value?: string | null) {
   const trimmed = value?.trim();
   return trimmed ? trimmed : null;
@@ -123,7 +192,7 @@ function buildThemeUrl(place: StaticMappablePlace, source: 'coordinates' | 'addr
   return { url: `${GOOGLE_STATIC_MAPS_BASE_URL}?${params.toString()}`, zoom };
 }
 
-export function buildPlaceStaticMap(place?: StaticMappablePlace | null): PlaceStaticMapDto | null {
+export function buildPlaceStaticMap(place?: StaticMappablePlace | null, options?: PlaceStaticMapBuildOptions | null): PlaceStaticMapDto | null {
   if (!env.googleStaticMapsEnabled) return null;
   if (!place || place.mode === 'remote') return null;
 
@@ -137,6 +206,7 @@ export function buildPlaceStaticMap(place?: StaticMappablePlace | null): PlaceSt
   const light = buildThemeUrl(place, source, 'light', templateFamily);
   const dark = buildThemeUrl(place, source, 'dark', templateFamily);
   if (!light || !dark) return null;
+  if (!reserveStaticMapBudget(options)) return null;
 
   return {
     provider: 'google_static_maps',
diff --git a/apps/api/src/modules/places/places.routes.ts b/apps/api/src/modules/places/places.routes.ts
index 29118e7..be9779c 100644
--- a/apps/api/src/modules/places/places.routes.ts
+++ b/apps/api/src/modules/places/places.routes.ts
@@ -266,7 +266,7 @@ function cleanPlaceForViewer(place: any, viewerId?: string | null, actorRole?: s
     defaultMeetingInstructions: canSeePrivateDetails ? place.defaultMeetingInstructions ?? null : null,
     usedInPlansCount: canSeePrivateDetails ? place.usedInPlansCount ?? 0 : undefined,
   };
-  return { ...cleaned, staticMap: buildPlaceStaticMap(cleaned) };
+  return { ...cleaned, staticMap: buildPlaceStaticMap(cleaned, { viewerId }) };
 }
 
 async function decoratePlaces(places: any[], viewerId?: string | null, visibility: MediaVisibility = 'owner', actorRole?: string | null) {
diff --git a/apps/api/src/modules/plans/plans.routes.ts b/apps/api/src/modules/plans/plans.routes.ts
index 4bab716..6d79017 100644
--- a/apps/api/src/modules/plans/plans.routes.ts
+++ b/apps/api/src/modules/plans/plans.routes.ts
@@ -174,7 +174,7 @@ function createPlanRequestError(code: string, publicMessage: string, statusCode
   return Object.assign(new Error(publicMessage), { code, publicMessage, statusCode });
 }
 
-function cleanReusablePlaceForViewer(place: any, canSeePrivateDetails: boolean) {
+function cleanReusablePlaceForViewer(place: any, canSeePrivateDetails: boolean, viewerId?: string | null) {
   if (!place) return null;
   const cleaned = {
     ...place,
@@ -184,7 +184,7 @@ function cleanReusablePlaceForViewer(place: any, canSeePrivateDetails: boolean)
     addressPrivateText: canSeePrivateDetails ? place.addressPrivateText ?? null : null,
     defaultMeetingInstructions: canSeePrivateDetails ? place.defaultMeetingInstructions ?? null : null,
   };
-  return { ...cleaned, staticMap: buildPlaceStaticMap(cleaned) };
+  return { ...cleaned, staticMap: buildPlaceStaticMap(cleaned, { viewerId }) };
 }
 
 function planPlaceSourceFor(place: any) {
@@ -206,7 +206,7 @@ function serializePlan(plan: any, viewerId: string | null) {
   return {
     ...plan,
     places: (plan.places ?? []).map((place: any) => {
-      const sourcePlace = canSeeSourcePlace ? cleanReusablePlaceForViewer(place.sourcePlace, canSeePrivatePlaceDetails) : null;
+      const sourcePlace = canSeeSourcePlace ? cleanReusablePlaceForViewer(place.sourcePlace, canSeePrivatePlaceDetails, viewerId) : null;
       const cleanedPlace = {
         ...place,
         source: planPlaceSourceFor(place),
@@ -214,7 +214,7 @@ function serializePlan(plan: any, viewerId: string | null) {
         mode: place.mode ?? 'local',
         addressPrivateText: canSeePrivatePlaceDetails ? place.addressPrivateText ?? null : null,
       };
-      return { ...cleanedPlace, staticMap: buildPlaceStaticMap(cleanedPlace) };
+      return { ...cleanedPlace, staticMap: buildPlaceStaticMap(cleanedPlace, { viewerId }) };
     }),
     participants: visibleParticipants,
     participantCount: acceptedParticipants.length,
