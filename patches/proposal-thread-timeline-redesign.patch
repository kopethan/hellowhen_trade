diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 9798801..53df114 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -530,6 +530,11 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .proposal-conversation-list .message-bubble--proposal-package { justify-self: stretch; width: 100%; max-width: 100%; border-radius: 24px; background: color-mix(in srgb, var(--proposal-bg) 72%, var(--app-surface)); }
 .proposal-conversation-list .message-bubble--proposal-package .proposal-side-preview { margin-top: 8px; }
 .proposal-conversation-list .message-bubble p { overflow-wrap: anywhere; }
+.proposal-timeline-list { gap: 10px; }
+.proposal-timeline-event { display: grid; gap: 10px; }
+.proposal-timeline-event__note { margin-top: 0; }
+.proposal-timeline-event__status { border-radius: 16px; background: color-mix(in srgb, var(--app-surface) 78%, transparent); color: var(--app-text-muted); font-size: 12px; font-weight: 850; line-height: 1.35; padding: 9px 10px; }
+.proposal-timeline-empty { justify-self: stretch; padding: 8px 2px 0; }
 .conversation-reply .field-error { grid-column: 1 / -1; }
 .proposal-confirm-modal { position: fixed; z-index: 190; inset: 0; display: grid; place-items: center; padding: calc(16px + env(safe-area-inset-top)) 14px calc(16px + env(safe-area-inset-bottom)); background: rgba(15, 23, 42, 0.46); backdrop-filter: blur(12px); }
 .proposal-confirm-modal__panel { display: grid; gap: 12px; width: min(100%, 420px); border: 1px solid color-mix(in srgb, var(--danger-border) 58%, var(--app-border)); border-radius: 28px; background: var(--app-bg); box-shadow: 0 28px 90px rgba(15, 23, 42, 0.34); padding: 16px; }
diff --git a/apps/web/src/features/trade/ProposalConversationClient.tsx b/apps/web/src/features/trade/ProposalConversationClient.tsx
index 0a9360b..b178447 100644
--- a/apps/web/src/features/trade/ProposalConversationClient.tsx
+++ b/apps/web/src/features/trade/ProposalConversationClient.tsx
@@ -493,84 +493,6 @@ export function ProposalConversationClient({
         {notice ? <p className="notice-box info">{notice}</p> : null}
       </section>
 
-      <section className="trade-social-section trade-social-section--compact">
-        <div className="trade-section-heading">
-          <div>
-            <p className="eyebrow">{t("trade.labels.privateThread")}</p>
-            <h2 className="icon-heading">
-              <WebIcon name="proposal" size={21} decorative />{" "}
-              {t("trade.proposals.tradeProposal")}
-            </h2>
-          </div>
-          <span className="semantic-badge proposal">
-            <WebIcon
-              name={proposalStatusIcon(proposal.status)}
-              size={14}
-              decorative
-            />{" "}
-            {getStatusLabel(proposal.status, i18n)}
-          </span>
-        </div>
-        <div className="proposal-conversation-package">
-          <UserIdentityLink
-            user={proposal.applicant}
-            userId={proposal.applicantId}
-            variant="compact"
-            avatarSize="sm"
-            statusText={proposalApplicantStatus(proposal, i18n)}
-            showHandle={false}
-            className="message-bubble__identity"
-          />
-          {sideItem ? (
-            <ProposalSidePreview
-              kind={sideItem.kind}
-              item={sideItem.item}
-              compact
-              i18n={i18n}
-            />
-          ) : null}
-          <p className="proposal-card__message">{proposal.message}</p>
-        </div>
-        <div className="proposal-card__actions proposal-detail-actions">
-          {canActOnProposal ? (
-            <button
-              type="button"
-              className="success"
-              onClick={() => void updateProposalStatus("accepted")}
-              disabled={Boolean(actionLoading)}
-            >
-              {actionLoading === "accepted"
-                ? t("trade.proposals.accepting")
-                : t("trade.proposals.accept")}
-            </button>
-          ) : null}
-          {canActOnProposal ? (
-            <button
-              type="button"
-              className="secondary"
-              onClick={() => void updateProposalStatus("declined")}
-              disabled={Boolean(actionLoading)}
-            >
-              {actionLoading === "declined"
-                ? t("trade.proposals.declining")
-                : t("trade.proposals.decline")}
-            </button>
-          ) : null}
-          {canWithdrawProposal ? (
-            <button
-              type="button"
-              className="secondary danger-text"
-              onClick={() => setWithdrawConfirmOpen(true)}
-              disabled={Boolean(actionLoading)}
-            >
-              {actionLoading === "withdrawn"
-                ? t("trade.proposals.withdrawing")
-                : t("trade.proposals.withdraw")}
-            </button>
-          ) : null}
-        </div>
-      </section>
-
       {withdrawConfirmOpen ? (
         <div
           className="proposal-confirm-modal"
@@ -613,7 +535,7 @@ export function ProposalConversationClient({
       <section className="trade-social-section trade-social-section--compact conversation-panel proposal-conversation-panel">
         <div className="trade-section-heading">
           <div>
-            <p className="eyebrow">{t("trade.labels.conversation")}</p>
+            <p className="eyebrow">{t("trade.labels.privateThread")}</p>
             <h2 className="icon-heading">
               <WebIcon
                 name={proposalStatusIcon(proposal.status)}
@@ -625,13 +547,96 @@ export function ProposalConversationClient({
                 : t("trade.proposals.proposalConversation")}
             </h2>
           </div>
-          {actionLoading === "reply" ? (
-            <span className="semantic-badge instruction">
-              {t("trade.proposals.sending")}
+          <div className="status-row">
+            <span className="semantic-badge proposal">
+              <WebIcon
+                name={proposalStatusIcon(proposal.status)}
+                size={14}
+                decorative
+              />{" "}
+              {getStatusLabel(proposal.status, i18n)}
             </span>
-          ) : null}
+            {actionLoading === "reply" ? (
+              <span className="semantic-badge instruction">
+                {t("trade.proposals.sending")}
+              </span>
+            ) : null}
+          </div>
         </div>
-        <div className="message-list proposal-conversation-list">
+        <div className="message-list proposal-conversation-list proposal-timeline-list">
+          <article className="message-bubble message-bubble--proposal-package proposal-timeline-event">
+            <UserIdentityLink
+              user={proposal.applicant}
+              userId={proposal.applicantId}
+              variant="compact"
+              avatarSize="sm"
+              statusText={
+                isApplicant
+                  ? t("trade.proposals.youSentProposal")
+                  : t("trade.proposals.sentProposal")
+              }
+              showHandle={false}
+              className="message-bubble__identity"
+            />
+            {sideItem ? (
+              <ProposalSidePreview
+                kind={sideItem.kind}
+                item={sideItem.item}
+                compact
+                i18n={i18n}
+              />
+            ) : null}
+            <p className="proposal-card__message proposal-timeline-event__note">
+              {proposal.message}
+            </p>
+            {proposal.status !== "pending" ? (
+              <p className="proposal-timeline-event__status">
+                {proposal.status === "accepted"
+                  ? t("trade.proposals.proposalContentLockedAccepted")
+                  : proposal.status === "withdrawn"
+                    ? t("trade.proposals.proposalContentLockedWithdrawn")
+                    : t("trade.proposals.proposalContentLockedDeclined")}
+              </p>
+            ) : null}
+            <div className="proposal-card__actions proposal-detail-actions">
+              {canActOnProposal ? (
+                <button
+                  type="button"
+                  className="success"
+                  onClick={() => void updateProposalStatus("accepted")}
+                  disabled={Boolean(actionLoading)}
+                >
+                  {actionLoading === "accepted"
+                    ? t("trade.proposals.accepting")
+                    : t("trade.proposals.accept")}
+                </button>
+              ) : null}
+              {canActOnProposal ? (
+                <button
+                  type="button"
+                  className="secondary"
+                  onClick={() => void updateProposalStatus("declined")}
+                  disabled={Boolean(actionLoading)}
+                >
+                  {actionLoading === "declined"
+                    ? t("trade.proposals.declining")
+                    : t("trade.proposals.decline")}
+                </button>
+              ) : null}
+              {canWithdrawProposal ? (
+                <button
+                  type="button"
+                  className="secondary danger-text"
+                  onClick={() => setWithdrawConfirmOpen(true)}
+                  disabled={Boolean(actionLoading)}
+                >
+                  {actionLoading === "withdrawn"
+                    ? t("trade.proposals.withdrawing")
+                    : t("trade.proposals.withdraw")}
+                </button>
+              ) : null}
+            </div>
+          </article>
           {visibleMessages.length ? (
             visibleMessages.map((message) => (
               <article
@@ -655,7 +660,9 @@ export function ProposalConversationClient({
               </article>
             ))
           ) : (
-            <p className="meta">{t("trade.proposals.noMessages")}</p>
+            <p className="meta proposal-timeline-empty">
+              {t("trade.proposals.noMessages")}
+            </p>
           )}
         </div>
         {canReply ? (
diff --git a/packages/i18n/src/locales/en/trade.ts b/packages/i18n/src/locales/en/trade.ts
index 5869829..db252ef 100644
--- a/packages/i18n/src/locales/en/trade.ts
+++ b/packages/i18n/src/locales/en/trade.ts
@@ -199,6 +199,11 @@ export const trade = {
     proposalAcceptedNative: 'Proposal accepted. The trade moved in progress and the accepted conversation is now below.',
     proposalDeclined: 'Proposal declined.',
     proposalWithdrawn: 'Proposal withdrawn.',
+    youSentProposal: 'You sent a proposal',
+    sentProposal: 'Sent a proposal',
+    proposalContentLockedAccepted: 'Proposal accepted. The proposal content is locked, but you can keep messaging here.',
+    proposalContentLockedDeclined: 'Proposal declined. This thread is read-only.',
+    proposalContentLockedWithdrawn: 'Proposal withdrawn. This thread is read-only.',
     proposalConversation: 'Proposal conversation',
     privateProposalConversation: 'Private proposal conversation',
     acceptedConversation: 'Accepted conversation',
diff --git a/packages/i18n/src/locales/fr/trade.ts b/packages/i18n/src/locales/fr/trade.ts
index 632118e..0ffa6c5 100644
--- a/packages/i18n/src/locales/fr/trade.ts
+++ b/packages/i18n/src/locales/fr/trade.ts
@@ -199,6 +199,11 @@ export const trade = {
     proposalAcceptedNative: 'Proposition acceptée. L’échange est passé en cours et la conversation acceptée est maintenant ci-dessous.',
     proposalDeclined: 'Proposition refusée.',
     proposalWithdrawn: 'Proposition retirée.',
+    youSentProposal: 'Vous avez envoyé une proposition',
+    sentProposal: 'A envoyé une proposition',
+    proposalContentLockedAccepted: 'Proposition acceptée. Le contenu de la proposition est verrouillé, mais vous pouvez continuer à échanger ici.',
+    proposalContentLockedDeclined: 'Proposition refusée. Ce fil est en lecture seule.',
+    proposalContentLockedWithdrawn: 'Proposition retirée. Ce fil est en lecture seule.',
     proposalConversation: 'Conversation de proposition',
     privateProposalConversation: 'Conversation privée de proposition',
     acceptedConversation: 'Conversation acceptée',
