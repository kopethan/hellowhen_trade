diff --git a/apps/mobile/src/features/explore/ExploreScreen.tsx b/apps/mobile/src/features/explore/ExploreScreen.tsx
index 576b455..96c9f48 100644
--- a/apps/mobile/src/features/explore/ExploreScreen.tsx
+++ b/apps/mobile/src/features/explore/ExploreScreen.tsx
@@ -22,18 +22,69 @@ import { ExploreConceptSquareDeck } from './ExploreConceptSquareDeck';
 const EXPLORE_DISCOVERY_LIMIT = 12;
 const tradeExploreIdeaKeys = feedTradeIdeaKeys.filter((ideaKey) => feedTradeIdeas[ideaKey].type === 'trade') as readonly FeedTradeIdeaKey[];
 
-type ExploreSection = 'trade' | 'plan' | 'need' | 'offer' | 'place';
+type ExploreConceptKind = 'trade' | 'plan' | 'need' | 'offer' | 'place';
 type InventoryTemplatesResponse = { templates?: InventoryTemplateDto[] };
 type PlacesResponse = { places?: PlaceDto[] };
 type TFunction = (key: string, values?: Record<string, string | number | boolean | null | undefined>) => string;
 
 type ExploreFeedItem =
-  | { kind: 'section'; key: string; section: ExploreSection; count: number }
-  | { kind: 'trade'; key: string; ideaKey: FeedTradeIdeaKey; index: number; total: number }
-  | { kind: 'plan'; key: string; ideaKey: (typeof starterPlanIdeaKeys)[number]; idea: StarterPlanIdea; preview: PlanDto; index: number; total: number }
-  | { kind: 'need'; key: string; template: InventoryTemplateDto; index: number; total: number }
-  | { kind: 'offer'; key: string; template: InventoryTemplateDto; index: number; total: number }
-  | { kind: 'place'; key: string; place: PlaceDto; index: number; total: number };
+  | { kind: 'trade'; key: string; ideaKey: FeedTradeIdeaKey }
+  | { kind: 'plan'; key: string; ideaKey: (typeof starterPlanIdeaKeys)[number]; idea: StarterPlanIdea; preview: PlanDto }
+  | { kind: 'need'; key: string; template: InventoryTemplateDto }
+  | { kind: 'offer'; key: string; template: InventoryTemplateDto }
+  | { kind: 'place'; key: string; place: PlaceDto };
+
+const EXPLORE_MIX_KIND_ORDER: readonly ExploreConceptKind[] = ['trade', 'place', 'need', 'plan', 'offer'];
+
+function hashExploreMixValue(value: string) {
+  let hash = 2166136261;
+  for (let index = 0; index < value.length; index += 1) {
+    hash ^= value.charCodeAt(index);
+    hash = Math.imul(hash, 16777619);
+  }
+  return hash >>> 0;
+}
+
+function orderExploreQueue(items: readonly ExploreFeedItem[], kind: ExploreConceptKind, seed: number) {
+  return [...items].sort((left, right) => {
+    const rankDelta = hashExploreMixValue(`${seed}:${kind}:${left.key}`) - hashExploreMixValue(`${seed}:${kind}:${right.key}`);
+    return rankDelta || left.key.localeCompare(right.key);
+  });
+}
+
+function buildBalancedMixedFeed(groups: Record<ExploreConceptKind, ExploreFeedItem[]>, seed: number) {
+  const queues = Object.fromEntries(EXPLORE_MIX_KIND_ORDER.map((kind) => (
+    [kind, orderExploreQueue(groups[kind], kind, seed)]
+  ))) as Record<ExploreConceptKind, ExploreFeedItem[]>;
+  const mixed: ExploreFeedItem[] = [];
+  let cycle = 0;
+  let previousKind: ExploreConceptKind | null = null;
+
+  while (EXPLORE_MIX_KIND_ORDER.some((kind) => queues[kind].length > 0)) {
+    const availableKinds = EXPLORE_MIX_KIND_ORDER.filter((kind) => queues[kind].length > 0);
+    const cycleKinds = [...availableKinds].sort((left, right) => (
+      hashExploreMixValue(`${seed}:${cycle}:${left}`) - hashExploreMixValue(`${seed}:${cycle}:${right}`)
+    ));
+
+    if (previousKind && cycleKinds.length > 1 && cycleKinds[0] === previousKind) {
+      const alternateIndex = cycleKinds.findIndex((kind) => kind !== previousKind);
+      if (alternateIndex > 0) {
+        const [alternate] = cycleKinds.splice(alternateIndex, 1);
+        if (alternate) cycleKinds.unshift(alternate);
+      }
+    }
+
+    for (const kind of cycleKinds) {
+      const item = queues[kind].shift();
+      if (!item) continue;
+      mixed.push(item);
+      previousKind = kind;
+    }
+    cycle += 1;
+  }
+
+  return mixed;
+}
 
 function planIdeaPreviewPlan(idea: StarterPlanIdea): PlanDto {
   const createdAt = new Date().toISOString();
@@ -108,14 +159,6 @@ function placeMeta(place: PlaceDto, t: TFunction) {
   return [mode, place.category, location].filter(Boolean).join(' · ');
 }
 
-function sectionTitle(section: ExploreSection, t: TFunction) {
-  if (section === 'trade') return t('trade.feedIdeas.badge');
-  if (section === 'plan') return t('plans.workspace.planIdeas.title');
-  if (section === 'need') return t('common.exploreDiscovery.needSection');
-  if (section === 'offer') return t('common.exploreDiscovery.offerSection');
-  return t('common.exploreDiscovery.placeSection');
-}
-
 export function ExploreScreen() {
   const navigation = useNavigation<NativeStackNavigationProp<RootStackParamList>>();
   const theme = useThemeTokens();
@@ -127,6 +170,7 @@ export function ExploreScreen() {
   const [discoveryLoaded, setDiscoveryLoaded] = useState(false);
   const [discoveryHasError, setDiscoveryHasError] = useState(false);
   const discoveryRequestSequence = useRef(0);
+  const discoveryMixSeed = useRef(Date.now()).current;
 
   const loadDiscovery = useCallback(async () => {
     const requestId = ++discoveryRequestSequence.current;
@@ -155,58 +199,41 @@ export function ExploreScreen() {
     return () => { discoveryRequestSequence.current += 1; };
   }, [loadDiscovery]);
 
-  const staticFeedItems = useMemo<ExploreFeedItem[]>(() => {
-    const items: ExploreFeedItem[] = [
-      { kind: 'section', key: 'trade-ideas-section', section: 'trade', count: tradeExploreIdeaKeys.length },
-      ...tradeExploreIdeaKeys.map((ideaKey, index) => ({
-        kind: 'trade' as const,
-        key: `trade-idea-${ideaKey}`,
-        ideaKey,
-        index,
-        total: tradeExploreIdeaKeys.length,
-      })),
-    ];
+  const staticConceptGroups = useMemo(() => {
+    const tradeItems: ExploreFeedItem[] = tradeExploreIdeaKeys.map((ideaKey) => ({
+      kind: 'trade',
+      key: `trade-idea-${ideaKey}`,
+      ideaKey,
+    }));
+    const planItems: ExploreFeedItem[] = [];
 
     if (betaFeatures.plansEnabled && betaFeatures.plansVisible) {
-      items.push({ kind: 'section', key: 'plan-ideas-section', section: 'plan', count: starterPlanIdeaKeys.length });
-      starterPlanIdeaKeys.forEach((ideaKey, index) => {
+      starterPlanIdeaKeys.forEach((ideaKey) => {
         const idea = starterPlanIdeas[ideaKey];
         if (!idea) return;
-        items.push({
+        planItems.push({
           kind: 'plan',
           key: `plan-idea-${ideaKey}`,
           ideaKey,
           idea,
           preview: planIdeaPreviewPlan(idea),
-          index,
-          total: starterPlanIdeaKeys.length,
         });
       });
     }
 
-    return items;
+    return { trade: tradeItems, plan: planItems };
   }, []);
 
-  const feedItems = useMemo<ExploreFeedItem[]>(() => {
-    const items = [...staticFeedItems];
-
-    if (needTemplates.length > 0) {
-      items.push({ kind: 'section', key: 'need-ideas-section', section: 'need', count: needTemplates.length });
-      needTemplates.forEach((template, index) => items.push({ kind: 'need', key: `need-idea-${template.id}`, template, index, total: needTemplates.length }));
-    }
-
-    if (offerTemplates.length > 0) {
-      items.push({ kind: 'section', key: 'offer-ideas-section', section: 'offer', count: offerTemplates.length });
-      offerTemplates.forEach((template, index) => items.push({ kind: 'offer', key: `offer-idea-${template.id}`, template, index, total: offerTemplates.length }));
-    }
-
-    if (betaFeatures.plansEnabled && betaFeatures.plansVisible && hellowhenPlaces.length > 0) {
-      items.push({ kind: 'section', key: 'hellowhen-places-section', section: 'place', count: hellowhenPlaces.length });
-      hellowhenPlaces.forEach((place, index) => items.push({ kind: 'place', key: `hellowhen-place-${place.id}`, place, index, total: hellowhenPlaces.length }));
-    }
-
-    return items;
-  }, [hellowhenPlaces, needTemplates, offerTemplates, staticFeedItems]);
+  const mixedFeedItems = useMemo<ExploreFeedItem[]>(() => buildBalancedMixedFeed({
+    trade: staticConceptGroups.trade,
+    plan: staticConceptGroups.plan,
+    need: needTemplates.map((template) => ({ kind: 'need' as const, key: `need-idea-${template.id}`, template })),
+    offer: offerTemplates.map((template) => ({ kind: 'offer' as const, key: `offer-idea-${template.id}`, template })),
+    place: betaFeatures.plansEnabled && betaFeatures.plansVisible
+      ? hellowhenPlaces.map((place) => ({ kind: 'place' as const, key: `hellowhen-place-${place.id}`, place }))
+      : [],
+  }, discoveryMixSeed), [discoveryMixSeed, hellowhenPlaces, needTemplates, offerTemplates, staticConceptGroups]);
+  const feedItems = discoveryLoaded ? mixedFeedItems : [];
 
   const discoveryItemCount = needTemplates.length + offerTemplates.length + hellowhenPlaces.length;
   const header = (
@@ -250,20 +277,10 @@ export function ExploreScreen() {
         windowSize={5}
         removeClippedSubviews={false}
         ListFooterComponent={footer}
-        renderItem={({ item }) => {
-          if (item.kind === 'section') {
-            const isFirstSection = item.section === 'trade';
-            return (
-              <View style={[styles.sectionHeader, !isFirstSection && styles.followingSectionHeader]}>
-                <AppText accessibilityRole="header" style={styles.sectionTitle}>{sectionTitle(item.section, t)}</AppText>
-                <AppText style={[styles.sectionCount, { color: theme.color.muted }]}>{item.count}</AppText>
-              </View>
-            );
-          }
-
+        renderItem={({ item, index: feedIndex }) => {
           if (item.kind === 'trade') {
             return (
-              <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+              <View style={[styles.conceptDeck, feedIndex === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
                 <TradeFeedIdeaCard
                   ideaKey={item.ideaKey}
                   onOpenIdea={(ideaKey) => navigation.navigate('TradeIdeaDetail', { ideaId: ideaKey })}
@@ -274,11 +291,11 @@ export function ExploreScreen() {
 
           if (item.kind === 'plan') {
             return (
-              <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+              <View style={[styles.conceptDeck, feedIndex === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
                 <PlanSquareDeck
                   plan={item.preview}
-                  index={item.index}
-                  total={item.total}
+                  index={0}
+                  total={1}
                   onOpen={() => navigation.navigate('PlanIdeaDetail', { ideaId: item.ideaKey })}
                   topBadgeLabel={t('plans.deck.ideaBadge', { pack: item.idea.pack })}
                   topBadgeTone="plan"
@@ -293,7 +310,7 @@ export function ExploreScreen() {
             const template = item.template;
             const badgeLabel = t(isNeed ? 'common.exploreDiscovery.needBadge' : 'common.exploreDiscovery.offerBadge');
             return (
-              <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+              <View style={[styles.conceptDeck, feedIndex === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
                 <ExploreConceptSquareDeck
                   conceptId={`inventory-template-${template.id}`}
                   title={template.title}
@@ -311,7 +328,7 @@ export function ExploreScreen() {
 
           const badgeLabel = t('common.exploreDiscovery.placeBadge');
           return (
-            <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+            <View style={[styles.conceptDeck, feedIndex === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
               <ExploreConceptSquareDeck
                 conceptId={`hellowhen-place-${item.place.id}`}
                 title={item.place.title}
@@ -336,10 +353,6 @@ const styles = StyleSheet.create({
   headerRow: { minHeight: 44, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12 },
   title: { flex: 1, minWidth: 0, fontSize: 24, lineHeight: 30, fontWeight: '900', letterSpacing: -0.45 },
   content: { paddingTop: 8, paddingBottom: 44 },
-  sectionHeader: { flexDirection: 'row', alignItems: 'baseline', justifyContent: 'space-between', gap: 12 },
-  followingSectionHeader: { marginTop: 36 },
-  sectionTitle: { fontSize: 18, lineHeight: 23, fontWeight: '900', letterSpacing: -0.25 },
-  sectionCount: { fontSize: 12, lineHeight: 16, fontWeight: '800' },
   conceptDeck: { alignItems: 'center', justifyContent: 'center', overflow: 'visible' },
   firstConceptDeck: { marginTop: 14 },
   nextConceptDeck: { marginTop: MOBILE_DECK_FEED_GAP },
diff --git a/scripts/mobile-app-review-smoke.mjs b/scripts/mobile-app-review-smoke.mjs
index d37e42c..ca50f71 100644
--- a/scripts/mobile-app-review-smoke.mjs
+++ b/scripts/mobile-app-review-smoke.mjs
@@ -5,7 +5,13 @@ function runIphoneAndIpadShellChecks() {
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'moveIdea(', 'The submitted Explore surface must not use global previous/next Trade idea controls.');
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'movePlanIdea(', 'The submitted Explore surface must not use global previous/next Plan idea controls.');
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'deckControls', 'The submitted Explore surface must not restore global concept navigation buttons.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AppText accessibilityRole="header" style={styles.sectionTitle}>', 'Explore discovery sections must remain semantic headings.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'section'", 'The submitted Explore surface must be one balanced mixed discovery feed rather than five grouped catalogs.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "const EXPLORE_MIX_KIND_ORDER: readonly ExploreConceptKind[] = ['trade', 'place', 'need', 'plan', 'offer']", 'The submitted Explore surface must keep all five discovery concept types in the balanced mix.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'function buildBalancedMixedFeed(', 'The submitted Explore surface must use controlled balanced interleaving.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'const discoveryMixSeed = useRef(Date.now()).current;', 'The submitted Explore ordering must stay stable while the discovery screen is mounted.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'if (previousKind && cycleKinds.length > 1 && cycleKinds[0] === previousKind)', 'The submitted Explore feed must avoid adjacent same-type concepts while alternatives remain.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'Math.random(', 'The submitted Explore feed must not rely on uncontrolled pure randomness.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'index={0}\n                  total={1}', 'The submitted mixed Plan idea decks must not expose old section-wide counters.');
   assertContains('packages/shared/src/appNavigation.ts', "icon: 'compass',\n    mobileTabName: 'ExploreTab'", 'The submitted Explore tab must use the dedicated compass icon.');
   assertContains('apps/mobile/src/components/MobileIcon.tsx', "case 'compass':", 'The submitted binary must keep the dedicated Explore compass glyph.');
   assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AccountHeaderActionButton', 'Explore must use the shared primary Account control.');
diff --git a/scripts/mobile-launch-smoke.mjs b/scripts/mobile-launch-smoke.mjs
index 6574692..4816d2d 100644
--- a/scripts/mobile-launch-smoke.mjs
+++ b/scripts/mobile-launch-smoke.mjs
@@ -6,7 +6,13 @@ function runNavigationChecks() {
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'moveIdea(', 'Explore must not use global previous/next controls to switch Trade concepts.');
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'movePlanIdea(', 'Explore must not use global previous/next controls to switch Plan concepts.');
   assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'deckControls', 'Explore must not reintroduce global concept navigation buttons.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AppText accessibilityRole="header" style={styles.sectionTitle}>', 'Explore discovery sections must remain semantic headings.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'section'", 'Explore must be one mixed discovery feed rather than grouped Trade/Plan/Need/Offer/Place sections.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "const EXPLORE_MIX_KIND_ORDER: readonly ExploreConceptKind[] = ['trade', 'place', 'need', 'plan', 'offer']", 'Explore must define one balanced mix across all five discovery concept types.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'function buildBalancedMixedFeed(', 'Explore must compose discovery through the balanced mixed-feed helper.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'const discoveryMixSeed = useRef(Date.now()).current;', 'Explore mixed ordering must stay stable for the mounted discovery session.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'if (previousKind && cycleKinds.length > 1 && cycleKinds[0] === previousKind)', 'Explore must avoid adjacent same-type concepts while alternative types are available.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'Math.random(', 'Explore must not use uncontrolled pure random ordering that can create long same-type runs or rerender churn.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'index={0}\n                  total={1}', 'Mixed Explore Plan concepts must not show a section-wide idea counter.');
   assertContains('packages/shared/src/appNavigation.ts', "NormalMobileAppNavIcon = NormalAppNavIcon | 'search' | 'compass'", 'The mobile navigation contract must support the Explore compass icon without changing web navigation.');
   assertContains('packages/shared/src/appNavigation.ts', "icon: 'compass',\n    mobileTabName: 'ExploreTab'", 'Explore must use the compass icon rather than the search icon.');
   assertContains('apps/mobile/src/components/MobileIcon.tsx', "case 'compass':", 'The native icon system must provide a dedicated compass glyph for Explore.');
