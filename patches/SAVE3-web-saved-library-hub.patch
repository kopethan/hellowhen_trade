diff --git a/apps/web/src/app/account/saved/page.tsx b/apps/web/src/app/account/saved/page.tsx
new file mode 100644
index 0000000..ab8bf84
--- /dev/null
+++ b/apps/web/src/app/account/saved/page.tsx
@@ -0,0 +1,16 @@
+import { MobilePage } from '../../../components/MobilePage';
+import { TranslatedPageIntro } from '../../../components/TranslatedPageIntro';
+import { SavedLibraryClient } from '../../../features/account/SavedLibraryClient';
+
+export default function AccountSavedPage() {
+  return (
+    <MobilePage>
+      <TranslatedPageIntro
+        eyebrowKey="account.eyebrow"
+        titleKey="account.saved.title"
+        bodyKey="account.saved.body"
+      />
+      <SavedLibraryClient />
+    </MobilePage>
+  );
+}
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 073b086..81c6e3c 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -202,6 +202,10 @@ h3 { font-size: 17px; line-height: 1.2; letter-spacing: -0.02em; margin-bottom:
 .button { display: inline-flex; align-items: center; justify-content: center; gap: 8px; min-height: 42px; border: 1px solid var(--app-border-strong); border-radius: 999px; background: var(--app-surface); color: var(--app-text); padding: 0 15px; font-weight: 900; }
 .button.primary { background: var(--app-text); border-color: var(--app-text); color: var(--app-bg); }
 .button.secondary { background: var(--app-surface-soft); color: var(--app-text); border-color: var(--app-border-strong); }
+.ghost-button { display: inline-flex; align-items: center; justify-content: center; gap: 8px; min-height: 42px; border: 1px solid transparent; border-radius: 999px; background: transparent; color: var(--app-text-muted); padding: 0 15px; font: inherit; font-weight: 900; cursor: pointer; }
+.ghost-button:hover { background: var(--app-surface-soft); color: var(--app-text); }
+.ghost-button:disabled { cursor: not-allowed; opacity: 0.62; }
+.ghost-button.danger-button { color: var(--danger-text); }
 .button.full { width: 100%; }
 .cta-row { display: flex; flex-wrap: wrap; gap: 10px; }
 .eyebrow, .badge { color: var(--app-text-soft); font-size: 11px; font-weight: 900; letter-spacing: .1em; line-height: 1.1; text-transform: uppercase; }
@@ -7957,3 +7961,42 @@ html[data-theme="dark"] .onboarding-guide-primary {
     width: 100%;
   }
 }
+
+.saved-library-panel { display: grid; gap: 14px; }
+.saved-library-summary-card { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 13px; align-items: start; border: 1px solid color-mix(in srgb, var(--proposal-border) 42%, var(--app-border)); border-radius: 28px; background: linear-gradient(135deg, color-mix(in srgb, var(--proposal-bg) 18%, var(--app-surface)), var(--app-surface)); padding: 16px; box-shadow: var(--app-card-shadow); }
+.saved-library-summary-card__icon { display: grid; place-items: center; width: 46px; height: 46px; border: 1px solid color-mix(in srgb, var(--proposal-border) 42%, var(--app-border)); border-radius: 18px; background: var(--app-surface); color: var(--app-text); box-shadow: var(--app-control-shadow); }
+.saved-library-summary-card h3, .saved-library-summary-card p { margin: 7px 0 0; }
+.saved-library-summary-card p { color: var(--app-text-muted); font-size: 13px; font-weight: 800; line-height: 1.45; }
+.saved-library-toolbar { display: grid; gap: 10px; border: 1px solid color-mix(in srgb, var(--app-border) 78%, transparent); border-radius: 24px; background: color-mix(in srgb, var(--app-surface) 90%, var(--app-surface-soft)); padding: 10px; box-shadow: var(--app-control-shadow); }
+.saved-library-tabs, .saved-library-collections-strip { display: flex; gap: 8px; overflow-x: auto; margin-inline: -2px; padding: 0 2px 2px; scrollbar-width: none; }
+.saved-library-tabs::-webkit-scrollbar, .saved-library-collections-strip::-webkit-scrollbar { display: none; }
+.saved-library-tabs button, .saved-library-collections-strip button { display: inline-flex; flex: 0 0 auto; align-items: center; justify-content: center; gap: 8px; min-height: 38px; max-width: min(220px, 70vw); border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface); color: var(--app-text-muted); padding: 0 13px; font-size: 12px; font-weight: 950; cursor: pointer; box-shadow: none; }
+.saved-library-tabs button.is-active, .saved-library-collections-strip button.is-active { border-color: var(--app-text); background: var(--app-text); color: var(--app-bg); }
+.saved-library-collections-strip button span { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+.saved-library-collections-strip button strong { display: grid; place-items: center; min-width: 23px; height: 23px; border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text); padding: 0 7px; font-size: 11px; }
+.saved-library-collections-strip button.is-active strong { background: color-mix(in srgb, var(--app-bg) 92%, transparent); }
+.saved-library-selected-collection { display: grid; gap: 8px; border: 1px solid color-mix(in srgb, var(--instruction-border) 44%, var(--app-border)); border-radius: 22px; background: color-mix(in srgb, var(--instruction-bg) 18%, var(--app-surface)); padding: 13px; }
+.saved-library-selected-collection h3, .saved-library-selected-collection p { margin: 0; }
+.saved-library-selected-collection p { color: var(--app-text-muted); font-size: 13px; font-weight: 800; line-height: 1.45; }
+.saved-library-list { display: grid; gap: 12px; }
+.saved-library-card { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 14px; align-items: start; border: 1px solid var(--app-border); border-radius: 26px; background: var(--app-surface); padding: 15px; box-shadow: var(--app-card-shadow); }
+.saved-library-card__main { display: grid; gap: 8px; min-width: 0; }
+.saved-library-card__main h3 { margin: 0; overflow: hidden; color: var(--app-text); font-size: 17px; line-height: 1.18; text-overflow: ellipsis; white-space: nowrap; }
+.saved-library-card__main p { display: -webkit-box; margin: 0; overflow: hidden; color: var(--app-text-muted); font-size: 13px; font-weight: 750; line-height: 1.45; -webkit-line-clamp: 2; -webkit-box-orient: vertical; }
+.saved-library-card__meta { display: flex; flex-wrap: wrap; gap: 7px; color: var(--app-text-soft); font-size: 11px; font-weight: 850; }
+.saved-library-card__collections { display: flex; flex-wrap: wrap; gap: 6px; }
+.saved-library-card__collections span { display: inline-flex; align-items: center; min-height: 26px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text-muted); padding: 0 9px; font-size: 11px; font-weight: 900; }
+.saved-library-card__actions { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: 8px; }
+.saved-library-card__actions .button, .saved-library-card__actions .ghost-button { min-height: 36px; padding: 0 12px; font-size: 12px; }
+.saved-library-empty-state { display: grid; place-items: center; gap: 9px; min-height: 250px; border: 2px dashed var(--app-border-strong); border-radius: 30px; background: var(--app-surface-soft); padding: 24px; text-align: center; }
+.saved-library-empty-state .web-icon { color: var(--app-text-soft); }
+.saved-library-empty-state h3, .saved-library-empty-state p { margin: 0; }
+.saved-library-empty-state p { max-width: 340px; color: var(--app-text-muted); font-weight: 800; line-height: 1.45; }
+.saved-library-collections-preview { display: grid; gap: 7px; }
+.saved-library-collections-preview h3, .saved-library-collections-preview p { margin: 0; }
+
+@media (max-width: 560px) {
+  .saved-library-summary-card, .saved-library-card { grid-template-columns: 1fr; }
+  .saved-library-card__actions { justify-content: stretch; }
+  .saved-library-card__actions .button, .saved-library-card__actions .ghost-button { flex: 1 1 120px; }
+}
diff --git a/apps/web/src/features/account/AccountHubClient.tsx b/apps/web/src/features/account/AccountHubClient.tsx
index 4df1a28..3c2bee7 100644
--- a/apps/web/src/features/account/AccountHubClient.tsx
+++ b/apps/web/src/features/account/AccountHubClient.tsx
@@ -30,6 +30,7 @@ export function AccountHubClient() {
 
   const accountItems = useMemo<AccountHubItem[]>(() => [
     { href: '/account/profile', titleKey: 'account.items.profile.title', bodyKey: 'account.items.profile.body', icon: 'profile' },
+    { href: '/account/saved', titleKey: 'account.items.saved.title', bodyKey: 'account.items.saved.body', icon: 'save' },
     { href: '/account/notifications', titleKey: 'account.items.notifications.title', bodyKey: 'account.items.notifications.body', icon: 'bell' },
     {
       href: '/onboarding-guide?replay=1&next=/account',
diff --git a/apps/web/src/features/account/SavedLibraryClient.tsx b/apps/web/src/features/account/SavedLibraryClient.tsx
new file mode 100644
index 0000000..0b43f0e
--- /dev/null
+++ b/apps/web/src/features/account/SavedLibraryClient.tsx
@@ -0,0 +1,300 @@
+'use client';
+
+import Link from 'next/link';
+import { useEffect, useMemo, useState } from 'react';
+import type { SavedCollectionDto, SavedItemDto, SavedItemType } from '@hellowhen/contracts';
+import { WebIcon } from '../../components/WebIcon';
+import { api } from '../../lib/api';
+import { getFriendlyApiErrorMessage } from '../../lib/webErrors';
+import { useWebAuth } from '../../providers/WebAuthProvider';
+import { useWebTranslation } from '../../providers/WebI18nProvider';
+import { formatDateTime } from './accountPresentation';
+
+type SavedFilter = 'all' | SavedItemType;
+
+type SavedTab = {
+  value: SavedFilter;
+  labelKey: string;
+};
+
+const savedTabs: SavedTab[] = [
+  { value: 'all', labelKey: 'account.saved.filters.all' },
+  { value: 'trade', labelKey: 'account.saved.filters.trades' },
+  { value: 'need', labelKey: 'account.saved.filters.needs' },
+  { value: 'offer', labelKey: 'account.saved.filters.offers' },
+  { value: 'user', labelKey: 'account.saved.filters.people' },
+];
+
+function itemTargetId(item: SavedItemDto) {
+  return item.tradeId ?? item.needId ?? item.offerId ?? item.targetUserId ?? '';
+}
+
+function itemHref(item: SavedItemDto) {
+  switch (item.itemType) {
+    case 'trade':
+      return item.tradeId ? `/trades/${item.tradeId}` : '';
+    case 'need':
+      return item.needId ? `/needs/${item.needId}` : '';
+    case 'offer':
+      return item.offerId ? `/offers/${item.offerId}` : '';
+    case 'user':
+      return item.targetUserId ? `/users/${item.targetUserId}` : '';
+  }
+}
+
+function itemTitle(item: SavedItemDto, t: (key: string) => string) {
+  switch (item.itemType) {
+    case 'trade':
+      return item.trade?.title ?? t('account.saved.unavailable.trade');
+    case 'need':
+      return item.need?.title ?? t('account.saved.unavailable.need');
+    case 'offer':
+      return item.offer?.title ?? t('account.saved.unavailable.offer');
+    case 'user': {
+      const profile = item.targetUser?.profile;
+      return profile?.displayName ?? (profile?.handle ? `@${profile.handle}` : t('account.saved.unavailable.user'));
+    }
+  }
+}
+
+function itemBody(item: SavedItemDto, t: (key: string) => string) {
+  switch (item.itemType) {
+    case 'trade':
+      return item.trade?.description || item.trade?.status || t('account.saved.itemBodies.trade');
+    case 'need':
+      return item.need?.description || t('account.saved.itemBodies.need');
+    case 'offer':
+      return item.offer?.description || t('account.saved.itemBodies.offer');
+    case 'user': {
+      const profile = item.targetUser?.profile;
+      if (profile?.handle) return `@${profile.handle}`;
+      if (profile?.countryCode) return profile.countryCode;
+      return t('account.saved.itemBodies.user');
+    }
+  }
+}
+
+function itemTypeClass(itemType: SavedItemType) {
+  if (itemType === 'need') return 'semantic-badge need';
+  if (itemType === 'offer') return 'semantic-badge offer';
+  if (itemType === 'trade') return 'semantic-badge trade';
+  return 'semantic-badge instruction';
+}
+
+function collectionItemCount(collection: SavedCollectionDto) {
+  return Array.isArray(collection.items) ? collection.items.length : 0;
+}
+
+export function SavedLibraryClient() {
+  const auth = useWebAuth();
+  const { t, language } = useWebTranslation();
+  const [filter, setFilter] = useState<SavedFilter>('all');
+  const [selectedCollectionId, setSelectedCollectionId] = useState<string | null>(null);
+  const [items, setItems] = useState<SavedItemDto[]>([]);
+  const [collections, setCollections] = useState<SavedCollectionDto[]>([]);
+  const [nextCursor, setNextCursor] = useState<string | null>(null);
+  const [loading, setLoading] = useState(true);
+  const [loadingMore, setLoadingMore] = useState(false);
+  const [updatingId, setUpdatingId] = useState<string | null>(null);
+  const [error, setError] = useState<string | null>(null);
+
+  const selectedCollection = useMemo(
+    () => collections.find((collection) => collection.id === selectedCollectionId) ?? null,
+    [collections, selectedCollectionId],
+  );
+
+  async function load({ append = false, cursor }: { append?: boolean; cursor?: string } = {}) {
+    if (!auth.hydrated || !auth.isAuthenticated) {
+      setLoading(false);
+      return;
+    }
+
+    if (append) {
+      setLoadingMore(true);
+    } else {
+      setLoading(true);
+    }
+    setError(null);
+
+    try {
+      const [savedResponse, collectionsResponse] = await Promise.all([
+        api.saved.list({
+          take: 50,
+          ...(filter !== 'all' ? { itemType: filter } : {}),
+          ...(selectedCollectionId ? { collectionId: selectedCollectionId } : {}),
+          ...(cursor ? { cursor } : {}),
+        }),
+        api.saved.collections(),
+      ]);
+      setItems((current) => append ? [...current, ...(savedResponse.items ?? [])] : (savedResponse.items ?? []));
+      setNextCursor(savedResponse.nextCursor ?? null);
+      setCollections(collectionsResponse.collections ?? []);
+    } catch (err) {
+      setError(getFriendlyApiErrorMessage(err, t('account.saved.loadError')));
+      if (!append) {
+        setItems([]);
+        setNextCursor(null);
+      }
+    } finally {
+      setLoading(false);
+      setLoadingMore(false);
+    }
+  }
+
+  useEffect(() => { void load(); }, [auth.hydrated, auth.isAuthenticated, filter, selectedCollectionId]);
+
+  async function removeSavedItem(savedItemId: string) {
+    setUpdatingId(savedItemId);
+    setError(null);
+    try {
+      await api.saved.remove(savedItemId);
+      setItems((current) => current.filter((item) => item.id !== savedItemId));
+      setCollections((current) => current.map((collection) => ({
+        ...collection,
+        items: collection.items?.filter((item) => item.savedItemId !== savedItemId),
+      })));
+    } catch (err) {
+      setError(getFriendlyApiErrorMessage(err, t('account.saved.removeError')));
+    } finally {
+      setUpdatingId(null);
+    }
+  }
+
+  if (!auth.hydrated || loading) {
+    return <section className="mobile-card mobile-card--soft"><p>{t('account.saved.loading')}</p></section>;
+  }
+
+  if (!auth.isAuthenticated) {
+    return (
+      <section className="mobile-card mobile-card--soft">
+        <span className="semantic-badge instruction">{t('common.states.signedOut')}</span>
+        <h3>{t('account.saved.signedOutTitle')}</h3>
+        <p>{t('account.saved.signedOutBody')}</p>
+        <Link href="/auth?next=/account/saved" className="button primary">{t('common.actions.loginOrRegister')}</Link>
+      </section>
+    );
+  }
+
+  const hasItems = items.length > 0;
+  const hasCollections = collections.length > 0;
+
+  return (
+    <div className="saved-library-panel">
+      <section className="saved-library-summary-card">
+        <div className="saved-library-summary-card__icon" aria-hidden="true">
+          <WebIcon name="save" size={24} decorative />
+        </div>
+        <div>
+          <span className="semantic-badge proposal">{t('account.saved.privateBadge')}</span>
+          <h3>{t('account.saved.summaryTitle')}</h3>
+          <p>{t('account.saved.summaryBody')}</p>
+        </div>
+      </section>
+
+      <section className="saved-library-toolbar" aria-label={t('account.saved.filtersLabel')}>
+        <div className="saved-library-tabs" role="tablist" aria-label={t('account.saved.filtersLabel')}>
+          {savedTabs.map((tab) => (
+            <button
+              key={tab.value}
+              type="button"
+              className={filter === tab.value ? 'is-active' : ''}
+              aria-pressed={filter === tab.value}
+              onClick={() => { setFilter(tab.value); setNextCursor(null); }}
+            >
+              {t(tab.labelKey)}
+            </button>
+          ))}
+        </div>
+
+        <div className="saved-library-collections-strip" aria-label={t('account.saved.collectionsLabel')}>
+          <button
+            type="button"
+            className={!selectedCollectionId ? 'is-active' : ''}
+            aria-pressed={!selectedCollectionId}
+            onClick={() => { setSelectedCollectionId(null); setNextCursor(null); }}
+          >
+            <span>{t('account.saved.collections.allSaved')}</span>
+          </button>
+          {collections.map((collection) => (
+            <button
+              key={collection.id}
+              type="button"
+              className={selectedCollectionId === collection.id ? 'is-active' : ''}
+              aria-pressed={selectedCollectionId === collection.id}
+              onClick={() => { setSelectedCollectionId(collection.id); setNextCursor(null); }}
+            >
+              <span>{collection.title}</span>
+              <strong>{collectionItemCount(collection)}</strong>
+            </button>
+          ))}
+        </div>
+      </section>
+
+      {error ? <p className="form-error">{error}</p> : null}
+
+      {selectedCollection ? (
+        <section className="saved-library-selected-collection">
+          <span className="semantic-badge instruction">{t('account.saved.collections.collection')}</span>
+          <div>
+            <h3>{selectedCollection.title}</h3>
+            <p>{selectedCollection.description || t('account.saved.collections.defaultBody')}</p>
+          </div>
+        </section>
+      ) : null}
+
+      {!hasItems ? (
+        <section className="saved-library-empty-state">
+          <WebIcon name="save" size={32} decorative />
+          <h3>{selectedCollection ? t('account.saved.emptyCollectionTitle') : t('account.saved.emptyTitle')}</h3>
+          <p>{selectedCollection ? t('account.saved.emptyCollectionBody') : t('account.saved.emptyBody')}</p>
+          <Link href="/trades" className="button primary">{t('account.saved.browseTrades')}</Link>
+        </section>
+      ) : (
+        <div className="saved-library-list">
+          {items.map((item) => {
+            const href = itemHref(item);
+            const targetId = itemTargetId(item);
+            return (
+              <article key={item.id} className="saved-library-card">
+                <div className="saved-library-card__main">
+                  <span className={itemTypeClass(item.itemType)}>{t(`account.saved.types.${item.itemType}`)}</span>
+                  <h3>{itemTitle(item, t)}</h3>
+                  <p>{itemBody(item, t)}</p>
+                  <div className="saved-library-card__meta">
+                    <span>{t('account.saved.savedAt', { date: formatDateTime(item.createdAt, language) })}</span>
+                    {targetId ? <span>{t('account.saved.targetId', { id: targetId.slice(0, 8) })}</span> : null}
+                  </div>
+                  {item.collections?.length ? (
+                    <div className="saved-library-card__collections" aria-label={t('account.saved.collectionsLabel')}>
+                      {item.collections.map((collection) => <span key={collection.id}>{collection.title}</span>)}
+                    </div>
+                  ) : null}
+                </div>
+                <div className="saved-library-card__actions">
+                  {href ? <Link href={href} className="button secondary">{t('common.actions.open')}</Link> : null}
+                  <button className="ghost-button" type="button" disabled={updatingId === item.id} onClick={() => { void removeSavedItem(item.id); }}>
+                    {updatingId === item.id ? t('common.states.working') : t('common.actions.remove')}
+                  </button>
+                </div>
+              </article>
+            );
+          })}
+        </div>
+      )}
+
+      {nextCursor ? (
+        <button className="button secondary full" type="button" disabled={loadingMore} onClick={() => { void load({ append: true, cursor: nextCursor }); }}>
+          {loadingMore ? t('common.states.loading') : t('account.saved.loadMore')}
+        </button>
+      ) : null}
+
+      {!hasCollections ? (
+        <section className="mobile-card mobile-card--soft saved-library-collections-preview">
+          <span className="semantic-badge instruction">{t('account.saved.collections.title')}</span>
+          <h3>{t('account.saved.collections.emptyTitle')}</h3>
+          <p>{t('account.saved.collections.emptyBody')}</p>
+        </section>
+      ) : null}
+    </div>
+  );
+}
diff --git a/packages/i18n/src/locales/en/account.ts b/packages/i18n/src/locales/en/account.ts
index cafb6c0..440c446 100644
--- a/packages/i18n/src/locales/en/account.ts
+++ b/packages/i18n/src/locales/en/account.ts
@@ -394,6 +394,61 @@ export const account = {
       trade_status_updated: '{{tradeTitle}} was {{statusLabel}}.',
     },
   },
+  saved: {
+    title: 'Saved Library',
+    body: 'Private saved trades, needs, offers, people, and collections live here.',
+    loading: 'Loading your saved library...',
+    loadError: 'Could not load your saved library.',
+    removeError: 'Could not remove this saved item.',
+    signedOutTitle: 'Sign in to open Saved Library',
+    signedOutBody: 'Saved trades, people, and collections are private to your account.',
+    privateBadge: 'Private',
+    summaryTitle: 'Your private saved space',
+    summaryBody: 'Saving does not notify anyone and does not create a follow or subscription relationship.',
+    filtersLabel: 'Saved Library filters',
+    collectionsLabel: 'Saved collections',
+    emptyTitle: 'Nothing saved yet',
+    emptyBody: 'Save public trades, needs, offers, or people to revisit them later.',
+    emptyCollectionTitle: 'This collection is empty',
+    emptyCollectionBody: 'Items added to this collection will appear here.',
+    browseTrades: 'Browse trades',
+    loadMore: 'Load more saved items',
+    savedAt: 'Saved {{date}}',
+    targetId: 'ID {{id}}',
+    filters: {
+      all: 'All',
+      trades: 'Trades',
+      needs: 'Needs',
+      offers: 'Offers',
+      people: 'People',
+    },
+    types: {
+      trade: 'Trade',
+      need: 'Need',
+      offer: 'Offer',
+      user: 'Person',
+    },
+    itemBodies: {
+      trade: 'Saved trade',
+      need: 'Saved need',
+      offer: 'Saved offer',
+      user: 'Saved person',
+    },
+    unavailable: {
+      trade: 'Unavailable trade',
+      need: 'Unavailable need',
+      offer: 'Unavailable offer',
+      user: 'Unavailable person',
+    },
+    collections: {
+      title: 'Collections',
+      allSaved: 'All saved',
+      collection: 'Collection',
+      defaultBody: 'Saved items grouped privately by you.',
+      emptyTitle: 'Collections arrive next',
+      emptyBody: 'This hub can already read collections. Creating and managing saved collections comes in SAVE6.',
+    },
+  },
   items: {
     profile: {
       title: 'Profile',
@@ -425,6 +480,12 @@ export const account = {
       bodyNative: 'Earnings, Stripe demo setup, and payout history.',
       badge: 'Payout',
     },
+    saved: {
+      title: 'Saved Library',
+      body: 'Private trades, needs, offers, people, and collections saved for later.',
+      bodyNative: 'Private saved trades, offers, needs, and people.',
+      badge: 'Saved',
+    },
     notifications: {
       title: 'Notifications',
       body: 'Proposal decisions, private replies, and updates related to your trades.',
diff --git a/packages/i18n/src/locales/fr/account.ts b/packages/i18n/src/locales/fr/account.ts
index 7dcedd6..c0af755 100644
--- a/packages/i18n/src/locales/fr/account.ts
+++ b/packages/i18n/src/locales/fr/account.ts
@@ -394,6 +394,61 @@ export const account = {
       trade_status_updated: '{{tradeTitle}} a été {{statusLabel}}.',
     },
   },
+  saved: {
+    title: 'Enregistrés',
+    body: 'Échanges, besoins, offres, personnes et collections enregistrés en privé.',
+    loading: 'Chargement de vos éléments enregistrés…',
+    loadError: 'Impossible de charger vos éléments enregistrés.',
+    removeError: 'Impossible de supprimer cet élément enregistré.',
+    signedOutTitle: 'Connectez-vous pour ouvrir vos Enregistrés',
+    signedOutBody: 'Les échanges, personnes et collections enregistrés restent privés sur votre compte.',
+    privateBadge: 'Privé',
+    summaryTitle: 'Votre espace privé enregistré',
+    summaryBody: 'Enregistrer ne prévient personne et ne crée pas de relation d’abonnement ou de suivi.',
+    filtersLabel: 'Filtres des éléments enregistrés',
+    collectionsLabel: 'Collections enregistrées',
+    emptyTitle: 'Rien d’enregistré pour le moment',
+    emptyBody: 'Enregistrez des échanges, besoins, offres ou personnes publics pour les retrouver plus tard.',
+    emptyCollectionTitle: 'Cette collection est vide',
+    emptyCollectionBody: 'Les éléments ajoutés à cette collection apparaîtront ici.',
+    browseTrades: 'Parcourir les échanges',
+    loadMore: 'Charger plus d’éléments enregistrés',
+    savedAt: 'Enregistré {{date}}',
+    targetId: 'ID {{id}}',
+    filters: {
+      all: 'Tout',
+      trades: 'Échanges',
+      needs: 'Besoins',
+      offers: 'Offres',
+      people: 'Personnes',
+    },
+    types: {
+      trade: 'Échange',
+      need: 'Besoin',
+      offer: 'Offre',
+      user: 'Personne',
+    },
+    itemBodies: {
+      trade: 'Échange enregistré',
+      need: 'Besoin enregistré',
+      offer: 'Offre enregistrée',
+      user: 'Personne enregistrée',
+    },
+    unavailable: {
+      trade: 'Échange indisponible',
+      need: 'Besoin indisponible',
+      offer: 'Offre indisponible',
+      user: 'Personne indisponible',
+    },
+    collections: {
+      title: 'Collections',
+      allSaved: 'Tous les enregistrés',
+      collection: 'Collection',
+      defaultBody: 'Éléments enregistrés regroupés en privé par vous.',
+      emptyTitle: 'Les collections arrivent ensuite',
+      emptyBody: 'Ce hub peut déjà lire les collections. La création et gestion des collections enregistrées arrivera dans SAVE6.',
+    },
+  },
   items: {
     profile: {
       title: 'Profil',
@@ -425,6 +480,12 @@ export const account = {
       bodyNative: 'Gains, configuration Stripe démo et historique des versements.',
       badge: 'Versement',
     },
+    saved: {
+      title: 'Enregistrés',
+      body: 'Échanges, besoins, offres, personnes et collections gardés en privé pour plus tard.',
+      bodyNative: 'Échanges, offres, besoins et personnes enregistrés en privé.',
+      badge: 'Enregistrés',
+    },
     notifications: {
       title: 'Notifications',
       body: 'Décisions de proposition, réponses privées et mises à jour liées à vos échanges.',
