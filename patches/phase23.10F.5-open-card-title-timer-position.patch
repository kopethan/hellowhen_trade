diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 390a8c8..d3599d9 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -763,14 +763,14 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 
 .trade-poster-card h2 {
   display: -webkit-box;
-  margin: 0 0 -0.06em;
-  padding-bottom: 0.1em;
+  margin: 0 0 -0.04em;
+  padding-bottom: 0.11em;
   overflow: hidden;
   color: #fff;
-  font-size: clamp(20px, 5.6vw, 26px);
+  font-size: clamp(18px, 4.85vw, 23px);
   font-weight: 950;
-  letter-spacing: -0.052em;
-  line-height: 1.16;
+  letter-spacing: -0.045em;
+  line-height: 1.19;
   text-shadow: 0 2px 18px rgba(0, 0, 0, 0.36);
   -webkit-box-orient: vertical;
   -webkit-line-clamp: 2;
@@ -807,7 +807,8 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-poster-card__footer {
   display: flex;
   align-items: center;
-  margin-top: 4px;
+  margin-top: 2px;
+  margin-bottom: 3px;
 }
 
 .trade-poster-card__countdown,
@@ -821,6 +822,7 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
   letter-spacing: .07em;
   line-height: 1.1;
   text-shadow: 0 1px 12px rgba(0, 0, 0, 0.34);
+  transform: translateY(-2px);
 }
 
 .trade-poster-card__countdown strong {
@@ -831,7 +833,7 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-poster-card__countdown.is-normal { color: #ef4444; }
 .trade-poster-card__countdown.is-soon { color: #dc2626; }
 .trade-poster-card__countdown.is-urgent { color: #b91c1c; }
-.trade-poster-card__expiry.is-none { color: rgba(255, 255, 255, 0.76); }
+.trade-poster-card__expiry.is-none { color: rgba(255, 255, 255, 0.88); }
 .trade-poster-card__expiry.is-expired { color: #b91c1c; }
 
 .trade-poster-card.has-fallback .trade-poster-card__countdown,
@@ -840,7 +842,7 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 }
 
 .trade-poster-card.has-fallback .trade-poster-card__expiry.is-none {
-  color: rgba(15, 23, 42, 0.58);
+  color: rgba(255, 255, 255, 0.74);
 }
 
 .trade-poster-card.has-fallback .trade-poster-card__countdown.is-normal { color: #ef4444; }
diff --git a/apps/web/src/features/trade/TradePosterCard.tsx b/apps/web/src/features/trade/TradePosterCard.tsx
index 5d393e2..52aaf86 100644
--- a/apps/web/src/features/trade/TradePosterCard.tsx
+++ b/apps/web/src/features/trade/TradePosterCard.tsx
@@ -91,6 +91,7 @@ export function TradePosterCard({ id, imageUrl, imageAlt, badge, eyebrow, title,
           {eyebrow ? <p className="trade-poster-card__eyebrow">{eyebrow}</p> : null}
           <h2>{title}</h2>
           {subtitle ? <p className="trade-poster-card__subtitle">{subtitle}</p> : null}
+          {footer ? <div className="trade-poster-card__footer">{footer}</div> : null}
           {visibleChips.length ? (
             <div className="trade-poster-card__chips" aria-label="Trade tags">
               {visibleChips.map((chip) => (
@@ -98,7 +99,6 @@ export function TradePosterCard({ id, imageUrl, imageAlt, badge, eyebrow, title,
               ))}
             </div>
           ) : null}
-          {footer ? <div className="trade-poster-card__footer">{footer}</div> : null}
         </div>
       </div>
     </article>
