diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index f173702..7a59201 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -1004,10 +1004,9 @@ html[data-theme="dark"] .trade-poster-card.has-fallback h2 {
 }
 
 @media (max-width: 759px) {
-  .square-stack-deck { padding: 10px var(--deck-mobile-stack-right-pad, 16px) var(--deck-mobile-stack-bottom-pad, 34px) 16px; }
+  .square-stack-deck { padding: 10px 24px 30px 0; }
   .square-stack-deck__control { display: none !important; width: 42px; height: 42px; }
   .square-stack-deck__control--prev { top: -13px; left: -13px; }
-  .square-stack-deck__dots { right: var(--deck-mobile-stack-right-pad, 16px); left: 16px; bottom: 12px; }
   .square-stack-deck--native-mobile .square-stack-deck__surface { touch-action: none; overscroll-behavior: contain; }
   .square-stack-deck--native-mobile.square-stack-deck--single-card:not(.square-stack-deck--scroll-locked) .square-stack-deck__surface { touch-action: pan-y; overscroll-behavior: auto; }
 }
diff --git a/apps/web/src/features/deck/SquareStackDeck.tsx b/apps/web/src/features/deck/SquareStackDeck.tsx
index 4c045dc..db1c1af 100644
--- a/apps/web/src/features/deck/SquareStackDeck.tsx
+++ b/apps/web/src/features/deck/SquareStackDeck.tsx
@@ -215,14 +215,10 @@ export function SquareStackDeck({ items, label, className, onOpen, lockScrollWit
   const maxVisibleBackLayers = Math.max(1, VISIBLE_LAYERS - 1);
   const visibleBackLayers = Math.min(Math.max(itemCount - activeIndex - 1, 0), maxVisibleBackLayers);
   const advanceControlOutset = 2 + Math.round(10 * (visibleBackLayers / maxVisibleBackLayers));
-  const mobileStackRightPad = 16 + visibleBackLayers * 6;
-  const mobileStackBottomPad = itemCount > 1 ? 34 + Math.min(visibleBackLayers, 2) * 3 : 18;
   const deckStyle = {
     '--deck-visible-back-layers': String(visibleBackLayers),
     '--deck-max-back-layers': String(maxVisibleBackLayers),
     '--deck-advance-control-outset': `${advanceControlOutset}px`,
-    '--deck-mobile-stack-right-pad': `${mobileStackRightPad}px`,
-    '--deck-mobile-stack-bottom-pad': `${mobileStackBottomPad}px`,
   } as CSSProperties;
 
   const commit = useCallback((direction: 'next' | 'prev') => {
