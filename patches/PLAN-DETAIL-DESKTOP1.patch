diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index c74573b..57a0719 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -9812,6 +9812,13 @@ html[data-theme="dark"] .onboarding-guide-primary {
   display: grid;
   gap: 0;
 }
+.plan-route-shell {
+  display: grid;
+  gap: 14px;
+}
+.plan-route-desktop-preview {
+  display: none;
+}
 .plan-route-stop {
   display: grid;
   grid-template-columns: 34px minmax(0, 1fr);
@@ -10091,6 +10098,151 @@ html[data-theme="dark"] .onboarding-guide-primary {
   height: 100%;
   object-fit: cover;
 }
+.plan-route-desktop-preview {
+  align-self: start;
+  gap: 12px;
+  border: 1px solid var(--app-border);
+  border-radius: 30px;
+  background:
+    radial-gradient(circle at 72% 6%, color-mix(in srgb, var(--place-text) 13%, transparent), transparent 36%),
+    color-mix(in srgb, var(--app-surface) 94%, var(--place-text) 6%);
+  box-shadow: var(--app-card-shadow);
+  padding: 12px;
+}
+.plan-route-desktop-preview__media {
+  display: grid;
+  place-items: center;
+  overflow: hidden;
+  width: 100%;
+  aspect-ratio: 4 / 3;
+  border: 1px solid color-mix(in srgb, var(--place-text) 14%, var(--app-border));
+  border-radius: 24px;
+  background: color-mix(in srgb, var(--place-text) 7%, var(--app-surface-soft));
+  color: var(--place-text);
+}
+.plan-route-desktop-preview__media img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  display: block;
+}
+.plan-route-desktop-preview__copy {
+  display: grid;
+  gap: 5px;
+  padding: 0 4px;
+}
+.plan-route-desktop-preview__copy .eyebrow {
+  margin: 0;
+}
+.plan-route-desktop-preview__copy h3 {
+  margin: 0;
+  color: var(--app-text);
+  font-size: 24px;
+  line-height: 1.04;
+  letter-spacing: -0.045em;
+}
+.plan-route-desktop-preview__copy p,
+.plan-route-desktop-preview__copy small,
+.plan-route-desktop-preview__copy span {
+  margin: 0;
+  color: var(--app-text-muted);
+  font-size: 13px;
+  line-height: 1.4;
+  font-weight: 800;
+}
+.plan-route-desktop-preview__copy small {
+  display: -webkit-box;
+  overflow: hidden;
+  -webkit-box-orient: vertical;
+  -webkit-line-clamp: 2;
+}
+.plan-route-desktop-preview__copy span {
+  display: -webkit-box;
+  overflow: hidden;
+  -webkit-box-orient: vertical;
+  -webkit-line-clamp: 3;
+}
+.plan-route-desktop-preview__stops {
+  display: grid;
+  gap: 0;
+  overflow: hidden;
+  border: 1px solid var(--app-divider);
+  border-radius: 22px;
+  background: color-mix(in srgb, var(--app-surface) 82%, transparent);
+}
+.plan-route-desktop-preview__stop {
+  display: grid;
+  grid-template-columns: 28px minmax(0, 1fr);
+  align-items: start;
+  gap: 9px;
+  padding: 10px;
+  border-top: 1px solid var(--app-divider);
+}
+.plan-route-desktop-preview__stop:first-child {
+  border-top: 0;
+}
+.plan-route-desktop-preview__stop > span {
+  display: grid;
+  place-items: center;
+  width: 26px;
+  height: 26px;
+  border-radius: 999px;
+  background: color-mix(in srgb, var(--place-text) 9%, var(--app-surface));
+  color: var(--place-text);
+  font-size: 11px;
+  font-weight: 950;
+}
+.plan-route-desktop-preview__stop div {
+  display: grid;
+  min-width: 0;
+  gap: 3px;
+}
+.plan-route-desktop-preview__stop strong {
+  overflow: hidden;
+  color: var(--app-text);
+  font-size: 13px;
+  font-weight: 950;
+  line-height: 1.2;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+.plan-route-desktop-preview__stop small {
+  overflow: hidden;
+  color: var(--app-text-muted);
+  font-size: 11px;
+  font-weight: 850;
+  line-height: 1.25;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+.plan-route-desktop-preview__stop.is-active {
+  background: color-mix(in srgb, var(--place-text) 7%, transparent);
+}
+.plan-route-desktop-preview__stop.is-active > span {
+  background: var(--place-bg);
+  color: var(--place-text);
+  box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--place-text) 22%, transparent);
+}
+@media (min-width: 940px) {
+  .plan-route-section--desktop-split {
+    gap: 16px;
+  }
+  .plan-route-section--desktop-split .plan-route-shell {
+    grid-template-columns: minmax(0, 1fr) minmax(300px, 36%);
+    align-items: start;
+    gap: 18px;
+  }
+  .plan-route-section--desktop-split .plan-route-desktop-preview {
+    position: sticky;
+    top: 86px;
+    display: grid;
+  }
+  .plan-route-section--desktop-split .plan-route-stop__media {
+    width: 100%;
+    max-width: 440px;
+  }
+}
+
 .plan-detail-list {
   display: grid;
   gap: 0;
@@ -10200,6 +10352,8 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .plan-detail-toolbar__actions .plans-internal-badge { display: none; }
   .plan-detail-hero-social h1 { max-width: 12ch; font-size: clamp(38px, 14vw, 58px); }
   .plan-route-section { gap: 17px; }
+  .plan-route-shell { display: block; }
+  .plan-route-desktop-preview { display: none; }
   .plan-route-list { gap: 0; }
   .plan-route-stop {
     position: relative;
diff --git a/apps/web/src/features/plans/PlanDetailClient.tsx b/apps/web/src/features/plans/PlanDetailClient.tsx
index 5777c2a..6cec7c5 100644
--- a/apps/web/src/features/plans/PlanDetailClient.tsx
+++ b/apps/web/src/features/plans/PlanDetailClient.tsx
@@ -48,6 +48,10 @@ function PlanPlaceImage({ media }: { media?: MediaAssetDto | null }) {
   return <img src={imageSrc} alt="" loading="lazy" />;
 }
 
+function planPlaceDisplayMedia(place: PlanPlaceDto) {
+  return place.media?.[0] ?? place.sourcePlace?.media?.[0] ?? null;
+}
+
 function detailStatusTone(status: PlanStatus) {
   if (status === 'open' || status === 'started') return 'success';
   if (status === 'cancelled' || status === 'expired' || status === 'hidden') return 'danger';
@@ -257,6 +261,54 @@ function PlanDetailItem({ label, value }: { label: string; value: string }) {
   );
 }
 
+function PlanRouteDesktopPreview({ places, planStartsAt }: { places: PlanPlaceDto[]; planStartsAt: string }) {
+  const themeMode = useResolvedPlaceVisualTheme();
+  const preview = useMemo(() => {
+    const entries = places.map((place) => {
+      const displayMedia = planPlaceDisplayMedia(place);
+      const visual = resolvePlaceVisual({ media: displayMedia, staticMap: place.staticMap ?? place.sourcePlace?.staticMap ?? null, themeMode });
+      return { place, displayMedia, visual };
+    });
+
+    return entries.find((entry) => entry.visual.url) ?? entries[0] ?? null;
+  }, [places, themeMode]);
+
+  if (!preview) return null;
+
+  const location = planPlaceLocation(preview.place);
+  const previewDescription = planPlaceDescription(preview.place);
+
+  return (
+    <aside className="plan-route-desktop-preview" aria-label="Route visual preview">
+      <div className="plan-route-desktop-preview__media">
+        {preview.visual.url ? (
+          preview.visual.kind === 'media' && preview.displayMedia ? <PlanPlaceImage media={preview.displayMedia} /> : <img src={preview.visual.url} alt="" loading="lazy" className="is-static-map" />
+        ) : (
+          <WebIcon name="location-on" size={34} decorative />
+        )}
+      </div>
+      <div className="plan-route-desktop-preview__copy">
+        <p className="eyebrow">Route preview</p>
+        <h3>{preview.place.title}</h3>
+        <p>{planPlaceTimeRange(preview.place, planStartsAt)}</p>
+        {location ? <small>{location.value}</small> : null}
+        {previewDescription ? <span>{previewDescription}</span> : null}
+      </div>
+      <div className="plan-route-desktop-preview__stops" aria-label="Plan route stops">
+        {places.map((place, index) => (
+          <div key={`route-preview-stop-${place.id}`} className={`plan-route-desktop-preview__stop${place.id === preview.place.id ? ' is-active' : ''}`}>
+            <span aria-hidden="true">{index + 1}</span>
+            <div>
+              <strong>{place.title}</strong>
+              <small>{planPlaceTimeRange(place, planStartsAt)} · {planPlaceModeDisplay(place)}</small>
+            </div>
+          </div>
+        ))}
+      </div>
+    </aside>
+  );
+}
+
 function PlanPlaceCard({
   place,
   index,
@@ -276,9 +328,7 @@ function PlanPlaceCard({
   presenceNotice?: PlanPlacePresenceNotice;
   onVerifyPresence: (place: PlanPlaceDto) => void;
 }) {
-  const media = place.media?.[0] ?? null;
-  const sourceMedia = place.sourcePlace?.media?.[0] ?? null;
-  const displayMedia = media ?? sourceMedia ?? null;
+  const displayMedia = planPlaceDisplayMedia(place);
   const themeMode = useResolvedPlaceVisualTheme();
   const placeVisual = resolvePlaceVisual({ media: displayMedia, staticMap: place.staticMap ?? place.sourcePlace?.staticMap ?? null, themeMode });
   const placeTime = planPlaceTimeRange(place, planStartsAt);
@@ -641,26 +691,29 @@ export function PlanDetailClient({ planId, plansEnabled, plansVisible }: PlanDet
               </div>
             </section>
 
-            <section className="plan-social-section plan-route-section">
+            <section className="plan-social-section plan-route-section plan-route-section--desktop-split">
               <div className="plan-section-heading">
                 <p className="eyebrow">Route</p>
                 <h2>Places and times</h2>
               </div>
-              <div className="plan-route-list">
-                {places.map((place, index) => (
-                  <PlanPlaceCard
-                    key={place.id}
-                    place={place}
-                    index={index}
-                    planStartsAt={plan.startsAt}
-                    showReport={showReportActions}
-                    canVerifyPresence={canVerifyPresence}
-                    isVerifyingPresence={verifyingPlaceId === place.id}
-                    presenceNotice={presenceNotices[place.id]}
-                    onVerifyPresence={(nextPlace) => { void verifyPlanPlacePresence(nextPlace); }}
-                  />
-                ))}
-                {places.length === 0 ? <p className="meta">No places added yet.</p> : null}
+              <div className="plan-route-shell">
+                <div className="plan-route-list">
+                  {places.map((place, index) => (
+                    <PlanPlaceCard
+                      key={place.id}
+                      place={place}
+                      index={index}
+                      planStartsAt={plan.startsAt}
+                      showReport={showReportActions}
+                      canVerifyPresence={canVerifyPresence}
+                      isVerifyingPresence={verifyingPlaceId === place.id}
+                      presenceNotice={presenceNotices[place.id]}
+                      onVerifyPresence={(nextPlace) => { void verifyPlanPlacePresence(nextPlace); }}
+                    />
+                  ))}
+                  {places.length === 0 ? <p className="meta">No places added yet.</p> : null}
+                </div>
+                <PlanRouteDesktopPreview places={places} planStartsAt={plan.startsAt} />
               </div>
             </section>
 
