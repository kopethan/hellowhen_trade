diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index ad98d1b..d849160 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -1865,16 +1865,71 @@ html[data-theme="dark"] .trade-poster-card.has-fallback h2 {
   .trade-activity-panel { padding: calc(26px + env(safe-area-inset-top)) 32px calc(30px + env(safe-area-inset-bottom)); }
   .trade-activity-panel > * { width: min(920px, 100%); }
   .feed-status-row { width: min(880px, 100%); margin: 0 auto; }
-  .trade-deck-grid, .plans-feed-deck-list { --trade-deck-max: 360px; --trade-deck-pad-left: 16px; --trade-deck-pad-right: 36px; --trade-deck-col: calc(var(--trade-deck-max) + var(--trade-deck-pad-left) + var(--trade-deck-pad-right)); display: grid; grid-template-columns: repeat(auto-fit, minmax(min(100%, var(--trade-deck-col)), var(--trade-deck-col))); justify-content: center; align-items: start; column-gap: clamp(22px, 3vw, 48px); row-gap: clamp(26px, 4vw, 56px); width: 100%; margin: 2px auto 0; padding: 6px 0 24px; }
-  .plans-feed-page .plans-feed-deck-list { gap: clamp(26px, 4vw, 56px); }
-  .plans-feed-deck-list .plan-deck-link { width: 100%; max-width: var(--trade-deck-col); }
+  .trade-feed-ideas { width: 100%; max-width: 100%; }
+  .trade-feed-page,
+  .plans-feed-page,
+  .trade-feed-ideas {
+    --feed-deck-visible: 360px;
+    --feed-deck-pad-left: 16px;
+    --feed-deck-pad-right: 36px;
+    --feed-deck-pad-bottom: 38px;
+    --feed-deck-column: calc(var(--feed-deck-visible) + var(--feed-deck-pad-left) + var(--feed-deck-pad-right));
+    --feed-deck-column-gap: clamp(22px, 3vw, 48px);
+    --feed-deck-row-gap: clamp(26px, 4vw, 56px);
+  }
+  .trade-deck-grid,
+  .plans-feed-deck-list,
+  .trade-feed-ideas__list {
+    display: grid;
+    grid-template-columns: repeat(auto-fit, minmax(min(100%, var(--feed-deck-column)), var(--feed-deck-column)));
+    justify-content: center;
+    align-items: start;
+    column-gap: var(--feed-deck-column-gap);
+    row-gap: var(--feed-deck-row-gap);
+    width: 100%;
+    margin: 2px auto 0;
+    padding: 6px 0 24px;
+  }
+  .plans-feed-page .plans-feed-deck-list { gap: var(--feed-deck-row-gap) var(--feed-deck-column-gap); }
+  .plans-feed-deck-list .plan-deck-link,
+  .trade-stack-deck,
+  .trade-feed-inline-idea,
+  .trade-feed-ideas__list > .trade-feed-idea-card {
+    width: 100%;
+    max-width: var(--feed-deck-column);
+  }
   .plans-feed-deck-list .plan-deck-link__meta { margin: 0 16px 0; }
-  .trade-stack-deck { width: 100%; max-width: var(--trade-deck-col); padding: 16px 36px 38px 16px; }
+  .trade-stack-deck,
+  .trade-feed-inline-idea,
+  .trade-feed-ideas__list > .trade-feed-idea-card {
+    padding: var(--feed-deck-pad-left) var(--feed-deck-pad-right) var(--feed-deck-pad-bottom) var(--feed-deck-pad-left);
+  }
+  .trade-stack-deck { max-width: var(--feed-deck-column); }
+  .trade-feed-ideas__list > .trade-feed-idea-card {
+    aspect-ratio: auto;
+    overflow: visible;
+    border: 0;
+    border-radius: 0;
+    background: transparent;
+    box-shadow: none;
+  }
+  .trade-feed-ideas__list > .trade-feed-idea-card .trade-feed-idea-card__open-link,
+  .trade-feed-ideas__list > .trade-feed-idea-card .trade-feed-idea-card__poster-link {
+    aspect-ratio: 1 / 1;
+    overflow: hidden;
+    border: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent);
+    border-radius: 34px;
+    background: var(--app-surface);
+    box-shadow: var(--app-card-shadow-strong);
+  }
+  .trade-feed-ideas__list > .trade-feed-idea-card--poster .trade-feed-idea-card__poster-link { border: 0; }
   .trade-stack-card__body h2 { font-size: clamp(23px, 1.85vw, 27px); }
 }
 
 @media (min-width: 1180px) {
-  .trade-deck-grid, .plans-feed-deck-list { --trade-deck-max: 370px; }
+  .trade-feed-page,
+  .plans-feed-page,
+  .trade-feed-ideas { --feed-deck-visible: 370px; }
 }
 
 @media (max-width: 759px) {
@@ -8993,12 +9048,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 
 @media (min-width: 760px) {
-  .trade-feed-inline-idea {
-    align-self: start;
-    width: 100%;
-    max-width: var(--trade-deck-col);
-    padding: 16px 36px 38px 16px;
-  }
+  .trade-feed-inline-idea { align-self: start; }
 
   .trade-feed-idea-card--inline {
     width: 100%;
@@ -9010,7 +9060,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .trade-feed-idea-card__side span { font-size: 11.5px; }
 }
 
-@media (max-width: 860px) {
+@media (max-width: 759px) {
   .trade-feed-ideas__list {
     grid-template-columns: minmax(0, min(360px, 100%));
   }
