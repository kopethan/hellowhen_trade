diff --git a/apps/api/prisma/migrations/20260907143000_plan_custom_stops/migration.sql b/apps/api/prisma/migrations/20260907143000_plan_custom_stops/migration.sql
new file mode 100644
index 0000000..97ed136
--- /dev/null
+++ b/apps/api/prisma/migrations/20260907143000_plan_custom_stops/migration.sql
@@ -0,0 +1,6 @@
+CREATE TYPE "PlanPlaceKind" AS ENUM ('place', 'pause', 'free_time', 'meeting_point', 'custom');
+
+ALTER TABLE "PlanPlace"
+ADD COLUMN "kind" "PlanPlaceKind" NOT NULL DEFAULT 'place';
+
+CREATE INDEX "PlanPlace_kind_idx" ON "PlanPlace"("kind");
diff --git a/apps/api/prisma/schema.prisma b/apps/api/prisma/schema.prisma
index dbb29aa..1114729 100644
--- a/apps/api/prisma/schema.prisma
+++ b/apps/api/prisma/schema.prisma
@@ -720,6 +720,14 @@ enum PlanPlaceMode {
   remote
 }
 
+enum PlanPlaceKind {
+  place
+  pause
+  free_time
+  meeting_point
+  custom
+}
+
 enum PlaceSource {
   user
   hellowhen_library
@@ -2792,6 +2800,7 @@ model PlanPlace {
   placeId            String?
   order              Int           @default(0)
   mode               PlanPlaceMode @default(local)
+  kind               PlanPlaceKind @default(place)
   title              String
   note               String?
   addressPublicText  String?
@@ -2820,6 +2829,7 @@ model PlanPlace {
   @@index([planId, order])
   @@index([placeId])
   @@index([mode])
+  @@index([kind])
   @@index([startsAt])
   @@index([googlePlaceId])
   @@index([latitude, longitude])
diff --git a/apps/api/src/modules/plans/__tests__/plans.safety.test.ts b/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
index 7cd73a7..e12cb37 100644
--- a/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
+++ b/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
@@ -126,6 +126,21 @@ test('generated Plan descriptions never persist viewer-formatted start times', (
   assert.equal(display.description.includes('Starts'), false);
 });
 
+test('generated Plan descriptions call mixed Place/custom-stop routes stops', () => {
+  const display = buildGeneratedPlanDisplay({
+    places: [
+      { kind: 'place', title: 'Pont Alexandre III', mode: 'local' },
+      { kind: 'pause', title: 'Pause', mode: 'local' },
+      { kind: 'place', title: 'Parc des Buttes-Chaumont', mode: 'local' },
+    ],
+    mode: 'local',
+    joinApprovalMode: 'automatic',
+  });
+
+  assert.equal(display.title, 'Pont Alexandre III → Parc des Buttes-Chaumont');
+  assert.deepEqual(display.summaryParts, ['3 stops', 'Local plan', 'Free join']);
+});
+
 test('legacy generated Plan descriptions drop only the frozen start-time segment', () => {
   assert.equal(
     stripLegacyGeneratedPlanStartTime('1 place · Starts Jan 1, 1:00 PM · Local plan · Free join'),
diff --git a/apps/api/src/modules/plans/plans.routes.ts b/apps/api/src/modules/plans/plans.routes.ts
index 96acded..6a3ecd4 100644
--- a/apps/api/src/modules/plans/plans.routes.ts
+++ b/apps/api/src/modules/plans/plans.routes.ts
@@ -536,6 +536,7 @@ async function loadPlanPlaceForPresenceVerification(planId: string, planPlaceId:
           id: true,
           placeId: true,
           mode: true,
+          kind: true,
           title: true,
           latitude: true,
           longitude: true,
@@ -812,7 +813,8 @@ function planUpdateData(input: ReturnType<typeof updatePlanRequestSchema.parse>)
   };
 }
 
-function hasPlanPlaceStaticMapCandidate(input: { mode?: string | null; latitude?: number | null; longitude?: number | null }, reusablePlace?: any | null) {
+function hasPlanPlaceStaticMapCandidate(input: { kind?: string | null; mode?: string | null; latitude?: number | null; longitude?: number | null }, reusablePlace?: any | null) {
+  if ((input.kind ?? 'place') !== 'place') return false;
   const mode = input.mode ?? reusablePlace?.mode ?? PLACE_OFFLINE_MODE;
   if (mode === PLACE_ONLINE_MODE) return false;
   if (typeof input.latitude === 'number' && typeof input.longitude === 'number') return true;
@@ -820,6 +822,7 @@ function hasPlanPlaceStaticMapCandidate(input: { mode?: string | null; latitude?
 }
 
 function assertPlanPlaceAddressPolicy(input: any) {
+  if ((input.kind ?? 'place') !== 'place') return;
   const mode = input.mode ?? PLACE_OFFLINE_MODE;
   if (mode === PLACE_ONLINE_MODE) {
     const missing = getMissingOnlineDestinationFields(input);
@@ -835,7 +838,7 @@ function assertPlanPlaceAddressPolicy(input: any) {
   }
 }
 
-function planPlaceStaticMapTemplateSnapshot(input: { mediaIds?: string[]; mode?: string | null; latitude?: number | null; longitude?: number | null }, reusablePlace?: any | null) {
+function planPlaceStaticMapTemplateSnapshot(input: { mediaIds?: string[]; kind?: string | null; mode?: string | null; latitude?: number | null; longitude?: number | null }, reusablePlace?: any | null) {
   if (reusablePlace?.staticMapTemplateFamily) {
     return {
       staticMapTemplateFamily: reusablePlace.staticMapTemplateFamily,
@@ -861,28 +864,31 @@ async function loadReusablePlaceForSnapshot(placeId: string, userId: string) {
 }
 
 function planPlaceSnapshotData(planId: string, input: ReturnType<typeof createPlanPlaceRequestSchema.parse>, fallbackOrder = 0, reusablePlace?: any | null) {
-  const mode = input.mode ?? reusablePlace?.mode ?? PLACE_OFFLINE_MODE;
-  const isOnline = mode === PLACE_ONLINE_MODE;
+  const kind = input.kind ?? 'place';
+  const isCustomStop = kind !== 'place';
+  const mode = isCustomStop ? PLACE_OFFLINE_MODE : input.mode ?? reusablePlace?.mode ?? PLACE_OFFLINE_MODE;
+  const isOnline = !isCustomStop && mode === PLACE_ONLINE_MODE;
   return {
     planId,
-    placeId: reusablePlace?.id ?? input.placeId ?? null,
+    placeId: isCustomStop ? null : reusablePlace?.id ?? input.placeId ?? null,
     order: input.order ?? fallbackOrder,
     mode,
+    kind,
     title: input.title ?? reusablePlace?.title,
     note: input.note ?? null,
-    addressPublicText: isOnline ? null : input.formattedAddress ?? reusablePlace?.formattedAddress ?? null,
-    addressPrivateText: isOnline ? null : input.addressPrivateText ?? reusablePlace?.addressPrivateText ?? null,
-    googlePlaceId: isOnline ? null : input.googlePlaceId ?? reusablePlace?.googlePlaceId ?? null,
-    googlePlaceName: isOnline ? null : input.googlePlaceName ?? reusablePlace?.googlePlaceName ?? null,
-    formattedAddress: isOnline ? null : input.formattedAddress ?? reusablePlace?.formattedAddress ?? null,
-    googleMapsUri: isOnline ? null : input.googleMapsUri ?? reusablePlace?.googleMapsUri ?? null,
-    latitude: isOnline ? null : input.latitude ?? reusablePlace?.latitude ?? null,
-    longitude: isOnline ? null : input.longitude ?? reusablePlace?.longitude ?? null,
-    locationSource: isOnline ? null : input.locationSource ?? reusablePlace?.locationSource ?? null,
-    addressValidationStatus: isOnline ? null : input.addressValidationStatus ?? reusablePlace?.addressValidationStatus ?? null,
+    addressPublicText: isCustomStop || isOnline ? null : input.formattedAddress ?? reusablePlace?.formattedAddress ?? null,
+    addressPrivateText: isCustomStop || isOnline ? null : input.addressPrivateText ?? reusablePlace?.addressPrivateText ?? null,
+    googlePlaceId: isCustomStop || isOnline ? null : input.googlePlaceId ?? reusablePlace?.googlePlaceId ?? null,
+    googlePlaceName: isCustomStop || isOnline ? null : input.googlePlaceName ?? reusablePlace?.googlePlaceName ?? null,
+    formattedAddress: isCustomStop || isOnline ? null : input.formattedAddress ?? reusablePlace?.formattedAddress ?? null,
+    googleMapsUri: isCustomStop || isOnline ? null : input.googleMapsUri ?? reusablePlace?.googleMapsUri ?? null,
+    latitude: isCustomStop || isOnline ? null : input.latitude ?? reusablePlace?.latitude ?? null,
+    longitude: isCustomStop || isOnline ? null : input.longitude ?? reusablePlace?.longitude ?? null,
+    locationSource: isCustomStop || isOnline ? null : input.locationSource ?? reusablePlace?.locationSource ?? null,
+    addressValidationStatus: isCustomStop || isOnline ? null : input.addressValidationStatus ?? reusablePlace?.addressValidationStatus ?? null,
     onlineLabel: isOnline ? input.onlineLabel ?? reusablePlace?.onlineLabel ?? null : null,
     onlineUrl: isOnline ? normalizedOnlineUrl(input.onlineUrl ?? reusablePlace?.onlineUrl) : null,
-    ...planPlaceStaticMapTemplateSnapshot(input, reusablePlace),
+    ...planPlaceStaticMapTemplateSnapshot({ ...input, kind }, reusablePlace),
     startsAt: input.startsAt ? new Date(input.startsAt) : null,
     endsAt: input.endsAt ? new Date(input.endsAt) : null,
   };
@@ -1230,6 +1236,9 @@ plansRoutes.post('/:planId/places/:placeId/verify-presence', requireAuth, requir
   }
 
   const { plan, place } = loaded;
+  if ((place.kind ?? 'place') !== 'place') {
+    return res.status(409).json({ error: 'not_verifiable_place', message: 'Custom Plan stops do not use location presence verification.' });
+  }
   if (place.mode !== 'local') {
     return res.status(409).json({ error: 'not_offline_place', message: 'Only offline places can be verified with device location.' });
   }
diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 4e0355c..91a269f 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -7,7 +7,7 @@ import { useFocusEffect, useNavigation } from '@react-navigation/native';
 import { KeyboardAwareScrollView } from 'react-native-keyboard-controller';
 import { useSafeAreaInsets } from 'react-native-safe-area-context';
 import type { NativeStackNavigationProp, NativeStackScreenProps } from '@react-navigation/native-stack';
-import { GOOGLE_PLACE_SEARCH_MIN_QUERY_LENGTH, type DiscoveryLanguage, type GooglePlacePrediction, type GoogleResolvedPlace, type InventoryTranslationDto, type ListPlansQuery, type ListPlacesQuery, type MediaAssetDto, type PlaceDto, type PlacePresenceVerificationResponse, type PlaceStaticMapDto, type PlanDto, type PlanParticipantDto, type PlanPlaceDto, type PlanPlaceMode } from '@hellowhen/contracts';
+import { GOOGLE_PLACE_SEARCH_MIN_QUERY_LENGTH, type DiscoveryLanguage, type GooglePlacePrediction, type GoogleResolvedPlace, type InventoryTranslationDto, type ListPlansQuery, type ListPlacesQuery, type MediaAssetDto, type PlaceDto, type PlacePresenceVerificationResponse, type PlaceStaticMapDto, type PlanDto, type PlanParticipantDto, type PlanPlaceDto, type PlanPlaceKind, type PlanPlaceMode } from '@hellowhen/contracts';
 import { formatLocalizedDateTime, type SupportedLanguage, type TranslationValues } from '@hellowhen/i18n';
 import { buildEstimatedPlanPlaceEndTimes, estimateFinalPlanPlaceEndTime, buildGeneratedPlanDisplay, buildPlanFeedItems, effectivePlanJoinClosesAt, isPlanJoinClosed, getOnlinePlaceProviderMetadata, hasConfirmedProviderOfflineAddress, hasOnlineDestination, mergeRecentStarterPlanIdeaIds, parseStarterPlanIdeaKey, resolveInventoryOriginalCopy, PLACE_ADDRESS_CONFIRMED_STATUS, PLACE_ADDRESS_PROVIDER_SOURCE, PLAN_MIN_STOP_START_GAP_MINUTES, selectStarterPlanIdeaKeys, starterPlanIdeas, starterPlanIdeaMode, starterPlanIdeaRequirementCounts, starterPlanIdeaStopDestinationPrompt, starterPlanIdeaStopRequirementLabel, type PlaceProviderAddressInput, type StarterPlanIdea, type StarterPlanIdeaKey, type StarterPlanIdeaStop } from '@hellowhen/shared';
 import { AppFixedHeaderScreen } from '../../components/AppFixedHeaderScreen';
@@ -246,11 +246,22 @@ function getOwnerName(plan: PlanDto, t?: Translate) {
   return plan.owner?.profile?.displayName || plan.owner?.profile?.handle || (t ? t('plans.common.member') : 'Hellowhen member');
 }
 
+function planRouteUnitLabel(places: readonly { kind?: PlanPlaceKind | null }[], t: Translate) {
+  const count = places.length;
+  const hasCustomStops = places.some((place) => (place.kind ?? 'place') !== 'place');
+  return t(hasCustomStops ? (count === 1 ? 'plans.row.stopOne' : 'plans.row.stopMany') : (count === 1 ? 'plans.row.placeOne' : 'plans.row.placeMany'), { count });
+}
+
 function getPlanMeta(plan: PlanDto, t?: Translate) {
-  const placeCount = plan.places?.length ?? 0;
+  const planPlaces = plan.places ?? [];
+  const placeCount = planPlaces.length;
   const participantCount = plan.participantCount ?? plan.participants?.filter((participant) => participant.status === 'accepted').length ?? 0;
-  if (!t) return `${placeCount} ${placeCount === 1 ? 'place' : 'places'} · ${participantCount} joined`;
-  const places = t(placeCount === 1 ? 'plans.row.placeOne' : 'plans.row.placeMany', { count: placeCount });
+  if (!t) {
+    const hasCustomStops = planPlaces.some((place) => (place.kind ?? 'place') !== 'place');
+    const unit = hasCustomStops ? (placeCount === 1 ? 'stop' : 'stops') : (placeCount === 1 ? 'place' : 'places');
+    return `${placeCount} ${unit} · ${participantCount} joined`;
+  }
+  const places = planRouteUnitLabel(planPlaces, t);
   const joined = t('plans.row.joined', { count: participantCount });
   return t('plans.row.meta', { places, joined });
 }
@@ -336,6 +347,7 @@ function selectedPlaceFromPlanIdeaStop(stop: StarterPlanIdeaStop, index: number,
   return {
     id: `mobile-plan-idea-place-${Date.now()}-${index}`,
     sourcePlaceSource: 'custom',
+    kind: 'place',
     mode: stop.mode,
     date,
     time: stop.time,
@@ -353,6 +365,7 @@ function selectedPlaceFromPlanIdeaStop(stop: StarterPlanIdeaStop, index: number,
 
 type SelectedPlanPlaceState = {
   id: string;
+  kind: PlanPlaceKind;
   sourcePlaceId?: string;
   sourcePlaceSource?: 'custom' | 'my_place' | 'hellowhen_library';
   sourcePlaceTitle?: string;
@@ -412,6 +425,7 @@ const PLAN_CREATE_DRAFT_TTL_MS = 7 * 24 * 60 * 60 * 1000;
 
 type PlanCreateDraftPlaceState = {
   id?: string;
+  kind?: PlanPlaceKind;
   sourcePlaceId?: string;
   sourcePlaceSource?: 'custom' | 'my_place' | 'hellowhen_library';
   sourcePlaceTitle?: string;
@@ -466,6 +480,7 @@ function makeSelectedPlanPlace(index: number, date = toDateInputValue(), time?:
   return {
     id: `mobile-plan-place-${Date.now()}-${index}`,
     sourcePlaceSource: 'custom',
+    kind: 'place',
     mode: 'local',
     date,
     time: time ?? (index === 0 ? '13:00' : ''),
@@ -484,6 +499,7 @@ function createPlanDraftPlaceFromSelected(place: SelectedPlanPlaceState): PlanCr
     sourcePlaceId: place.sourcePlaceId,
     sourcePlaceSource: place.sourcePlaceSource,
     sourcePlaceTitle: place.sourcePlaceTitle,
+    kind: place.kind,
     mode: place.mode,
     date: place.date,
     time: place.time,
@@ -501,6 +517,7 @@ function selectedPlanPlaceFromDraft(place: PlanCreateDraftPlaceState, index: num
     sourcePlaceId: place.sourcePlaceId,
     sourcePlaceSource: place.sourcePlaceSource ?? 'custom',
     sourcePlaceTitle: place.sourcePlaceTitle,
+    kind: place.kind === 'pause' || place.kind === 'free_time' || place.kind === 'meeting_point' || place.kind === 'custom' ? place.kind : 'place',
     mode: place.mode === 'remote' ? 'remote' : 'local',
     date: place.date || toDateInputValue(),
     time: place.time || (index === 0 ? '13:00' : ''),
@@ -514,6 +531,38 @@ function selectedPlanPlaceFromDraft(place: PlanCreateDraftPlaceState, index: num
   };
 }
 
+
+type CustomPlanStopKind = Exclude<PlanPlaceKind, 'place'>;
+
+function customPlanStopTitle(kind: CustomPlanStopKind, t: PlanTranslationFunction) {
+  if (kind === 'pause') return t('plans.create.customStop.pause');
+  if (kind === 'free_time') return t('plans.create.customStop.freeTime');
+  if (kind === 'meeting_point') return t('plans.create.customStop.meetingPoint');
+  return t('plans.create.customStop.custom');
+}
+
+function makeSelectedPlanCustomStop(kind: CustomPlanStopKind, index: number, t: PlanTranslationFunction, date = toDateInputValue(), time?: string): SelectedPlanPlaceState {
+  return {
+    id: `mobile-plan-custom-stop-${Date.now()}-${index}`,
+    sourcePlaceSource: 'custom',
+    kind,
+    mode: 'local',
+    date,
+    time: time ?? (index === 0 ? '13:00' : ''),
+    title: kind === 'custom' ? '' : customPlanStopTitle(kind, t),
+    location: '',
+    providerAddress: null,
+    onlineLabel: '',
+    onlineUrl: '',
+    existingMedia: null,
+    existingStaticMap: null,
+  };
+}
+
+function isCustomPlanStop(place: Pick<SelectedPlanPlaceState, 'kind'> | Pick<PlanPlaceDto, 'kind'>) {
+  return (place.kind ?? 'place') !== 'place';
+}
+
 function makePlanJoinDeadlineState(): PlanJoinDeadlineState {
   return { preset: 'start', date: '', time: '' };
 }
@@ -904,6 +953,7 @@ function selectedPlaceFromReusable(place: PlaceDto, index: number, date = toDate
     sourcePlaceId: place.id,
     sourcePlaceSource: libraryPlaceSource(place),
     sourcePlaceTitle: place.title,
+    kind: 'place',
     mode: place.mode ?? 'local',
     date,
     time: time ?? (index === 0 ? '13:00' : ''),
@@ -994,6 +1044,7 @@ function resetSelectedPlaceToCustom(place: SelectedPlanPlaceState): SelectedPlan
     sourcePlaceId: undefined,
     sourcePlaceSource: 'custom',
     sourcePlaceTitle: undefined,
+    kind: 'place',
     providerAddress: place.sourcePlaceId ? null : place.providerAddress,
     existingMedia: null,
     existingStaticMap: null,
@@ -1015,6 +1066,19 @@ function parseLocalDateTime(dateValue: string, timeValue: string) {
   return date;
 }
 
+function planStopChronologyWarning(places: SelectedPlanPlaceState[], index: number, t: PlanTranslationFunction) {
+  if (index <= 0) return '';
+  const previous = places[index - 1];
+  const current = places[index];
+  if (!previous || !current) return '';
+  const previousDateTime = parseLocalDateTime(previous.date, previous.time);
+  const currentDateTime = parseLocalDateTime(current.date, current.time);
+  if (!previousDateTime || !currentDateTime) return '';
+  const minimum = previousDateTime.getTime() + PLAN_MIN_STOP_START_GAP_MINUTES * 60_000;
+  if (currentDateTime.getTime() >= minimum) return '';
+  return t('plans.create.validation.stopOrderInline', { minutes: PLAN_MIN_STOP_START_GAP_MINUTES });
+}
+
 function addDaysToDate(date: Date, days: number) {
   const next = new Date(date);
   next.setDate(next.getDate() + days);
@@ -1359,18 +1423,21 @@ function mobilePlanEndSummary(
 }
 
 function planModeFromSelectedPlaces(places: SelectedPlanPlaceState[]) {
-  const modes = new Set(places.map((place) => place.mode));
+  const actualPlaces = places.filter((place) => !isCustomPlanStop(place));
+  const modes = new Set(actualPlaces.map((place) => place.mode));
   if (modes.size > 1) return 'hybrid' as const;
   return modes.has('remote') ? 'remote' as const : 'local' as const;
 }
 
 function placePreviewLocation(place: SelectedPlanPlaceState, t?: PlanTranslationFunction) {
+  if (isCustomPlanStop(place)) return t?.('plans.create.customStop.noAddressRequired') ?? 'Custom stop · no address required';
   if (place.mode === 'remote') return place.onlineLabel.trim() || place.onlineUrl.trim() || place.location.trim() || (t?.('plans.create.place.onlinePlace') ?? 'Online place');
   return place.location.trim() || (t?.('plans.create.place.offlinePlace') ?? 'Offline place');
 }
 
 
 function getPlanPlaceAddressRuleError(place: SelectedPlanPlaceState, index: number, t?: PlanTranslationFunction) {
+  if (isCustomPlanStop(place)) return '';
   const label = t?.('plans.create.place.label', { index: index + 1 }) ?? `Place ${index + 1}`;
   if (place.mode === 'remote') {
     const onlineError = getOnlineDestinationRequirementMessage(place, t);
@@ -1393,7 +1460,7 @@ function getPlanPlacesAddressRuleError(places: SelectedPlanPlaceState[], t?: Pla
 function incompleteOfflinePlanPlaceIndexes(places: SelectedPlanPlaceState[]) {
   return places.reduce<number[]>((indexes, place, index) => {
     const hasContent = Boolean(place.title.trim() || place.sourcePlaceId || place.sourcePlaceTitle?.trim());
-    if (hasContent && place.mode === 'local' && !hasValidOfflineProviderAddress(place.providerAddress)) indexes.push(index);
+    if (hasContent && !isCustomPlanStop(place) && place.mode === 'local' && !hasValidOfflineProviderAddress(place.providerAddress)) indexes.push(index);
     return indexes;
   }, []);
 }
@@ -1705,6 +1772,7 @@ function buildPlatformMapsUrl(value: string) {
 }
 
 function getPlanPlaceLocationDetails(place: PlanPlaceDto, t: PlanTranslationFunction): PlanPlaceLocationDetails | null {
+  if ((place.kind ?? 'place') !== 'place') return null;
   if (place.mode === 'remote') {
     const value = place.onlineUrl || place.onlineLabel || null;
     if (!value) return null;
@@ -1728,6 +1796,11 @@ function getPlanPlaceLocationDetails(place: PlanPlaceDto, t: PlanTranslationFunc
 }
 
 function getPlanPlaceModeDisplay(place: PlanPlaceDto, t: PlanTranslationFunction) {
+  const kind = place.kind ?? 'place';
+  if (kind === 'pause') return t('plans.detail.customStop.pause');
+  if (kind === 'free_time') return t('plans.detail.customStop.freeTime');
+  if (kind === 'meeting_point') return t('plans.detail.customStop.meetingPoint');
+  if (kind === 'custom') return t('plans.detail.customStop.custom');
   return place.mode === 'remote' ? t('plans.detail.values.online') : t('plans.detail.values.local');
 }
 
@@ -1796,7 +1869,7 @@ function getPlanPlaceVerificationCoordinates(place: PlanPlaceDto) {
 }
 
 function isOfflinePlanPlace(place: PlanPlaceDto) {
-  return place.mode !== 'remote';
+  return (place.kind ?? 'place') === 'place' && place.mode !== 'remote';
 }
 
 function formatPresenceDistance(value: number | null | undefined, language: SupportedLanguage) {
@@ -2173,7 +2246,11 @@ function PlanList({ scope, navigation, filters = [], searchQuery = '', focusStar
         : hasActiveSearchOrFilters
           ? t('plans.list.empty.filtered')
           : t('plans.list.empty.feed');
-    return <EmptyBlock title={t('plans.list.empty.title')} body={body} actionLabel={scope === 'mine' ? t('plans.list.empty.create') : undefined} onAction={scope === 'mine' ? () => navigation.navigate('CreatePlan') : undefined} />;
+    const emptyBlock = <EmptyBlock title={t('plans.list.empty.title')} body={body} actionLabel={scope === 'mine' ? t('plans.list.empty.create') : undefined} onAction={scope === 'mine' ? () => navigation.navigate('CreatePlan') : undefined} />;
+    if (scope === 'feed' && !hasActiveSearchOrFilters) {
+      return <View style={styles.planFeedEmptyCenter}>{emptyBlock}</View>;
+    }
+    return emptyBlock;
   }
 
 
@@ -3044,7 +3121,7 @@ function PlanPlaceTimelineCard({
             </View>
           ) : null}
 
-          <PlanPlaceWeatherDetail planId={planId} place={place} />
+          {(place.kind ?? 'place') === 'place' ? <PlanPlaceWeatherDetail planId={planId} place={place} /> : null}
 
           {showPresenceVerification ? (
             <View style={[styles.planPresenceCompactRow, { borderColor: theme.color.border, backgroundColor: theme.color.surface }]}>
@@ -3355,7 +3432,7 @@ export function PlanDetailScreen({ route, navigation }: PlanDetailProps) {
             <View style={styles.planDetailChips}>
               <SemanticBadge label={isCancelled && isOwner ? t('plans.detail.actions.cancelledTitle') : presentationLabel} tone={isCancelled && isOwner ? 'danger' : getPlanPresentationTone(presentationState!)} size="sm" />
               <SemanticBadge label={getPlanJoinModeLabel(plan, t)} tone="proposal" size="sm" />
-              <SemanticBadge label={t(places.length === 1 ? 'plans.row.placeOne' : 'plans.row.placeMany', { count: places.length })} tone="place" size="sm" />
+              <SemanticBadge label={planRouteUnitLabel(places, t)} tone="place" size="sm" />
               <SemanticBadge label={getPlanModeLabel(plan, t)} tone="muted" size="sm" />
             </View>
             {plan.description ? <AppText style={[styles.planDetailDescription, { color: theme.color.text }]}>{plan.description}</AppText> : null}
@@ -3793,6 +3870,7 @@ function PlanPickerSheetFrame({
   children: React.ReactNode;
 }) {
   const theme = useThemeTokens();
+  const insets = useSafeAreaInsets();
   const { t } = useTranslation();
   if (!visible) return null;
 
@@ -3800,7 +3878,7 @@ function PlanPickerSheetFrame({
     <Modal visible={visible} transparent animationType="slide" onRequestClose={onClose}>
       <View style={styles.sourceSheetOverlay}>
         <Pressable accessibilityRole="button" accessibilityLabel={t('plans.create.time.closePicker')} onPress={onClose} style={styles.sourceSheetScrim} />
-        <View style={[styles.planPickerSheet, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
+        <View style={[styles.planPickerSheet, { backgroundColor: theme.color.surface, borderColor: theme.color.border, paddingBottom: Math.max(24, insets.bottom + 16) }]}>
           <View style={[styles.sourceSheetHandle, { backgroundColor: theme.color.border }]} />
           <View style={styles.sourceSheetTopbar}>
             <View style={styles.sourceSheetTitleBlock}>
@@ -4508,7 +4586,7 @@ function PlaceTimelineRow({ place, index, onPress, onDatePress, onTimePress }: {
       ) : null}
       <View style={styles.timelineCopy}>
         <PlanStopTimeCompactRow place={place} index={index} onDatePress={onDatePress} onTimePress={onTimePress} />
-        <AppText style={styles.rowTitle}>{place.title || place.sourcePlaceTitle || t('plans.create.place.label', { index: index + 1 })}</AppText>
+        <AppText style={styles.rowTitle}>{place.title || place.sourcePlaceTitle || t(isCustomPlanStop(place) ? 'plans.create.customStop.detailTitle' : 'plans.create.place.label', { index: index + 1 })}</AppText>
         <AppText style={[styles.metaText, { color: theme.color.muted }]} numberOfLines={2}>{meta}</AppText>
       </View>
       <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
@@ -4704,6 +4782,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
   const [clearDraftConfirmVisible, setClearDraftConfirmVisible] = useState(false);
   const [placeSourceSheetOpen, setPlaceSourceSheetOpen] = useState(false);
   const [placePickerOpen, setPlacePickerOpen] = useState(false);
+  const [customStopPickerOpen, setCustomStopPickerOpen] = useState(false);
   const [placeSourceTarget, setPlaceSourceTarget] = useState<PlaceSourceTarget>('new');
   const [detailPlaceIndex, setDetailPlaceIndex] = useState<number | null>(null);
   const [timeSheet, setTimeSheet] = useState<PlanStopTimeSheetState>(null);
@@ -4768,7 +4847,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     category: advancedDetails.category.trim() || null,
     tags: parsePlanTagsInput(advancedDetails.tags),
     mode: planModeFromSelectedPlaces(placesForGeneratedDisplay),
-    locationLabel: placesForGeneratedDisplay.length === 0 ? null : t(placesForGeneratedDisplay.length === 1 ? 'plans.row.placeOne' : 'plans.row.placeMany', { count: placesForGeneratedDisplay.length }),
+    locationLabel: placesForGeneratedDisplay.length === 0 ? null : planRouteUnitLabel(placesForGeneratedDisplay, t),
     startsAt: schedule.startsAt || new Date().toISOString(),
     joinClosesAt: joinDeadlineSummary.joinClosesAt || schedule.startsAt || new Date().toISOString(),
     endsAt: explicitPlanEnd.endsAt || schedule.endsAt || null,
@@ -4785,18 +4864,19 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
       source: place.sourcePlaceId ? (place.sourcePlaceSource === 'hellowhen_library' ? 'hellowhen_library' : 'my_place') : 'custom',
       order: index,
       mode: place.mode,
+      kind: place.kind,
       title: planPreviewPlaceTitle(place, index, t),
-      addressPublicText: place.mode === 'local' ? (place.providerAddress?.formattedAddress ?? place.location.trim()) || null : null,
-      googlePlaceId: place.mode === 'local' ? place.providerAddress?.googlePlaceId ?? null : null,
-      googlePlaceName: place.mode === 'local' ? place.providerAddress?.googlePlaceName ?? null : null,
-      formattedAddress: place.mode === 'local' ? place.providerAddress?.formattedAddress ?? null : null,
-      googleMapsUri: place.mode === 'local' ? place.providerAddress?.googleMapsUri ?? null : null,
-      latitude: place.mode === 'local' && typeof place.providerAddress?.latitude === 'number' ? place.providerAddress.latitude : null,
-      longitude: place.mode === 'local' && typeof place.providerAddress?.longitude === 'number' ? place.providerAddress.longitude : null,
-      locationSource: place.mode === 'local' ? place.providerAddress?.locationSource ?? null : null,
-      addressValidationStatus: place.mode === 'local' ? place.providerAddress?.addressValidationStatus ?? null : null,
-      onlineLabel: place.mode === 'remote' ? place.onlineLabel.trim() || place.location.trim() || null : null,
-      onlineUrl: place.mode === 'remote' ? place.onlineUrl.trim() || null : null,
+      addressPublicText: !isCustomPlanStop(place) && place.mode === 'local' ? (place.providerAddress?.formattedAddress ?? place.location.trim()) || null : null,
+      googlePlaceId: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.googlePlaceId ?? null : null,
+      googlePlaceName: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.googlePlaceName ?? null : null,
+      formattedAddress: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.formattedAddress ?? null : null,
+      googleMapsUri: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.googleMapsUri ?? null : null,
+      latitude: !isCustomPlanStop(place) && place.mode === 'local' && typeof place.providerAddress?.latitude === 'number' ? place.providerAddress.latitude : null,
+      longitude: !isCustomPlanStop(place) && place.mode === 'local' && typeof place.providerAddress?.longitude === 'number' ? place.providerAddress.longitude : null,
+      locationSource: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.locationSource ?? null : null,
+      addressValidationStatus: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.addressValidationStatus ?? null : null,
+      onlineLabel: !isCustomPlanStop(place) && place.mode === 'remote' ? place.onlineLabel.trim() || place.location.trim() || null : null,
+      onlineUrl: !isCustomPlanStop(place) && place.mode === 'remote' ? place.onlineUrl.trim() || null : null,
       startsAt: schedule.placeStartsAt[index] ?? null,
       endsAt: schedule.placeEndsAt[index] ?? null,
       createdAt: new Date().toISOString(),
@@ -4936,6 +5016,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     setStage('build');
     setPlaceSourceSheetOpen(false);
     setPlacePickerOpen(false);
+    setCustomStopPickerOpen(false);
     setPlaceSourceTarget('new');
     setDetailPlaceIndex(null);
     setTimeSheet(null);
@@ -5008,6 +5089,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
   function closePlaceSourceSheet() {
     setPlaceSourceSheetOpen(false);
     setPlacePickerOpen(false);
+    setCustomStopPickerOpen(false);
     setPlaceQuery('');
   }
 
@@ -5016,32 +5098,43 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     setDetailPlaceIndex(null);
     setPlaceSourceSheetOpen(true);
     setPlacePickerOpen(false);
+    setCustomStopPickerOpen(false);
     setPlaceQuery('');
   }
 
   function choosePlaceSource(source: PlacePickerTab) {
     setPickerTab(source);
+    setCustomStopPickerOpen(false);
     setPlacePickerOpen(true);
     setPlaceQuery('');
   }
 
-  function addCustomPlace() {
+  function chooseCustomStopSource() {
+    setPlacePickerOpen(false);
+    setCustomStopPickerOpen(true);
+    setPlaceQuery('');
+  }
+
+  function addCustomStop(kind: CustomPlanStopKind) {
     if (placeSourceTarget === 'new') {
       const nextIndex = places.length;
       setPlaces((current) => {
         const nextStart = nextPlanStopDateTimeFromPlaces(current);
-        return [...current, makeSelectedPlanPlace(current.length, nextStart.date, nextStart.time)];
+        return [...current, makeSelectedPlanCustomStop(kind, current.length, t, nextStart.date, nextStart.time)];
       });
       setDetailPlaceIndex(nextIndex);
     } else {
       const targetIndex = placeSourceTarget;
       setPlaces((current) => current.map((item, index) => {
         if (index !== targetIndex) return item;
-        return { ...makeSelectedPlanPlace(index, item.date || toDateInputValue()), id: item.id, date: item.date, time: item.time };
+        const next = makeSelectedPlanCustomStop(kind, index, t, item.date || toDateInputValue(), item.time);
+        return { ...next, id: item.id, date: item.date, time: item.time };
       }));
       setDetailPlaceIndex(targetIndex);
     }
     closePlaceSourceSheet();
+    setMessage(null);
+    setError(null);
   }
 
   function addReusablePlace(place: PlaceDto, explicitTarget: PlaceSourceTarget = placeSourceTarget) {
@@ -5191,6 +5284,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     setDetailPlaceIndex(null);
     setPlaceSourceSheetOpen(false);
     setPlacePickerOpen(false);
+    setCustomStopPickerOpen(false);
     const firstMissingId = missingIds[0] ?? null;
     setExpandedAddressPlaceIds(firstMissingId ? [firstMissingId] : []);
     setAddressFocusPlaceId(firstMissingId);
@@ -5216,6 +5310,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
   function showPreviewStage() {
     setError(null);
     if (places.length === 0) { setError(t('plans.create.validation.addBeforePreview')); return; }
+    if (!places.some((place) => !isCustomPlanStop(place))) { setError(t('plans.create.validation.addRealPlace')); return; }
     if (focusDraftPlacesNeedingReview()) return;
     if (focusMissingOfflineAddresses()) return;
     if (schedule.error) { setError(schedule.error); return; }
@@ -5238,6 +5333,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     const nextJoinDeadline = resolvePlanJoinDeadline(joinDeadline, nextSchedule.startsAt, t);
     const nextParticipantCapacity = resolvePlanParticipantCapacity(participantCapacity, t);
     if (usablePlaces.length === 0) { setError(t('plans.create.validation.addAtLeastOne')); return; }
+    if (!usablePlaces.some((place) => !isCustomPlanStop(place))) { setError(t('plans.create.validation.addRealPlace')); return; }
     if (focusDraftPlacesNeedingReview()) return;
     if (focusMissingOfflineAddresses()) return;
     if (nextSchedule.error || !nextSchedule.startsAt) { setError(nextSchedule.error || t('plans.create.validation.addAtLeastOne')); return; }
@@ -5273,13 +5369,14 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
         joinApprovalMode: 'automatic',
         status: 'open',
         places: usablePlaces.map((place, index) => ({
-          placeId: place.sourcePlaceId,
+          kind: place.kind,
+          placeId: isCustomPlanStop(place) ? undefined : place.sourcePlaceId,
           mode: place.mode,
           title: place.title,
-          addressPublicText: place.mode === 'local' ? place.providerAddress?.formattedAddress || undefined : undefined,
-          ...(place.mode === 'local' ? providerAddressPayload(place.providerAddress) : {}),
-          onlineLabel: place.mode === 'remote' ? place.onlineLabel.trim() || undefined : undefined,
-          onlineUrl: place.mode === 'remote' ? place.onlineUrl.trim() || undefined : undefined,
+          addressPublicText: !isCustomPlanStop(place) && place.mode === 'local' ? place.providerAddress?.formattedAddress || undefined : undefined,
+          ...(!isCustomPlanStop(place) && place.mode === 'local' ? providerAddressPayload(place.providerAddress) : {}),
+          onlineLabel: !isCustomPlanStop(place) && place.mode === 'remote' ? place.onlineLabel.trim() || undefined : undefined,
+          onlineUrl: !isCustomPlanStop(place) && place.mode === 'remote' ? place.onlineUrl.trim() || undefined : undefined,
           startsAt: nextSchedule.placeStartsAt[index],
           endsAt: nextSchedule.placeEndsAt[index],
           order: index,
@@ -5354,6 +5451,12 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                       onDatePress={() => setTimeSheet({ placeIndex: index, mode: 'date' })}
                       onTimePress={() => setTimeSheet({ placeIndex: index, mode: 'time' })}
                     />
+                    {planStopChronologyWarning(places, index, t) ? (
+                      <View style={[styles.planStopChronologyWarning, { borderTopColor: theme.color.border, backgroundColor: theme.semantic.warning.softBg }]}>
+                        <MobileIcon name="warning" size={16} color={theme.semantic.warning.text} />
+                        <AppText style={[styles.planStopChronologyWarningText, { color: theme.semantic.warning.text }]}>{planStopChronologyWarning(places, index, t)}</AppText>
+                      </View>
+                    ) : null}
                     {place.draftReview ? (
                       <View style={[styles.planDraftReview, { borderTopColor: theme.color.border, backgroundColor: theme.semantic.warning.softBg }]}>
                         <View style={styles.planDraftReviewHeader}>
@@ -5374,7 +5477,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                         ) : null}
                       </View>
                     ) : null}
-                    {expandedAddressPlaceIds.includes(place.id) && place.mode === 'local' && !hasValidOfflineProviderAddress(place.providerAddress) ? (
+                    {expandedAddressPlaceIds.includes(place.id) && !isCustomPlanStop(place) && place.mode === 'local' && !hasValidOfflineProviderAddress(place.providerAddress) ? (
                       <View
                         onLayout={(event) => recordAddressGuidanceLayout(place.id, event)}
                         style={[styles.planAddressGuidance, { borderTopColor: theme.color.border, backgroundColor: theme.semantic.place.softBg }]}
@@ -5453,7 +5556,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                   <View style={styles.previewInlineMeta}>
                     <SemanticBadge label={schedule.startsAt ? formatDate(schedule.startsAt, language, t) : t('plans.create.preview.startNotSet')} tone="time" size="sm" />
                     {endSummary ? <SemanticBadge label={`${endSummary.label}: ${endSummary.endLabel}`} tone="time" size="sm" /> : null}
-                    <SemanticBadge label={t(places.length === 1 ? 'plans.row.placeOne' : 'plans.row.placeMany', { count: places.length })} tone="place" size="sm" />
+                    <SemanticBadge label={planRouteUnitLabel(places, t)} tone="place" size="sm" />
                     <SemanticBadge label={t('plans.detail.values.freeJoin')} tone="plan" size="sm" />
                     <SemanticBadge label={t('plans.create.joinDeadline.previewBadge', { date: joinDeadlineSummary.deadlineLabel })} tone="plan" size="sm" />
                     <SemanticBadge label={participantCapacitySummary.previewLabel} tone="proposal" size="sm" />
@@ -5527,7 +5630,12 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
             mode={timeSheet.mode}
             date={timeSheetPlace.date}
             time={timeSheetPlace.time}
-            title={t(timeSheet.mode === 'date' ? 'plans.create.time.dateForPlace' : 'plans.create.time.timeForPlace', { index: timeSheet.placeIndex + 1 })}
+            title={t(
+              isCustomPlanStop(timeSheetPlace)
+                ? (timeSheet.mode === 'date' ? 'plans.create.time.dateForStop' : 'plans.create.time.timeForStop')
+                : (timeSheet.mode === 'date' ? 'plans.create.time.dateForPlace' : 'plans.create.time.timeForPlace'),
+              { index: timeSheet.placeIndex + 1 },
+            )}
             subtitle={timeSheetPlace.title || timeSheetPlace.sourcePlaceTitle || t('plans.create.time.stopSubtitle')}
             onChange={(patch) => updateSelectedPlaceSchedule(timeSheet.placeIndex, patch)}
             onClose={() => setTimeSheet(null)}
@@ -5575,7 +5683,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
               <View style={[styles.sourceSheet, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, Platform.OS === 'android' ? { paddingBottom: Math.max(24, insets.bottom + 16) } : null]}>
                 <View style={styles.wizardStepRow}>
                   <View style={styles.timelineCopy}>
-                    <AppText style={styles.sectionTitle}>{t('plans.create.place.label', { index: detailPlaceIndex + 1 })}</AppText>
+                    <AppText style={styles.sectionTitle}>{isCustomPlanStop(detailPlace) ? t('plans.create.customStop.detailTitle', { index: detailPlaceIndex + 1 }) : t('plans.create.place.label', { index: detailPlaceIndex + 1 })}</AppText>
                   </View>
                   <Pressable accessibilityRole="button" accessibilityLabel={t('common.actions.close')} onPress={() => setDetailPlaceIndex(null)} style={[styles.headerAction, { borderColor: theme.color.border }]}>
                     <MobileIcon name="close" color={theme.color.text} size={18} />
@@ -5583,7 +5691,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                 </View>
                 <ScrollView ref={detailPlaceScrollRef} keyboardShouldPersistTaps="handled" style={styles.sourceListScroll} contentContainerStyle={styles.placeDetailSheetContent}>
                   <View style={styles.rowTop}>
-                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? t('plans.create.placeDetail.sourceLibrary') : t('plans.create.placeDetail.sourceMine')} tone="place" size="sm" /> : <SemanticBadge label={t('plans.create.placeDetail.sourceCustom')} tone="place" size="sm" />}
+                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? t('plans.create.placeDetail.sourceLibrary') : t('plans.create.placeDetail.sourceMine')} tone="place" size="sm" /> : <SemanticBadge label={isCustomPlanStop(detailPlace) ? t('plans.create.customStop.badge') : t('plans.create.placeDetail.sourceCustom')} tone={isCustomPlanStop(detailPlace) ? 'time' : 'place'} size="sm" />}
                   </View>
                   {detailPlace.sourcePlaceId ? (
                     <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}>
@@ -5594,6 +5702,42 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                       {detailPlace.sourcePlaceSource === 'hellowhen_library' ? <SecondaryButton label={t('plans.create.placeDetail.copyToEdit')} onPress={() => copyLibraryPlaceFromDetail(detailPlaceIndex)} /> : null}
                       <SecondaryButton label={t('plans.create.placeDetail.makeCustom')} onPress={() => updateSelectedPlace(detailPlaceIndex, resetSelectedPlaceToCustom(detailPlace))} />
                     </View>
+                  ) : isCustomPlanStop(detailPlace) ? (
+                    <>
+                      <View style={styles.quickPickerGroup}>
+                        <AppText style={[styles.quickPickerLabel, { color: theme.color.muted }]}>{t('plans.create.customStop.type')}</AppText>
+                        <View style={styles.quickChoiceRow}>
+                          {(['pause', 'free_time', 'meeting_point', 'custom'] as CustomPlanStopKind[]).map((kind) => (
+                            <QuickChoiceButton
+                              key={kind}
+                              label={customPlanStopTitle(kind, t)}
+                              active={detailPlace.kind === kind}
+                              onPress={() => updateSelectedPlace(detailPlaceIndex, {
+                                kind,
+                                mode: 'local',
+                                sourcePlaceId: undefined,
+                                sourcePlaceTitle: undefined,
+                                location: '',
+                                providerAddress: null,
+                                onlineLabel: '',
+                                onlineUrl: '',
+                                title: detailPlace.title.trim() ? detailPlace.title : kind === 'custom' ? '' : customPlanStopTitle(kind, t),
+                              })}
+                            />
+                          ))}
+                        </View>
+                      </View>
+                      <TextField
+                        label={t('plans.create.customStop.label')}
+                        value={detailPlace.title}
+                        onChangeText={(title) => updateSelectedPlace(detailPlaceIndex, { title })}
+                        placeholder={t('plans.create.customStop.labelPlaceholder')}
+                        maxLength={120}
+                        onKeyboardInputFocus={detailPlaceKeyboardVisibility.onInputFocus}
+                        onKeyboardInputBlur={detailPlaceKeyboardVisibility.onInputBlur}
+                      />
+                      <InfoNotice tone="info" title={t('plans.create.customStop.noAddressTitle')} body={t('plans.create.customStop.noAddressBody')} />
+                    </>
                   ) : (
                     <>
                       <ModeSegment
@@ -5660,14 +5804,14 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
               <View style={[styles.sourceSheetHandle, { backgroundColor: theme.color.border }]} />
               <View style={styles.sourceSheetTopbar}>
                 <View style={styles.sourceSheetTitleBlock}>
-                  <AppText style={styles.sourceSheetTitle}>{placePickerOpen ? (pickerTab === 'mine' ? t('plans.create.sourcePicker.myPlaces') : t('plans.create.sourcePicker.library')) : t('plans.create.sourcePicker.addPlace')}</AppText>
+                  <AppText style={styles.sourceSheetTitle}>{customStopPickerOpen ? t('plans.create.customStop.title') : placePickerOpen ? (pickerTab === 'mine' ? t('plans.create.sourcePicker.myPlaces') : t('plans.create.sourcePicker.library')) : t('plans.create.sourcePicker.addPlace')}</AppText>
                 </View>
                 <Pressable accessibilityRole="button" accessibilityLabel={t('common.actions.close')} onPress={closePlaceSourceSheet} style={[styles.headerAction, { borderColor: theme.color.border }]}>
                   <MobileIcon name="close" color={theme.color.text} size={18} />
                 </Pressable>
               </View>
 
-              {!placePickerOpen ? (
+              {!placePickerOpen && !customStopPickerOpen ? (
                 <View style={styles.sourceOptionList}>
                   <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('mine')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="save" color={theme.semantic.place.text} size={17} /></View>
@@ -5684,12 +5828,36 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                     <View style={styles.sourceOptionCopy}><AppText style={styles.sourceOptionTitle}>{t('plans.create.sourcePicker.newPlace')}</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
-                  <Pressable accessibilityRole="button" onPress={addCustomPlace} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}>
+                  <Pressable accessibilityRole="button" onPress={chooseCustomStopSource} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="more" color={theme.semantic.place.text} size={17} /></View>
                     <View style={styles.sourceOptionCopy}><AppText style={styles.sourceOptionTitle}>{t('plans.create.sourcePicker.customStop')}</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
                 </View>
+              ) : customStopPickerOpen ? (
+                <View style={styles.sourceOptionList}>
+                  <AppText style={[styles.metaText, { color: theme.color.muted }]}>{t('plans.create.customStop.helper')}</AppText>
+                  {(['pause', 'free_time', 'meeting_point', 'custom'] as CustomPlanStopKind[]).map((kind) => (
+                    <Pressable
+                      key={kind}
+                      accessibilityRole="button"
+                      onPress={() => addCustomStop(kind)}
+                      style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}
+                    >
+                      <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.time.softBg, borderColor: theme.semantic.time.border }]}>
+                        <MobileIcon name={kind === 'meeting_point' ? 'location-on' : kind === 'pause' ? 'calendar' : 'more'} color={theme.semantic.time.text} size={17} />
+                      </View>
+                      <View style={styles.sourceOptionCopy}>
+                        <AppText style={styles.sourceOptionTitle}>{customPlanStopTitle(kind, t)}</AppText>
+                        <AppText style={[styles.choiceMeta, { color: theme.color.muted }]}>{t(`plans.create.customStop.${kind === 'free_time' ? 'freeTimeBody' : kind === 'meeting_point' ? 'meetingPointBody' : kind === 'pause' ? 'pauseBody' : 'customBody'}`)}</AppText>
+                      </View>
+                      <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
+                    </Pressable>
+                  ))}
+                  <View style={[styles.sourcePickerFooter, { borderTopColor: theme.color.border }]}>
+                    <SecondaryButton label={t('plans.create.sourcePicker.sources')} onPress={() => setCustomStopPickerOpen(false)} />
+                  </View>
+                </View>
               ) : (
                 <View style={styles.sourceOptionList}>
                   <View style={styles.sourcePickerToolbar}>
@@ -6166,6 +6334,7 @@ const styles = StyleSheet.create({
   largeIcon: { width: 64, height: 64, borderRadius: 32, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
   centerTitle: { fontSize: 25, fontWeight: '900', textAlign: 'center', letterSpacing: -0.4 },
   centerBody: { textAlign: 'center', lineHeight: 21, fontWeight: '700' },
+  planFeedEmptyCenter: { flex: 1, justifyContent: 'center', paddingBottom: 72 },
   feedIntroStack: { gap: 10, marginBottom: 4 },
   feedHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12 },
   feedTitleWrap: { flex: 1, gap: 6 },
@@ -6217,6 +6386,8 @@ const styles = StyleSheet.create({
   placeTimelineRowMeta: { gap: 7 },
   planAddressGuidance: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 11, gap: 10 },
   planAddressGuidanceBody: { fontSize: 12, lineHeight: 17, fontWeight: '800' },
+  planStopChronologyWarning: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 9, flexDirection: 'row', alignItems: 'center', gap: 8 },
+  planStopChronologyWarningText: { flex: 1, fontSize: 12, lineHeight: 17, fontWeight: '800' },
   planDraftReview: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 13, paddingHorizontal: 2, gap: 12 },
   planDraftReviewHeader: { flexDirection: 'row', alignItems: 'flex-start', gap: 10 },
   placeDetailSheetContent: { gap: 10, paddingBottom: 12 },
@@ -6224,7 +6395,7 @@ const styles = StyleSheet.create({
   sourceSheetOverlay: { flex: 1, justifyContent: 'flex-end' },
   sourceSheetScrim: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(0,0,0,0.38)' },
   sourceSheet: { maxHeight: '86%', borderTopLeftRadius: 26, borderTopRightRadius: 26, borderWidth: 1, paddingHorizontal: 16, paddingTop: 14, paddingBottom: 24, gap: 10 },
-  planPickerSheet: { maxHeight: '78%', borderTopLeftRadius: 26, borderTopRightRadius: 26, borderWidth: 1, paddingHorizontal: 16, paddingTop: 12, paddingBottom: Platform.OS === 'ios' ? 28 : 22, gap: 10 },
+  planPickerSheet: { maxHeight: '88%', borderTopLeftRadius: 26, borderTopRightRadius: 26, borderWidth: 1, paddingHorizontal: 16, paddingTop: 12, paddingBottom: Platform.OS === 'ios' ? 28 : 22, gap: 10 },
   planPickerSheetContent: { gap: 13 },
   planPickerSummaryRow: { borderRadius: 18, borderWidth: StyleSheet.hairlineWidth, padding: 11, flexDirection: 'row', alignItems: 'center', gap: 10 },
   planPickerSummaryIcon: { width: 36, height: 36, borderRadius: 18, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
diff --git a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
index 8b811bc..71afc8e 100644
--- a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
+++ b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
@@ -110,7 +110,7 @@ function formatPlanPlaceDate(value: string | null | undefined, language: 'en' |
 }
 
 function getPlaceLocationLabel(place: PlanPlaceDto | undefined) {
-  if (!place) return '';
+  if (!place || (place.kind ?? 'place') !== 'place') return '';
   if (place.mode === 'remote') return place.onlineLabel || place.onlineUrl || '';
   return place.addressPublicText || place.sourcePlace?.areaLabel || '';
 }
@@ -135,13 +135,14 @@ function PlanPlaceDeckCardView({ card, deckIndex, deckTotal, onOpen, topBadgeLab
   const fallback = useMemo(() => fallbackModel(card.id), [card.id]);
   const place = card.place;
   const isEmpty = card.kind === 'emptyPlace' || !place;
+  const isCustomStop = Boolean(place && (place.kind ?? 'place') !== 'place');
   const auth = useAuth();
   const { unit: temperatureUnit, toggleUnit: toggleTemperatureUnit } = useTemperatureUnitPreference();
   const weatherCandidate = usePlanPlaceWeather(
     card.plan.id,
     place,
     auth.user?.id,
-    Boolean(auth.user && place && !isEmpty && !isSyntheticPlanWeatherPlanId(card.plan.id)),
+    Boolean(auth.user && place && !isEmpty && !isCustomStop && !isSyntheticPlanWeatherPlanId(card.plan.id)),
   );
   const attributionLogoUrl = weatherCandidate
     ? (theme.mode === 'dark' ? weatherCandidate.attribution.logoDarkUrl : weatherCandidate.attribution.logoLightUrl)
@@ -157,15 +158,17 @@ function PlanPlaceDeckCardView({ card, deckIndex, deckTotal, onOpen, topBadgeLab
   const weatherVisible = Boolean(weatherCandidate && attributionLogoUrl && attributionLogoReady && !attributionLogoFailed);
   const temperatureLabel = weatherCandidate && weatherVisible ? formatPlanTemperature(weatherCandidate.temperatureC, temperatureUnit) : '';
   const cardCounter = isEmpty ? t('plans.deck.noPlacesCount') : `${String(card.placeIndex + 1).padStart(2, '0')}/${String(card.placeTotal).padStart(2, '0')}`;
-  const modeLabel = place?.mode === 'remote' ? t('plans.deck.online') : t('plans.deck.offline');
+  const modeLabel = isCustomStop
+    ? t(`plans.deck.customStop.${place?.kind === 'free_time' ? 'freeTime' : place?.kind === 'meeting_point' ? 'meetingPoint' : place?.kind === 'pause' ? 'pause' : 'custom'}`)
+    : place?.mode === 'remote' ? t('plans.deck.online') : t('plans.deck.offline');
   const modeWeatherLabel = temperatureLabel ? `${modeLabel} · ${temperatureLabel}` : modeLabel;
   const placeTitle = place?.title ?? t('plans.deck.noPlaces');
   const languageLabel = isEmpty ? '' : getPlaceLanguageLabel(place);
-  const locationLabel = isEmpty ? '' : [languageLabel, getPlaceLocationLabel(place)].filter(Boolean).join(' · ');
+  const locationLabel = isEmpty ? '' : isCustomStop ? t('plans.deck.customStop.noAddress') : [languageLabel, getPlaceLocationLabel(place)].filter(Boolean).join(' · ');
   const timeLabel = isEmpty
     ? t('plans.deck.participants', { count: getPlanParticipantCount(card.plan) })
     : formatPlanPlaceDate(place?.startsAt ?? card.plan.startsAt, language, t('plans.common.flexibleTime'));
-  const contentBadgeLabel = topBadgeLabel ?? t('plans.deck.placeBadge', { counter: cardCounter });
+  const contentBadgeLabel = topBadgeLabel ?? (isCustomStop ? t('plans.deck.customStopBadge', { counter: cardCounter }) : t('plans.deck.placeBadge', { counter: cardCounter }));
   const deckCounter = `${String(deckIndex + 1).padStart(2, '0')}/${String(Math.max(deckTotal, 1)).padStart(2, '0')}`;
   const primaryBadgeLabel = deckTotal > 1 ? `${deckCounter} · ${contentBadgeLabel}` : contentBadgeLabel;
   const presentationState = getPlanPresentationState(card.plan);
diff --git a/packages/contracts/src/plans.ts b/packages/contracts/src/plans.ts
index 549a15a..6f70194 100644
--- a/packages/contracts/src/plans.ts
+++ b/packages/contracts/src/plans.ts
@@ -35,6 +35,13 @@ export const planParticipantStatusSchema = z.enum([
   "removed",
 ]);
 export const planPlaceModeSchema = z.enum(["local", "remote"]);
+export const planPlaceKindSchema = z.enum([
+  "place",
+  "pause",
+  "free_time",
+  "meeting_point",
+  "custom",
+]);
 export const placeSourceSchema = z.enum(["user", "hellowhen_library"]);
 export const placeStatusSchema = z.enum([
   "draft",
@@ -309,6 +316,7 @@ export const listPlacesQuerySchema = z.object({
 });
 
 const planPlaceInputBaseSchema = z.object({
+  kind: planPlaceKindSchema.optional(),
   placeId: z.string().trim().min(1).max(120).optional(),
   mode: planPlaceModeSchema.optional(),
   title: z.string().trim().min(3).max(120).optional(),
@@ -340,6 +348,13 @@ export const planPlaceInputSchema = planPlaceInputBaseSchema
     message: "Choose a saved place or enter a place title.",
     path: ["title"],
   })
+  .refine(
+    (value) => value.kind === "place" || !value.placeId,
+    {
+      message: "Custom Plan stops cannot reference a saved Place.",
+      path: ["placeId"],
+    },
+  )
   .refine(
     (value) => {
       if (!value.startsAt || !value.endsAt) return true;
@@ -384,6 +399,13 @@ export const createPlanRequestSchema = z
       path: ["endsAt"],
     },
   )
+  .refine(
+    (value) => !value.places?.length || value.places.some((place) => (place.kind ?? "place") === "place"),
+    {
+      message: "A Plan with custom stops must include at least one real Place.",
+      path: ["places"],
+    },
+  )
   .refine(
     (value) => {
       if (!value.joinClosesAt) return true;
@@ -673,6 +695,7 @@ export const planPlaceSchema = z
     source: planPlaceSourceSchema.optional(),
     order: z.number().int(),
     mode: planPlaceModeSchema.default("local"),
+    kind: planPlaceKindSchema.optional(),
     title: z.string(),
     note: z.string().nullable().optional(),
     addressPublicText: z.string().nullable().optional(),
@@ -894,6 +917,7 @@ export type PlanStatus = z.infer<typeof planStatusSchema>;
 export type PlanJoinApprovalMode = z.infer<typeof planJoinApprovalModeSchema>;
 export type PlanParticipantStatus = z.infer<typeof planParticipantStatusSchema>;
 export type PlanPlaceMode = z.infer<typeof planPlaceModeSchema>;
+export type PlanPlaceKind = z.infer<typeof planPlaceKindSchema>;
 export type PlaceSource = z.infer<typeof placeSourceSchema>;
 export type PlaceStatus = z.infer<typeof placeStatusSchema>;
 export type PlaceVisibility = z.infer<typeof placeVisibilitySchema>;
diff --git a/packages/i18n/src/locales/en/plans.ts b/packages/i18n/src/locales/en/plans.ts
index db7d4c6..5756b1f 100644
--- a/packages/i18n/src/locales/en/plans.ts
+++ b/packages/i18n/src/locales/en/plans.ts
@@ -49,6 +49,8 @@ export const plans = {
     startsWith: 'Starts with {{place}}',
     placeOne: '{{count}} place',
     placeMany: '{{count}} places',
+    stopOne: '{{count}} stop',
+    stopMany: '{{count}} stops',
     joined: '{{count}} joined',
     meta: '{{places}} · {{joined}}',
   },
@@ -71,6 +73,14 @@ export const plans = {
   },
   deck: {
     placeBadge: 'Place · {{counter}}',
+    customStopBadge: 'Stop · {{counter}}',
+    customStop: {
+      pause: 'Pause',
+      freeTime: 'Free time',
+      meetingPoint: 'Meeting point',
+      custom: 'Custom stop',
+      noAddress: 'No address required',
+    },
     online: 'Online',
     offline: 'In person',
     noPlaces: 'No places yet',
@@ -254,6 +264,12 @@ export const plans = {
       mine: 'My place',
       custom: 'Custom stop',
     },
+    customStop: {
+      pause: 'Pause',
+      freeTime: 'Free time',
+      meetingPoint: 'Meeting point',
+      custom: 'Custom stop',
+    },
     people: {
       memberFallback: 'Hellowhen user',
       joinedSummary: '{{capacity}} joined · {{mode}}',
@@ -524,6 +540,8 @@ export const plans = {
       addressNeeded: 'Address needed',
       addBeforePreview: 'Add at least one place before preview.',
       addAtLeastOne: 'Add at least one place.',
+      addRealPlace: 'Add at least one real Place. Custom stops can sit between Places, but cannot be the whole Plan.',
+      stopOrderInline: 'This stop must start at least {{minutes}} minutes after the previous stop.',
       addValidPlace: 'Add at least one place with a valid date and time.',
       validDateTime: 'Add a valid date and time for Place {{index}}.',
       minimumGap: 'Place {{index}} must start at least {{minutes}} minutes after Place {{previous}}.',
@@ -549,6 +567,8 @@ export const plans = {
       systemTimeHint: 'Use the system picker to choose a custom time.',
       dateForPlace: 'Place {{index}} date',
       timeForPlace: 'Place {{index}} time',
+      dateForStop: 'Stop {{index}} date',
+      timeForStop: 'Stop {{index}} time',
       stopSubtitle: 'Choose when this stop happens.',
       chooseDateAccessibility: 'Choose date for Place {{index}}',
       chooseTimeAccessibility: 'Choose time for Place {{index}}',
@@ -689,6 +709,26 @@ export const plans = {
       addOnlineUrl: 'Add a valid online URL before using this Place.',
       fixAddress: 'Fix this Place first by selecting a confirmed Google address.',
     },
+    customStop: {
+      title: 'Add custom stop',
+      badge: 'Custom stop',
+      detailTitle: 'Stop {{index}}',
+      helper: 'Use a custom stop for time periods that do not need a map address.',
+      type: 'Stop type',
+      pause: 'Pause',
+      pauseBody: 'A short break between Places.',
+      freeTime: 'Free time',
+      freeTimeBody: 'An open period without a fixed location.',
+      meetingPoint: 'Meeting point',
+      meetingPointBody: 'A named coordination point without requiring a map address.',
+      custom: 'Other',
+      customBody: 'Name your own non-location period.',
+      label: 'Stop label',
+      labelPlaceholder: 'Lunch break, free time, check-in…',
+      noAddressRequired: 'Custom stop · no address required',
+      noAddressTitle: 'No address required',
+      noAddressBody: 'Custom stops represent a period in the Plan. The next stop start marks the end of this period, and custom stops are excluded from Maps, weather, and presence checks.',
+    },
     google: {
       defaultLabel: 'Address or place',
       defaultPlaceholder: 'Search a real address or place',
diff --git a/packages/i18n/src/locales/es/plans.ts b/packages/i18n/src/locales/es/plans.ts
index 46f5844..6fd8d39 100644
--- a/packages/i18n/src/locales/es/plans.ts
+++ b/packages/i18n/src/locales/es/plans.ts
@@ -48,6 +48,8 @@ export const plans = {
     startsWith: 'Empieza por {{place}}',
     placeOne: '{{count}} lugar',
     placeMany: '{{count}} lugares',
+    stopOne: '{{count}} parada',
+    stopMany: '{{count}} paradas',
     joined: '{{count}} participantes',
     meta: '{{places}} · {{joined}}',
   },
@@ -70,6 +72,14 @@ export const plans = {
   },
   deck: {
     placeBadge: 'Lugar · {{counter}}',
+    customStopBadge: 'Parada · {{counter}}',
+    customStop: {
+      pause: 'Pausa',
+      freeTime: 'Tiempo libre',
+      meetingPoint: 'Punto de encuentro',
+      custom: 'Parada personalizada',
+      noAddress: 'No requiere dirección',
+    },
     online: 'En línea',
     offline: 'Presencial',
     noPlaces: 'Todavía no hay lugares',
@@ -252,6 +262,12 @@ export const plans = {
       mine: 'Mi lugar',
       custom: 'Parada personalizada',
     },
+    customStop: {
+      pause: 'Pausa',
+      freeTime: 'Tiempo libre',
+      meetingPoint: 'Punto de encuentro',
+      custom: 'Parada personalizada',
+    },
     people: {
       memberFallback: 'Miembro de Hellowhen',
       joinedSummary: '{{capacity}} participantes · {{mode}}',
@@ -522,6 +538,8 @@ export const plans = {
       addressNeeded: 'Dirección necesaria',
       addBeforePreview: 'Añade al menos un lugar antes de la vista previa.',
       addAtLeastOne: 'Añade al menos un lugar.',
+      addRealPlace: 'Añade al menos un Lugar real. Las paradas personalizadas pueden ir entre Lugares, pero no pueden formar todo el Plan.',
+      stopOrderInline: 'Esta parada debe comenzar al menos {{minutes}} minutos después de la parada anterior.',
       addValidPlace: 'Añade al menos un lugar con fecha y hora válidas.',
       validDateTime: 'Añade una fecha y hora válidas para el lugar {{index}}.',
       minimumGap: 'El lugar {{index}} debe comenzar al menos {{minutes}} minutos después del lugar {{previous}}.',
@@ -547,6 +565,8 @@ export const plans = {
       systemTimeHint: 'Usa el selector del sistema para elegir una hora personalizada.',
       dateForPlace: 'Fecha del lugar {{index}}',
       timeForPlace: 'Hora del lugar {{index}}',
+      dateForStop: 'Fecha de la parada {{index}}',
+      timeForStop: 'Hora de la parada {{index}}',
       stopSubtitle: 'Elige cuándo sucede esta parada.',
       chooseDateAccessibility: 'Elegir fecha del lugar {{index}}',
       chooseTimeAccessibility: 'Elegir hora del lugar {{index}}',
@@ -687,6 +707,26 @@ export const plans = {
       addOnlineUrl: 'Añade una URL en línea válida antes de usar este lugar.',
       fixAddress: 'Corrige primero este lugar seleccionando una dirección confirmada por Google.',
     },
+    customStop: {
+      title: 'Añadir parada personalizada',
+      badge: 'Parada personalizada',
+      detailTitle: 'Parada {{index}}',
+      helper: 'Usa una parada personalizada para periodos que no necesitan una dirección en el mapa.',
+      type: 'Tipo de parada',
+      pause: 'Pausa',
+      pauseBody: 'Un breve descanso entre Lugares.',
+      freeTime: 'Tiempo libre',
+      freeTimeBody: 'Un periodo abierto sin ubicación fija.',
+      meetingPoint: 'Punto de encuentro',
+      meetingPointBody: 'Un punto de coordinación con nombre sin exigir una dirección en el mapa.',
+      custom: 'Otro',
+      customBody: 'Pon nombre a tu propio periodo sin ubicación.',
+      label: 'Nombre de la parada',
+      labelPlaceholder: 'Pausa para comer, tiempo libre, registro…',
+      noAddressRequired: 'Parada personalizada · sin dirección',
+      noAddressTitle: 'No requiere dirección',
+      noAddressBody: 'Una parada personalizada representa un periodo del Plan. El inicio de la siguiente parada marca su final, y se excluye de Mapas, clima y verificaciones de presencia.',
+    },
     google: {
       defaultLabel: 'Dirección o lugar',
       defaultPlaceholder: 'Buscar una dirección o lugar real',
diff --git a/packages/i18n/src/locales/fr/plans.ts b/packages/i18n/src/locales/fr/plans.ts
index acf83f0..0f1a253 100644
--- a/packages/i18n/src/locales/fr/plans.ts
+++ b/packages/i18n/src/locales/fr/plans.ts
@@ -49,6 +49,8 @@ export const plans = {
     startsWith: 'Commence par {{place}}',
     placeOne: '{{count}} lieu',
     placeMany: '{{count}} lieux',
+    stopOne: '{{count}} étape',
+    stopMany: '{{count}} étapes',
     joined: '{{count}} participant(s)',
     meta: '{{places}} · {{joined}}',
   },
@@ -71,6 +73,14 @@ export const plans = {
   },
   deck: {
     placeBadge: 'Lieu · {{counter}}',
+    customStopBadge: 'Étape · {{counter}}',
+    customStop: {
+      pause: 'Pause',
+      freeTime: 'Temps libre',
+      meetingPoint: 'Point de rendez-vous',
+      custom: 'Étape personnalisée',
+      noAddress: 'Aucune adresse requise',
+    },
     online: 'En ligne',
     offline: 'Sur place',
     noPlaces: 'Aucun lieu pour le moment',
@@ -254,6 +264,12 @@ export const plans = {
       mine: 'Mon lieu',
       custom: 'Étape personnalisée',
     },
+    customStop: {
+      pause: 'Pause',
+      freeTime: 'Temps libre',
+      meetingPoint: 'Point de rendez-vous',
+      custom: 'Étape personnalisée',
+    },
     people: {
       memberFallback: 'Membre Hellowhen',
       joinedSummary: '{{capacity}} participant(s) · {{mode}}',
@@ -524,6 +540,8 @@ export const plans = {
       addressNeeded: 'Adresse requise',
       addBeforePreview: 'Ajoutez au moins un lieu avant l’aperçu.',
       addAtLeastOne: 'Ajoutez au moins un lieu.',
+      addRealPlace: 'Ajoutez au moins un vrai lieu. Les étapes personnalisées peuvent se placer entre des lieux, mais ne peuvent pas constituer tout le Plan.',
+      stopOrderInline: 'Cette étape doit commencer au moins {{minutes}} minutes après l’étape précédente.',
       addValidPlace: 'Ajoutez au moins un lieu avec une date et une heure valides.',
       validDateTime: 'Ajoutez une date et une heure valides pour le lieu {{index}}.',
       minimumGap: 'Le lieu {{index}} doit commencer au moins {{minutes}} minutes après le lieu {{previous}}.',
@@ -549,6 +567,8 @@ export const plans = {
       systemTimeHint: 'Utilisez le sélecteur du système pour choisir une heure personnalisée.',
       dateForPlace: 'Date du lieu {{index}}',
       timeForPlace: 'Heure du lieu {{index}}',
+      dateForStop: 'Date de l’étape {{index}}',
+      timeForStop: 'Heure de l’étape {{index}}',
       stopSubtitle: 'Choisissez quand cette étape a lieu.',
       chooseDateAccessibility: 'Choisir la date du lieu {{index}}',
       chooseTimeAccessibility: 'Choisir l’heure du lieu {{index}}',
@@ -689,6 +709,26 @@ export const plans = {
       addOnlineUrl: 'Ajoutez une URL en ligne valide avant d’utiliser ce lieu.',
       fixAddress: 'Corrigez d’abord ce lieu en sélectionnant une adresse Google confirmée.',
     },
+    customStop: {
+      title: 'Ajouter une étape personnalisée',
+      badge: 'Étape personnalisée',
+      detailTitle: 'Étape {{index}}',
+      helper: 'Utilisez une étape personnalisée pour une période qui ne nécessite pas d’adresse cartographique.',
+      type: 'Type d’étape',
+      pause: 'Pause',
+      pauseBody: 'Une courte pause entre deux lieux.',
+      freeTime: 'Temps libre',
+      freeTimeBody: 'Une période libre sans lieu fixe.',
+      meetingPoint: 'Point de rendez-vous',
+      meetingPointBody: 'Un point de coordination nommé sans exiger une adresse cartographique.',
+      custom: 'Autre',
+      customBody: 'Nommez votre propre période sans lieu.',
+      label: 'Nom de l’étape',
+      labelPlaceholder: 'Pause déjeuner, temps libre, point de contrôle…',
+      noAddressRequired: 'Étape personnalisée · aucune adresse requise',
+      noAddressTitle: 'Aucune adresse requise',
+      noAddressBody: 'Une étape personnalisée représente une période du Plan. Le début de l’étape suivante marque sa fin, et elle est exclue des cartes, de la météo et des contrôles de présence.',
+    },
     google: {
       defaultLabel: 'Adresse ou lieu',
       defaultPlaceholder: 'Rechercher une adresse ou un lieu réel',
diff --git a/packages/shared/src/plans.ts b/packages/shared/src/plans.ts
index 2b6fcae..d3d820b 100644
--- a/packages/shared/src/plans.ts
+++ b/packages/shared/src/plans.ts
@@ -21,6 +21,7 @@ export function isPlanJoinClosed(plan: PlanJoinDeadlineInput, now: string | Date
 export type GeneratedPlanMode = 'local' | 'remote' | 'hybrid' | string;
 
 export type GeneratedPlanPlaceInput = {
+  kind?: 'place' | 'pause' | 'free_time' | 'meeting_point' | 'custom' | string | null;
   title?: string | null;
   sourcePlaceTitle?: string | null;
   mode?: GeneratedPlanMode | null;
@@ -32,6 +33,8 @@ export type GeneratedPlanDisplayLabels = {
   placeFallback: string;
   place: string;
   places: string;
+  stop: string;
+  stops: string;
   starts: string;
   localPlan: string;
   remotePlan: string;
@@ -60,6 +63,8 @@ const defaultGeneratedPlanDisplayLabels: GeneratedPlanDisplayLabels = {
   placeFallback: 'Place',
   place: 'place',
   places: 'places',
+  stop: 'stop',
+  stops: 'stops',
   starts: 'Starts',
   localPlan: 'Local plan',
   remotePlan: 'Online plan',
@@ -103,7 +108,7 @@ function generatedPlanPlaceTitle(place: GeneratedPlanPlaceInput | null | undefin
 }
 
 function inferGeneratedPlanMode(places: readonly GeneratedPlanPlaceInput[]) {
-  const modes = new Set(places.map((place) => cleanText(place.mode)).filter(Boolean));
+  const modes = new Set(places.filter((place) => cleanText(place.kind || 'place') === 'place').map((place) => cleanText(place.mode)).filter(Boolean));
   if (modes.size > 1) return 'hybrid';
   if (modes.has('remote')) return 'remote';
   return 'local';
@@ -118,7 +123,9 @@ function generatedPlanModeLabel(mode: GeneratedPlanMode | null | undefined, labe
 export function buildGeneratedPlanTitle(input: GeneratedPlanDisplayInput) {
   const labels = labelsFor(input.labels);
   const places = input.places ?? [];
-  const names = places.map((place, index) => generatedPlanPlaceTitle(place, index, labels)).filter(Boolean);
+  const titlePlaces = places.filter((place) => cleanText(place.kind || 'place') === 'place');
+  const titleCandidates = titlePlaces.length ? titlePlaces : places;
+  const names = titleCandidates.map((place, index) => generatedPlanPlaceTitle(place, index, labels)).filter(Boolean);
   const firstName = names[0] ?? labels.newPlan;
   const secondName = names[1] ?? `${labels.placeFallback} 2`;
 
@@ -136,8 +143,9 @@ export function buildGeneratedPlanSummaryParts(input: GeneratedPlanDisplayInput)
   const places = input.places ?? [];
   const mode = input.mode ?? inferGeneratedPlanMode(places);
   const placeCount = places.length;
+  const hasCustomStops = places.some((place) => cleanText(place.kind || 'place') !== 'place');
   const parts = [
-    placeCount > 0 ? `${placeCount} ${placeCount === 1 ? labels.place : labels.places}` : labels.placeFirstPlan,
+    placeCount > 0 ? `${placeCount} ${hasCustomStops ? (placeCount === 1 ? labels.stop : labels.stops) : (placeCount === 1 ? labels.place : labels.places)}` : labels.placeFirstPlan,
     generatedPlanModeLabel(mode, labels),
     input.joinApprovalMode === 'owner_approval' ? null : labels.freeJoin,
   ];
diff --git a/scripts/plans-hidden-smoke.mjs b/scripts/plans-hidden-smoke.mjs
index e98c18f..314027a 100644
--- a/scripts/plans-hidden-smoke.mjs
+++ b/scripts/plans-hidden-smoke.mjs
@@ -70,6 +70,7 @@ async function runEnabledSmoke() {
   const firstStopDate = new Date(Date.now() + 36 * 60 * 60 * 1000);
   firstStopDate.setHours(13, 0, 0, 0);
   const firstStopAt = firstStopDate.toISOString();
+  const customStopAt = new Date(firstStopDate.getTime() + 60 * 60 * 1000).toISOString();
   const secondStopDate = new Date(firstStopDate.getTime() + 2 * 60 * 60 * 1000);
   const secondStopAt = secondStopDate.toISOString();
   const remoteStopAt = new Date(firstStopDate.getTime() + 4 * 60 * 60 * 1000).toISOString();
@@ -96,6 +97,14 @@ async function runEnabledSmoke() {
           startsAt: firstStopAt,
           order: 0,
         },
+        {
+          kind: 'pause',
+          mode: 'local',
+          title: 'Smoke pause',
+          note: 'A custom non-location period should not require an address.',
+          startsAt: customStopAt,
+          order: 1,
+        },
         {
           mode: 'remote',
           title: 'Second smoke stop',
@@ -103,7 +112,7 @@ async function runEnabledSmoke() {
           onlineLabel: 'Second smoke link',
           onlineUrl: `https://meet.example/plan-second-${encodeURIComponent(stamp)}`,
           startsAt: secondStopAt,
-          order: 1,
+          order: 2,
         },
       ],
     }),
@@ -113,9 +122,11 @@ async function runEnabledSmoke() {
   assert(planId, 'Plan creation did not return a plan id.');
   assert(created.plan.joinApprovalMode === 'automatic', 'Simplified Plans should default to automatic join.');
   assert(created.plan.status === 'open', 'Simplified Plans should be open after create.');
-  assert(created.plan.places?.length === 2, 'Plan should include the created places.');
+  assert(created.plan.places?.length === 3, 'Plan should include the created Places and custom stop.');
   assert(created.plan.places?.[0]?.mode === 'remote', 'First place should store remote mode.');
-  assert(created.plan.places?.[1]?.mode === 'remote', 'Second place should store remote mode.');
+  assert(created.plan.places?.[1]?.kind === 'pause', 'Custom pause should persist its stop kind.');
+  assert(!created.plan.places?.[1]?.formattedAddress, 'Custom pause should not require or persist an address.');
+  assert(created.plan.places?.[2]?.mode === 'remote', 'Second real place should store remote mode.');
 
   const publicPlan = await request(`/plans/${planId}`);
   assert(publicPlan.plan?.places?.[0]?.onlineUrl?.startsWith('https://meet.example/plan-opening-'), 'Anonymous viewer should see the public online destination.');
