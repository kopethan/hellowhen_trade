diff --git a/apps/api/src/modules/plans/__tests__/plans.safety.test.ts b/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
index 5b95294..7cd73a7 100644
--- a/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
+++ b/apps/api/src/modules/plans/__tests__/plans.safety.test.ts
@@ -9,7 +9,7 @@ import {
   rangesConflictWithRequiredGap,
   restoredPlanStatus,
 } from '../plans.safety.testkit.js';
-import { buildGeneratedPlanDisplay, stripLegacyGeneratedPlanStartTime } from '@hellowhen/shared';
+import { buildGeneratedPlanDisplay, isPlanJoinClosed, stripLegacyGeneratedPlanStartTime } from '@hellowhen/shared';
 import { normalizedPlanLifecycleStatus } from '../plans.lifecycle.js';
 
 const at = (hour: number, minute = 0) => `2026-07-20T${String(hour).padStart(2, '0')}:${String(minute).padStart(2, '0')}:00.000Z`;
@@ -105,6 +105,13 @@ test('owners can read non-public drafts while public viewers cannot', () => {
   assert.equal(canReadPlan(draft, false), false);
 });
 
+test('Plan join deadline is still open only while its effective deadline is in the future', () => {
+  assert.equal(isPlanJoinClosed({ startsAt: at(12) }, at(11, 59)), false);
+  assert.equal(isPlanJoinClosed({ startsAt: at(12) }, at(12)), true);
+  assert.equal(isPlanJoinClosed({ startsAt: at(14), joinClosesAt: at(13) }, at(12, 59)), false);
+  assert.equal(isPlanJoinClosed({ startsAt: at(14), joinClosesAt: at(13) }, at(13)), true);
+});
+
 test('generated Plan descriptions never persist viewer-formatted start times', () => {
   const display = buildGeneratedPlanDisplay({
     places: [{ title: 'Jardins du Trocadéro', mode: 'local' }],
diff --git a/apps/api/src/modules/plans/plans.routes.ts b/apps/api/src/modules/plans/plans.routes.ts
index e84d6d1..96acded 100644
--- a/apps/api/src/modules/plans/plans.routes.ts
+++ b/apps/api/src/modules/plans/plans.routes.ts
@@ -753,6 +753,13 @@ function planDeletedResponse() {
   };
 }
 
+function planJoinDeadlineClosedAtCreationResponse() {
+  return {
+    error: 'plan_join_deadline_closed',
+    message: 'Join deadline must be in the future when publishing a Plan.',
+  };
+}
+
 function planRemovedResponse() {
   return {
     removed: true,
@@ -1058,8 +1065,12 @@ plansRoutes.get('/joined', requireAuth, asyncRoute(async (req, res) => {
 }));
 
 plansRoutes.post('/', requireAuth, requireActiveAccount, asyncRoute(async (req, res) => {
-  await syncDuePlanLifecycleStatuses(new Date(), { ownerId: req.user!.id });
+  const now = new Date();
+  await syncDuePlanLifecycleStatuses(now, { ownerId: req.user!.id });
   const input = createPlanRequestSchema.parse(req.body ?? {});
+  if (input.status !== 'draft' && isPlanJoinClosed(input, now)) {
+    return res.status(400).json(planJoinDeadlineClosedAtCreationResponse());
+  }
   const stopGapViolation = findPlanRequestStopGapViolation(input);
   if (stopGapViolation) return res.status(400).json(planStopGapViolationResponse(stopGapViolation));
   const conflictingPlan = await findOwnedPlanTimeConflict(req.user!.id, input);
diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 9033f1c..8115c43 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -9,7 +9,7 @@ import { useSafeAreaInsets } from 'react-native-safe-area-context';
 import type { NativeStackNavigationProp, NativeStackScreenProps } from '@react-navigation/native-stack';
 import { GOOGLE_PLACE_SEARCH_MIN_QUERY_LENGTH, type DiscoveryLanguage, type GooglePlacePrediction, type GoogleResolvedPlace, type InventoryTranslationDto, type ListPlansQuery, type ListPlacesQuery, type MediaAssetDto, type PlaceDto, type PlacePresenceVerificationResponse, type PlaceStaticMapDto, type PlanDto, type PlanParticipantDto, type PlanPlaceDto, type PlanPlaceMode } from '@hellowhen/contracts';
 import { formatLocalizedDateTime, type SupportedLanguage, type TranslationValues } from '@hellowhen/i18n';
-import { buildEstimatedPlanPlaceEndTimes, estimateFinalPlanPlaceEndTime, buildGeneratedPlanDisplay, buildPlanFeedItems, effectivePlanJoinClosesAt, getOnlinePlaceProviderMetadata, hasConfirmedProviderOfflineAddress, hasOnlineDestination, mergeRecentStarterPlanIdeaIds, parseStarterPlanIdeaKey, resolveInventoryOriginalCopy, PLACE_ADDRESS_CONFIRMED_STATUS, PLACE_ADDRESS_PROVIDER_SOURCE, PLAN_MIN_STOP_START_GAP_MINUTES, selectStarterPlanIdeaKeys, starterPlanIdeas, starterPlanIdeaMode, starterPlanIdeaRequirementCounts, starterPlanIdeaStopDestinationPrompt, starterPlanIdeaStopRequirementLabel, type PlaceProviderAddressInput, type StarterPlanIdea, type StarterPlanIdeaKey, type StarterPlanIdeaStop } from '@hellowhen/shared';
+import { buildEstimatedPlanPlaceEndTimes, estimateFinalPlanPlaceEndTime, buildGeneratedPlanDisplay, buildPlanFeedItems, effectivePlanJoinClosesAt, isPlanJoinClosed, getOnlinePlaceProviderMetadata, hasConfirmedProviderOfflineAddress, hasOnlineDestination, mergeRecentStarterPlanIdeaIds, parseStarterPlanIdeaKey, resolveInventoryOriginalCopy, PLACE_ADDRESS_CONFIRMED_STATUS, PLACE_ADDRESS_PROVIDER_SOURCE, PLAN_MIN_STOP_START_GAP_MINUTES, selectStarterPlanIdeaKeys, starterPlanIdeas, starterPlanIdeaMode, starterPlanIdeaRequirementCounts, starterPlanIdeaStopDestinationPrompt, starterPlanIdeaStopRequirementLabel, type PlaceProviderAddressInput, type StarterPlanIdea, type StarterPlanIdeaKey, type StarterPlanIdeaStop } from '@hellowhen/shared';
 import { AppFixedHeaderScreen } from '../../components/AppFixedHeaderScreen';
 import type { AppCollapsibleHeaderScrollProps } from '../../components/AppCollapsibleHeaderScreen';
 import { AppSmartHeaderScreen } from '../../components/AppSmartHeaderScreen';
@@ -1142,6 +1142,9 @@ function resolvePlanJoinDeadline(state: PlanJoinDeadlineState, startsAt: string,
   if (deadline.getTime() > start.getTime()) {
     return { joinClosesAt: '', presetLabel: planJoinDeadlinePresetLabel(state.preset, t), deadlineLabel: formatCompactIsoDateTime(deadline.toISOString()), error: t('plans.create.joinDeadline.afterStart') };
   }
+  if (isPlanJoinClosed({ startsAt, joinClosesAt: deadline }, new Date())) {
+    return { joinClosesAt: '', presetLabel: planJoinDeadlinePresetLabel(state.preset, t), deadlineLabel: formatCompactIsoDateTime(deadline.toISOString()), error: t('plans.create.joinDeadline.past') };
+  }
 
   return {
     joinClosesAt: deadline.toISOString(),
diff --git a/packages/i18n/src/locales/en/plans.ts b/packages/i18n/src/locales/en/plans.ts
index 8035714..4bfc2e8 100644
--- a/packages/i18n/src/locales/en/plans.ts
+++ b/packages/i18n/src/locales/en/plans.ts
@@ -574,6 +574,7 @@ export const plans = {
       customBoth: 'Choose both a join deadline date and time.',
       customInvalid: 'Choose a valid join deadline date and time.',
       afterStart: 'Join deadline must be at or before the Plan start.',
+      past: 'Join deadline must still be in the future.',
       summary: '{{preset}} · {{date}}',
       previewBadge: 'Join closes {{date}}',
     },
diff --git a/packages/i18n/src/locales/es/plans.ts b/packages/i18n/src/locales/es/plans.ts
index b6c5bd1..8902e6e 100644
--- a/packages/i18n/src/locales/es/plans.ts
+++ b/packages/i18n/src/locales/es/plans.ts
@@ -572,6 +572,7 @@ export const plans = {
       customBoth: 'Elige una fecha y una hora de cierre.',
       customInvalid: 'Elige una fecha y una hora de cierre válidas.',
       afterStart: 'El cierre para unirse debe ser igual o anterior al inicio del Plan.',
+      past: 'El cierre para unirse todavía debe estar en el futuro.',
       summary: '{{preset}} · {{date}}',
       previewBadge: 'Cierra {{date}}',
     },
diff --git a/packages/i18n/src/locales/fr/plans.ts b/packages/i18n/src/locales/fr/plans.ts
index 751fc37..cd7b28e 100644
--- a/packages/i18n/src/locales/fr/plans.ts
+++ b/packages/i18n/src/locales/fr/plans.ts
@@ -574,6 +574,7 @@ export const plans = {
       customBoth: 'Choisissez une date et une heure de clôture.',
       customInvalid: 'Choisissez une date et une heure de clôture valides.',
       afterStart: 'La clôture des participations doit être fixée au plus tard au début du Plan.',
+      past: 'La clôture des participations doit encore être dans le futur.',
       summary: '{{preset}} · {{date}}',
       previewBadge: 'Clôture {{date}}',
     },
