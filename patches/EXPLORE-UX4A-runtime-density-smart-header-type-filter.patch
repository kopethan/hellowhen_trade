diff --git a/apps/mobile/src/features/explore/ExploreScreen.tsx b/apps/mobile/src/features/explore/ExploreScreen.tsx
index 2e40327..e2090df 100644
--- a/apps/mobile/src/features/explore/ExploreScreen.tsx
+++ b/apps/mobile/src/features/explore/ExploreScreen.tsx
@@ -4,7 +4,9 @@ import { useNavigation } from '@react-navigation/native';
 import type { InventoryTemplateDto, PlaceDto, PlanDto } from '@hellowhen/contracts';
 import { starterPlanIdeaKeys, starterPlanIdeas, starterPlanIdeaMode, type StarterPlanIdea } from '@hellowhen/shared';
 import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
-import { AppFixedHeaderScreen } from '../../components/AppFixedHeaderScreen';
+import { AppSmartHeaderScreen } from '../../components/AppSmartHeaderScreen';
+import { AppHeaderActionButton } from '../../components/AppHeaderActionButton';
+import { LibraryFilterGroup, LibraryFilterOption, LibraryFilterScreen } from '../../components/library';
 import { AccountHeaderActionButton } from '../../components/AccountHeaderActionButton';
 import { AppText } from '../../components/AppText';
 import { api } from '../../lib/api';
@@ -15,7 +17,7 @@ import { useTranslation } from '../../providers/MobileI18nProvider';
 import { PlanSquareDeck } from '../plans/components/PlanSquareDeck';
 import { TradeFeedIdeaCard } from '../trade/TradeDeckFeedScreen';
 import { feedTradeIdeaKeys, feedTradeIdeas, type FeedTradeIdeaKey } from '../trade/tradeFeedIdeas';
-import { MOBILE_DECK_FEED_GAP, MOBILE_TRADE_DECK_AVAILABLE_HEIGHT } from '../trade/components/tradeDeckGeometry';
+import { MOBILE_DECK_FEED_GAP } from '../trade/components/tradeDeckGeometry';
 import { categoryLabel, itemTypeLabel, modeLabel } from '../trade/components/InventoryFormFields';
 import { ExploreConceptSquareDeck } from './ExploreConceptSquareDeck';
 
@@ -23,6 +25,7 @@ const EXPLORE_DISCOVERY_LIMIT = 12;
 const tradeExploreIdeaKeys = feedTradeIdeaKeys.filter((ideaKey) => feedTradeIdeas[ideaKey].type === 'trade') as readonly FeedTradeIdeaKey[];
 
 type ExploreConceptKind = 'trade' | 'plan' | 'need' | 'offer' | 'place';
+type ExploreTypeFilter = 'all' | ExploreConceptKind;
 type InventoryTemplatesResponse = { templates?: InventoryTemplateDto[] };
 type PlacesResponse = { places?: PlaceDto[] };
 type TFunction = (key: string, values?: Record<string, string | number | boolean | null | undefined>) => string;
@@ -169,6 +172,9 @@ export function ExploreScreen() {
   const [discoveryLoading, setDiscoveryLoading] = useState(true);
   const [discoveryLoaded, setDiscoveryLoaded] = useState(false);
   const [discoveryHasError, setDiscoveryHasError] = useState(false);
+  const [typeFilter, setTypeFilter] = useState<ExploreTypeFilter>('all');
+  const [draftTypeFilter, setDraftTypeFilter] = useState<ExploreTypeFilter>('all');
+  const [filterVisible, setFilterVisible] = useState(false);
   const discoveryRequestSequence = useRef(0);
   const discoveryMixSeed = useRef(Date.now()).current;
 
@@ -234,12 +240,47 @@ export function ExploreScreen() {
       : [],
   }, discoveryMixSeed), [discoveryMixSeed, hellowhenPlaces, needTemplates, offerTemplates, staticConceptGroups]);
   const feedItems = discoveryLoaded ? mixedFeedItems : [];
+  const filteredFeedItems = useMemo(
+    () => typeFilter === 'all' ? feedItems : feedItems.filter((item) => item.kind === typeFilter),
+    [feedItems, typeFilter],
+  );
+  const draftFilteredCount = useMemo(
+    () => draftTypeFilter === 'all' ? feedItems.length : feedItems.filter((item) => item.kind === draftTypeFilter).length,
+    [draftTypeFilter, feedItems],
+  );
 
   const discoveryItemCount = needTemplates.length + offerTemplates.length + hellowhenPlaces.length;
+  const openTypeFilters = () => {
+    setDraftTypeFilter(typeFilter);
+    setFilterVisible(true);
+  };
+  const resetTypeFilters = () => setDraftTypeFilter('all');
+  const applyTypeFilters = () => {
+    setTypeFilter(draftTypeFilter);
+    setFilterVisible(false);
+  };
+
+  const filterOptions: Array<{ value: ExploreTypeFilter; label: string }> = [
+    { value: 'all', label: t('inventory.itemTypes.all') },
+    { value: 'trade', label: t('trade.feedIdeas.badge') },
+    { value: 'plan', label: t('plans.workspace.planIdeas.title') },
+    { value: 'need', label: t('common.exploreDiscovery.needSection') },
+    { value: 'offer', label: t('common.exploreDiscovery.offerSection') },
+    { value: 'place', label: t('common.exploreDiscovery.placeSection') },
+  ];
+
   const header = (
     <View style={styles.headerRow}>
       <AppText accessibilityRole="header" style={styles.title}>{t('navigation.tabs.explore')}</AppText>
-      <AccountHeaderActionButton onPress={() => navigation.navigate('Account')} />
+      <View style={styles.headerActions}>
+        <AppHeaderActionButton
+          icon="filter"
+          accessibilityLabel={t('common.exploreDiscovery.filterOpen')}
+          onPress={openTypeFilters}
+          badgeCount={typeFilter === 'all' ? 0 : 1}
+        />
+        <AccountHeaderActionButton onPress={() => navigation.navigate('Account')} />
+      </View>
     </View>
   );
 
@@ -266,18 +307,27 @@ export function ExploreScreen() {
   ) : null;
 
   return (
-    <AppFixedHeaderScreen header={header}>
-      <FlatList
-        data={feedItems}
-        keyExtractor={(item) => item.key}
-        contentContainerStyle={styles.content}
-        showsVerticalScrollIndicator={false}
-        initialNumToRender={4}
-        maxToRenderPerBatch={4}
-        windowSize={5}
-        removeClippedSubviews={false}
-        ListFooterComponent={footer}
-        renderItem={({ item, index: feedIndex }) => {
+    <>
+      <AppSmartHeaderScreen header={header} resetKey={typeFilter}>
+        {(scrollProps) => (
+          <FlatList
+            {...scrollProps.scrollViewProps}
+            key={typeFilter}
+            data={filteredFeedItems}
+            keyExtractor={(item) => item.key}
+            contentContainerStyle={[styles.content, scrollProps.contentInsetStyle]}
+            showsVerticalScrollIndicator={false}
+            initialNumToRender={4}
+            maxToRenderPerBatch={4}
+            windowSize={5}
+            removeClippedSubviews={false}
+            ListEmptyComponent={discoveryLoaded && !discoveryLoading ? (
+              <View style={styles.discoveryState}>
+                <AppText style={[styles.discoveryStateText, { color: theme.color.muted }]}>{t('common.exploreDiscovery.empty')}</AppText>
+              </View>
+            ) : null}
+            ListFooterComponent={footer}
+            renderItem={({ item, index: feedIndex }) => {
           if (item.kind === 'trade') {
             return (
               <View style={[styles.conceptDeck, feedIndex === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
@@ -343,17 +393,44 @@ export function ExploreScreen() {
               />
             </View>
           );
-        }}
-      />
-    </AppFixedHeaderScreen>
+            }}
+          />
+        )}
+      </AppSmartHeaderScreen>
+
+      <LibraryFilterScreen
+        visible={filterVisible}
+        title={t('inventory.libraryFilters.title')}
+        body={t('common.exploreDiscovery.filterBody')}
+        closeAccessibilityLabel={t('inventory.libraryFilters.closeFilters')}
+        resetLabel={t('inventory.libraryFilters.reset')}
+        applyLabel={t('inventory.libraryFilters.showResults', { count: draftFilteredCount })}
+        onClose={() => setFilterVisible(false)}
+        onReset={resetTypeFilters}
+        onApply={applyTypeFilters}
+        resetDisabled={draftTypeFilter === 'all'}
+      >
+        <LibraryFilterGroup title={t('inventory.libraryFilters.type')}>
+          {filterOptions.map((option) => (
+            <LibraryFilterOption
+              key={option.value}
+              label={option.label}
+              selected={draftTypeFilter === option.value}
+              onPress={() => setDraftTypeFilter(option.value)}
+            />
+          ))}
+        </LibraryFilterGroup>
+      </LibraryFilterScreen>
+    </>
   );
 }
 
 const styles = StyleSheet.create({
   headerRow: { minHeight: 44, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12 },
+  headerActions: { flexDirection: 'row', alignItems: 'center', gap: 10 },
   title: { flex: 1, minWidth: 0, fontSize: 24, lineHeight: 30, fontWeight: '900', letterSpacing: -0.45 },
   content: { paddingTop: 8, paddingBottom: 44 },
-  conceptDeck: { minHeight: MOBILE_TRADE_DECK_AVAILABLE_HEIGHT, alignItems: 'center', justifyContent: 'center', overflow: 'visible' },
+  conceptDeck: { alignItems: 'center', justifyContent: 'center', overflow: 'visible' },
   firstConceptDeck: { marginTop: 14 },
   nextConceptDeck: { marginTop: MOBILE_DECK_FEED_GAP },
   discoveryState: { alignItems: 'center', justifyContent: 'center', gap: 10, paddingHorizontal: 24, paddingTop: 28 },
diff --git a/packages/i18n/src/locales/en/common.ts b/packages/i18n/src/locales/en/common.ts
index 09216ec..3deec75 100644
--- a/packages/i18n/src/locales/en/common.ts
+++ b/packages/i18n/src/locales/en/common.ts
@@ -6,6 +6,8 @@ export const common = {
     needBadge: 'NEED IDEA',
     offerBadge: 'OFFER IDEA',
     placeBadge: 'HELLOWHEN PLACE',
+    filterOpen: 'Filter Explore',
+    filterBody: 'Choose the kinds of ideas and places you want to see.',
     loading: 'Loading Need, Offer, and Place ideas...',
     partialError: 'Some Hellowhen discovery content could not be loaded.',
     empty: 'More Need, Offer, and Place ideas are coming soon.',
diff --git a/packages/i18n/src/locales/es/common.ts b/packages/i18n/src/locales/es/common.ts
index a4f4019..5740b72 100644
--- a/packages/i18n/src/locales/es/common.ts
+++ b/packages/i18n/src/locales/es/common.ts
@@ -6,6 +6,8 @@ export const common = {
     needBadge: 'IDEA DE NECESIDAD',
     offerBadge: 'IDEA DE OFERTA',
     placeBadge: 'LUGAR HELLOWHEN',
+    filterOpen: 'Filtrar Explorar',
+    filterBody: 'Elige los tipos de ideas y lugares que quieres ver.',
     loading: 'Cargando ideas de necesidades, ofertas y lugares...',
     partialError: 'No se pudo cargar parte del contenido de descubrimiento de Hellowhen.',
     empty: 'Pronto habrá más ideas de necesidades, ofertas y lugares.',
diff --git a/packages/i18n/src/locales/fr/common.ts b/packages/i18n/src/locales/fr/common.ts
index b1575bc..857a0a5 100644
--- a/packages/i18n/src/locales/fr/common.ts
+++ b/packages/i18n/src/locales/fr/common.ts
@@ -6,6 +6,8 @@ export const common = {
     needBadge: 'IDÉE BESOIN',
     offerBadge: 'IDÉE OFFRE',
     placeBadge: 'LIEU HELLOWHEN',
+    filterOpen: 'Filtrer Explorer',
+    filterBody: 'Choisissez les types d’idées et de lieux que vous souhaitez voir.',
     loading: 'Chargement des idées de besoins, d’offres et de lieux...',
     partialError: 'Une partie du contenu de découverte Hellowhen n’a pas pu être chargée.',
     empty: 'D’autres idées de besoins, d’offres et de lieux arrivent bientôt.',
