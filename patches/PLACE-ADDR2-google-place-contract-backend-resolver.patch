diff --git a/.env.example b/.env.example
index b397d44..c3c19e5 100644
--- a/.env.example
+++ b/.env.example
@@ -440,3 +440,12 @@ EXPO_PUBLIC_FIRST_LAUNCH_GUARDS_ENABLED=false
 EXPO_PUBLIC_PLANS_ENABLED=false
 EXPO_PUBLIC_PLANS_VISIBLE=false
 EXPO_PUBLIC_MAIN_NAV_PLANS_ME_TRADE=false
+
+# Google-confirmed offline place search. Keep disabled until API keys and quota limits are ready.
+GOOGLE_PLACES_ENABLED=false
+GOOGLE_ADDRESS_VALIDATION_ENABLED=false
+GOOGLE_MAPS_SERVER_API_KEY=
+GOOGLE_PLACES_DEFAULT_LANGUAGE=en
+# Optional comma-separated ISO country codes, e.g. FR or FR,BE,CH.
+GOOGLE_PLACES_COUNTRY_CODES=FR
+GOOGLE_PLACES_REQUEST_TIMEOUT_MS=4500
diff --git a/apps/api/src/config/env.ts b/apps/api/src/config/env.ts
index 9c24768..5045fb4 100644
--- a/apps/api/src/config/env.ts
+++ b/apps/api/src/config/env.ts
@@ -137,6 +137,12 @@ export const env = {
   googleWebClientId: process.env.GOOGLE_WEB_CLIENT_ID ?? '',
   googleIosClientId: process.env.GOOGLE_IOS_CLIENT_ID ?? '',
   googleAndroidClientId: process.env.GOOGLE_ANDROID_CLIENT_ID ?? '',
+  googlePlacesEnabled: enabled(process.env.GOOGLE_PLACES_ENABLED),
+  googleAddressValidationEnabled: enabled(process.env.GOOGLE_ADDRESS_VALIDATION_ENABLED),
+  googleMapsServerApiKey: process.env.GOOGLE_MAPS_SERVER_API_KEY ?? process.env.GOOGLE_PLACES_SERVER_API_KEY ?? '',
+  googlePlacesDefaultLanguage: process.env.GOOGLE_PLACES_DEFAULT_LANGUAGE ?? 'en',
+  googlePlacesCountryCodes: parseCsv(process.env.GOOGLE_PLACES_COUNTRY_CODES).map((value) => value.toUpperCase()).filter((value) => /^[A-Z]{2}$/.test(value)),
+  googlePlacesRequestTimeoutMs: boundedInt(process.env.GOOGLE_PLACES_REQUEST_TIMEOUT_MS, 4500, 1000, 15000),
   resendApiKey: process.env.RESEND_API_KEY ?? '',
   emailFrom: process.env.EMAIL_FROM ?? 'Hellowhen <support@mail.hellowhen.com>',
   passwordResetTtlMinutes: Number(process.env.PASSWORD_RESET_TTL_MINUTES ?? 45),
diff --git a/apps/api/src/modules/places/googlePlaces.routes.ts b/apps/api/src/modules/places/googlePlaces.routes.ts
new file mode 100644
index 0000000..3f7920c
--- /dev/null
+++ b/apps/api/src/modules/places/googlePlaces.routes.ts
@@ -0,0 +1,245 @@
+import { Router } from 'express';
+import {
+  googleAddressValidationRequestSchema,
+  googlePlaceDetailsQuerySchema,
+  googlePlaceSearchQuerySchema,
+  type GooglePlacePrediction,
+  type GoogleResolvedPlace,
+} from '@hellowhen/contracts';
+import { env } from '../../config/env.js';
+import { asyncRoute } from '../../lib/asyncRoute.js';
+import { requireActiveAccount, requireAuth } from '../../middleware/auth.js';
+import { createRateLimiter } from '../../middleware/rateLimit.js';
+
+export const googlePlacesRoutes = Router();
+
+const placesAutocompleteUrl = 'https://places.googleapis.com/v1/places:autocomplete';
+const placesDetailsBaseUrl = 'https://places.googleapis.com/v1/places';
+const addressValidationUrl = 'https://addressvalidation.googleapis.com/v1:validateAddress';
+
+const googlePlacesRateLimit = createRateLimiter({
+  keyPrefix: 'google-places',
+  windowMs: 60 * 1000,
+  max: 45,
+  message: 'Too many place searches. Please wait and try again.',
+});
+
+function disabledResponse(res: any, error: string, message: string, status = 404) {
+  return res.status(status).json({ error, message });
+}
+
+function requireGooglePlacesReady(res: any) {
+  if (!env.googlePlacesEnabled) {
+    return disabledResponse(res, 'google_places_disabled', 'Google place search is disabled for this launch.');
+  }
+  if (!env.googleMapsServerApiKey) {
+    return disabledResponse(res, 'google_places_not_configured', 'Google place search is not configured yet.', 503);
+  }
+  return null;
+}
+
+function requireAddressValidationReady(res: any) {
+  if (!env.googleAddressValidationEnabled) {
+    return disabledResponse(res, 'google_address_validation_disabled', 'Google address validation is disabled for this launch.');
+  }
+  if (!env.googleMapsServerApiKey) {
+    return disabledResponse(res, 'google_places_not_configured', 'Google place search is not configured yet.', 503);
+  }
+  return null;
+}
+
+function safeString(value: unknown) {
+  return typeof value === 'string' ? value.trim() : '';
+}
+
+function pickText(value: unknown) {
+  if (typeof value === 'string') return value.trim();
+  if (value && typeof value === 'object' && 'text' in value) return safeString((value as { text?: unknown }).text);
+  return '';
+}
+
+function cleanLanguageCode(input?: string) {
+  return input?.trim() || env.googlePlacesDefaultLanguage || 'en';
+}
+
+function cleanCountryCode(input?: string) {
+  const value = input?.trim().toUpperCase();
+  if (value && /^[A-Z]{2}$/.test(value)) return value;
+  const fallback = env.googlePlacesCountryCodes[0];
+  return fallback && /^[A-Z]{2}$/.test(fallback) ? fallback : undefined;
+}
+
+function uniqueStrings(value: unknown) {
+  if (!Array.isArray(value)) return [];
+  return Array.from(new Set(value.filter((item): item is string => typeof item === 'string' && item.trim().length > 0).map((item) => item.trim())));
+}
+
+function googleApiHeaders(fieldMask?: string) {
+  return {
+    'content-type': 'application/json',
+    'x-goog-api-key': env.googleMapsServerApiKey,
+    ...(fieldMask ? { 'x-goog-fieldmask': fieldMask } : {}),
+  };
+}
+
+async function fetchGoogleJson(url: string, init: RequestInit) {
+  const controller = new AbortController();
+  const timeout = setTimeout(() => controller.abort(), env.googlePlacesRequestTimeoutMs);
+  try {
+    const response = await fetch(url, { ...init, signal: controller.signal });
+    const json = await response.json().catch(() => null) as any;
+    if (!response.ok) {
+      const message = safeString(json?.error?.message) || safeString(json?.error_message) || 'Google place lookup failed.';
+      throw Object.assign(new Error(message), { statusCode: response.status, publicMessage: 'Google place lookup failed. Please try again.', providerMessage: message });
+    }
+    return json;
+  } finally {
+    clearTimeout(timeout);
+  }
+}
+
+function normalizePrediction(suggestion: any): GooglePlacePrediction | null {
+  const prediction = suggestion?.placePrediction;
+  const placeId = safeString(prediction?.placeId);
+  if (!placeId) return null;
+
+  const structured = prediction?.structuredFormat;
+  const mainText = pickText(structured?.mainText) || pickText(prediction?.text) || placeId;
+  const secondaryText = pickText(structured?.secondaryText) || null;
+  const description = pickText(prediction?.text) || [mainText, secondaryText].filter(Boolean).join(', ');
+
+  return {
+    placeId,
+    description,
+    mainText,
+    secondaryText,
+    types: uniqueStrings(prediction?.types),
+  };
+}
+
+function normalizeViewport(value: any) {
+  if (!value || typeof value !== 'object') return null;
+  const low = value.low;
+  const high = value.high;
+  return {
+    ...(typeof low?.latitude === 'number' && typeof low?.longitude === 'number' ? { low: { latitude: low.latitude, longitude: low.longitude } } : {}),
+    ...(typeof high?.latitude === 'number' && typeof high?.longitude === 'number' ? { high: { latitude: high.latitude, longitude: high.longitude } } : {}),
+  };
+}
+
+function normalizeAddressComponents(value: unknown) {
+  if (!Array.isArray(value)) return [];
+  return value.map((component) => {
+    const item = component as any;
+    return {
+      longText: safeString(item.longText) || null,
+      shortText: safeString(item.shortText) || null,
+      types: uniqueStrings(item.types),
+      languageCode: safeString(item.languageCode) || null,
+    };
+  });
+}
+
+function validationStatusFor(place: { formattedAddress?: string | null; latitude?: number | null; longitude?: number | null }) {
+  if (place.formattedAddress && typeof place.latitude === 'number' && typeof place.longitude === 'number') return 'confirmed' as const;
+  if (typeof place.latitude === 'number' && typeof place.longitude === 'number') return 'needs_review' as const;
+  return 'unsupported' as const;
+}
+
+function normalizePlace(place: any, fallbackPlaceId?: string): GoogleResolvedPlace {
+  const placeId = safeString(place?.id) || safeString(fallbackPlaceId) || safeString(place?.name).replace(/^places\//, '');
+  const location = place?.location;
+  const normalized = {
+    source: 'google_places' as const,
+    placeId,
+    name: pickText(place?.displayName) || null,
+    formattedAddress: safeString(place?.formattedAddress) || safeString(place?.postalAddress?.formattedAddress) || null,
+    latitude: typeof location?.latitude === 'number' ? location.latitude : null,
+    longitude: typeof location?.longitude === 'number' ? location.longitude : null,
+    googleMapsUri: safeString(place?.googleMapsUri) || null,
+    types: uniqueStrings(place?.types),
+    addressComponents: normalizeAddressComponents(place?.addressComponents),
+    viewport: normalizeViewport(place?.viewport),
+    validationStatus: 'unsupported' as const,
+  };
+  return { ...normalized, validationStatus: validationStatusFor(normalized) };
+}
+
+googlePlacesRoutes.use(requireAuth, requireActiveAccount, googlePlacesRateLimit);
+
+googlePlacesRoutes.get('/search', asyncRoute(async (req, res) => {
+  const disabled = requireGooglePlacesReady(res);
+  if (disabled) return disabled;
+
+  const input = googlePlaceSearchQuerySchema.parse(req.query ?? {});
+  const country = cleanCountryCode(input.country);
+  const body = {
+    input: input.q,
+    languageCode: cleanLanguageCode(input.languageCode),
+    ...(country ? { includedRegionCodes: [country] } : {}),
+    ...(input.sessionToken ? { sessionToken: input.sessionToken } : {}),
+  };
+
+  const json = await fetchGoogleJson(placesAutocompleteUrl, {
+    method: 'POST',
+    headers: googleApiHeaders('suggestions.placePrediction.placeId,suggestions.placePrediction.text,suggestions.placePrediction.structuredFormat,suggestions.placePrediction.types'),
+    body: JSON.stringify(body),
+  });
+
+  const predictions = Array.isArray(json?.suggestions)
+    ? json.suggestions.map(normalizePrediction).filter((item: GooglePlacePrediction | null): item is GooglePlacePrediction => Boolean(item)).slice(0, input.take ?? 5)
+    : [];
+
+  res.json({ predictions });
+}));
+
+googlePlacesRoutes.get('/details', asyncRoute(async (req, res) => {
+  const disabled = requireGooglePlacesReady(res);
+  if (disabled) return disabled;
+
+  const input = googlePlaceDetailsQuerySchema.parse(req.query ?? {});
+  const placeId = input.placeId.replace(/^places\//, '');
+  const url = new URL(`${placesDetailsBaseUrl}/${encodeURIComponent(placeId)}`);
+  url.searchParams.set('languageCode', cleanLanguageCode(input.languageCode));
+  if (input.sessionToken) url.searchParams.set('sessionToken', input.sessionToken);
+
+  const json = await fetchGoogleJson(url.toString(), {
+    method: 'GET',
+    headers: googleApiHeaders('id,displayName,formattedAddress,location,viewport,addressComponents,types,googleMapsUri'),
+  });
+
+  res.json({ place: normalizePlace(json, placeId) });
+}));
+
+googlePlacesRoutes.post('/validate-address', asyncRoute(async (req, res) => {
+  const disabled = requireAddressValidationReady(res);
+  if (disabled) return disabled;
+
+  const input = googleAddressValidationRequestSchema.parse(req.body ?? {});
+  const regionCode = cleanCountryCode(input.regionCode);
+  const json = await fetchGoogleJson(`${addressValidationUrl}?key=${encodeURIComponent(env.googleMapsServerApiKey)}`, {
+    method: 'POST',
+    headers: { 'content-type': 'application/json' },
+    body: JSON.stringify({
+      address: {
+        addressLines: [input.address],
+        ...(regionCode ? { regionCode } : {}),
+        languageCode: cleanLanguageCode(input.languageCode),
+      },
+    }),
+  });
+
+  const result = json?.result;
+  const geocode = result?.geocode;
+  const location = geocode?.location;
+  const place = location
+    ? normalizePlace({
+      id: safeString(geocode.placeId),
+      formattedAddress: safeString(result?.address?.formattedAddress) || input.address,
+      location,
+      addressComponents: result?.address?.addressComponents,
+    }, safeString(geocode.placeId))
+    : null;
+
+  res.json({ place, validationStatus: place?.validationStatus ?? 'unsupported' });
+}));
diff --git a/apps/api/src/modules/places/places.routes.ts b/apps/api/src/modules/places/places.routes.ts
index 6170156..69f57d6 100644
--- a/apps/api/src/modules/places/places.routes.ts
+++ b/apps/api/src/modules/places/places.routes.ts
@@ -16,6 +16,7 @@ import { syncInventoryTranslations, withInventoryTranslations, withOneInventoryT
 import { usersHaveBlockBetween } from '../users/userBlocks.js';
 import { loadMembershipAccessStateForUser } from '../subscriptions/membershipEntitlements.js';
 import { plusConfigSnapshot } from '../subscriptions/plus.routes.js';
+import { googlePlacesRoutes } from './googlePlaces.routes.js';
 
 export const placesRoutes = Router();
 
@@ -212,6 +213,8 @@ async function decoratePlace(place: any, viewerId?: string | null, visibility: M
   return decorated;
 }
 
+placesRoutes.use('/google', googlePlacesRoutes);
+
 async function loadVisiblePlace(placeId: string, viewerId?: string | null) {
   const place = await prisma.place.findFirst({
     where: {
diff --git a/packages/api-client/src/index.ts b/packages/api-client/src/index.ts
index 80ac38a..a2a56c3 100644
--- a/packages/api-client/src/index.ts
+++ b/packages/api-client/src/index.ts
@@ -1,4 +1,4 @@
-import type { LoginRequest, RegisterRequest, GoogleAuthRequest, ForgotPasswordRequest, ResetPasswordRequest, ChangePasswordRequest, AuthResponse, AuthMeResponse, LoginResponse, ForgotPasswordResponse, ResetPasswordResponse, ChangePasswordResponse, LogoutRequest, RefreshSessionRequest, TwoFactorChallengeRequest, TwoFactorCodeRequest, DisableTwoFactorRequest, ReauthenticateRequest, UpdateProfileRequest, UpdateSettingsRequest, CreateNeedRequest, CreateOfferRequest, CreateTradeRequest, UpdateNeedRequest, UpdateOfferRequest, ListInventoryTemplatesQuery, CloneInventoryTemplateRequest, ListInventoryFoldersQuery, CreateInventoryFolderRequest, UpdateInventoryFolderRequest, AddInventoryFolderItemRequest, InventoryFoldersResponse, InventoryFolderResponse, InventoryFolderItemResponse, CreateSavedItemRequest, SavedItemStatusQuery, ListSavedItemsQuery, CreateSavedCollectionRequest, UpdateSavedCollectionRequest, AddSavedCollectionItemRequest, SavedItemsResponse, SavedItemResponse, SavedItemStatusResponse, SavedCollectionsResponse, SavedCollectionResponse, SavedCollectionItemResponse, ListAgendaItemsQuery, AgendaIcsExportQuery, CreateAgendaItemRequest, UpdateAgendaItemRequest, AgendaItemsResponse, AgendaItemResponse, UpdateTradeStatusRequest, RenewTradeRequest, CreateTradeProposalRequest, UpdateProposalStatusRequest, UpdateProposalMessageRequest, CreateProposalMessageRequest, UpdateProposalPrivateMessageRequest, CreateTradePublicMessageRequest, UpdateTradePublicMessageRequest, UpdateMediaStatusRequest, CreateCheckoutSessionRequest, DemoTopUpRequest, DemoPayoutRequest, CreateSupportTicketRequest, CreateGuestSupportTicketRequest, CreateSupportMessageRequest, UpdateSupportTicketStatusRequest, AdminUpdateSupportTicketRequest, AdminCreateSupportMessageRequest, AdminUpdateTrustTierRequest, AdminPayoutActionRequest, AdminTradeDisputeActionRequest, AcknowledgeMoneySafetyRequest, MoneyProviderOnboardingLinkRequest, MoneyProviderWalletBalancesSyncRequest, CreateBusinessProfileRequest, UpdateBusinessProfileRequest, RequestBusinessReviewRequest, PublicBusinessProfileResponse, BusinessProviderOnboardingLinkRequest, BusinessInviteMemberRequest, BusinessUpdateMemberRequest, BusinessRemoveMemberRequest, BusinessInvitationActionRequest, BusinessAcceptInvitationRequest, BusinessCreateInventoryTemplateRequest, BusinessUpdateInventoryTemplateRequest, BusinessListInventoryTemplatesQuery, BusinessInventoryTemplateReviewRequest, BusinessInventoryTemplateArchiveRequest, BusinessCreateNeedRequest, BusinessCreateOfferRequest, BusinessUpdateNeedRequest, BusinessUpdateOfferRequest, BusinessListOwnedInventoryQuery, BusinessOwnedInventoryReviewRequest, BusinessOwnedInventoryArchiveRequest, BusinessSponsoredPlacementListQuery, BusinessCreateSponsoredPlacementRequest, BusinessUpdateSponsoredPlacementRequest, BusinessSponsoredPlacementReviewRequest, BusinessSponsoredPlacementArchiveRequest, BusinessCampaignListQuery, BusinessCreateCampaignRequest, BusinessUpdateCampaignRequest, BusinessCampaignReviewRequest, BusinessCampaignArchiveRequest, BusinessCreateCampaignItemRequest, BusinessUpdateCampaignItemRequest, BusinessCampaignItemActionRequest, BusinessBudgetListQuery, BusinessCreateBudgetRequest, BusinessUpdateBudgetRequest, BusinessBudgetReviewRequest, BusinessBudgetArchiveRequest, AdminBusinessCampaignActionRequest, AdminBusinessBudgetActionRequest, AdminBusinessSponsoredPlacementActionRequest, AdminBusinessProfileActionRequest, PublicProfileResponse, AdminOverviewResponse, CreateReportRequest, CreateDealProblemReportRequest, AdminReportActionRequest, AdminPlaceActionRequest, CreateAccountDeletionRequest, AccountDeletionRequestResponse, CreateUserBlockRequest, UserBlockResponse, UserBlocksResponse, CreatePlaceRequest, UpdatePlaceRequest, ListPlacesQuery, PlaceResponse, PlacesResponse, CreatePlanRequest, UpdatePlanRequest, CreatePlanPlaceRequest, UpdatePlanPlaceRequest, ListPlansQuery, CreatePlanJoinRequest, UpdatePlanParticipantRequest, UpdateMyPlanParticipantRequest, PlanResponse, PlansResponse, PlanParticipantResponse, PlanParticipantsResponse, CreatePlanPublicMessageRequest, UpdatePlanPublicMessageRequest, ListPlanPublicMessagesQuery, PlanPublicMessagesResponse, PlanPublicMessageResponse, UsageHeartbeatRequest, NotificationsResponse, NotificationResponse, NotificationUnreadCountResponse, MarkAllNotificationsReadResponse, ListNotificationsQuery, PlusSubscriptionSnapshotResponse, AdminPlusGrantRequest, AdminPlusUsersResponse, AdminPlusGrantResponse, AdminPlusAiAssistUsageResponse, AiAssistRequest, AiAssistResponse, ListTradeSearchSuggestionsQuery, RecordTradeSearchKeywordRequest, TradeSearchSuggestionsResponse, TradeSearchKeywordResponse, CreateMembershipCheckoutSessionRequest, MembershipCheckoutSessionResponse, MembershipCustomerPortalSessionResponse, SyncAppleStoreKitPurchaseRequest, AppleStoreKitSyncResponse, GooglePlayPurchaseSyncRequest, GooglePlaySyncResponse } from '@hellowhen/contracts';
+import type { LoginRequest, RegisterRequest, GoogleAuthRequest, ForgotPasswordRequest, ResetPasswordRequest, ChangePasswordRequest, AuthResponse, AuthMeResponse, LoginResponse, ForgotPasswordResponse, ResetPasswordResponse, ChangePasswordResponse, LogoutRequest, RefreshSessionRequest, TwoFactorChallengeRequest, TwoFactorCodeRequest, DisableTwoFactorRequest, ReauthenticateRequest, UpdateProfileRequest, UpdateSettingsRequest, CreateNeedRequest, CreateOfferRequest, CreateTradeRequest, UpdateNeedRequest, UpdateOfferRequest, ListInventoryTemplatesQuery, CloneInventoryTemplateRequest, ListInventoryFoldersQuery, CreateInventoryFolderRequest, UpdateInventoryFolderRequest, AddInventoryFolderItemRequest, InventoryFoldersResponse, InventoryFolderResponse, InventoryFolderItemResponse, CreateSavedItemRequest, SavedItemStatusQuery, ListSavedItemsQuery, CreateSavedCollectionRequest, UpdateSavedCollectionRequest, AddSavedCollectionItemRequest, SavedItemsResponse, SavedItemResponse, SavedItemStatusResponse, SavedCollectionsResponse, SavedCollectionResponse, SavedCollectionItemResponse, ListAgendaItemsQuery, AgendaIcsExportQuery, CreateAgendaItemRequest, UpdateAgendaItemRequest, AgendaItemsResponse, AgendaItemResponse, UpdateTradeStatusRequest, RenewTradeRequest, CreateTradeProposalRequest, UpdateProposalStatusRequest, UpdateProposalMessageRequest, CreateProposalMessageRequest, UpdateProposalPrivateMessageRequest, CreateTradePublicMessageRequest, UpdateTradePublicMessageRequest, UpdateMediaStatusRequest, CreateCheckoutSessionRequest, DemoTopUpRequest, DemoPayoutRequest, CreateSupportTicketRequest, CreateGuestSupportTicketRequest, CreateSupportMessageRequest, UpdateSupportTicketStatusRequest, AdminUpdateSupportTicketRequest, AdminCreateSupportMessageRequest, AdminUpdateTrustTierRequest, AdminPayoutActionRequest, AdminTradeDisputeActionRequest, AcknowledgeMoneySafetyRequest, MoneyProviderOnboardingLinkRequest, MoneyProviderWalletBalancesSyncRequest, CreateBusinessProfileRequest, UpdateBusinessProfileRequest, RequestBusinessReviewRequest, PublicBusinessProfileResponse, BusinessProviderOnboardingLinkRequest, BusinessInviteMemberRequest, BusinessUpdateMemberRequest, BusinessRemoveMemberRequest, BusinessInvitationActionRequest, BusinessAcceptInvitationRequest, BusinessCreateInventoryTemplateRequest, BusinessUpdateInventoryTemplateRequest, BusinessListInventoryTemplatesQuery, BusinessInventoryTemplateReviewRequest, BusinessInventoryTemplateArchiveRequest, BusinessCreateNeedRequest, BusinessCreateOfferRequest, BusinessUpdateNeedRequest, BusinessUpdateOfferRequest, BusinessListOwnedInventoryQuery, BusinessOwnedInventoryReviewRequest, BusinessOwnedInventoryArchiveRequest, BusinessSponsoredPlacementListQuery, BusinessCreateSponsoredPlacementRequest, BusinessUpdateSponsoredPlacementRequest, BusinessSponsoredPlacementReviewRequest, BusinessSponsoredPlacementArchiveRequest, BusinessCampaignListQuery, BusinessCreateCampaignRequest, BusinessUpdateCampaignRequest, BusinessCampaignReviewRequest, BusinessCampaignArchiveRequest, BusinessCreateCampaignItemRequest, BusinessUpdateCampaignItemRequest, BusinessCampaignItemActionRequest, BusinessBudgetListQuery, BusinessCreateBudgetRequest, BusinessUpdateBudgetRequest, BusinessBudgetReviewRequest, BusinessBudgetArchiveRequest, AdminBusinessCampaignActionRequest, AdminBusinessBudgetActionRequest, AdminBusinessSponsoredPlacementActionRequest, AdminBusinessProfileActionRequest, PublicProfileResponse, AdminOverviewResponse, CreateReportRequest, CreateDealProblemReportRequest, AdminReportActionRequest, AdminPlaceActionRequest, CreateAccountDeletionRequest, AccountDeletionRequestResponse, CreateUserBlockRequest, UserBlockResponse, UserBlocksResponse, CreatePlaceRequest, UpdatePlaceRequest, ListPlacesQuery, PlaceResponse, PlacesResponse, GooglePlaceSearchQuery, GooglePlaceSearchResponse, GooglePlaceDetailsQuery, GooglePlaceDetailsResponse, GoogleAddressValidationRequest, GoogleAddressValidationResponse, CreatePlanRequest, UpdatePlanRequest, CreatePlanPlaceRequest, UpdatePlanPlaceRequest, ListPlansQuery, CreatePlanJoinRequest, UpdatePlanParticipantRequest, UpdateMyPlanParticipantRequest, PlanResponse, PlansResponse, PlanParticipantResponse, PlanParticipantsResponse, CreatePlanPublicMessageRequest, UpdatePlanPublicMessageRequest, ListPlanPublicMessagesQuery, PlanPublicMessagesResponse, PlanPublicMessageResponse, UsageHeartbeatRequest, NotificationsResponse, NotificationResponse, NotificationUnreadCountResponse, MarkAllNotificationsReadResponse, ListNotificationsQuery, PlusSubscriptionSnapshotResponse, AdminPlusGrantRequest, AdminPlusUsersResponse, AdminPlusGrantResponse, AdminPlusAiAssistUsageResponse, AiAssistRequest, AiAssistResponse, ListTradeSearchSuggestionsQuery, RecordTradeSearchKeywordRequest, TradeSearchSuggestionsResponse, TradeSearchKeywordResponse, CreateMembershipCheckoutSessionRequest, MembershipCheckoutSessionResponse, MembershipCustomerPortalSessionResponse, SyncAppleStoreKitPurchaseRequest, AppleStoreKitSyncResponse, GooglePlayPurchaseSyncRequest, GooglePlaySyncResponse } from '@hellowhen/contracts';
 import { requestFormData, requestJson, requestText, type ApiClientOptions } from './http';
 
 function buildQueryString(query?: Record<string, unknown>) {
@@ -84,7 +84,7 @@ export function createApiClient(options: ApiClientOptions) {
     proposals: { mine: () => requestJson(options, '/proposals/mine'), get: (proposalId: string) => requestJson(options, `/proposals/${proposalId}`), updateStatus: (proposalId: string, body: UpdateProposalStatusRequest) => requestJson(options, `/proposals/${proposalId}/status`, { method: 'PATCH', body: JSON.stringify(body) }), updateMessage: (proposalId: string, body: UpdateProposalMessageRequest) => requestJson(options, `/proposals/${proposalId}/message`, { method: 'PATCH', body: JSON.stringify(body) }), deleteMessage: (proposalId: string) => requestJson(options, `/proposals/${proposalId}/message`, { method: 'DELETE' }), messages: (proposalId: string) => requestJson(options, `/proposals/${proposalId}/messages`), sendMessage: (proposalId: string, body: CreateProposalMessageRequest) => requestJson(options, `/proposals/${proposalId}/messages`, { method: 'POST', body: JSON.stringify(body) }), updatePrivateMessage: (proposalId: string, messageId: string, body: UpdateProposalPrivateMessageRequest) => requestJson(options, `/proposals/${proposalId}/messages/${messageId}`, { method: 'PATCH', body: JSON.stringify(body) }), deletePrivateMessage: (proposalId: string, messageId: string) => requestJson(options, `/proposals/${proposalId}/messages/${messageId}`, { method: 'DELETE' }), reportProblem: (proposalId: string, body: CreateDealProblemReportRequest) => requestJson(options, `/proposals/${proposalId}/problem-report`, { method: 'POST', body: JSON.stringify(body) }) },
     notifications: { mine: (query?: ListNotificationsQuery) => requestJson<NotificationsResponse>(options, `/notifications/mine${buildQueryString(query as Record<string, unknown> | undefined)}`), unreadCount: () => requestJson<NotificationUnreadCountResponse>(options, '/notifications/unread-count'), markRead: (notificationId: string) => requestJson<NotificationResponse>(options, `/notifications/${notificationId}/read`, { method: 'PATCH' }), markAllRead: () => requestJson<MarkAllNotificationsReadResponse>(options, '/notifications/mark-all-read', { method: 'PATCH' }) },
     media: { uploadImage: (formData: FormData) => requestFormData(options, '/media/image', formData), remove: (mediaId: string) => requestJson(options, `/media/${mediaId}`, { method: 'DELETE' }) },
-    places: { mine: (query?: ListPlacesQuery) => requestJson<PlacesResponse>(options, `/places/mine${buildQueryString(query as Record<string, unknown> | undefined)}`), library: (query?: ListPlacesQuery) => requestJson<PlacesResponse>(options, `/places/library${buildQueryString(query as Record<string, unknown> | undefined)}`), get: (placeId: string) => requestJson<PlaceResponse>(options, `/places/${placeId}`), create: (body: CreatePlaceRequest) => requestJson<PlaceResponse>(options, '/places', { method: 'POST', body: JSON.stringify(body) }), update: (placeId: string, body: UpdatePlaceRequest) => requestJson<PlaceResponse>(options, `/places/${placeId}`, { method: 'PATCH', body: JSON.stringify(body) }), archive: (placeId: string) => requestJson<PlaceResponse>(options, `/places/${placeId}`, { method: 'DELETE' }) },
+    places: { mine: (query?: ListPlacesQuery) => requestJson<PlacesResponse>(options, `/places/mine${buildQueryString(query as Record<string, unknown> | undefined)}`), library: (query?: ListPlacesQuery) => requestJson<PlacesResponse>(options, `/places/library${buildQueryString(query as Record<string, unknown> | undefined)}`), googleSearch: (query: GooglePlaceSearchQuery) => requestJson<GooglePlaceSearchResponse>(options, `/places/google/search${buildQueryString(query as Record<string, unknown>)}`), googleDetails: (query: GooglePlaceDetailsQuery) => requestJson<GooglePlaceDetailsResponse>(options, `/places/google/details${buildQueryString(query as Record<string, unknown>)}`), googleValidateAddress: (body: GoogleAddressValidationRequest) => requestJson<GoogleAddressValidationResponse>(options, '/places/google/validate-address', { method: 'POST', body: JSON.stringify(body) }), get: (placeId: string) => requestJson<PlaceResponse>(options, `/places/${placeId}`), create: (body: CreatePlaceRequest) => requestJson<PlaceResponse>(options, '/places', { method: 'POST', body: JSON.stringify(body) }), update: (placeId: string, body: UpdatePlaceRequest) => requestJson<PlaceResponse>(options, `/places/${placeId}`, { method: 'PATCH', body: JSON.stringify(body) }), archive: (placeId: string) => requestJson<PlaceResponse>(options, `/places/${placeId}`, { method: 'DELETE' }) },
     admin: { overview: () => requestJson<AdminOverviewResponse>(options, '/admin/overview'), plusUsers: (query?: string) => requestJson<AdminPlusUsersResponse>(options, `/admin/plus/users${query ?? ''}`), plusAiAssistUsage: (query?: string) => requestJson<AdminPlusAiAssistUsageResponse>(options, `/admin/plus/ai-assist-usage${query ?? ''}`), updatePlusGrant: (userId: string, body: AdminPlusGrantRequest) => requestJson<AdminPlusGrantResponse>(options, `/admin/plus/users/${userId}/grant`, { method: 'PATCH', body: JSON.stringify(body) }), reports: (query = '') => requestJson(options, `/admin/reports${query}`), updateReportAction: (reportId: string, body: AdminReportActionRequest) => requestJson(options, `/admin/reports/${reportId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), plans: (query = '') => requestJson(options, `/admin/plans${query}`), plan: (planId: string) => requestJson(options, `/admin/plans/${planId}`), planPublicMessages: (planId: string, query = '') => requestJson(options, `/admin/plans/${planId}/public-messages${query}`), updatePlanPublicMessageAction: (planId: string, messageId: string, body: { action: 'hide' | 'restore' | 'mark_reviewed'; note?: string }) => requestJson(options, `/admin/plans/${planId}/public-messages/${messageId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), updatePlanAction: (planId: string, body: { action: 'hide' | 'restore' | 'cancel' | 'mark_reviewed'; note?: string }) => requestJson(options, `/admin/plans/${planId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), adminPlaces: (query = '') => requestJson(options, `/admin/places${query}`), adminPlace: (placeId: string) => requestJson(options, `/admin/places/${placeId}`), updateAdminPlaceAction: (placeId: string, body: AdminPlaceActionRequest) => requestJson(options, `/admin/places/${placeId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), usage: () => requestJson(options, '/admin/usage'), moneySafety: () => requestJson(options, '/admin/money-safety'), businessProfiles: (query?: string) => requestJson(options, `/admin/business-profiles${query ?? ''}`), updateBusinessProfileAction: (businessProfileId: string, body: AdminBusinessProfileActionRequest) => requestJson(options, `/admin/business-profiles/${businessProfileId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), businessSponsoredPlacements: (query?: string) => requestJson(options, `/admin/business-sponsored-placements${query ?? ''}`), updateBusinessSponsoredPlacementAction: (placementId: string, body: AdminBusinessSponsoredPlacementActionRequest) => requestJson(options, `/admin/business-sponsored-placements/${placementId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), businessCampaigns: (query?: string) => requestJson(options, `/admin/business-campaigns${query ?? ''}`), updateBusinessCampaignAction: (campaignId: string, body: AdminBusinessCampaignActionRequest) => requestJson(options, `/admin/business-campaigns/${campaignId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), businessBudgets: (query?: string) => requestJson(options, `/admin/business-budgets${query ?? ''}`), updateBusinessBudgetAction: (budgetId: string, body: AdminBusinessBudgetActionRequest) => requestJson(options, `/admin/business-budgets/${budgetId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), updateLibraryTemplateAction: (templateId: string, body: { action: 'approve' | 'reject' | 'hide' | 'restore'; note?: string }) => requestJson(options, `/admin/library/${templateId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), media: (status?: string) => requestJson(options, `/admin/media${status ? `?status=${encodeURIComponent(status)}` : ''}`), updateMediaStatus: (mediaId: string, body: UpdateMediaStatusRequest) => requestJson(options, `/admin/media/${mediaId}/status`, { method: 'PATCH', body: JSON.stringify(body) }), creditPurchases: (status?: string) => requestJson(options, `/admin/credits/purchases${status ? `?status=${encodeURIComponent(status)}` : ''}`), supportTickets: (query = '') => requestJson(options, `/admin/support/tickets${query}`), supportTicket: (ticketId: string) => requestJson(options, `/admin/support/tickets/${ticketId}`), updateSupportTicket: (ticketId: string, body: AdminUpdateSupportTicketRequest) => requestJson(options, `/admin/support/tickets/${ticketId}`, { method: 'PATCH', body: JSON.stringify(body) }), sendSupportMessage: (ticketId: string, body: AdminCreateSupportMessageRequest) => requestJson(options, `/admin/support/tickets/${ticketId}/messages`, { method: 'POST', body: JSON.stringify(body) }), users: () => requestJson(options, '/admin/users'), updateUserTrustTier: (userId: string, body: AdminUpdateTrustTierRequest) => requestJson(options, `/admin/users/${userId}/trust-tier`, { method: 'PATCH', body: JSON.stringify(body) }), payouts: (query?: string) => requestJson(options, `/admin/payouts${query ?? ''}`), payoutDetails: (payoutId: string) => requestJson(options, `/admin/payouts/${payoutId}`), updatePayoutAction: (payoutId: string, body: AdminPayoutActionRequest) => requestJson(options, `/admin/payouts/${payoutId}/action`, { method: 'PATCH', body: JSON.stringify(body) }), syncProviderPayout: (payoutId: string, body?: { scaToken?: string }) => requestJson(options, `/admin/payouts/${payoutId}/provider-sync`, { method: 'POST', body: JSON.stringify(body ?? {}) }), tradeDisputes: () => requestJson(options, '/admin/trades/disputes'), resolveTradeDispute: (tradeId: string, body: AdminTradeDisputeActionRequest) => requestJson(options, `/admin/trades/${tradeId}/dispute`, { method: 'PATCH', body: JSON.stringify(body) }), stripeConnectAccounts: () => requestJson(options, '/admin/stripe/connect-accounts'), stripeEvents: () => requestJson(options, '/admin/stripe/events'), moneyProviderAccounts: () => requestJson(options, '/admin/money/provider-accounts'), moneyProviderEvents: () => requestJson(options, '/admin/money/provider-events'), moneyProviderBalances: () => requestJson(options, '/admin/money/provider-balances'), moneyProviderTransactions: () => requestJson(options, '/admin/money/provider-transactions'), syncMoneyProviderBalances: (accountId: string, body?: MoneyProviderWalletBalancesSyncRequest) => requestJson(options, `/admin/money/provider-accounts/${accountId}/sync-balances`, { method: 'POST', body: JSON.stringify(body ?? {}) }) },
     credits: { packages: () => requestJson(options, '/credits/packages'), createCheckoutSession: (body: CreateCheckoutSessionRequest) => requestJson(options, '/credits/checkout-session', { method: 'POST', body: JSON.stringify(body) }), purchasesMine: () => requestJson(options, '/credits/purchases/mine') },
     account: { deletionRequest: () => requestJson<AccountDeletionRequestResponse>(options, '/account/deletion-request'), requestDeletion: (body?: CreateAccountDeletionRequest) => requestJson<AccountDeletionRequestResponse>(options, '/account/deletion-request', { method: 'POST', body: JSON.stringify(body ?? {}) }), cancelDeletionRequest: () => requestJson<AccountDeletionRequestResponse>(options, '/account/deletion-request/cancel', { method: 'PATCH' }) },
diff --git a/packages/contracts/src/plans.ts b/packages/contracts/src/plans.ts
index 0d771e7..be7c25f 100644
--- a/packages/contracts/src/plans.ts
+++ b/packages/contracts/src/plans.ts
@@ -12,6 +12,58 @@ export const placeStatusSchema = z.enum(['draft', 'active', 'archived', 'hidden'
 export const placeVisibilitySchema = z.enum(['private', 'public', 'library']);
 export const planPlaceSourceSchema = z.enum(['custom', 'my_place', 'hellowhen_library']);
 
+export const googlePlaceValidationStatusSchema = z.enum(['confirmed', 'needs_review', 'unsupported']);
+export const googlePlaceAddressComponentSchema = z.object({
+  longText: z.string().nullable().optional(),
+  shortText: z.string().nullable().optional(),
+  types: z.array(z.string()).optional(),
+  languageCode: z.string().nullable().optional(),
+}).passthrough();
+export const googlePlaceViewportSchema = z.object({
+  low: z.object({ latitude: z.number(), longitude: z.number() }).optional(),
+  high: z.object({ latitude: z.number(), longitude: z.number() }).optional(),
+}).passthrough();
+export const googlePlacePredictionSchema = z.object({
+  placeId: z.string(),
+  description: z.string(),
+  mainText: z.string(),
+  secondaryText: z.string().nullable().optional(),
+  types: z.array(z.string()).optional(),
+});
+export const googleResolvedPlaceSchema = z.object({
+  source: z.literal('google_places'),
+  placeId: z.string(),
+  name: z.string().nullable().optional(),
+  formattedAddress: z.string().nullable().optional(),
+  latitude: z.number().nullable().optional(),
+  longitude: z.number().nullable().optional(),
+  googleMapsUri: z.string().nullable().optional(),
+  types: z.array(z.string()).optional(),
+  addressComponents: z.array(googlePlaceAddressComponentSchema).optional(),
+  viewport: googlePlaceViewportSchema.nullable().optional(),
+  validationStatus: googlePlaceValidationStatusSchema,
+}).passthrough();
+export const googlePlaceSearchQuerySchema = z.object({
+  q: z.string().trim().min(2).max(160),
+  languageCode: z.string().trim().min(2).max(12).optional(),
+  sessionToken: z.string().trim().min(8).max(120).optional(),
+  country: z.string().trim().min(2).max(2).optional(),
+  take: z.coerce.number().int().min(1).max(8).optional(),
+});
+export const googlePlaceDetailsQuerySchema = z.object({
+  placeId: z.string().trim().min(3).max(240),
+  languageCode: z.string().trim().min(2).max(12).optional(),
+  sessionToken: z.string().trim().min(8).max(120).optional(),
+});
+export const googleAddressValidationRequestSchema = z.object({
+  address: z.string().trim().min(3).max(300),
+  regionCode: z.string().trim().min(2).max(2).optional(),
+  languageCode: z.string().trim().min(2).max(12).optional(),
+});
+export const googlePlaceSearchResponseSchema = z.object({ predictions: z.array(googlePlacePredictionSchema) });
+export const googlePlaceDetailsResponseSchema = z.object({ place: googleResolvedPlaceSchema });
+export const googleAddressValidationResponseSchema = z.object({ place: googleResolvedPlaceSchema.nullable(), validationStatus: googlePlaceValidationStatusSchema });
+
 export const PLAN_PLACE_MEDIA_LIMITS = {
   free: 1,
   plus: 5,
@@ -355,6 +407,15 @@ export type PlaceSource = z.infer<typeof placeSourceSchema>;
 export type PlaceStatus = z.infer<typeof placeStatusSchema>;
 export type PlaceVisibility = z.infer<typeof placeVisibilitySchema>;
 export type PlanPlaceSource = z.infer<typeof planPlaceSourceSchema>;
+export type GooglePlaceValidationStatus = z.infer<typeof googlePlaceValidationStatusSchema>;
+export type GooglePlacePrediction = z.infer<typeof googlePlacePredictionSchema>;
+export type GoogleResolvedPlace = z.infer<typeof googleResolvedPlaceSchema>;
+export type GooglePlaceSearchQuery = z.infer<typeof googlePlaceSearchQuerySchema>;
+export type GooglePlaceDetailsQuery = z.infer<typeof googlePlaceDetailsQuerySchema>;
+export type GoogleAddressValidationRequest = z.infer<typeof googleAddressValidationRequestSchema>;
+export type GooglePlaceSearchResponse = z.infer<typeof googlePlaceSearchResponseSchema>;
+export type GooglePlaceDetailsResponse = z.infer<typeof googlePlaceDetailsResponseSchema>;
+export type GoogleAddressValidationResponse = z.infer<typeof googleAddressValidationResponseSchema>;
 export type CreatePlaceRequest = z.infer<typeof createPlaceRequestSchema>;
 export type UpdatePlaceRequest = z.infer<typeof updatePlaceRequestSchema>;
 export type ListPlacesQuery = z.infer<typeof listPlacesQuerySchema>;
