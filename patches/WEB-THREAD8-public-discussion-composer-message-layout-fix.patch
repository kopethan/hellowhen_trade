diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 46a8070..9f41f21 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -5023,6 +5023,11 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .public-discussion-empty-text { margin: 26px 0; color: var(--app-text-muted); font-size: 14px; font-weight: 850; text-align: center; }
 .public-discussion-section--messages-only .public-message-date { margin: 14px 0; }
 .public-discussion-section--messages-only .public-message { margin-inline: -16px; padding: 13px 16px; border-bottom: 1px solid var(--app-divider-strong); }
+.public-discussion-section--messages-only .public-message-list--thread { margin-inline: -16px; }
+.public-discussion-section--messages-only .public-message-list--thread .public-message { margin-inline: 0; }
+.public-discussion-section--messages-only .notice-box { margin: 12px 0; }
+.public-discussion-section--messages-only .public-message__header { align-items: center; }
+.public-discussion-section--messages-only .public-message__meta { min-width: fit-content; }
 .public-discussion-section--messages-only .public-message__body { font-size: 14px; font-weight: 760; line-height: 1.52; }
 .public-discussion-section--messages-only .public-message__menu { padding-top: 3px; }
 .public-discussion-composer--messages-only { position: sticky; bottom: calc(var(--app-tab-height) + env(safe-area-inset-bottom) + 4px); z-index: 30; grid-template-columns: minmax(0, 1fr) auto; align-items: end; gap: 8px; margin: 10px -16px 0; border: 0; border-top: 1px solid var(--app-divider-strong); border-radius: 0; background: color-mix(in srgb, var(--app-bg) 94%, transparent); padding: 10px 16px; box-shadow: none; backdrop-filter: blur(16px); }
@@ -5373,7 +5378,8 @@ html[data-theme="dark"] .onboarding-guide-primary {
   flex: 1 1 auto;
   flex-direction: column;
   overflow-y: auto;
-  padding: 0 16px;
+  overscroll-behavior: contain;
+  padding: 0 16px 16px;
   scrollbar-width: none;
 }
 
@@ -5385,10 +5391,35 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 
 .public-discussion-composer--messages-only {
-  bottom: 0;
+  position: static;
+  z-index: 20;
   flex: 0 0 auto;
-  margin: auto -16px 0;
+  grid-template-columns: minmax(0, 1fr) auto;
+  align-items: end;
+  gap: 8px;
+  margin: 0;
+  border: 0;
+  border-top: 1px solid var(--app-divider-strong);
+  border-radius: 0;
+  background: color-mix(in srgb, var(--app-bg) 96%, transparent);
   padding: 10px 16px max(10px, env(safe-area-inset-bottom));
+  box-shadow: none;
+  backdrop-filter: blur(16px);
+}
+
+.public-discussion-composer--messages-only textarea {
+  min-height: 44px;
+  max-height: 120px;
+  border-radius: 18px;
+  background: var(--app-surface);
+}
+
+.public-discussion-bottom-notice {
+  flex: 0 0 auto;
+  margin: 0;
+  border-width: 1px 0 0;
+  border-radius: 0;
+  padding: 12px 16px max(12px, env(safe-area-inset-bottom));
 }
 
 .private-proposals-page--messages-only .private-proposals-section--clean {
diff --git a/apps/web/src/features/trade/TradePublicDiscussionClient.tsx b/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
index f1b5e01..c01cf0b 100644
--- a/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
+++ b/apps/web/src/features/trade/TradePublicDiscussionClient.tsx
@@ -324,7 +324,7 @@ export function TradePublicDiscussionClient({ tradeId }: { tradeId: string }) {
       <section className="trade-social-section public-discussion-section public-discussion-section--messages-only">
         {notice ? <p className={`notice-box ${notice.tone}`}>{notice.body}</p> : null}
         {messages.length ? (
-          <div className="public-message-list">
+          <div className="public-message-list public-message-list--thread">
             {groupedMessages.map(({ message, showDate }) => {
               const ownMessage = message.authorId === auth.user?.id;
               const deleted = message.status === 'deleted' || Boolean(message.deletedAt);
@@ -386,17 +386,17 @@ export function TradePublicDiscussionClient({ tradeId }: { tradeId: string }) {
         ) : (
           <p className="public-discussion-empty-text">{t('trade.publicDiscussion.emptyTitle')}</p>
         )}
-
-        {canWrite ? (
-          <form className="conversation-reply public-discussion-composer public-discussion-composer--messages-only" onSubmit={submitMessage}>
-            <label className="sr-only" htmlFor="public-discussion-message">{t('trade.publicDiscussion.placeholder')}</label>
-            <textarea id="public-discussion-message" value={body} onChange={(event) => setBody(event.target.value)} placeholder={t('trade.publicDiscussion.placeholder')} rows={1} />
-            <button type="submit" disabled={sending || trimmedBody.length < 1}>{sending ? t('common.states.sending') : t('common.actions.send')}</button>
-          </form>
-        ) : (
-          <p className="notice-box warning">{t('trade.publicDiscussion.restricted')}</p>
-        )}
       </section>
+
+      {canWrite ? (
+        <form className="conversation-reply public-discussion-composer public-discussion-composer--messages-only" onSubmit={submitMessage}>
+          <label className="sr-only" htmlFor="public-discussion-message">{t('trade.publicDiscussion.placeholder')}</label>
+          <textarea id="public-discussion-message" value={body} onChange={(event) => setBody(event.target.value)} placeholder={t('trade.publicDiscussion.placeholder')} rows={1} />
+          <button type="submit" disabled={sending || trimmedBody.length < 1}>{sending ? t('common.states.sending') : t('common.actions.send')}</button>
+        </form>
+      ) : (
+        <p className="notice-box warning public-discussion-bottom-notice">{t('trade.publicDiscussion.restricted')}</p>
+      )}
     </article>
   );
 }
