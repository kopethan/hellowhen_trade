diff --git a/apps/mobile/src/components/LowerImageAtmosphere.tsx b/apps/mobile/src/components/LowerImageAtmosphere.tsx
index 096c8e8..4ef1fd1 100644
--- a/apps/mobile/src/components/LowerImageAtmosphere.tsx
+++ b/apps/mobile/src/components/LowerImageAtmosphere.tsx
@@ -42,25 +42,25 @@ type AtmospherePresetConfig = {
 const POSTER_ATMOSPHERE_PRESET_CONFIGS: Record<LowerImageAtmospherePreset, AtmospherePresetConfig> = {
   trade: {
     startPercent: 50,
-    tintBandCount: 7,
-    tintBandOverlap: 1.6,
-    tintStrength: 0.46,
+    tintBandCount: 5,
+    tintBandOverlap: 7,
+    tintStrength: 0.34,
     imageBands: [
-      { top: 56, height: 34, blur: 14, opacity: 0.045 },
-      { top: 70, height: 32, blur: 28, opacity: 0.105 },
+      { top: 58, height: 32, blur: 14, opacity: 0.035 },
+      { top: 72, height: 30, blur: 26, opacity: 0.085 },
     ],
-    tint: { dark: 'rgba(1,7,7,0.72)', light: 'rgba(2,22,18,0.72)' },
+    tint: { dark: 'rgba(1,7,7,0.68)', light: 'rgba(2,22,18,0.68)' },
   },
   plan: {
     startPercent: 50,
-    tintBandCount: 7,
-    tintBandOverlap: 1.6,
-    tintStrength: 0.5,
+    tintBandCount: 5,
+    tintBandOverlap: 7,
+    tintStrength: 0.38,
     imageBands: [
-      { top: 56, height: 34, blur: 14, opacity: 0.045 },
-      { top: 70, height: 32, blur: 28, opacity: 0.11 },
+      { top: 58, height: 32, blur: 14, opacity: 0.038 },
+      { top: 72, height: 30, blur: 26, opacity: 0.09 },
     ],
-    tint: { dark: 'rgba(4,4,12,0.72)', light: 'rgba(5,12,22,0.72)' },
+    tint: { dark: 'rgba(4,4,12,0.68)', light: 'rgba(5,12,22,0.68)' },
   },
 };
 
@@ -86,7 +86,7 @@ function createProfile(preset: LowerImageAtmospherePreset): LowerImageAtmosphere
       return {
         top: safeStart + index * tintBandHeight,
         height: tintBandHeight + config.tintBandOverlap,
-        opacity: clamp(eased * config.tintStrength + bottomGuard * 0.12, 0, 0.62),
+        opacity: clamp(eased * config.tintStrength + bottomGuard * 0.1, 0, 0.5),
       };
     }),
   };
diff --git a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
index e26ca94..d2236ab 100644
--- a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
+++ b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
@@ -120,12 +120,12 @@ function PlanPlaceDeckCardView({ card, onOpen, topBadgeLabel, topBadgeTone = 'in
   const timeLabel = isEmpty ? getPlanParticipantLabel(card.plan) : getPlaceDateLabel(place, card.plan.startsAt);
   const primaryBadgeLabel = topBadgeLabel ?? `Place · ${cardCounter}`;
   const hasPosterImage = Boolean(imageUrl);
-  const posterTextShadow = hasPosterImage ? 'rgba(0,0,0,0.56)' : isDark ? 'rgba(0,0,0,0.42)' : 'rgba(255,255,255,0.48)';
+  const posterTextShadow = hasPosterImage ? 'rgba(0,0,0,0.5)' : isDark ? 'rgba(0,0,0,0.42)' : 'rgba(255,255,255,0.48)';
   const posterTitleColor = hasPosterImage ? '#FFFFFF' : theme.color.text;
-  const posterMutedColor = hasPosterImage ? 'rgba(255,255,255,0.88)' : theme.color.muted;
-  const posterSubtleColor = hasPosterImage ? 'rgba(255,255,255,0.86)' : theme.color.muted;
-  const posterPillBg = hasPosterImage ? 'rgba(12,17,24,0.5)' : undefined;
-  const posterPillBorder = hasPosterImage ? 'rgba(255,255,255,0.16)' : undefined;
+  const posterMutedColor = hasPosterImage ? 'rgba(255,255,255,0.9)' : theme.color.muted;
+  const posterSubtleColor = hasPosterImage ? 'rgba(255,255,255,0.84)' : theme.color.muted;
+  const posterPillBg = hasPosterImage ? 'rgba(12,17,24,0.38)' : undefined;
+  const posterPillBorder = hasPosterImage ? 'rgba(255,255,255,0.12)' : undefined;
   const posterPillText = hasPosterImage ? 'rgba(255,255,255,0.94)' : undefined;
 
   return (
@@ -135,7 +135,7 @@ function PlanPlaceDeckCardView({ card, onOpen, topBadgeLabel, topBadgeTone = 'in
       onPress={onOpen}
       style={({ pressed }) => [
         styles.card,
-        { backgroundColor: imageUrl ? '#0B1016' : theme.semantic.place.softBg, borderColor: imageUrl ? 'rgba(255,255,255,0.34)' : theme.semantic.place.border },
+        { backgroundColor: imageUrl ? '#0B1016' : theme.semantic.place.softBg, borderColor: imageUrl ? 'rgba(255,255,255,0.22)' : theme.semantic.place.border },
         pressed && styles.pressed,
       ]}
     >
@@ -229,7 +229,7 @@ const styles = StyleSheet.create({
     borderRadius: 28,
     borderWidth: 1,
     overflow: 'hidden',
-    padding: 18,
+    padding: 17,
     justifyContent: 'space-between',
   },
   cardImage: {
@@ -279,12 +279,12 @@ const styles = StyleSheet.create({
     flexWrap: 'wrap',
   },
   posterPill: {
-    minHeight: 28,
+    minHeight: 26,
     maxWidth: '60%',
     borderRadius: 999,
     borderWidth: StyleSheet.hairlineWidth,
-    paddingHorizontal: 12,
-    paddingVertical: 6,
+    paddingHorizontal: 10,
+    paddingVertical: 5,
     alignItems: 'center',
     justifyContent: 'center',
   },
@@ -299,46 +299,46 @@ const styles = StyleSheet.create({
     textTransform: 'uppercase',
   },
   cardCopy: {
-    gap: 6,
-    paddingTop: 128,
+    gap: 5,
+    paddingTop: 134,
   },
   planTitle: {
-    fontSize: 12,
+    fontSize: 11,
     fontWeight: '900',
     textTransform: 'uppercase',
     letterSpacing: 0.9,
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 8,
+    textShadowRadius: 7,
   },
   placeTitle: {
-    fontSize: 31,
-    lineHeight: 35,
+    fontSize: 29,
+    lineHeight: 33,
     fontWeight: '900',
     letterSpacing: -1,
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 10,
+    textShadowRadius: 8,
   },
   emptyHint: {
     fontSize: 14,
     lineHeight: 19,
     fontWeight: '800',
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 8,
+    textShadowRadius: 7,
   },
   placeMetaText: {
-    marginTop: 6,
+    marginTop: 4,
     fontSize: 13,
     lineHeight: 18,
     fontWeight: '800',
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 8,
+    textShadowRadius: 7,
   },
   placeTimeText: {
     fontSize: 13,
     lineHeight: 18,
     fontWeight: '900',
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 7,
+    textShadowRadius: 6,
   },
   pressed: {
     opacity: 0.76,
diff --git a/apps/mobile/src/features/trade/components/TradePosterCard.tsx b/apps/mobile/src/features/trade/components/TradePosterCard.tsx
index 1df155d..aee7954 100644
--- a/apps/mobile/src/features/trade/components/TradePosterCard.tsx
+++ b/apps/mobile/src/features/trade/components/TradePosterCard.tsx
@@ -89,12 +89,12 @@ function TradePosterCardInner({ id, imageUrl, badge, eyebrow, title, subtitle, c
   const titleColor = hasPosterImage ? '#FFFFFF' : isDark ? '#FFFFFF' : '#101010';
   const bodyColor = hasPosterImage ? 'rgba(255,255,255,0.9)' : isDark ? 'rgba(255,255,255,0.86)' : 'rgba(15,20,28,0.86)';
   const eyebrowColor = hasPosterImage ? 'rgba(255,255,255,0.94)' : isDark ? 'rgba(255,255,255,0.92)' : 'rgba(12,18,28,0.84)';
-  const overlayTextShadow = hasPosterImage ? 'rgba(0,0,0,0.54)' : isDark ? 'rgba(0,0,0,0.50)' : 'rgba(255,255,255,0.38)';
-  const topPillBg = hasPosterImage ? 'rgba(12,17,24,0.5)' : isDark ? 'rgba(10,16,24,0.36)' : 'rgba(255,255,255,0.84)';
-  const topPillBorder = hasPosterImage ? 'rgba(255,255,255,0.16)' : isDark ? 'rgba(255,255,255,0.16)' : 'rgba(15,23,42,0.07)';
-  const lowerPillBg = hasPosterImage ? 'rgba(12,17,24,0.34)' : topPillBg;
-  const lowerPillBorder = hasPosterImage ? 'rgba(255,255,255,0.13)' : topPillBorder;
-  const cardBorder = hasPosterImage ? 'rgba(255,255,255,0.34)' : 'rgba(15,23,42,0.08)';
+  const overlayTextShadow = hasPosterImage ? 'rgba(0,0,0,0.48)' : isDark ? 'rgba(0,0,0,0.50)' : 'rgba(255,255,255,0.38)';
+  const topPillBg = hasPosterImage ? 'rgba(12,17,24,0.38)' : isDark ? 'rgba(10,16,24,0.36)' : 'rgba(255,255,255,0.84)';
+  const topPillBorder = hasPosterImage ? 'rgba(255,255,255,0.12)' : isDark ? 'rgba(255,255,255,0.16)' : 'rgba(15,23,42,0.07)';
+  const lowerPillBg = hasPosterImage ? 'rgba(12,17,24,0.24)' : topPillBg;
+  const lowerPillBorder = hasPosterImage ? 'rgba(255,255,255,0.1)' : topPillBorder;
+  const cardBorder = hasPosterImage ? 'rgba(255,255,255,0.22)' : 'rgba(15,23,42,0.08)';
   const statusColor = status?.tone === 'none'
     ? (hasPosterImage ? 'rgba(255,255,255,0.9)' : isDark ? 'rgba(255,255,255,0.58)' : 'rgba(15,23,42,0.58)')
     : status?.tone === 'expired'
@@ -248,9 +248,9 @@ const styles = StyleSheet.create({
   contentLayer: {
     ...StyleSheet.absoluteFillObject,
     justifyContent: 'space-between',
-    paddingHorizontal: 14,
-    paddingTop: 14,
-    paddingBottom: 18,
+    paddingHorizontal: 15,
+    paddingTop: 15,
+    paddingBottom: 17,
   },
   topBar: {
     flexDirection: 'row',
@@ -259,12 +259,12 @@ const styles = StyleSheet.create({
     gap: 8,
   },
   badge: {
-    minHeight: 26,
+    minHeight: 25,
     maxWidth: '58%',
     borderRadius: 999,
     borderWidth: StyleSheet.hairlineWidth,
-    paddingHorizontal: 11,
-    paddingVertical: 5,
+    paddingHorizontal: 10,
+    paddingVertical: 4,
     alignItems: 'center',
     justifyContent: 'center',
   },
@@ -274,13 +274,13 @@ const styles = StyleSheet.create({
     alignItems: 'flex-end',
   },
   topMetaBadge: {
-    minHeight: 26,
+    minHeight: 25,
     maxWidth: '48%',
     flexShrink: 1,
     borderRadius: 999,
     borderWidth: StyleSheet.hairlineWidth,
-    paddingHorizontal: 10,
-    paddingVertical: 5,
+    paddingHorizontal: 9,
+    paddingVertical: 4,
     alignItems: 'center',
     justifyContent: 'center',
   },
@@ -310,22 +310,22 @@ const styles = StyleSheet.create({
     textTransform: 'uppercase',
     letterSpacing: 0.75,
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 8,
+    textShadowRadius: 7,
   },
   title: {
-    fontSize: 20,
-    lineHeight: 24,
+    fontSize: 19,
+    lineHeight: 23,
     fontWeight: '900',
     letterSpacing: -0.45,
     textShadowOffset: { width: 0, height: 2 },
-    textShadowRadius: 9,
+    textShadowRadius: 8,
   },
   subtitle: {
     fontSize: 12,
     lineHeight: 16,
     fontWeight: '800',
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 8,
+    textShadowRadius: 7,
   },
   statusText: {
     alignSelf: 'flex-start',
@@ -335,7 +335,7 @@ const styles = StyleSheet.create({
     fontWeight: '900',
     letterSpacing: 0.8,
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 7,
+    textShadowRadius: 6,
   },
   footerLabel: {
     alignSelf: 'flex-start',
@@ -345,7 +345,7 @@ const styles = StyleSheet.create({
     fontWeight: '900',
     letterSpacing: 0.45,
     textShadowOffset: { width: 0, height: 1 },
-    textShadowRadius: 7,
+    textShadowRadius: 6,
   },
   chipRow: {
     flexDirection: 'row',
@@ -357,8 +357,8 @@ const styles = StyleSheet.create({
     maxWidth: 132,
     borderRadius: 999,
     borderWidth: StyleSheet.hairlineWidth,
-    paddingHorizontal: 10,
-    paddingVertical: 5,
+    paddingHorizontal: 9,
+    paddingVertical: 4,
   },
   chipText: {
     fontSize: 11,
