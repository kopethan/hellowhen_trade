diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 9421c48..ccdf2c5 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -1186,6 +1186,26 @@ html.trade-tools-open, html.trade-tools-open body { overflow: hidden; }
 .account-overview-card { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 14px; align-items: center; border: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent); border-radius: 30px; background: var(--app-surface); padding: 16px; box-shadow: var(--app-card-shadow); }
 .account-overview-card h2 { margin: 8px 0 4px; }
 .account-overview-card p { margin: 0; }
+
+.membership-hub-preview-card {
+  display: grid;
+  grid-template-columns: auto minmax(0, 1fr) auto auto;
+  gap: 12px;
+  align-items: center;
+  border: 1px solid color-mix(in srgb, var(--success-border) 44%, var(--app-border));
+  border-radius: 28px;
+  background: linear-gradient(135deg, color-mix(in srgb, var(--success-bg) 18%, var(--app-surface)), var(--app-surface));
+  color: inherit;
+  padding: 14px;
+  box-shadow: var(--app-card-shadow);
+}
+.membership-hub-preview-card:hover { border-color: color-mix(in srgb, var(--success-border) 68%, var(--app-border)); text-decoration: none; transform: translateY(-1px); }
+.membership-hub-preview-card__icon { display: grid; place-items: center; width: 44px; height: 44px; border: 1px solid color-mix(in srgb, var(--success-border) 42%, var(--app-border)); border-radius: 17px; background: var(--app-surface); color: var(--app-text); font-size: 17px; font-weight: 950; box-shadow: var(--app-control-shadow); }
+.membership-hub-preview-card__body { display: grid; gap: 4px; min-width: 0; }
+.membership-hub-preview-card__body > strong { overflow: hidden; color: var(--app-text); font-size: 18px; font-weight: 950; letter-spacing: -0.025em; text-overflow: ellipsis; white-space: nowrap; }
+.membership-hub-preview-card__body > span:not(.semantic-badge), .membership-hub-preview-card__body > small { color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
+.membership-hub-preview-card__meta { display: flex; flex-wrap: wrap; justify-content: flex-end; gap: 7px; max-width: 190px; }
+.membership-hub-preview-card__arrow { color: var(--app-text-soft); }
 .account-avatar { display: grid; place-items: center; overflow: hidden; width: 58px; height: 58px; border: 1px solid var(--app-border-strong); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text); font-size: 24px; font-weight: 950; }
 .account-avatar--large { width: 86px; height: 86px; font-size: 34px; }
 .account-avatar img { width: 100%; height: 100%; object-fit: cover; display: block; }
@@ -1209,7 +1229,8 @@ html.trade-tools-open, html.trade-tools-open body { overflow: hidden; }
 @media (max-width: 390px) {
   .wallet-metric-grid, .wallet-hero-card__actions, .wallet-preview-strip { grid-template-columns: 1fr; }
   .quick-amount-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
-  .profile-photo-panel, .account-overview-card { grid-template-columns: 1fr; }
+  .profile-photo-panel, .account-overview-card, .membership-hub-preview-card { grid-template-columns: 1fr; }
+  .membership-hub-preview-card__meta { justify-content: flex-start; max-width: none; }
 }
 
 /* Phase 19.6: responsive desktop shell and trade deck grid */
diff --git a/apps/web/src/features/account/AccountHubClient.tsx b/apps/web/src/features/account/AccountHubClient.tsx
index ece3ccf..dbd358b 100644
--- a/apps/web/src/features/account/AccountHubClient.tsx
+++ b/apps/web/src/features/account/AccountHubClient.tsx
@@ -2,7 +2,8 @@
 
 import Link from 'next/link';
 import { useEffect, useMemo, useState } from 'react';
-import type { PayoutSummaryDto, WalletDto } from '@hellowhen/contracts';
+import type { PlusSubscriptionSnapshotResponse, PayoutSummaryDto, WalletDto } from '@hellowhen/contracts';
+import { PERSONAL_MEMBERSHIP_TIER_METADATA, normalizePersonalMembershipTier, normalizeSubscriptionStatus, type SubscriptionStatus } from '@hellowhen/shared';
 import { WebIcon, type WebIconName } from '../../components/WebIcon';
 import { api } from '../../lib/api';
 import { betaFeatures } from '../../lib/betaFeatures';
@@ -10,6 +11,22 @@ import { useWebAuth } from '../../providers/WebAuthProvider';
 import { useWebTranslation } from '../../providers/WebI18nProvider';
 import { assetUrl, fallbackCurrency, formatMoney, formatPayoutFeeRate, normalizePayoutFeeRateBps, normalizePayouts, normalizeWallet } from './accountPresentation';
 
+
+function membershipStatusTone(status: SubscriptionStatus) {
+  switch (status) {
+    case 'active':
+    case 'trialing':
+      return 'success';
+    case 'past_due':
+    case 'expired':
+      return 'warning';
+    case 'canceled':
+      return 'neutral';
+    default:
+      return 'neutral';
+  }
+}
+
 type AccountHubItem = {
   href: string;
   titleKey: string;
@@ -27,6 +44,9 @@ export function AccountHubClient() {
   const [wallet, setWallet] = useState<WalletDto | null>(null);
   const [summary, setSummary] = useState<PayoutSummaryDto | null>(null);
   const [notificationUnreadCount, setNotificationUnreadCount] = useState(0);
+  const [membershipSnapshot, setMembershipSnapshot] = useState<PlusSubscriptionSnapshotResponse | null>(null);
+  const [membershipPreviewLoaded, setMembershipPreviewLoaded] = useState(false);
+  const [membershipPreviewError, setMembershipPreviewError] = useState(false);
 
   const accountItems = useMemo<AccountHubItem[]>(() => [
     { href: '/account/profile', titleKey: 'account.items.profile.title', bodyKey: 'account.items.profile.body', icon: 'profile' },
@@ -74,6 +94,37 @@ export function AccountHubClient() {
     return () => { mounted = false; };
   }, [auth.hydrated, auth.isAuthenticated]);
 
+
+  useEffect(() => {
+    let mounted = true;
+    async function loadMembershipPreview() {
+      if (!auth.hydrated || !auth.isAuthenticated) {
+        if (mounted) {
+          setMembershipSnapshot(null);
+          setMembershipPreviewLoaded(auth.hydrated);
+          setMembershipPreviewError(false);
+        }
+        return;
+      }
+      try {
+        const response = await api.plus.me();
+        if (mounted) {
+          setMembershipSnapshot(response);
+          setMembershipPreviewLoaded(true);
+          setMembershipPreviewError(false);
+        }
+      } catch {
+        if (mounted) {
+          setMembershipSnapshot(null);
+          setMembershipPreviewLoaded(true);
+          setMembershipPreviewError(true);
+        }
+      }
+    }
+    void loadMembershipPreview();
+    return () => { mounted = false; };
+  }, [auth.hydrated, auth.isAuthenticated]);
+
   useEffect(() => {
     let mounted = true;
     async function loadNotificationPreview() {
@@ -94,6 +145,10 @@ export function AccountHubClient() {
 
   const currency = wallet?.currency ?? auth.user?.profile?.preferredCurrency ?? fallbackCurrency;
   const platformFeeRateBps = normalizePayoutFeeRateBps(summary?.platformFeeRateBps);
+  const membershipTier = normalizePersonalMembershipTier(membershipSnapshot?.state.subscriptionTier);
+  const membershipStatus = normalizeSubscriptionStatus(membershipSnapshot?.state.subscriptionStatus);
+  const membershipMetadata = PERSONAL_MEMBERSHIP_TIER_METADATA[membershipTier];
+  const hasPlusAccess = Boolean(membershipSnapshot?.access.hasPlusAccess);
 
   return (
     <div className="mobile-page">
@@ -117,6 +172,23 @@ export function AccountHubClient() {
         </section>
       ) : null}
 
+      {auth.isAuthenticated ? (
+        <Link href="/account/membership" className="membership-hub-preview-card" aria-label={t('account.membershipPreview.openAria')}>
+          <span className="membership-hub-preview-card__icon" aria-hidden="true">★</span>
+          <span className="membership-hub-preview-card__body">
+            <span className="semantic-badge instruction">{t('account.items.membership.badge')}</span>
+            <strong>{membershipPreviewError ? t('account.membershipPreview.unavailable') : membershipPreviewLoaded ? membershipMetadata.displayName : t('account.membershipPreview.loading')}</strong>
+            <span>{membershipPreviewError ? t('account.membershipPreview.unavailableBody') : membershipPreviewLoaded ? t(`account.membershipPreview.status.${membershipStatus}`) : t('account.membershipPreview.loadingBody')}</span>
+            <small>{t('account.membershipPreview.boundary')}</small>
+          </span>
+          <span className="membership-hub-preview-card__meta">
+            <span className={`semantic-badge ${membershipPreviewError ? 'warning' : membershipStatusTone(membershipStatus)}`}>{membershipPreviewError ? t('account.membershipPreview.unavailableShort') : membershipPreviewLoaded ? t(`account.membershipPreview.statusShort.${membershipStatus}`) : t('account.membershipPreview.loadingShort')}</span>
+            <span className={`semantic-badge ${hasPlusAccess ? 'success' : 'neutral'}`}>{hasPlusAccess ? t('account.membershipPreview.plusActive') : t('account.membershipPreview.plusInactive')}</span>
+          </span>
+          <WebIcon name="arrow-right" size={17} decorative className="membership-hub-preview-card__arrow" />
+        </Link>
+      ) : null}
+
       {auth.isAuthenticated && (betaFeatures.walletVisible || betaFeatures.payoutsVisible) ? (
         <section className="wallet-preview-strip">
           <div>
diff --git a/packages/i18n/src/locales/en/account.ts b/packages/i18n/src/locales/en/account.ts
index 6c69074..ecc9127 100644
--- a/packages/i18n/src/locales/en/account.ts
+++ b/packages/i18n/src/locales/en/account.ts
@@ -639,6 +639,34 @@ export const account = {
       privacyNote: 'Manual notes are private. Agenda is not a booking system and has no external calendar sync in this version.',
     },
   },
+  membershipPreview: {
+    openAria: 'Open Membership details',
+    loading: 'Loading membership…',
+    loadingBody: 'Checking your account status.',
+    loadingShort: 'Loading',
+    unavailable: 'Membership status unavailable',
+    unavailableBody: 'Open Membership to retry the account status snapshot.',
+    unavailableShort: 'Unavailable',
+    boundary: 'Personal membership is separate from Business / organization identity.',
+    plusActive: 'Plus access',
+    plusInactive: 'No Plus access',
+    status: {
+      none: 'No active subscription status is attached to this account.',
+      trialing: 'Your personal membership is in a trial period.',
+      active: 'Your personal membership is active.',
+      past_due: 'Your personal membership needs attention.',
+      canceled: 'Your personal membership is canceled.',
+      expired: 'Your personal membership is expired.',
+    },
+    statusShort: {
+      none: 'None',
+      trialing: 'Trial',
+      active: 'Active',
+      past_due: 'Past due',
+      canceled: 'Canceled',
+      expired: 'Expired',
+    },
+  },
   items: {
     profile: {
       title: 'Profile',
diff --git a/packages/i18n/src/locales/fr/account.ts b/packages/i18n/src/locales/fr/account.ts
index 169e985..8ed0c74 100644
--- a/packages/i18n/src/locales/fr/account.ts
+++ b/packages/i18n/src/locales/fr/account.ts
@@ -639,6 +639,34 @@ export const account = {
       privacyNote: 'Les notes manuelles sont privées. Agenda n’est pas un système de réservation et n’a pas de synchronisation calendrier externe dans cette version.',
     },
   },
+  membershipPreview: {
+    openAria: 'Ouvrir les détails Membership',
+    loading: 'Chargement du membership…',
+    loadingBody: 'Vérification du statut du compte.',
+    loadingShort: 'Chargement',
+    unavailable: 'Statut Membership indisponible',
+    unavailableBody: 'Ouvrez Membership pour réessayer le statut du compte.',
+    unavailableShort: 'Indisponible',
+    boundary: 'Le membership personnel est séparé de l’identité Business / organisation.',
+    plusActive: 'Accès Plus',
+    plusInactive: 'Pas d’accès Plus',
+    status: {
+      none: 'Aucun statut d’abonnement actif n’est attaché à ce compte.',
+      trialing: 'Votre membership personnel est en période d’essai.',
+      active: 'Votre membership personnel est actif.',
+      past_due: 'Votre membership personnel demande une attention.',
+      canceled: 'Votre membership personnel est annulé.',
+      expired: 'Votre membership personnel a expiré.',
+    },
+    statusShort: {
+      none: 'Aucun',
+      trialing: 'Essai',
+      active: 'Actif',
+      past_due: 'Retard',
+      canceled: 'Annulé',
+      expired: 'Expiré',
+    },
+  },
   items: {
     profile: {
       title: 'Profil',
