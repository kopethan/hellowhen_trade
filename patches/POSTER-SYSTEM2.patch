diff --git a/apps/mobile/src/components/PosterGlassFooter.tsx b/apps/mobile/src/components/PosterGlassFooter.tsx
new file mode 100644
index 0000000..898dc71
--- /dev/null
+++ b/apps/mobile/src/components/PosterGlassFooter.tsx
@@ -0,0 +1,60 @@
+import React, { type PropsWithChildren } from 'react';
+import { StyleSheet, View, type StyleProp, type ViewStyle } from 'react-native';
+
+type PosterGlassFooterProps = PropsWithChildren<{
+  enabled?: boolean;
+  isDark: boolean;
+  style?: StyleProp<ViewStyle>;
+  glassStyle?: StyleProp<ViewStyle>;
+  contentStyle?: StyleProp<ViewStyle>;
+}>;
+
+export function PosterGlassFooter({ enabled = true, isDark, style, glassStyle, contentStyle, children }: PosterGlassFooterProps) {
+  if (!enabled) {
+    return <View style={style}>{children}</View>;
+  }
+
+  return (
+    <View style={style}>
+      <View
+        style={[
+          styles.glass,
+          {
+            backgroundColor: isDark ? 'rgba(3,9,13,0.2)' : 'rgba(4,13,12,0.17)',
+            borderColor: 'rgba(255,255,255,0.1)',
+          },
+          glassStyle,
+        ]}
+      >
+        <View pointerEvents="none" style={[styles.softWash, { backgroundColor: isDark ? 'rgba(255,255,255,0.035)' : 'rgba(255,255,255,0.045)' }]} />
+        <View pointerEvents="none" style={[styles.lowerVeil, { backgroundColor: isDark ? 'rgba(0,0,0,0.04)' : 'rgba(0,0,0,0.035)' }]} />
+        <View style={[styles.content, contentStyle]}>{children}</View>
+      </View>
+    </View>
+  );
+}
+
+const styles = StyleSheet.create({
+  glass: {
+    overflow: 'hidden',
+    borderWidth: StyleSheet.hairlineWidth,
+    borderRadius: 22,
+  },
+  softWash: {
+    position: 'absolute',
+    left: 0,
+    right: 0,
+    top: 0,
+    height: '46%',
+  },
+  lowerVeil: {
+    position: 'absolute',
+    left: 0,
+    right: 0,
+    bottom: 0,
+    height: '42%',
+  },
+  content: {
+    position: 'relative',
+  },
+});
diff --git a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
index 05613fb..b79de10 100644
--- a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
+++ b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
@@ -4,6 +4,7 @@ import type { MediaAssetDto, PlaceStaticMapDto, PlanDto, PlanPlaceDto } from '@h
 import type { SemanticColorName } from '@hellowhen/theme';
 import { AppText } from '../../../components/AppText';
 import { LowerImageAtmosphere } from '../../../components/LowerImageAtmosphere';
+import { PosterGlassFooter } from '../../../components/PosterGlassFooter';
 import { SemanticBadge } from '../../../components/SemanticUI';
 import { useThemeTokens } from '../../../providers/ThemeProvider';
 import { ContinuousSquareStackDeck, type SquareStackDeckCard } from '../../trade/deck';
@@ -208,13 +209,19 @@ function PlanPlaceDeckCardView({ card, onOpen, topBadgeLabel, topBadgeTone = 'in
         )) : null}
       </View>
 
-      <View style={[styles.cardCopy, hasPosterImage && styles.posterCardCopy]}>
+      <PosterGlassFooter
+        enabled={hasPosterImage}
+        isDark={isDark}
+        style={[styles.cardCopy, hasPosterImage && styles.posterCardCopy]}
+        glassStyle={styles.posterCardGlass}
+        contentStyle={styles.posterCardGlassContent}
+      >
         <AppText style={[styles.planTitle, { color: posterSubtleColor, textShadowColor: posterTextShadow }]} numberOfLines={1}>{card.plan.title}</AppText>
         <AppText style={[styles.placeTitle, { color: posterTitleColor, textShadowColor: posterTextShadow }]} numberOfLines={2}>{placeTitle}</AppText>
         {isEmpty ? <AppText style={[styles.emptyHint, { color: posterMutedColor, textShadowColor: posterTextShadow }]} numberOfLines={2}>Add a first stop to turn this Plan into route cards.</AppText> : null}
         {!isEmpty && locationLabel ? <AppText style={[styles.placeMetaText, { color: posterMutedColor, textShadowColor: posterTextShadow }]} numberOfLines={1}>{locationLabel}</AppText> : null}
         <AppText style={[styles.placeTimeText, { color: posterMutedColor, textShadowColor: posterTextShadow }]} numberOfLines={1}>{timeLabel}</AppText>
-      </View>
+      </PosterGlassFooter>
     </Pressable>
   );
 }
@@ -323,9 +330,19 @@ const styles = StyleSheet.create({
     paddingTop: 150,
   },
   posterCardCopy: {
+    paddingTop: 154,
+    paddingRight: 0,
+  },
+  posterCardGlass: {
+    marginHorizontal: -8,
+    marginBottom: -8,
+    borderRadius: 23,
+  },
+  posterCardGlassContent: {
     gap: 3,
-    paddingTop: 160,
-    paddingRight: 4,
+    paddingHorizontal: 9,
+    paddingTop: 8,
+    paddingBottom: 9,
   },
   planTitle: {
     fontSize: 10,
diff --git a/apps/mobile/src/features/trade/components/TradePosterCard.tsx b/apps/mobile/src/features/trade/components/TradePosterCard.tsx
index f70ab79..ffac8d4 100644
--- a/apps/mobile/src/features/trade/components/TradePosterCard.tsx
+++ b/apps/mobile/src/features/trade/components/TradePosterCard.tsx
@@ -3,6 +3,7 @@ import { Image, Pressable, StyleSheet, View } from 'react-native';
 import { normalizePreviewCardTheme } from '@hellowhen/shared';
 import { AppText } from '../../../components/AppText';
 import { LowerImageAtmosphere } from '../../../components/LowerImageAtmosphere';
+import { PosterGlassFooter } from '../../../components/PosterGlassFooter';
 import { useThemeTokens } from '../../../providers/ThemeProvider';
 
 export type TradePosterCardVariant = 'trade' | 'need' | 'offer';
@@ -150,15 +151,12 @@ function TradePosterCardInner({ id, imageUrl, badge, eyebrow, title, subtitle, c
           ) : null}
         </View>
 
-        <View
-          style={[
-            styles.copyBlock,
-            hasPosterImage && styles.copyGlassBlock,
-            hasPosterImage && {
-              backgroundColor: isDark ? 'rgba(4,8,13,0.13)' : 'rgba(4,12,11,0.12)',
-              borderColor: 'rgba(255,255,255,0.085)',
-            },
-          ]}
+        <PosterGlassFooter
+          enabled={hasPosterImage}
+          isDark={isDark}
+          style={styles.copyBlock}
+          glassStyle={styles.copyGlassBlock}
+          contentStyle={styles.copyGlassContent}
         >
           {eyebrow ? (
             <AppText
@@ -219,7 +217,7 @@ function TradePosterCardInner({ id, imageUrl, badge, eyebrow, title, subtitle, c
               ))}
             </View>
           ) : null}
-        </View>
+        </PosterGlassFooter>
       </View>
     </Pressable>
   );
@@ -313,13 +311,15 @@ const styles = StyleSheet.create({
     paddingBottom: 0,
   },
   copyGlassBlock: {
-    marginHorizontal: -6,
-    marginBottom: -7,
-    borderRadius: 20,
-    borderWidth: StyleSheet.hairlineWidth,
-    paddingHorizontal: 6,
-    paddingTop: 6,
-    paddingBottom: 7,
+    marginHorizontal: -7,
+    marginBottom: -8,
+    borderRadius: 22,
+  },
+  copyGlassContent: {
+    gap: 4,
+    paddingHorizontal: 8,
+    paddingTop: 7,
+    paddingBottom: 8,
   },
   eyebrow: {
     fontSize: 11,
