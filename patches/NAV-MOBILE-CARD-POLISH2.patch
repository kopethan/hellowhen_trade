diff --git a/apps/mobile/src/components/LowerImageAtmosphere.tsx b/apps/mobile/src/components/LowerImageAtmosphere.tsx
index 2123abe..804f9ca 100644
--- a/apps/mobile/src/components/LowerImageAtmosphere.tsx
+++ b/apps/mobile/src/components/LowerImageAtmosphere.tsx
@@ -18,9 +18,16 @@ type TintBand = {
   opacity: number;
 };
 
+type WashBand = {
+  top: number;
+  height: number;
+  opacity: number;
+};
+
 type LowerImageAtmosphereProfile = {
   imageBands: readonly BlurBand[];
   tintBands: readonly TintBand[];
+  baseWash: WashBand;
   tint: ToneValue;
 };
 
@@ -34,33 +41,42 @@ type AtmospherePresetConfig = {
   startPercent: number;
   tintBandCount: number;
   tintBandOverlap: number;
+  tintBaseOpacity: number;
   tintStrength: number;
+  tintMaxOpacity: number;
+  baseWashOpacity: number;
   imageBands: readonly BlurBand[];
   tint: ToneValue;
 };
 
 const POSTER_ATMOSPHERE_PRESET_CONFIGS: Record<LowerImageAtmospherePreset, AtmospherePresetConfig> = {
   trade: {
-    startPercent: 44,
-    tintBandCount: 6,
-    tintBandOverlap: 22,
-    tintStrength: 0.34,
+    startPercent: 42,
+    tintBandCount: 9,
+    tintBandOverlap: 8,
+    tintBaseOpacity: 0.028,
+    tintStrength: 0.22,
+    tintMaxOpacity: 0.4,
+    baseWashOpacity: 0.14,
     imageBands: [
-      { top: 54, height: 40, blur: 12, opacity: 0.04 },
-      { top: 70, height: 32, blur: 24, opacity: 0.082 },
+      { top: 50, height: 44, blur: 14, opacity: 0.045 },
+      { top: 66, height: 36, blur: 26, opacity: 0.085 },
     ],
-    tint: { dark: 'rgba(1,5,5,0.72)', light: 'rgba(4,12,11,0.72)' },
+    tint: { dark: 'rgba(1,5,5,0.78)', light: 'rgba(4,12,11,0.76)' },
   },
   plan: {
-    startPercent: 46,
-    tintBandCount: 6,
-    tintBandOverlap: 22,
-    tintStrength: 0.33,
+    startPercent: 44,
+    tintBandCount: 9,
+    tintBandOverlap: 8,
+    tintBaseOpacity: 0.026,
+    tintStrength: 0.21,
+    tintMaxOpacity: 0.38,
+    baseWashOpacity: 0.13,
     imageBands: [
-      { top: 56, height: 38, blur: 12, opacity: 0.038 },
-      { top: 72, height: 30, blur: 24, opacity: 0.078 },
+      { top: 52, height: 42, blur: 14, opacity: 0.042 },
+      { top: 68, height: 34, blur: 26, opacity: 0.082 },
     ],
-    tint: { dark: 'rgba(2,3,8,0.7)', light: 'rgba(5,10,14,0.7)' },
+    tint: { dark: 'rgba(2,3,8,0.76)', light: 'rgba(5,10,14,0.74)' },
   },
 };
 
@@ -78,25 +94,25 @@ function createProfile(preset: LowerImageAtmospherePreset): LowerImageAtmosphere
   return {
     imageBands: config.imageBands,
     tint: config.tint,
+    baseWash: {
+      top: safeStart,
+      height: overlayHeight,
+      opacity: config.baseWashOpacity,
+    },
     tintBands: Array.from({ length: config.tintBandCount }, (_, index) => {
-      const progress = (index + 1) / config.tintBandCount;
-      const eased = smoothstep((progress - 0.08) / 0.92);
-      const bottomGuard = smoothstep((progress - 0.68) / 0.32);
+      const progress = (index + 0.5) / config.tintBandCount;
+      const eased = Math.pow(progress, 1.45);
+      const tailLift = Math.pow(progress, 3.2) * 0.12;
 
       return {
         top: safeStart + index * tintBandHeight,
         height: tintBandHeight + config.tintBandOverlap,
-        opacity: clamp(eased * config.tintStrength + bottomGuard * 0.16, 0, 0.56),
+        opacity: clamp(config.tintBaseOpacity + eased * config.tintStrength + tailLift, 0, config.tintMaxOpacity),
       };
     }),
   };
 }
 
-function smoothstep(value: number) {
-  const progress = clamp(value, 0, 1);
-  return progress * progress * (3 - 2 * progress);
-}
-
 function clamp(value: number, min: number, max: number) {
   return Math.min(Math.max(value, min), max);
 }
@@ -128,6 +144,17 @@ export function LowerImageAtmosphere({ imageUrl, isDark, preset = 'trade' }: Low
           <Image source={{ uri: imageUrl }} resizeMode="cover" blurRadius={band.blur} style={StyleSheet.absoluteFillObject} />
         </View>
       ))}
+      <View
+        style={[
+          styles.band,
+          {
+            top: `${profile.baseWash.top}%`,
+            height: `${profile.baseWash.height}%`,
+            backgroundColor: imageTint,
+            opacity: profile.baseWash.opacity,
+          },
+        ]}
+      />
       {profile.tintBands.map((band, index) => (
         <View
           key={`${preset}-lower-image-atmosphere-tint-band-${index}`}
