diff --git a/apps/mobile/src/features/trade/CreateTradeScreen.tsx b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
index 0477b7f..5bd4cd4 100644
--- a/apps/mobile/src/features/trade/CreateTradeScreen.tsx
+++ b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
@@ -12,6 +12,7 @@ import { api } from '../../lib/api';
 import { getFriendlyApiErrorMessage } from '../../lib/errors';
 import { betaFeatures } from '../../lib/betaFeatures';
 import { useUnsavedChangesWarning } from '../../hooks/useUnsavedChangesWarning';
+import { AppActionSheet, type AppActionSheetAction } from '../../components/AppActionSheet';
 import { AppCard } from '../../components/AppCard';
 import { AppConfirmSheet } from '../../components/AppConfirmSheet';
 import { AppText } from '../../components/AppText';
@@ -165,6 +166,8 @@ export function CreateTradeScreen({ route, navigation }: Props) {
   const [loading, setLoading] = useState(false);
   const [submitting, setSubmitting] = useState(false);
   const [error, setError] = useState<string | null>(null);
+  const [menuSheetVisible, setMenuSheetVisible] = useState(false);
+  const [helpSheetVisible, setHelpSheetVisible] = useState(false);
   const processedSelectionRef = useRef<TradeCreateSideSelection | null>(null);
 
   const usableNeeds = useMemo(() => needs.filter(isNeedAvailable), [needs]);
@@ -215,7 +218,6 @@ export function CreateTradeScreen({ route, navigation }: Props) {
     {
       id: 'type',
       title: t('trade.create.publishQuestion'),
-      description: t('trade.create.chooseKindBody'),
       completed: Boolean(postType),
     },
     {
@@ -404,10 +406,64 @@ export function CreateTradeScreen({ route, navigation }: Props) {
 
   const footerHelperText = loading ? t('trade.create.loadingSavedInventory') : undefined;
 
+  async function resetWizardDraft() {
+    setMenuSheetVisible(false);
+    setError(null);
+    setPostType(null);
+    setNeedSelection(null);
+    setOfferSelection(null);
+    setExpiryDays(14);
+    setActiveStepId('type');
+    await tradeWizardDraft.clearDraft();
+  }
+
+  const menuActions: AppActionSheetAction[] = [
+    {
+      key: 'full-form',
+      label: t('inventory.wizard.openFullForm'),
+      icon: 'edit',
+      onPress: () => {
+        setMenuSheetVisible(false);
+        navigateToFullForm();
+      },
+    },
+    {
+      key: 'reset-draft',
+      label: t('trade.create.resetDraft'),
+      icon: 'refresh',
+      tone: 'danger',
+      disabled: submitting || !hasDraft,
+      onPress: () => { void resetWizardDraft(); },
+    },
+    {
+      key: 'help',
+      label: t('trade.create.wizardHelpTitle'),
+      icon: 'help',
+      onPress: () => {
+        setMenuSheetVisible(false);
+        setHelpSheetVisible(true);
+      },
+    },
+  ];
+
+  const headerMenuButton = (
+    <Pressable
+      accessibilityRole="button"
+      accessibilityLabel={t('trade.create.wizardMenuTitle')}
+      disabled={submitting}
+      onPress={() => setMenuSheetVisible(true)}
+      hitSlop={10}
+      style={({ pressed }) => [styles.headerMenuButton, { backgroundColor: theme.color.subtleSurface, borderColor: theme.color.border }, pressed && styles.pressed, submitting && styles.disabled]}
+    >
+      <MobileIcon name="more" size={19} color={theme.color.muted} />
+    </Pressable>
+  );
+
   return (
     <WizardShell
       title={t('trade.create.title')}
       onBack={() => navigation.goBack()}
+      rightSlot={headerMenuButton}
       steps={steps}
       activeStepId={activeStepId}
       stepLabel={t('inventory.wizard.stepLabel')}
@@ -422,9 +478,6 @@ export function CreateTradeScreen({ route, navigation }: Props) {
           secondaryLabel={isFirstStep ? undefined : t('common.actions.back')}
           onSecondary={isFirstStep ? undefined : handlePrevious}
           secondaryDisabled={submitting}
-          tertiaryLabel={t('inventory.wizard.openFullForm')}
-          onTertiary={navigateToFullForm}
-          tertiaryDisabled={submitting}
           helperText={footerHelperText}
         />
       )}
@@ -462,6 +515,22 @@ export function CreateTradeScreen({ route, navigation }: Props) {
         />
       )}
       <AppConfirmSheet {...unsavedChangesConfirm} />
+      <AppActionSheet
+        visible={menuSheetVisible}
+        title={t('trade.create.wizardMenuTitle')}
+        body={t('trade.create.wizardMenuBody')}
+        actions={menuActions}
+        cancelLabel={t('common.actions.cancel')}
+        onClose={() => setMenuSheetVisible(false)}
+      />
+      <AppActionSheet
+        visible={helpSheetVisible}
+        title={t('trade.create.wizardHelpTitle')}
+        body={t('trade.create.wizardHelpBody')}
+        actions={[]}
+        cancelLabel={t('common.actions.close')}
+        onClose={() => setHelpSheetVisible(false)}
+      />
     </WizardShell>
   );
 }
@@ -702,6 +771,7 @@ function PostTypeCard({ option, selected, theme, onPress }: { option: PostTypeOp
 }
 
 const styles = StyleSheet.create({
+  headerMenuButton: { width: 38, height: 38, borderRadius: 19, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
   postTypeList: { gap: 10 },
   postTypeCard: { borderRadius: 20, borderWidth: 1, padding: 13, flexDirection: 'row', alignItems: 'center', gap: 11 },
   postTypeIcon: { width: 42, height: 42, borderRadius: 21, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
diff --git a/packages/i18n/src/locales/en/trade.ts b/packages/i18n/src/locales/en/trade.ts
index 654e248..40b2cc9 100644
--- a/packages/i18n/src/locales/en/trade.ts
+++ b/packages/i18n/src/locales/en/trade.ts
@@ -417,6 +417,11 @@ export const trade = {
   create: {
     title: 'Create Trade',
     publishQuestion: 'What do you want to publish?',
+    wizardMenuTitle: 'Create trade options',
+    wizardMenuBody: 'Keep the wizard compact. Use these actions only when needed.',
+    wizardHelpTitle: 'Create trade help',
+    wizardHelpBody: 'Trade: publish what you need and what you offer. Open Need: publish only a Need so others can propose Offers. Open Offer: publish only an Offer so others can propose Needs.',
+    resetDraft: 'Reset draft',
     choosePublishType: 'Choose what you want to publish first.',
     changePublishType: 'Change publish type',
     missingSide: 'Missing side',
diff --git a/packages/i18n/src/locales/fr/trade.ts b/packages/i18n/src/locales/fr/trade.ts
index 4f28bbe..04c4e67 100644
--- a/packages/i18n/src/locales/fr/trade.ts
+++ b/packages/i18n/src/locales/fr/trade.ts
@@ -417,6 +417,11 @@ export const trade = {
   create: {
     title: 'Créer un échange',
     publishQuestion: 'Que voulez-vous publier ?',
+    wizardMenuTitle: 'Options de création',
+    wizardMenuBody: 'Le parcours reste compact. Utilisez ces actions seulement si nécessaire.',
+    wizardHelpTitle: 'Aide création d’échange',
+    wizardHelpBody: 'Échange : publiez ce dont vous avez besoin et ce que vous offrez. Besoin ouvert : publiez seulement un besoin pour recevoir des offres. Offre ouverte : publiez seulement une offre pour recevoir des besoins.',
+    resetDraft: 'Réinitialiser le brouillon',
     choosePublishType: 'Choisissez d’abord ce que vous voulez publier.',
     changePublishType: 'Changer le type de publication',
     missingSide: 'Côté manquant',
