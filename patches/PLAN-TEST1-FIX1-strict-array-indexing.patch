diff --git a/apps/api/src/modules/plans/plans.safety.testkit.ts b/apps/api/src/modules/plans/plans.safety.testkit.ts
index 136d623..490843f 100644
--- a/apps/api/src/modules/plans/plans.safety.testkit.ts
+++ b/apps/api/src/modules/plans/plans.safety.testkit.ts
@@ -40,8 +40,12 @@ export function findStopGapViolation(input: PlanScheduleInput) {
     .filter((place): place is typeof place & { startsAt: string | Date } => Boolean(place.startsAt));
   const minGapMs = PLAN_STOP_MIN_GAP_MINUTES * 60 * 1000;
   for (let index = 1; index < ordered.length; index += 1) {
-    const previous = asDate(ordered[index - 1].startsAt);
-    const current = asDate(ordered[index].startsAt);
+    const previousStop = ordered[index - 1];
+    const currentStop = ordered[index];
+    if (!previousStop || !currentStop) continue;
+
+    const previous = asDate(previousStop.startsAt);
+    const current = asDate(currentStop.startsAt);
     if (current.getTime() - previous.getTime() < minGapMs) {
       return { previousIndex: index - 1, currentIndex: index };
     }
