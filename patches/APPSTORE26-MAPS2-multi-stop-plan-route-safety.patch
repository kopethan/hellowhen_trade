diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index f1a48d8..5f031ec 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -1401,12 +1401,20 @@ function buildGoogleMapsSearchUrl(value: string) {
 type PlanRouteMapsLink = {
   appleHref: string;
   googleHref: string;
+  appleLabel: string;
+  googleLabel: string;
   label: string;
   body: string;
   providerBody: string;
   stopCount: number;
   totalStopCount: number;
   skippedOnlineCount: number;
+  skippedMissingLocationCount: number;
+};
+
+type PlanRouteStop = {
+  place: PlanPlaceDto;
+  query: string;
 };
 
 function getPlanPlaceMapsQuery(place: PlanPlaceDto) {
@@ -1417,11 +1425,9 @@ function getPlanPlaceMapsQuery(place: PlanPlaceDto) {
 }
 
 function buildAppleMapsDirectionsUrl(queries: string[]) {
-  const origin = queries[0];
-  if (queries.length <= 1 || !origin) return buildAppleMapsSearchUrl(origin ?? '');
-  const destination = queries[queries.length - 1];
-  if (!destination) return buildAppleMapsSearchUrl(origin);
-  return `https://maps.apple.com/?saddr=${encodeURIComponent(origin)}&daddr=${encodeURIComponent(destination)}&dirflg=d`;
+  const firstStop = queries[0];
+  if (queries.length <= 1 || !firstStop) return buildAppleMapsSearchUrl(firstStop ?? '');
+  return `https://maps.apple.com/?daddr=${encodeURIComponent(firstStop)}&dirflg=d`;
 }
 
 function buildGoogleMapsDirectionsUrl(queries: string[]) {
@@ -1440,26 +1446,42 @@ function buildGoogleMapsDirectionsUrl(queries: string[]) {
 }
 
 function buildPlanRouteMapsLink(places: PlanPlaceDto[], t: PlanTranslationFunction): PlanRouteMapsLink | null {
-  const offlineQueries = places.map(getPlanPlaceMapsQuery).filter((value): value is string => Boolean(value));
-  if (!offlineQueries.length) return null;
-  const includedQueries = offlineQueries.slice(0, GOOGLE_MAPS_MAX_ROUTE_STOPS);
+  const offlinePlaces = places.filter(isOfflinePlanPlace);
+  const routeStops = offlinePlaces
+    .map((place): PlanRouteStop | null => {
+      const query = getPlanPlaceMapsQuery(place);
+      return query ? { place, query } : null;
+    })
+    .filter((stop): stop is PlanRouteStop => Boolean(stop));
+  if (!routeStops.length) return null;
+
+  const includedStops = routeStops.slice(0, GOOGLE_MAPS_MAX_ROUTE_STOPS);
+  const includedQueries = includedStops.map((stop) => stop.query);
   const skippedOnlineCount = places.filter((place) => place.mode === 'remote').length;
-  const truncatedCount = Math.max(offlineQueries.length - includedQueries.length, 0);
-  const routeLabel = includedQueries.length > 1 ? t('plans.detail.route.openRoute') : t('plans.detail.route.openSingle');
+  const skippedMissingLocationCount = Math.max(offlinePlaces.length - routeStops.length, 0);
+  const truncatedCount = Math.max(routeStops.length - includedStops.length, 0);
+  const hasMultiStopRoute = includedStops.length > 1;
+  const routeLabel = hasMultiStopRoute ? t('plans.detail.route.openRoute') : t('plans.detail.route.openSingle');
+  const firstStopTitle = includedStops[0]?.place.title?.trim() || t('plans.detail.route.firstStopFallback');
   const bodyParts = [
-    t(includedQueries.length === 1 ? 'plans.detail.route.offlineStopOne' : 'plans.detail.route.offlineStopMany', { count: includedQueries.length }),
+    t(includedStops.length === 1 ? 'plans.detail.route.offlineStopOne' : 'plans.detail.route.offlineStopMany', { count: includedStops.length }),
     skippedOnlineCount ? t(skippedOnlineCount === 1 ? 'plans.detail.route.skippedOnlineOne' : 'plans.detail.route.skippedOnlineMany', { count: skippedOnlineCount }) : '',
+    skippedMissingLocationCount ? t(skippedMissingLocationCount === 1 ? 'plans.detail.route.skippedMissingLocationOne' : 'plans.detail.route.skippedMissingLocationMany', { count: skippedMissingLocationCount }) : '',
     truncatedCount ? t(truncatedCount === 1 ? 'plans.detail.route.skippedLaterOne' : 'plans.detail.route.skippedLaterMany', { count: truncatedCount }) : '',
   ].filter(Boolean);
+
   return {
     appleHref: buildAppleMapsDirectionsUrl(includedQueries),
     googleHref: buildGoogleMapsDirectionsUrl(includedQueries),
+    appleLabel: t(hasMultiStopRoute ? 'plans.detail.route.appleMapsFirstStop' : 'plans.detail.route.appleMaps'),
+    googleLabel: t(hasMultiStopRoute ? 'plans.detail.route.googleMapsPlanRoute' : 'plans.detail.route.googleMaps'),
     label: routeLabel,
     body: bodyParts.join(' · '),
-    providerBody: t(includedQueries.length === 1 ? 'plans.detail.route.mapsProviderOneBody' : 'plans.detail.route.mapsProviderManyBody', { count: includedQueries.length }),
-    stopCount: includedQueries.length,
-    totalStopCount: offlineQueries.length,
+    providerBody: t(hasMultiStopRoute ? 'plans.detail.route.mapsProviderManyBody' : 'plans.detail.route.mapsProviderOneBody', { count: includedStops.length, stop: firstStopTitle }),
+    stopCount: includedStops.length,
+    totalStopCount: routeStops.length,
     skippedOnlineCount,
+    skippedMissingLocationCount,
   };
 }
 
@@ -1502,8 +1524,8 @@ async function openPlanRouteMaps(routeMaps: PlanRouteMapsLink, t: PlanTranslatio
     showIosMapsProviderPicker({
       title: t('plans.detail.route.mapsProviderTitle'),
       body: routeMaps.providerBody,
-      appleLabel: t('plans.detail.route.appleMaps'),
-      googleLabel: t('plans.detail.route.googleMaps'),
+      appleLabel: routeMaps.appleLabel,
+      googleLabel: routeMaps.googleLabel,
       cancelLabel: t('plans.detail.route.cancel'),
       appleHref: routeMaps.appleHref,
       googleHref: routeMaps.googleHref,
@@ -2888,6 +2910,7 @@ export function PlanDetailScreen({ route, navigation }: PlanDetailProps) {
                 <Pressable
                   accessibilityRole="button"
                   accessibilityLabel={routeMaps.label}
+                  accessibilityHint={Platform.OS === 'ios' ? t('plans.detail.route.openPickerHint') : t('plans.detail.route.openExternalHint')}
                   onPress={() => { void openPlanRouteMaps(routeMaps, t); }}
                   style={({ pressed }) => [styles.planRouteMapsButton, { backgroundColor: theme.semantic.place.bg, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}
                 >
diff --git a/docs/launch/mobile-store-readiness-checklist.md b/docs/launch/mobile-store-readiness-checklist.md
index 8b60eeb..fec6822 100644
--- a/docs/launch/mobile-store-readiness-checklist.md
+++ b/docs/launch/mobile-store-readiness-checklist.md
@@ -89,8 +89,13 @@ Verify on both an iPhone and an iPad review device:
 - Opening a Plan route on iOS offers **Apple Maps** and **Google Maps**.
 - Apple Maps is listed first and opens through an `https://maps.apple.com` link.
 - A single offline stop opens as a place search.
-- A route with multiple offline stops explains that Apple Maps opens the first-to-last route while Google Maps can include the available intermediate stops.
+- A route with multiple offline stops labels Apple Maps as the first-stop option and opens directions from the current location to the first available mapped offline stop.
+- The Google Maps option is labelled as the Plan route and keeps mapped offline stops in Plan order, up to the supported five-stop URL limit.
 - Online Places are skipped by route navigation and continue to open as normal web links.
+- Offline Places without coordinates or a usable public address are skipped and the route summary discloses how many were omitted.
+- The route summary discloses when later mapped stops exceed the supported external-route limit.
+- Every offline Place row still offers its own Apple Maps action, so reviewers can open any omitted or later stop separately when it has a mappable location.
+- Test one-stop, two-stop, mixed online/offline, missing-address, and six-or-more-offline-stop Plans.
 - Cancelling the provider dialog leaves the user inside Hellowhen.
 - Failure to open either provider shows a localized error instead of silently doing nothing.
 
diff --git a/packages/i18n/src/locales/en/plans.ts b/packages/i18n/src/locales/en/plans.ts
index 2692fe2..2eaa468 100644
--- a/packages/i18n/src/locales/en/plans.ts
+++ b/packages/i18n/src/locales/en/plans.ts
@@ -183,16 +183,23 @@ export const plans = {
       openSingle: 'Open in Maps',
       mapsProviderTitle: 'Open with',
       mapsProviderOneBody: 'Choose Apple Maps or Google Maps for this offline stop.',
-      mapsProviderManyBody: 'Apple Maps opens directions from the first to the last offline stop. Google Maps can include up to {{count}} offline stops.',
+      mapsProviderManyBody: 'Apple Maps opens directions from your current location to the first available offline stop, {{stop}}. Google Maps follows up to {{count}} mapped offline stops in Plan order.',
       appleMaps: 'Apple Maps',
+      appleMapsFirstStop: 'Apple Maps (first stop)',
       googleMaps: 'Google Maps',
+      googleMapsPlanRoute: 'Google Maps (Plan route)',
       cancel: 'Cancel',
-      offlineStopOne: '{{count}} offline stop',
-      offlineStopMany: '{{count}} offline stops',
+      firstStopFallback: 'the first offline stop',
+      openPickerHint: 'Choose Apple Maps or Google Maps.',
+      openExternalHint: 'Opens the available offline route in your maps app.',
+      offlineStopOne: '{{count}} mapped offline stop',
+      offlineStopMany: '{{count}} mapped offline stops',
       skippedOnlineOne: '{{count}} online place is skipped',
       skippedOnlineMany: '{{count}} online places are skipped',
-      skippedLaterOne: '{{count}} later stop is skipped',
-      skippedLaterMany: '{{count}} later stops are skipped',
+      skippedMissingLocationOne: '{{count}} offline place without a mappable location is skipped',
+      skippedMissingLocationMany: '{{count}} offline places without a mappable location are skipped',
+      skippedLaterOne: '{{count}} later mapped stop is skipped',
+      skippedLaterMany: '{{count}} later mapped stops are skipped',
       openErrorTitle: 'Could not open Maps',
       openErrorBody: 'Try opening one address from the route instead.',
       emptyTitle: 'No places yet',
diff --git a/packages/i18n/src/locales/es/plans.ts b/packages/i18n/src/locales/es/plans.ts
index f6d6d21..96dac0f 100644
--- a/packages/i18n/src/locales/es/plans.ts
+++ b/packages/i18n/src/locales/es/plans.ts
@@ -181,16 +181,23 @@ export const plans = {
       openSingle: 'Abrir en Mapas',
       mapsProviderTitle: 'Abrir con',
       mapsProviderOneBody: 'Elige Mapas de Apple o Google Maps para esta parada presencial.',
-      mapsProviderManyBody: 'Mapas de Apple abre indicaciones entre la primera y la última parada presencial. Google Maps puede incluir hasta {{count}} paradas presenciales.',
+      mapsProviderManyBody: 'Mapas de Apple abre indicaciones desde tu ubicación hasta la primera parada presencial disponible, {{stop}}. Google Maps sigue hasta {{count}} paradas ubicables en el orden del Plan.',
       appleMaps: 'Mapas de Apple',
+      appleMapsFirstStop: 'Mapas de Apple (primera parada)',
       googleMaps: 'Google Maps',
+      googleMapsPlanRoute: 'Google Maps (ruta del Plan)',
       cancel: 'Cancelar',
-      offlineStopOne: '{{count}} parada presencial',
-      offlineStopMany: '{{count}} paradas presenciales',
+      firstStopFallback: 'la primera parada presencial',
+      openPickerHint: 'Elige Mapas de Apple o Google Maps.',
+      openExternalHint: 'Abre la ruta presencial disponible en tu aplicación de mapas.',
+      offlineStopOne: '{{count}} parada presencial ubicable',
+      offlineStopMany: '{{count}} paradas presenciales ubicables',
       skippedOnlineOne: 'Se omite {{count}} lugar en línea',
       skippedOnlineMany: 'Se omiten {{count}} lugares en línea',
-      skippedLaterOne: 'Se omite {{count}} parada posterior',
-      skippedLaterMany: 'Se omiten {{count}} paradas posteriores',
+      skippedMissingLocationOne: 'Se omite {{count}} lugar presencial sin ubicación utilizable',
+      skippedMissingLocationMany: 'Se omiten {{count}} lugares presenciales sin ubicación utilizable',
+      skippedLaterOne: 'Se omite {{count}} parada ubicable posterior',
+      skippedLaterMany: 'Se omiten {{count}} paradas ubicables posteriores',
       openErrorTitle: 'No se pudo abrir Mapas',
       openErrorBody: 'Prueba a abrir una dirección de la ruta.',
       emptyTitle: 'Todavía no hay lugares',
diff --git a/packages/i18n/src/locales/fr/plans.ts b/packages/i18n/src/locales/fr/plans.ts
index b2e316d..11f5c87 100644
--- a/packages/i18n/src/locales/fr/plans.ts
+++ b/packages/i18n/src/locales/fr/plans.ts
@@ -183,16 +183,23 @@ export const plans = {
       openSingle: 'Ouvrir dans une app de cartographie',
       mapsProviderTitle: 'Ouvrir avec',
       mapsProviderOneBody: 'Choisissez Plans d’Apple ou Google Maps pour cette étape sur place.',
-      mapsProviderManyBody: 'Plans d’Apple ouvre l’itinéraire entre la première et la dernière étape sur place. Google Maps peut inclure jusqu’à {{count}} étapes sur place.',
+      mapsProviderManyBody: 'Plans d’Apple ouvre l’itinéraire depuis votre position vers la première étape sur place disponible, {{stop}}. Google Maps suit jusqu’à {{count}} étapes cartographiées dans l’ordre du Plan.',
       appleMaps: 'Plans d’Apple',
+      appleMapsFirstStop: 'Plans d’Apple (première étape)',
       googleMaps: 'Google Maps',
+      googleMapsPlanRoute: 'Google Maps (itinéraire du Plan)',
       cancel: 'Annuler',
-      offlineStopOne: '{{count}} étape sur place',
-      offlineStopMany: '{{count}} étapes sur place',
+      firstStopFallback: 'la première étape sur place',
+      openPickerHint: 'Choisissez Plans d’Apple ou Google Maps.',
+      openExternalHint: 'Ouvre l’itinéraire sur place disponible dans votre application de cartographie.',
+      offlineStopOne: '{{count}} étape sur place cartographiée',
+      offlineStopMany: '{{count}} étapes sur place cartographiées',
       skippedOnlineOne: '{{count}} lieu en ligne ignoré',
       skippedOnlineMany: '{{count}} lieux en ligne ignorés',
-      skippedLaterOne: '{{count}} étape suivante ignorée',
-      skippedLaterMany: '{{count}} étapes suivantes ignorées',
+      skippedMissingLocationOne: '{{count}} lieu sur place sans position cartographiable ignoré',
+      skippedMissingLocationMany: '{{count}} lieux sur place sans position cartographiable ignorés',
+      skippedLaterOne: '{{count}} étape cartographiée suivante ignorée',
+      skippedLaterMany: '{{count}} étapes cartographiées suivantes ignorées',
       openErrorTitle: 'Impossible d’ouvrir l’app de cartographie',
       openErrorBody: 'Essayez plutôt d’ouvrir une adresse du parcours.',
       emptyTitle: 'Aucun lieu pour le moment',
diff --git a/scripts/mobile-store-release-smoke.mjs b/scripts/mobile-store-release-smoke.mjs
index f85501e..ba7c322 100644
--- a/scripts/mobile-store-release-smoke.mjs
+++ b/scripts/mobile-store-release-smoke.mjs
@@ -130,10 +130,20 @@ function runAppleMapsParityChecks() {
   assert(plansScreen.includes('function buildAppleMapsSearchUrl'), 'Native Plans must build Apple Maps place links.');
   assert(plansScreen.includes('function buildAppleMapsDirectionsUrl'), 'Native Plans must build Apple Maps route links.');
   assert(plansScreen.includes('https://maps.apple.com/?q='), 'Native Plans must use the public Apple Maps search URL.');
-  assert(plansScreen.includes('https://maps.apple.com/?saddr='), 'Native Plans must use the public Apple Maps directions URL.');
-  assert(plansScreen.includes("appleLabel: t('plans.detail.route.appleMaps')"), 'Plan routes must offer Apple Maps on iOS.');
+  assert(plansScreen.includes('https://maps.apple.com/?daddr='), 'Multi-stop Plans must open Apple Maps toward the first available offline stop.');
+  assert(!plansScreen.includes('https://maps.apple.com/?saddr='), 'Apple Maps must not imply that a first-to-last link represents the complete multi-stop Plan route.');
+  assert(plansScreen.includes('const offlinePlaces = places.filter(isOfflinePlanPlace);'), 'Plan routing must explicitly exclude online Places.');
+  assert(plansScreen.includes('const includedStops = routeStops.slice(0, GOOGLE_MAPS_MAX_ROUTE_STOPS);'), 'Plan routing must preserve ordered mapped stops and keep the external route limit.');
+  assert(plansScreen.includes('skippedMissingLocationCount'), 'Plan routing must disclose offline Places that have no mappable location.');
+  assert(plansScreen.includes('appleLabel: routeMaps.appleLabel'), 'Multi-stop Plan routes must use the Apple Maps first-stop label.');
   assert(plansScreen.includes("appleLabel: t('plans.detail.location.appleMaps')"), 'Offline Place actions must offer Apple Maps on iOS.');
-  assert(plansScreen.includes("googleLabel: t('plans.detail.route.googleMaps')"), 'Plan routes may retain Google Maps as an optional provider.');
+  assert(plansScreen.includes('googleLabel: routeMaps.googleLabel'), 'Multi-stop Plan routes must distinguish the Google Maps Plan route.');
+  assert(englishPlans.includes("appleMapsFirstStop: 'Apple Maps (first stop)'"), 'English copy must explain the Apple Maps first-stop behavior.');
+  assert(frenchPlans.includes("appleMapsFirstStop: 'Plans d’Apple (première étape)'"), 'French copy must explain the Apple Maps first-stop behavior.');
+  assert(spanishPlans.includes("appleMapsFirstStop: 'Mapas de Apple (primera parada)'"), 'Spanish copy must explain the Apple Maps first-stop behavior.');
+  assert(englishPlans.includes('skippedMissingLocationOne'), 'English route copy must disclose unmappable offline Places.');
+  assert(frenchPlans.includes('skippedMissingLocationOne'), 'French route copy must disclose unmappable offline Places.');
+  assert(spanishPlans.includes('skippedMissingLocationOne'), 'Spanish route copy must disclose unmappable offline Places.');
   assert(!englishPlans.includes('Open route in Google Maps'), 'English Plan route actions must not be Google-only.');
   assert(!frenchPlans.includes('Ouvrir le parcours dans Google Maps'), 'French Plan route actions must not be Google-only.');
   assert(!spanishPlans.includes('Abrir ruta en Google Maps'), 'Spanish Plan route actions must not be Google-only.');
