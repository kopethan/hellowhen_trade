diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 9889258..d4367f8 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -317,8 +317,8 @@ function DisabledPlansScreen({ onBack }: { onBack: () => void }) {
   return (
     <AppFixedHeaderScreen header={<AppHeader title="Plans" onBack={onBack} />}>
       <View style={styles.centerState}>
-        <View style={[styles.largeIcon, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-          <MobileIcon name="calendar" color={theme.semantic.instruction.text} size={30} />
+        <View style={[styles.largeIcon, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
+          <MobileIcon name="calendar" color={theme.semantic.plan.text} size={30} />
         </View>
         <AppText style={styles.centerTitle}>Plans are hidden</AppText>
         <AppText style={[styles.centerBody, { color: theme.color.muted }]}>The mobile Plan route skeleton is ready, but Plans stay hidden until the Plan feature flags are enabled.</AppText>
@@ -364,7 +364,7 @@ function PlanRow({ plan, onPress }: { plan: PlanDto; onPress: () => void }) {
         <MobileIcon name="activity" size={15} color={theme.color.muted} />
         <AppText style={[styles.metaText, { color: theme.color.muted }]}>{getPlanMeta(plan)}</AppText>
       </View>
-      {firstPlace ? <AppText style={[styles.placePreview, { color: theme.semantic.instruction.text }]}>Starts with {firstPlace.title}</AppText> : null}
+      {firstPlace ? <AppText style={[styles.placePreview, { color: theme.semantic.plan.text }]}>Starts with {firstPlace.title}</AppText> : null}
     </Pressable>
   );
 }
@@ -569,8 +569,8 @@ export function PlansScreen(props: Partial<PlansScreenProps> = {}) {
       <View style={styles.bodyWrap}>
         {filtersOpen ? (
           <View style={[styles.filterNotice, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
-            <View style={[styles.menuIcon, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-              <MobileIcon name="filter" size={17} color={theme.semantic.instruction.text} />
+            <View style={[styles.menuIcon, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
+              <MobileIcon name="filter" size={17} color={theme.semantic.plan.text} />
             </View>
             <View style={styles.menuCopy}>
               <AppText style={styles.menuTitle}>Open Plans feed</AppText>
@@ -593,7 +593,7 @@ function MenuItem({ item }: { item: PlanMenuItem }) {
   const theme = useThemeTokens();
   return (
     <Pressable accessibilityRole="button" onPress={item.onPress} style={({ pressed }) => [styles.menuItem, { borderBottomColor: theme.color.border }, pressed && styles.pressed]}>
-      <View style={[styles.menuIcon, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}><MobileIcon name={item.icon} size={17} color={theme.semantic.instruction.text} /></View>
+      <View style={[styles.menuIcon, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}><MobileIcon name={item.icon} size={17} color={theme.semantic.plan.text} /></View>
       <View style={styles.menuCopy}>
         <AppText style={styles.menuTitle}>{item.title}</AppText>
         <AppText style={[styles.menuBody, { color: theme.color.muted }]}>{item.body}</AppText>
@@ -617,8 +617,8 @@ function ParticipantCompactRow({ participant }: { participant: PlanParticipantDt
   const theme = useThemeTokens();
   return (
     <View style={[styles.participantRow, { borderBottomColor: theme.color.border }]}>
-      <View style={[styles.participantAvatar, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-        <AppText style={[styles.participantInitial, { color: theme.semantic.instruction.text }]}>{getParticipantInitial(participant)}</AppText>
+      <View style={[styles.participantAvatar, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
+        <AppText style={[styles.participantInitial, { color: theme.semantic.plan.text }]}>{getParticipantInitial(participant)}</AppText>
       </View>
       <View style={styles.participantCopy}>
         <AppText style={styles.participantName}>{getParticipantName(participant)}</AppText>
@@ -645,14 +645,14 @@ function PlanPlaceTimelineCard({ place, index, showReport }: { place: PlanPlaceD
         <View style={styles.timelineCardHeader}>
           <View style={styles.timelineCopy}>
             <View style={styles.rowTop}>
-              <SemanticBadge label={`Place ${index + 1}`} tone="instruction" size="sm" />
+              <SemanticBadge label={`Place ${index + 1}`} tone="place" size="sm" />
               <SemanticBadge label={place.mode === 'remote' ? 'Online' : 'Offline'} tone="muted" size="sm" />
               <SemanticBadge label={getPlanPlaceSourceLabel(place)} tone="muted" size="sm" />
             </View>
             <AppText style={styles.rowTitle}>{place.title}</AppText>
           </View>
-          <View style={[styles.timelineMedia, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-            {mediaUrl ? <Image source={{ uri: mediaUrl }} resizeMode="cover" style={styles.timelineImage} /> : <MobileIcon name="calendar" size={24} color={theme.semantic.instruction.text} />}
+          <View style={[styles.timelineMedia, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}>
+            {mediaUrl ? <Image source={{ uri: mediaUrl }} resizeMode="cover" style={styles.timelineImage} /> : <MobileIcon name="calendar" size={24} color={theme.semantic.place.text} />}
           </View>
         </View>
         <View style={styles.detailMetaStack}>
@@ -793,7 +793,7 @@ export function PlanDetailScreen({ route, navigation }: PlanDetailProps) {
             </View>
           </View>
 
-          <View style={styles.sectionTitleRow}><AppText style={styles.sectionTitle}>Plan route</AppText><SemanticBadge label={`${places.length}`} tone="instruction" size="sm" /></View>
+          <View style={styles.sectionTitleRow}><AppText style={styles.sectionTitle}>Plan route</AppText><SemanticBadge label={`${places.length}`} tone="place" size="sm" /></View>
           {places.length === 0 ? <EmptyBlock title="No places yet" body="This Plan does not have places attached yet." /> : null}
           {places.map((place, index) => <PlanPlaceTimelineCard key={place.id} place={place} index={index} showReport={showReportActions} />)}
 
@@ -908,8 +908,8 @@ function TextField({
 function PillButton({ label, onPress, active, disabled }: { label: string; onPress: () => void; active?: boolean; disabled?: boolean }) {
   const theme = useThemeTokens();
   return (
-    <Pressable accessibilityRole="button" disabled={disabled} onPress={onPress} style={({ pressed }) => [styles.pillButton, { borderColor: active ? theme.color.text : theme.color.border, backgroundColor: active ? theme.color.text : theme.color.surface }, pressed && styles.pressed, disabled && styles.disabled]}>
-      <AppText style={[styles.pillButtonText, { color: active ? theme.color.background : theme.color.text }]}>{label}</AppText>
+    <Pressable accessibilityRole="button" disabled={disabled} onPress={onPress} style={({ pressed }) => [styles.pillButton, { borderColor: active ? theme.semantic.plan.border : theme.color.border, backgroundColor: active ? theme.semantic.plan.softBg : theme.color.surface }, pressed && styles.pressed, disabled && styles.disabled]}>
+      <AppText style={[styles.pillButtonText, { color: active ? theme.semantic.plan.text : theme.color.text }]}>{label}</AppText>
     </Pressable>
   );
 }
@@ -931,18 +931,18 @@ function PlaceChoiceCard({ place, onAdd }: { place: PlaceDto; onAdd: () => void
     .join(' · ');
   return (
     <Pressable accessibilityRole="button" onPress={onAdd} style={({ pressed }) => [styles.choiceCard, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}>
-      <View style={[styles.choiceIcon, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-        <MobileIcon name={place.mode === 'remote' ? 'send' : 'calendar'} size={18} color={theme.semantic.instruction.text} />
+      <View style={[styles.choiceIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}>
+        <MobileIcon name={place.mode === 'remote' ? 'send' : 'calendar'} size={18} color={theme.semantic.place.text} />
       </View>
       <View style={styles.choiceCopy}>
         <View style={styles.rowTop}>
-          <SemanticBadge label={placeSourceLabel(place)} tone={place.source === 'hellowhen_library' ? 'instruction' : 'proposal'} size="sm" />
+          <SemanticBadge label={placeSourceLabel(place)} tone="place" size="sm" />
           <SemanticBadge label={place.mode === 'remote' ? 'Online' : 'Offline'} tone="muted" size="sm" />
         </View>
         <AppText style={styles.choiceTitle}>{place.title}</AppText>
         <AppText style={[styles.choiceMeta, { color: theme.color.muted }]} numberOfLines={2}>{meta || place.description || 'Reusable Place'}</AppText>
       </View>
-      <View style={[styles.addMini, { backgroundColor: theme.color.text }]}>
+      <View style={[styles.addMini, { backgroundColor: theme.semantic.place.bg }]}>
         <MobileIcon name="add" size={16} color={theme.color.background} />
       </View>
     </Pressable>
@@ -959,8 +959,8 @@ function PlaceTimelineRow({ place, index, onPress }: { place: SelectedPlanPlaceS
     <Pressable accessibilityRole="button" onPress={onPress} style={({ pressed }) => [styles.placeTimelineRow, { borderColor: theme.color.border }, pressed && styles.pressed]}>
       <View style={styles.timelineCopy}>
         <View style={styles.rowTop}>
-          <SemanticBadge label={`Place ${index + 1}`} tone="instruction" size="sm" />
-          {place.sourcePlaceId ? <SemanticBadge label={place.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'} tone="proposal" size="sm" /> : <SemanticBadge label="Custom" tone="muted" size="sm" />}
+          <SemanticBadge label={`Place ${index + 1}`} tone="place" size="sm" />
+          {place.sourcePlaceId ? <SemanticBadge label={place.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'} tone="place" size="sm" /> : <SemanticBadge label="Custom" tone="place" size="sm" />}
         </View>
         <AppText style={styles.rowTitle}>{place.title || place.sourcePlaceTitle || `Place ${index + 1}`}</AppText>
         <AppText style={[styles.metaText, { color: theme.color.muted }]} numberOfLines={2}>{meta}</AppText>
@@ -983,7 +983,7 @@ function PlanTimeCard({
   return (
     <View style={[styles.timeCard, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
       <View style={styles.timeCardHeader}>
-        <SemanticBadge label={`Place ${index + 1}`} tone="instruction" size="sm" />
+        <SemanticBadge label={`Place ${index + 1}`} tone="place" size="sm" />
         <View style={styles.timelineCopy}>
           <AppText style={styles.rowTitle}>{place.title || place.sourcePlaceTitle || `Place ${index + 1}`}</AppText>
           <AppText style={[styles.metaText, { color: theme.color.muted }]}>{index === 0 ? 'Plan start · required' : 'Optional · same time or after the previous timed place'}</AppText>
@@ -1020,7 +1020,7 @@ function AdvancedPlanDetailsCard({
           <AppText style={styles.rowTitle}>More options</AppText>
           <AppText style={[styles.metaText, { color: theme.color.muted }]}>{open ? 'Hide custom Plan details' : 'Optional title, description, category, and tags'}</AppText>
         </View>
-        <SemanticBadge label={open ? '−' : '+'} tone="instruction" size="sm" />
+        <SemanticBadge label={open ? '−' : '+'} tone="plan" size="sm" />
       </Pressable>
       {open ? (
         <View style={styles.advancedPanel}>
@@ -1252,8 +1252,8 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
     <AppFixedHeaderScreen header={<AppHeader title="Create plan" onBack={() => navigation.goBack()} />}>
       <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined} style={styles.keyboardWrap}>
         <ScrollView contentContainerStyle={styles.listContent} showsVerticalScrollIndicator={false} keyboardShouldPersistTaps="handled">
-          <View style={[styles.hero, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
-            <SemanticBadge label="Free join" tone="success" />
+          <View style={[styles.hero, styles.planCreateHero, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
+            <SemanticBadge label="Plan" tone="plan" />
             <AppText style={styles.heroTitle}>Create a Plan from Places</AppText>
             <AppText style={[styles.heroBody, { color: theme.color.muted }]}>Add places and times in one clean route, then preview the feed deck before publishing.</AppText>
           </View>
@@ -1265,10 +1265,10 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
 
           {stage === 'build' ? (
             <>
-              <View style={styles.timelineDividerBlock}>
-                <View style={styles.planTimelineLineItem}>
+              <View style={[styles.timelineDividerBlock, { borderTopColor: theme.color.border, borderBottomColor: theme.color.border }]}>
+                <View style={[styles.planTimelineLineItem, { borderTopColor: theme.color.border }]}>
                   <View style={styles.timelineCopy}>
-                    <SemanticBadge label="Date / time" tone="instruction" size="sm" />
+                    <SemanticBadge label="Date / time" tone="time" size="sm" />
                     <AppText style={styles.sectionTitle}>Start time recommended</AppText>
                     <AppText style={[styles.metaText, { color: theme.color.muted }]}>Place 1 becomes the Plan start.</AppText>
                   </View>
@@ -1290,7 +1290,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                   />
                 ))}
 
-                <View style={styles.planTimelineLineItem}>
+                <View style={[styles.planTimelineLineItem, styles.planTimelinePlaceAction, { borderTopColor: theme.color.border }]}>
                   <View style={styles.timelineCopy}>
                     <AppText style={styles.sectionTitle}>Add a place</AppText>
                     <AppText style={[styles.metaText, { color: theme.color.muted }]}>Open place choices only when needed, so this screen stays light.</AppText>
@@ -1300,9 +1300,9 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                   </View>
                 </View>
 
-                <View style={styles.planTimelineLineItem}>
+                <View style={[styles.planTimelineLineItem, { borderTopColor: theme.color.border }]}>
                   <View style={styles.timelineCopy}>
-                    <SemanticBadge label="Optional" tone="muted" size="sm" />
+                    <SemanticBadge label="Optional" tone="time" size="sm" />
                     <AppText style={styles.sectionTitle}>End date / time</AppText>
                     <AppText style={[styles.metaText, { color: theme.color.muted }]}>Leave empty if the Plan ends at the last timed place.</AppText>
                   </View>
@@ -1326,18 +1326,18 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
               {explicitPlanEnd.error ? <InfoNotice tone="warning" title="End time" body={explicitPlanEnd.error} /> : null}
               {message ? <InfoNotice tone="success" title="Plans" body={message} /> : null}
               {error ? <InfoNotice tone="warning" title="Could not preview" body={error} /> : null}
-              <Pressable accessibilityRole="button" onPress={showPreviewStage} style={({ pressed }) => [styles.primaryButton, { backgroundColor: theme.color.text }, pressed && styles.pressed]}>
+              <Pressable accessibilityRole="button" onPress={showPreviewStage} style={({ pressed }) => [styles.primaryButton, { backgroundColor: theme.semantic.plan.bg }, pressed && styles.pressed]}>
                 <AppText style={[styles.primaryButtonText, { color: theme.color.background }]}>Preview Plan</AppText>
               </Pressable>
             </>
           ) : (
             <>
               <View style={[styles.previewCard, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
-                <View style={styles.wizardStepRow}><View style={styles.timelineCopy}><AppText style={styles.sectionTitle}>Preview / Confirm</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Review the place-only deck before creating the Plan.</AppText></View><SemanticBadge label="Place-only feed deck" tone="instruction" size="sm" /></View>
-                <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
+                <View style={styles.wizardStepRow}><View style={styles.timelineCopy}><AppText style={styles.sectionTitle}>Preview / Confirm</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Review the place-only deck before creating the Plan.</AppText></View><SemanticBadge label="Place-only feed deck" tone="plan" size="sm" /></View>
+                <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
                   <View style={styles.timelineCopy}>
                     <AppText style={styles.rowTitle}>{previewTitle}</AppText>
-                    <AppText style={[styles.metaText, { color: theme.semantic.instruction.text }]}>{previewDescription}</AppText>
+                    <AppText style={[styles.metaText, { color: theme.semantic.plan.text }]}>{previewDescription}</AppText>
                   </View>
                 </View>
                 {places.map((place, index) => (
@@ -1351,7 +1351,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                 ))}
                 <View style={styles.rowTop}>
                   <SemanticBadge label="Free join" tone="success" size="sm" />
-                  <SemanticBadge label="Open / Public" tone="instruction" size="sm" />
+                  <SemanticBadge label="Open / Public" tone="plan" size="sm" />
                   <SemanticBadge label={`${places.length} ${places.length === 1 ? 'place' : 'places'}`} tone="muted" size="sm" />
                 </View>
                 {schedule.error && places.length > 0 ? <InfoNotice tone="warning" title="Schedule check" body={schedule.error} /> : null}
@@ -1362,7 +1362,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
               {error ? <InfoNotice tone="warning" title="Could not save" body={error} /> : null}
               <View style={styles.actionGrid}>
                 <SecondaryButton label="Back" onPress={() => setStage('build')} />
-                <Pressable accessibilityRole="button" disabled={saving} onPress={() => { void submit(); }} style={({ pressed }) => [styles.primaryButton, { backgroundColor: theme.color.text, flex: 1 }, pressed && styles.pressed, saving && styles.disabled]}>
+                <Pressable accessibilityRole="button" disabled={saving} onPress={() => { void submit(); }} style={({ pressed }) => [styles.primaryButton, { backgroundColor: theme.semantic.plan.bg, flex: 1 }, pressed && styles.pressed, saving && styles.disabled]}>
                   <AppText style={[styles.primaryButtonText, { color: theme.color.background }]}>{saving ? 'Creating...' : 'Create Plan'}</AppText>
                 </Pressable>
               </View>
@@ -1385,16 +1385,16 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                 </View>
                 <ScrollView keyboardShouldPersistTaps="handled" style={styles.sourceListScroll} contentContainerStyle={styles.placeDetailSheetContent}>
                   <View style={styles.rowTop}>
-                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'} tone="proposal" size="sm" /> : <SemanticBadge label="Custom one-off stop" tone="muted" size="sm" />}
-                    <SemanticBadge label={detailPlaceIndex === 0 ? 'Plan start' : 'Optional time'} tone="instruction" size="sm" />
+                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'} tone="place" size="sm" /> : <SemanticBadge label="Custom one-off stop" tone="place" size="sm" />}
+                    <SemanticBadge label={detailPlaceIndex === 0 ? 'Plan start' : 'Optional time'} tone="time" size="sm" />
                   </View>
                   <View style={styles.twoColumnRow}>
                     <TextField label="Date" value={detailPlace.date} onChangeText={(date) => updateSelectedPlace(detailPlaceIndex, { date })} placeholder="YYYY-MM-DD" keyboardType="numbers-and-punctuation" />
                     <TextField label={detailPlaceIndex === 0 ? 'Time' : 'Time optional'} value={detailPlace.time} onChangeText={(time) => updateSelectedPlace(detailPlaceIndex, { time })} placeholder={detailPlaceIndex === 0 ? '13:00' : 'Optional'} keyboardType="numbers-and-punctuation" />
                   </View>
                   {detailPlace.sourcePlaceId ? (
-                    <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }]}>
-                      <AppText style={[styles.snapshotText, { color: theme.semantic.instruction.text }]} numberOfLines={3}>Copied from {detailPlace.sourcePlaceTitle || detailPlace.title}. This Plan keeps a snapshot.</AppText>
+                    <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.plan.softBg, borderColor: theme.semantic.plan.border }]}>
+                      <AppText style={[styles.snapshotText, { color: theme.semantic.plan.text }]} numberOfLines={3}>Copied from {detailPlace.sourcePlaceTitle || detailPlace.title}. This Plan keeps a snapshot.</AppText>
                       <SecondaryButton label="Use as custom" onPress={() => updateSelectedPlace(detailPlaceIndex, resetSelectedPlaceToCustom(detailPlace))} />
                     </View>
                   ) : (
@@ -1442,23 +1442,23 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
 
               {!placePickerOpen ? (
                 <View style={styles.sourceOptionList}>
-                  <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('mine')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.semantic.instruction.softBg, borderColor: theme.semantic.instruction.border }, pressed && styles.pressed]}>
-                    <View style={[styles.sourceOptionIcon, { borderColor: theme.semantic.instruction.border }]}><MobileIcon name="save" color={theme.semantic.instruction.text} size={19} /></View>
+                  <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('mine')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
+                    <View style={[styles.sourceOptionIcon, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }]}><MobileIcon name="save" color={theme.semantic.place.text} size={19} /></View>
                     <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Use one of mine</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Choose from your private saved Places.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
-                  <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('library')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.subtleSurface, borderColor: theme.color.border }, pressed && styles.pressed]}>
-                    <View style={[styles.sourceOptionIcon, { borderColor: theme.color.border }]}><MobileIcon name="search" color={theme.color.text} size={19} /></View>
+                  <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('library')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
+                    <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="search" color={theme.semantic.place.text} size={19} /></View>
                     <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Use Hellowhen Library</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Start from reusable Hellowhen Places.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
-                  <Pressable accessibilityRole="button" onPress={() => { closePlaceSourceSheet(); navigation.navigate('CreatePlace', { returnToCreatePlan: true }); }} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.subtleSurface, borderColor: theme.color.border }, pressed && styles.pressed]}>
-                    <View style={[styles.sourceOptionIcon, { borderColor: theme.color.border }]}><MobileIcon name="add" color={theme.color.text} size={19} /></View>
+                  <Pressable accessibilityRole="button" onPress={() => { closePlaceSourceSheet(); navigation.navigate('CreatePlace', { returnToCreatePlan: true }); }} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
+                    <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="add" color={theme.semantic.place.text} size={19} /></View>
                     <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Create a new Place</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Save it, then return with it selected.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
-                  <Pressable accessibilityRole="button" onPress={addCustomPlace} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.subtleSurface, borderColor: theme.color.border }, pressed && styles.pressed]}>
-                    <View style={[styles.sourceOptionIcon, { borderColor: theme.color.border }]}><MobileIcon name="more" color={theme.color.text} size={19} /></View>
+                  <Pressable accessibilityRole="button" onPress={addCustomPlace} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
+                    <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="more" color={theme.semantic.place.text} size={19} /></View>
                     <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Custom one-off stop</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Use only inside this Plan.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
@@ -1589,9 +1589,10 @@ const styles = StyleSheet.create({
   menuBody: { fontSize: 12, lineHeight: 17, fontWeight: '700' },
   listContent: { gap: 12, paddingBottom: 34 },
   stageSwitchRow: { flexDirection: 'row', gap: 8, flexWrap: 'wrap' },
-  timelineDividerBlock: { gap: 0 },
-  planTimelineLineItem: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 15, gap: 12 },
-  placeTimelineRow: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 15, flexDirection: 'row', alignItems: 'center', gap: 12 },
+  timelineDividerBlock: { gap: 0, borderTopWidth: StyleSheet.hairlineWidth, borderBottomWidth: StyleSheet.hairlineWidth },
+  planTimelineLineItem: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 16, gap: 12 },
+  planTimelinePlaceAction: { paddingVertical: 18 },
+  placeTimelineRow: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 16, flexDirection: 'row', alignItems: 'center', gap: 12 },
   placeDetailSheetContent: { gap: 12, paddingBottom: 12 },
   placePickerPanel: { borderRadius: 24, borderWidth: 1, padding: 14, gap: 12 },
   sourceSheetOverlay: { flex: 1, justifyContent: 'flex-end' },
@@ -1623,6 +1624,7 @@ const styles = StyleSheet.create({
   primaryButton: { minHeight: 48, borderRadius: 17, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 18, paddingVertical: 13, alignSelf: 'stretch' },
   primaryButtonText: { fontWeight: '900' },
   hero: { borderRadius: 28, borderWidth: 1, padding: 18, gap: 12 },
+  planCreateHero: { borderRadius: 24 },
   heroTitle: { fontSize: 30, lineHeight: 35, fontWeight: '900', letterSpacing: -0.9 },
   heroBody: { lineHeight: 21, fontWeight: '700' },
   detailContent: { gap: 14, paddingBottom: 34 },
@@ -1685,7 +1687,7 @@ const styles = StyleSheet.create({
   twoColumnRow: { flexDirection: 'row', gap: 10 },
   timeCard: { borderRadius: 22, borderWidth: 1, padding: 12, gap: 11 },
   timeCardHeader: { flexDirection: 'row', alignItems: 'flex-start', gap: 9 },
-  advancedCard: { borderRadius: 24, borderWidth: 1, padding: 13, gap: 10 },
+  advancedCard: { borderRadius: 0, borderWidth: 0, borderTopWidth: StyleSheet.hairlineWidth, paddingTop: 13, gap: 10 },
   advancedToggle: { minHeight: 52, flexDirection: 'row', alignItems: 'center', gap: 10 },
   advancedPanel: { gap: 12, paddingTop: 2 },
   previewCard: { borderRadius: 24, borderWidth: 1, padding: 15, gap: 12 },
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 90b1c8f..81d5a13 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -28,6 +28,8 @@
   --offer-bg: #DCFCE7; --offer-text: #166534; --offer-border: #86EFAC;
   --trade-bg: #EDE9FE; --trade-text: #5B21B6; --trade-border: #C4B5FD;
   --proposal-bg: #CCFBF1; --proposal-text: #115E59; --proposal-border: #5EEAD4;
+  --plan-bg: #EDE9FE; --plan-text: #5B21B6; --plan-border: #C4B5FD;
+  --place-bg: #F3E8FF; --place-text: #6B21A8; --place-border: #D8B4FE;
   --time-bg: #FFEDD5; --time-text: #9A3412; --time-border: #FDBA74;
   --danger-bg: #FEE2E2; --danger-text: #991B1B; --danger-border: #FCA5A5;
   --instruction-bg: #EEF2FF; --instruction-text: #3730A3; --instruction-border: #A5B4FC;
@@ -61,6 +63,8 @@
     --offer-bg: #052E16; --offer-text: #BBF7D0; --offer-border: #15803D;
     --trade-bg: #2E1065; --trade-text: #DDD6FE; --trade-border: #6D28D9;
     --proposal-bg: #042F2E; --proposal-text: #99F6E4; --proposal-border: #0F766E;
+    --plan-bg: #2E1065; --plan-text: #DDD6FE; --plan-border: #6D28D9;
+    --place-bg: #3B0764; --place-text: #F3E8FF; --place-border: #7E22CE;
     --time-bg: #431407; --time-text: #FED7AA; --time-border: #C2410C;
     --danger-bg: #450A0A; --danger-text: #FECACA; --danger-border: #B91C1C;
     --instruction-bg: #1E1B4B; --instruction-text: #C7D2FE; --instruction-border: #4338CA;
@@ -94,6 +98,8 @@ html[data-theme="dark"] {
   --offer-bg: #052E16; --offer-text: #BBF7D0; --offer-border: #15803D;
   --trade-bg: #2E1065; --trade-text: #DDD6FE; --trade-border: #6D28D9;
   --proposal-bg: #042F2E; --proposal-text: #99F6E4; --proposal-border: #0F766E;
+  --plan-bg: #2E1065; --plan-text: #DDD6FE; --plan-border: #6D28D9;
+  --place-bg: #3B0764; --place-text: #F3E8FF; --place-border: #7E22CE;
   --time-bg: #431407; --time-text: #FED7AA; --time-border: #C2410C;
   --danger-bg: #450A0A; --danger-text: #FECACA; --danger-border: #B91C1C;
   --instruction-bg: #1E1B4B; --instruction-text: #C7D2FE; --instruction-border: #4338CA;
@@ -216,6 +222,8 @@ h3 { font-size: 17px; line-height: 1.2; letter-spacing: -0.02em; margin-bottom:
 .semantic-badge.offer { background: var(--offer-bg); color: var(--offer-text); border-color: var(--offer-border); }
 .semantic-badge.trade { background: var(--trade-bg); color: var(--trade-text); border-color: var(--trade-border); }
 .semantic-badge.proposal { background: var(--proposal-bg); color: var(--proposal-text); border-color: var(--proposal-border); }
+.semantic-badge.plan { background: var(--plan-bg); color: var(--plan-text); border-color: var(--plan-border); }
+.semantic-badge.place { background: var(--place-bg); color: var(--place-text); border-color: var(--place-border); }
 .semantic-badge.time { background: var(--time-bg); color: var(--time-text); border-color: var(--time-border); }
 .semantic-badge.danger { background: var(--danger-bg); color: var(--danger-text); border-color: var(--danger-border); }
 .semantic-badge.instruction { background: var(--instruction-bg); color: var(--instruction-text); border-color: var(--instruction-border); }
@@ -2696,7 +2704,8 @@ html[data-theme="dark"] .inventory-empty-state__plus {
 .plan-form > label, .plan-form__row label { display: grid; gap: 7px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; }
 .plan-form input, .plan-form textarea, .plan-form select, .proposal-composer textarea { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface-soft); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
 .plan-form textarea, .proposal-composer textarea { min-height: 110px; resize: vertical; line-height: 1.35; }
-.plan-form input:focus, .plan-form textarea:focus, .plan-form select:focus, .proposal-composer textarea:focus { border-color: var(--proposal-border); box-shadow: 0 0 0 3px color-mix(in srgb, var(--proposal-border) 24%, transparent); }
+.plan-form input:focus, .plan-form textarea:focus, .plan-form select:focus { border-color: var(--plan-border); box-shadow: 0 0 0 3px color-mix(in srgb, var(--plan-border) 24%, transparent); }
+.proposal-composer textarea:focus { border-color: var(--proposal-border); box-shadow: 0 0 0 3px color-mix(in srgb, var(--proposal-border) 24%, transparent); }
 .plan-form__row { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
 .plan-form hr { width: 100%; border: 0; border-top: 1px solid var(--app-border); margin: 4px 0; }
 .plan-image-picker { display: grid; gap: 12px; border: 1px dashed var(--app-border-strong); border-radius: 24px; background: var(--app-surface-soft); padding: 12px; }
@@ -9051,9 +9060,9 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-place-picker-panel input, .plan-place-picker-panel textarea { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface-soft); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
 .plan-place-picker-panel textarea { min-height: 94px; resize: vertical; line-height: 1.35; }
 .plan-place-picker-list { display: grid; gap: 8px; max-height: 420px; overflow: auto; padding-right: 2px; }
-.plan-place-picker-card { display: grid; grid-template-columns: 58px minmax(0, 1fr) auto; align-items: center; gap: 10px; width: 100%; border: 1px solid var(--app-border); border-radius: 20px; background: var(--app-surface-soft); color: inherit; padding: 8px; text-align: left; cursor: pointer; }
-.plan-place-picker-card:hover { border-color: var(--app-border-strong); background: var(--app-surface); }
-.plan-place-picker-card__media { display: grid; place-items: center; overflow: hidden; width: 58px; height: 58px; border-radius: 16px; background: var(--app-surface); color: var(--app-text-soft); font-size: 22px; font-weight: 950; }
+.plan-place-picker-card { display: grid; grid-template-columns: 58px minmax(0, 1fr) auto; align-items: center; gap: 10px; width: 100%; border: 1px solid color-mix(in srgb, var(--place-border) 38%, var(--app-border)); border-radius: 20px; background: color-mix(in srgb, var(--place-bg) 22%, var(--app-surface)); color: inherit; padding: 8px; text-align: left; cursor: pointer; }
+.plan-place-picker-card:hover { border-color: var(--place-border); background: color-mix(in srgb, var(--place-bg) 34%, var(--app-surface)); }
+.plan-place-picker-card__media { display: grid; place-items: center; overflow: hidden; width: 58px; height: 58px; border-radius: 16px; background: var(--place-bg); color: var(--place-text); font-size: 22px; font-weight: 950; }
 .plan-place-picker-card__media img { width: 100%; height: 100%; object-fit: cover; }
 .plan-place-picker-card__body { display: grid; gap: 3px; min-width: 0; }
 .plan-place-picker-card__body strong, .plan-place-picker-card__body small, .plan-place-picker-card__body span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
@@ -9066,8 +9075,8 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-place-source-grid { display: grid; gap: 10px; }
 .plan-place-source-option { display: grid; grid-template-columns: 42px minmax(0, 1fr); align-items: center; gap: 12px; width: 100%; min-height: 78px; border: 1px solid var(--app-border); border-radius: 22px; background: var(--app-surface-soft); color: inherit; padding: 12px; text-align: left; cursor: pointer; }
 .plan-place-source-option:hover { border-color: var(--app-border-strong); background: var(--app-surface); }
-.plan-place-source-option--primary { border-color: color-mix(in srgb, var(--app-accent, #14b8a6) 34%, var(--app-border)); background: color-mix(in srgb, var(--app-accent, #14b8a6) 14%, var(--app-surface)); }
-.plan-place-source-option__icon { display: grid; place-items: center; width: 42px; height: 42px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface); color: var(--app-text); font-size: 18px; font-weight: 950; }
+.plan-place-source-option--primary { border-color: color-mix(in srgb, var(--place-border) 72%, var(--app-border)); background: color-mix(in srgb, var(--place-bg) 46%, var(--app-surface)); }
+.plan-place-source-option__icon { display: grid; place-items: center; width: 42px; height: 42px; border: 1px solid var(--place-border); border-radius: 999px; background: var(--place-bg); color: var(--place-text); font-size: 18px; font-weight: 950; }
 .plan-place-source-option span:last-child { display: grid; gap: 3px; min-width: 0; }
 .plan-place-source-option strong { color: var(--app-text); font-size: 15px; font-weight: 950; }
 .plan-place-source-option small { color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
@@ -9099,11 +9108,11 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-time-row input { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
 
 .plan-advanced-details { display: grid; gap: 10px; border-top: 1px solid var(--app-border); padding-top: 14px; }
-.plan-advanced-details__toggle { width: 100%; display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: 8px; border: 1px solid var(--app-border); border-radius: 20px; background: var(--app-surface-soft); color: var(--app-text); padding: 12px 14px; text-align: left; cursor: pointer; }
+.plan-advanced-details__toggle { width: 100%; display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: center; gap: 8px; border: 0; border-radius: 0; background: transparent; color: var(--app-text); padding: 12px 0; text-align: left; cursor: pointer; }
 .plan-advanced-details__toggle span { font-weight: 950; letter-spacing: -0.01em; }
 .plan-advanced-details__toggle small { display: block; min-width: 0; color: var(--app-text-muted); font-size: 12px; font-weight: 800; }
-.plan-advanced-details__toggle strong { grid-row: 1 / span 2; grid-column: 2; display: grid; place-items: center; width: 28px; height: 28px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface); font-size: 18px; line-height: 1; }
-.plan-advanced-details__panel { display: grid; gap: 12px; border: 1px solid var(--app-border); border-radius: 22px; background: var(--app-surface-soft); padding: 12px; }
+.plan-advanced-details__toggle strong { grid-row: 1 / span 2; grid-column: 2; display: grid; place-items: center; width: 28px; height: 28px; border: 1px solid var(--plan-border); border-radius: 999px; background: var(--plan-bg); color: var(--plan-text); font-size: 18px; line-height: 1; }
+.plan-advanced-details__panel { display: grid; gap: 12px; border-top: 1px solid var(--app-divider); background: transparent; padding: 14px 0 0; }
 .plan-advanced-details__panel label, .plan-advanced-details__panel .plan-form__row label { display: grid; gap: 7px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; }
 .plan-advanced-details__panel input, .plan-advanced-details__panel textarea { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
 .plan-advanced-details__panel textarea { min-height: 96px; resize: vertical; line-height: 1.35; }
@@ -9112,24 +9121,28 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .plan-time-row { grid-template-columns: 1fr; align-items: stretch; }
 }
 
-.plan-stage-tabs { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }
-.plan-stage-tabs button { border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text-muted); padding: 10px 12px; font: inherit; font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; cursor: pointer; }
-.plan-stage-tabs button.is-active { border-color: var(--app-border-strong); background: var(--app-surface); color: var(--app-text); }
+.plan-stage-tabs { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; padding: 4px; border: 1px solid color-mix(in srgb, var(--plan-border) 45%, var(--app-border)); border-radius: 999px; background: color-mix(in srgb, var(--plan-bg) 42%, var(--app-surface)); }
+.plan-stage-tabs button { border: 0; border-radius: 999px; background: transparent; color: var(--app-text-muted); padding: 10px 12px; font: inherit; font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; cursor: pointer; }
+.plan-stage-tabs button.is-active { background: var(--plan-bg); color: var(--plan-text); box-shadow: 0 8px 22px color-mix(in srgb, var(--plan-border) 22%, transparent); }
 .plan-form--timeline { gap: 16px; }
-.plan-build-timeline { display: grid; gap: 0; }
-.plan-timeline-row { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: center; border-top: 1px solid var(--app-border); padding: 16px 0; }
-.plan-timeline-row:first-child { border-top: 0; padding-top: 0; }
+.plan-form--clean { background: transparent; border: 0; box-shadow: none; padding: 0; }
+.plan-build-timeline { display: grid; gap: 0; border-top: 1px solid var(--app-divider); border-bottom: 1px solid var(--app-divider); }
+.plan-timeline-row { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: center; border-top: 1px solid var(--app-divider); padding: 17px 0; }
+.plan-timeline-row:first-child { border-top: 0; }
+.plan-timeline-row--time { background: linear-gradient(90deg, color-mix(in srgb, var(--time-bg) 26%, transparent), transparent 38%); }
+.plan-timeline-row--add { background: linear-gradient(90deg, color-mix(in srgb, var(--place-bg) 34%, transparent), transparent 44%); }
 .plan-timeline-row__main { display: grid; gap: 6px; min-width: 0; }
 .plan-timeline-row__main h3 { margin: 0; font-size: 18px; letter-spacing: -0.02em; }
 .plan-timeline-row__main--button { width: 100%; border: 0; background: transparent; color: inherit; text-align: left; cursor: pointer; padding: 0; }
 .plan-timeline-row__main--button strong { color: var(--app-text); font-size: 18px; font-weight: 950; letter-spacing: -0.02em; }
 .plan-timeline-row__main--button small { color: var(--app-text-muted); font-size: 13px; font-weight: 800; line-height: 1.35; }
-.plan-timeline-row__main--button:hover strong { text-decoration: underline; text-underline-offset: 3px; }
+.plan-timeline-row__main--button:hover strong { color: var(--place-text); text-decoration: underline; text-underline-offset: 3px; }
 .plan-timeline-row__heading { display: flex; align-items: center; gap: 7px; flex-wrap: wrap; }
 .plan-timeline-row__actions { display: flex; align-items: center; justify-content: flex-end; gap: 8px; flex-wrap: wrap; }
 .plan-timeline-row__fields { display: grid; grid-template-columns: repeat(2, minmax(130px, 1fr)); gap: 10px; align-items: end; }
 .plan-timeline-row__fields label, .plan-timeline-row__details label, .plan-stop-note label { display: grid; gap: 7px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; }
-.plan-timeline-row__fields input, .plan-timeline-row__details input, .plan-stop-note textarea { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface-soft); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
+.plan-timeline-row__fields input, .plan-timeline-row__details input, .plan-stop-note textarea { width: 100%; border: 1px solid color-mix(in srgb, var(--time-border) 38%, var(--app-border-strong)); border-radius: 18px; background: color-mix(in srgb, var(--app-surface) 76%, var(--time-bg)); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
+.plan-timeline-row__fields input:focus, .plan-timeline-row__details input:focus, .plan-stop-note textarea:focus { border-color: var(--time-border); box-shadow: 0 0 0 3px color-mix(in srgb, var(--time-border) 24%, transparent); }
 .plan-timeline-row__fields--optional { grid-column: 1 / -1; max-width: 360px; }
 .plan-timeline-row__details { grid-column: 1 / -1; display: grid; gap: 10px; padding-top: 4px; }
 .plan-stop-note { grid-column: 1 / -1; display: grid; gap: 10px; }
@@ -9138,7 +9151,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-place-detail-sheet { max-width: 680px; }
 .plan-place-detail-actions { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; border-top: 1px solid var(--app-border); padding-top: 12px; }
 .plan-source-place-strip span { min-width: 0; color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
-.plan-preview-stage { border-top: 1px solid var(--app-border); padding-top: 14px; }
+.plan-preview-stage { border-top: 1px solid var(--app-divider); padding-top: 14px; }
 .plan-preview-confirmation, .plan-preview-actions { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
 .plan-preview-actions { justify-content: flex-end; border-top: 1px solid var(--app-border); padding-top: 14px; }
 @media (max-width: 760px) {
diff --git a/apps/web/src/features/plans/PlanCreateClient.tsx b/apps/web/src/features/plans/PlanCreateClient.tsx
index eee14e7..cf64827 100644
--- a/apps/web/src/features/plans/PlanCreateClient.tsx
+++ b/apps/web/src/features/plans/PlanCreateClient.tsx
@@ -709,7 +709,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
         ) : null}
 
         {auth.isAuthenticated ? (
-          <form className="mobile-card plan-form plan-form--timeline" onSubmit={submit}>
+          <form className="plan-form plan-form--timeline plan-form--clean" onSubmit={submit}>
             <div className="plan-stage-tabs" aria-label="Create Plan stages">
               <button type="button" className={stage === 'build' ? 'is-active' : ''} onClick={() => setStage('build')}>Build plan</button>
               <button type="button" className={stage === 'preview' ? 'is-active' : ''} onClick={showPreviewStage}>Preview / Confirm</button>
@@ -720,7 +720,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                 <section className="plan-build-timeline" aria-label="Build Plan timeline">
                   <div className="plan-timeline-row plan-timeline-row--time">
                     <div className="plan-timeline-row__main">
-                      <span className="semantic-badge instruction">Date / time</span>
+                      <span className="semantic-badge time">Date / time</span>
                       <h3>Start time recommended</h3>
                       <p className="meta">Place 1 becomes the Plan start. Later stops can keep their own optional time.</p>
                     </div>
@@ -744,8 +744,8 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                     <div className="plan-timeline-row plan-timeline-row--place" key={place.id}>
                       <button type="button" className="plan-timeline-row__main plan-timeline-row__main--button" onClick={() => setDetailPlaceIndex(index)}>
                         <span className="plan-timeline-row__heading">
-                          <span className="semantic-badge instruction">Place {index + 1}</span>
-                          {place.sourcePlaceId ? <span className="semantic-badge success">{place.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'}</span> : <span className="semantic-badge muted">Custom</span>}
+                          <span className="semantic-badge place">Place {index + 1}</span>
+                          {place.sourcePlaceId ? <span className="semantic-badge place">{place.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'}</span> : <span className="semantic-badge place">Custom</span>}
                         </span>
                         <strong>{place.title.trim() || place.sourcePlaceTitle || `Place ${index + 1}`}</strong>
                         <small>{[placePreviewLocation(place) || 'No location yet', index === 0 ? `${place.date || 'Date'} ${place.time || 'Time'}` : place.time.trim() ? `${place.date || 'Date'} ${place.time}` : 'Time optional'].filter(Boolean).join(' · ')}</small>
@@ -768,7 +768,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
 
                   <div className="plan-timeline-row plan-timeline-row--time">
                     <div className="plan-timeline-row__main">
-                      <span className="semantic-badge muted">Optional</span>
+                      <span className="semantic-badge time">Optional</span>
                       <h3>End date / time</h3>
                       <p className="meta">Leave empty if the Plan ends at the last timed place.</p>
                     </div>
@@ -820,8 +820,8 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                     />
                   </div>
                   <div className="plan-preview-confirmation">
-                    <span className="semantic-badge success">Free join</span>
-                    <span className="semantic-badge instruction">Open / Public</span>
+                    <span className="semantic-badge plan">Free join</span>
+                    <span className="semantic-badge plan">Open / Public</span>
                     <span className="semantic-badge muted">{places.length} {places.length === 1 ? 'place' : 'places'}</span>
                   </div>
                 </section>
@@ -851,8 +851,8 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
               </div>
               <div className="plan-place-picker-panel">
                 <div className="plan-timeline-row__heading">
-                  {detailPlace.sourcePlaceId ? <span className="semantic-badge success">{detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'}</span> : <span className="semantic-badge muted">Custom one-off stop</span>}
-                  <span className="semantic-badge instruction">{detailPlaceIndex === 0 ? 'Plan start' : 'Optional time'}</span>
+                  {detailPlace.sourcePlaceId ? <span className="semantic-badge place">{detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'}</span> : <span className="semantic-badge place">Custom one-off stop</span>}
+                  <span className="semantic-badge time">{detailPlaceIndex === 0 ? 'Plan start' : 'Optional time'}</span>
                 </div>
                 <div className="plan-timeline-row__fields">
                   <label>
diff --git a/packages/theme/src/tokens.ts b/packages/theme/src/tokens.ts
index 1ba03e8..77946b5 100644
--- a/packages/theme/src/tokens.ts
+++ b/packages/theme/src/tokens.ts
@@ -23,6 +23,18 @@ export const semanticColors = {
     softBg: '#CCFBF1',
     border: '#5EEAD4',
   },
+  plan: {
+    bg: '#7C3AED',
+    text: '#5B21B6',
+    softBg: '#EDE9FE',
+    border: '#C4B5FD',
+  },
+  place: {
+    bg: '#A855F7',
+    text: '#6B21A8',
+    softBg: '#F3E8FF',
+    border: '#D8B4FE',
+  },
   time: {
     bg: '#F97316',
     text: '#9A3412',
@@ -88,6 +100,8 @@ export const semanticColorsDark: SemanticPalette = {
   offer: { bg: '#4ADE80', text: '#BBF7D0', softBg: '#052E16', border: '#15803D' },
   trade: { bg: '#F8FAFC', text: '#F8FAFC', softBg: '#1F2937', border: '#475569' },
   proposal: { bg: '#2DD4BF', text: '#CCFBF1', softBg: '#042F2E', border: '#0F766E' },
+  plan: { bg: '#A78BFA', text: '#DDD6FE', softBg: '#2E1065', border: '#6D28D9' },
+  place: { bg: '#C084FC', text: '#F3E8FF', softBg: '#3B0764', border: '#7E22CE' },
   time: { bg: '#FB923C', text: '#FED7AA', softBg: '#431407', border: '#C2410C' },
   danger: { bg: '#F87171', text: '#FECACA', softBg: '#450A0A', border: '#B91C1C' },
   instruction: { bg: '#CBD5E1', text: '#E2E8F0', softBg: '#1E293B', border: '#475569' },
