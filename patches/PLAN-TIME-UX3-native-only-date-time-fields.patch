diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 0f7c1e1..69da40f 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -636,15 +636,13 @@ function timePresetValue(preset: 'morning' | 'afternoon' | 'evening') {
 
 function formatInputDateLabel(value: string) {
   const date = parseLocalDateTime(value, '12:00');
-  if (!date) return value || 'Date not set';
-  return date.toLocaleDateString([], { weekday: 'short', month: 'short', day: 'numeric' });
+  if (!date) return value || 'DD/MM/YYYY';
+  return `${padDatePart(date.getDate())}/${padDatePart(date.getMonth() + 1)}/${date.getFullYear()}`;
 }
 
 function formatInputTimeLabel(value: string) {
-  if (!/^\d{2}:\d{2}$/.test(value)) return value || 'Time not set';
-  const date = parseLocalDateTime(toDateInputValueFromDate(new Date()), value);
-  if (!date) return value;
-  return date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
+  if (!/^\d{2}:\d{2}$/.test(value)) return value || 'HH:MM';
+  return value;
 }
 
 type PlanNativePickerMode = 'date' | 'time';
@@ -2730,70 +2728,35 @@ function PlanDateTimePickerSheet({
   onChange: (patch: { date?: string; time?: string }) => void;
   onClose: () => void;
 }) {
-  const theme = useThemeTokens();
-  const [customPickerOpen, setCustomPickerOpen] = useState(false);
-  const today = datePresetValue('today');
-  const tomorrow = datePresetValue('tomorrow');
-  const weekend = datePresetValue('weekend');
-  const nextWeek = datePresetValue('next_week');
-
-  useEffect(() => {
-    if (!visible) setCustomPickerOpen(false);
-  }, [visible]);
+  if (!visible) return null;
+  const pickerValue = nativePickerDateValue(date, time, mode);
 
-  function chooseDate(nextDate: string) {
-    onChange({ date: nextDate });
-    setCustomPickerOpen(false);
+  function handleAndroidChange(event: DateTimePickerEvent, selectedDate?: Date) {
     onClose();
+    if (event.type === 'dismissed' || !selectedDate) return;
+    onChange(nativePickerPatch(mode, selectedDate));
   }
 
-  function chooseTime(nextTime: string) {
-    onChange({ time: nextTime });
-    setCustomPickerOpen(false);
-    onClose();
+  function handleIosChange(patch: { date?: string; time?: string }) {
+    onChange(patch);
   }
 
-  function chooseCustom(patch: { date?: string; time?: string }) {
-    onChange(patch);
-    if (Platform.OS === 'android') onClose();
+  if (Platform.OS === 'android') {
+    return (
+      <DateTimePicker
+        value={pickerValue}
+        mode={mode}
+        display={mode === 'date' ? 'calendar' : 'clock'}
+        is24Hour
+        minuteInterval={mode === 'time' ? 5 : undefined}
+        onChange={handleAndroidChange}
+      />
+    );
   }
 
   return (
     <PlanPickerSheetFrame visible={visible} title={title} subtitle={subtitle} onClose={onClose}>
-      <View style={[styles.planPickerSummaryRow, { backgroundColor: theme.semantic.time.softBg, borderColor: theme.semantic.time.border }]}>
-        <View style={[styles.planPickerSummaryIcon, { backgroundColor: theme.semantic.time.softBg, borderColor: theme.semantic.time.border }]}>
-          <MobileIcon name="calendar" color={theme.semantic.time.text} size={18} />
-        </View>
-        <View style={styles.timelineCopy}>
-          <AppText style={styles.rowTitle}>{formatInputDateLabel(date)} · {formatInputTimeLabel(time)}</AppText>
-          <AppText style={[styles.metaText, { color: theme.color.muted }]}>{mode === 'date' ? 'Pick a date for this stop.' : 'Pick the start time for this stop.'}</AppText>
-        </View>
-      </View>
-
-      {mode === 'date' ? (
-        <View style={styles.quickPickerGroup}>
-          <AppText style={[styles.quickPickerLabel, { color: theme.color.muted }]}>Quick date</AppText>
-          <View style={styles.quickChoiceRow}>
-            <QuickChoiceButton label="Today" active={date === today} onPress={() => chooseDate(today)} />
-            <QuickChoiceButton label="Tomorrow" active={date === tomorrow} onPress={() => chooseDate(tomorrow)} />
-            <QuickChoiceButton label="This weekend" active={date === weekend} onPress={() => chooseDate(weekend)} />
-            <QuickChoiceButton label="Next week" active={date === nextWeek} onPress={() => chooseDate(nextWeek)} />
-            <QuickChoiceButton label="Custom" active={customPickerOpen} onPress={() => setCustomPickerOpen((open) => !open)} />
-          </View>
-          {customPickerOpen ? <PlanNativeDateTimePicker mode="date" date={date} time={time} onChange={chooseCustom} onClose={() => setCustomPickerOpen(false)} /> : null}
-        </View>
-      ) : (
-        <View style={styles.quickPickerGroup}>
-          <AppText style={[styles.quickPickerLabel, { color: theme.color.muted }]}>Quick time</AppText>
-          <View style={styles.quickChoiceRow}>
-            <QuickChoiceButton label="Morning" active={time === timePresetValue('morning')} onPress={() => chooseTime(timePresetValue('morning'))} />
-            <QuickChoiceButton label="Afternoon" active={time === timePresetValue('afternoon')} onPress={() => chooseTime(timePresetValue('afternoon'))} />
-            <QuickChoiceButton label="Evening" active={time === timePresetValue('evening')} onPress={() => chooseTime(timePresetValue('evening'))} />
-            <QuickChoiceButton label="Custom" active={customPickerOpen} onPress={() => setCustomPickerOpen((open) => !open)} />
-          </View>
-          {customPickerOpen ? <PlanNativeDateTimePicker mode="time" date={date} time={time} onChange={chooseCustom} onClose={() => setCustomPickerOpen(false)} /> : null}
-        </View>
-      )}
+      <PlanNativeDateTimePicker mode={mode} date={date} time={time} onChange={handleIosChange} onClose={onClose} />
     </PlanPickerSheetFrame>
   );
 }
