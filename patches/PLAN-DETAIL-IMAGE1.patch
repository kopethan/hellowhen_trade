diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 24d2490..7896d03 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -9778,7 +9778,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 .plan-route-stop__content {
   display: grid;
-  grid-template-columns: minmax(0, 1fr) 96px;
+  grid-template-columns: minmax(0, 1fr);
   align-items: start;
   gap: 15px;
   margin-top: 7px;
@@ -9798,8 +9798,10 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 .plan-route-stop__media {
   overflow: hidden;
-  width: 96px;
-  height: 96px;
+  width: min(100%, 320px);
+  aspect-ratio: 16 / 10;
+  height: auto;
+  margin: 10px 0 9px;
   border-radius: 24px;
   border: 1px solid var(--app-border);
   background: var(--app-surface-soft);
@@ -9919,7 +9921,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .plan-detail-toolbar__actions .plans-internal-badge { display: none; }
   .plan-detail-hero-social h1 { max-width: 12ch; font-size: clamp(38px, 14vw, 58px); }
   .plan-route-stop__content { grid-template-columns: 1fr; }
-  .plan-route-stop__media { width: 100%; height: auto; aspect-ratio: 16 / 9; border-radius: 22px; }
+  .plan-route-stop__media { width: 100%; aspect-ratio: 16 / 9; border-radius: 22px; }
   .plan-owner-row { display: grid; }
   .plan-participant-row--social { grid-template-columns: auto minmax(0, 1fr); }
   .plan-participant-row--social .button { grid-column: 1 / -1; }
diff --git a/apps/web/src/features/plans/PlanDetailClient.tsx b/apps/web/src/features/plans/PlanDetailClient.tsx
index 455b9b4..9e92a91 100644
--- a/apps/web/src/features/plans/PlanDetailClient.tsx
+++ b/apps/web/src/features/plans/PlanDetailClient.tsx
@@ -146,14 +146,14 @@ function PlanPlaceCard({ place, index, planStartsAt, showReport }: { place: Plan
           <div className="plan-route-stop__copy">
             <h4>{place.title}</h4>
             {location ? <p className="meta">{location}</p> : null}
+            {displayMedia ? (
+              <div className="plan-route-stop__media">
+                <PlanPlaceImage media={displayMedia} />
+              </div>
+            ) : null}
             {description ? <p>{description}</p> : null}
             {showReport ? <ReportContentButton targetType="plan_place" targetId={place.id} /> : null}
           </div>
-          {displayMedia ? (
-            <div className="plan-route-stop__media">
-              <PlanPlaceImage media={displayMedia} />
-            </div>
-          ) : null}
         </div>
       </div>
     </article>
