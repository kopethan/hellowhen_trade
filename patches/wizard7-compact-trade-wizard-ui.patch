diff --git a/apps/mobile/src/features/trade/CreateTradeScreen.tsx b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
index e034e78..d6fa290 100644
--- a/apps/mobile/src/features/trade/CreateTradeScreen.tsx
+++ b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
@@ -44,6 +44,7 @@ export type TradeSidePickerParams = {
   proposalId?: string;
   proposalNeedId?: string;
   proposalOfferId?: string;
+  initialSourceMode?: InlineSourceMode;
 };
 export type TradeCreateReturnParams = { selectedTradeSide?: TradeCreateSideSelection; initialPostType?: TradePostType | null; initialNeedSelection?: TradeCreateSideSelection | null; initialOfferSelection?: TradeCreateSideSelection | null; initialExpiryDays?: number | null } | undefined;
 
@@ -54,6 +55,7 @@ type CreateTradeResponse = { trade: TradeDeckItem };
 
 type PostTypeOption = { value: TradePostType; labelKey: string; badgeKey: string; titleKey: string; bodyKey: string; icon: 'trade' | 'need' | 'offer'; tone: 'trade' | 'need' | 'offer' };
 type TradeWizardStepId = 'type' | 'need' | 'offer' | 'review';
+type InlineSourceMode = 'mine' | 'starter' | 'cashPromise';
 type TradeCreateWizardPersistedDraft = {
   activeStepId: TradeWizardStepId;
   postType: TradePostType | null;
@@ -312,8 +314,17 @@ export function CreateTradeScreen({ route, navigation }: Props) {
     setActiveStepId(nextPostType === 'open_offer' ? 'offer' : 'need');
   }
 
-  function openSidePicker(side: TradeCreateSide) {
-    navigation.navigate('TradeSidePicker', { side, selection: side === 'need' ? needSelection : offerSelection, postType: postType ?? undefined, returnTo: 'createTrade' });
+  function openSidePicker(side: TradeCreateSide, initialSourceMode?: InlineSourceMode) {
+    navigation.navigate('TradeSidePicker', { side, selection: side === 'need' ? needSelection : offerSelection, postType: postType ?? undefined, returnTo: 'createTrade', initialSourceMode });
+  }
+
+  function createNewSide(side: TradeCreateSide) {
+    const params = { returnTo: 'createTrade' as const };
+    if (side === 'need') {
+      navigation.navigate('CreateNeed', params);
+      return;
+    }
+    navigation.navigate('CreateOffer', params);
   }
 
   function navigateToFullForm() {
@@ -407,7 +418,6 @@ export function CreateTradeScreen({ route, navigation }: Props) {
   return (
     <WizardShell
       title={t('trade.create.title')}
-      subtitle={postType ? postTypeBody(postType, t) : t('trade.create.chooseKindBody')}
       onBack={() => navigation.goBack()}
       steps={steps}
       activeStepId={activeStepId}
@@ -439,24 +449,30 @@ export function CreateTradeScreen({ route, navigation }: Props) {
           theme={theme}
           side="need"
           title={t('trade.labels.iNeed')}
-          emptyTitle={t('trade.create.selectedNeedEmptyTitle')}
-          emptyBody={postType === 'open_need' ? t('trade.create.chooseOpenNeedBody') : t('trade.create.chooseSavedNeedBody')}
+          body={postType === 'open_need' ? t('trade.create.chooseOpenNeedBody') : t('trade.create.chooseSavedNeedBody')}
           selection={needSelection}
           need={selectedNeed}
           offer={null}
-          onPress={() => openSidePicker('need')}
+          cashPromiseAvailable={postType === 'need_offer' && betaFeatures.cashPromiseEnabled && betaFeatures.cashPromiseVisible}
+          onMine={() => openSidePicker('need', 'mine')}
+          onStarter={() => openSidePicker('need', 'starter')}
+          onCashPromise={() => openSidePicker('need', 'cashPromise')}
+          onCreate={() => createNewSide('need')}
         />
       ) : activeStepId === 'offer' ? (
         <SideStep
           theme={theme}
           side="offer"
           title={t('trade.labels.iOffer')}
-          emptyTitle={t('trade.create.selectedOfferEmptyTitle')}
-          emptyBody={postType === 'open_offer' ? t('trade.create.chooseOpenOfferBody') : t('trade.create.chooseSavedOfferBody')}
+          body={postType === 'open_offer' ? t('trade.create.chooseOpenOfferBody') : t('trade.create.chooseSavedOfferBody')}
           selection={offerSelection}
           need={null}
           offer={selectedOffer}
-          onPress={() => openSidePicker('offer')}
+          cashPromiseAvailable={postType === 'need_offer' && betaFeatures.cashPromiseEnabled && betaFeatures.cashPromiseVisible}
+          onMine={() => openSidePicker('offer', 'mine')}
+          onStarter={() => openSidePicker('offer', 'starter')}
+          onCashPromise={() => openSidePicker('offer', 'cashPromise')}
+          onCreate={() => createNewSide('offer')}
         />
       ) : (
         <ReviewStep
@@ -469,8 +485,8 @@ export function CreateTradeScreen({ route, navigation }: Props) {
           expiryDays={expiryDays}
           previewTrade={previewTrade}
           previewCardCount={previewCardCount}
-          onChooseNeed={() => openSidePicker('need')}
-          onChooseOffer={() => openSidePicker('offer')}
+          onChooseNeed={() => openSidePicker('need', 'mine')}
+          onChooseOffer={() => openSidePicker('offer', 'mine')}
           onExpiryChange={setExpiryDays}
         />
       )}
@@ -483,17 +499,77 @@ function PostTypeStep({ theme, postType, onChoose }: { theme: ThemeTokens; postT
   const { t } = useTranslation();
   return (
     <AppCard>
-      <View style={styles.postTypeHeader}>
-        <AppText style={styles.sectionTitle}>{t('trade.create.publishQuestion')}</AppText>
-        {postType ? <AppText style={[styles.selectedPostTypeLabel, { color: theme.color.muted }]}>{postTypeLabel(postType, t)}</AppText> : null}
-      </View>
       <View style={styles.postTypeList}>{postTypeOptions.map((option) => <PostTypeCard key={option.value} option={option} selected={postType === option.value} theme={theme} onPress={() => onChoose(option.value)} />)}</View>
     </AppCard>
   );
 }
 
-function SideStep({ theme, side, title, emptyTitle, emptyBody, selection, need, offer, onPress }: { theme: ThemeTokens; side: TradeCreateSide; title: string; emptyTitle: string; emptyBody: string; selection: TradeCreateSideSelection | null; need: NeedItem | null; offer: OfferItem | null; onPress: () => void }) {
-  return <SideSelectionCard theme={theme} side={side} title={title} emptyTitle={emptyTitle} emptyBody={emptyBody} selection={selection} need={need} offer={offer} onPress={onPress} />;
+function SideStep({ theme, side, title, body, selection, need, offer, cashPromiseAvailable, onMine, onStarter, onCashPromise, onCreate }: { theme: ThemeTokens; side: TradeCreateSide; title: string; body: string; selection: TradeCreateSideSelection | null; need: NeedItem | null; offer: OfferItem | null; cashPromiseAvailable: boolean; onMine: () => void; onStarter: () => void; onCashPromise: () => void; onCreate: () => void }) {
+  return <SideSourceStep theme={theme} side={side} title={title} body={body} selection={selection} need={need} offer={offer} cashPromiseAvailable={cashPromiseAvailable} onMine={onMine} onStarter={onStarter} onCashPromise={onCashPromise} onCreate={onCreate} />;
+}
+
+
+function SideSourceStep({ theme, side, title, body, selection, need, offer, cashPromiseAvailable, onMine, onStarter, onCashPromise, onCreate }: { theme: ThemeTokens; side: TradeCreateSide; title: string; body: string; selection: TradeCreateSideSelection | null; need: NeedItem | null; offer: OfferItem | null; cashPromiseAvailable: boolean; onMine: () => void; onStarter: () => void; onCashPromise: () => void; onCreate: () => void }) {
+  const { t } = useTranslation();
+  const itemLabel = side === 'need' ? t('inventory.labels.need') : t('inventory.labels.offer');
+  const pluralLabel = side === 'need' ? t('inventory.labels.needs').toLowerCase() : t('inventory.labels.offers').toLowerCase();
+  const hasSelection = selection?.kind === 'cash_promise' || (side === 'need' ? Boolean(need) : Boolean(offer));
+
+  return (
+    <View style={styles.sideSourceStack}>
+      {hasSelection ? (
+        <SideSelectionCard
+          theme={theme}
+          side={side}
+          title={title}
+          emptyTitle={side === 'need' ? t('trade.create.selectedNeedEmptyTitle') : t('trade.create.selectedOfferEmptyTitle')}
+          emptyBody={body}
+          selection={selection}
+          need={need}
+          offer={offer}
+          onPress={onMine}
+        />
+      ) : null}
+      <AppCard style={styles.sideSourceCard}>
+        <View style={styles.sourceIntro}>
+          <SemanticBadge label={title} tone={side === 'need' ? 'need' : 'offer'} size="sm" />
+          <AppText style={styles.sourceIntroTitle}>{hasSelection ? t('trade.sidePicker.changeSource') : t('trade.sidePicker.chooseSourceTitle', { item: itemLabel.toLowerCase() })}</AppText>
+          <AppText style={[styles.sourceIntroBody, { color: theme.color.muted }]}>{body}</AppText>
+        </View>
+        <View style={styles.sourceChoiceList}>
+          <SourceActionCard theme={theme} icon={side} title={t('trade.sidePicker.useMine')} body={t('trade.sidePicker.useMineBody', { items: pluralLabel })} tone={side === 'need' ? 'need' : 'offer'} onPress={onMine} />
+          <SourceActionCard theme={theme} icon="search" title={t('trade.sidePicker.useStarter')} body={t('trade.sidePicker.useStarterBody')} tone="instruction" onPress={onStarter} />
+          <SourceActionCard theme={theme} icon={side} title={t('trade.sidePicker.createNew', { item: itemLabel })} body={t('trade.sidePicker.createNewBody')} tone={side === 'need' ? 'need' : 'offer'} dashed onPress={onCreate} />
+          {cashPromiseAvailable ? <SourceActionCard theme={theme} icon="wallet" title={t('trade.sidePicker.useCashPromise')} body={t('trade.sidePicker.useCashPromiseBody')} tone="warning" onPress={onCashPromise} /> : null}
+        </View>
+      </AppCard>
+    </View>
+  );
+}
+
+function SourceActionCard({ theme, icon, title, body, tone, dashed = false, onPress }: { theme: ThemeTokens; icon: 'need' | 'offer' | 'search' | 'wallet'; title: string; body: string; tone: 'need' | 'offer' | 'instruction' | 'warning'; dashed?: boolean; onPress: () => void }) {
+  const semantic = theme.semantic[tone];
+  return (
+    <Pressable
+      accessibilityRole="button"
+      onPress={onPress}
+      style={({ pressed }) => [
+        styles.sourceActionCard,
+        { backgroundColor: theme.color.surface, borderColor: semantic.border },
+        dashed && styles.sourceActionCardDashed,
+        pressed && styles.pressed,
+      ]}
+    >
+      <View style={[styles.sourceActionIcon, { backgroundColor: semantic.softBg, borderColor: semantic.border }]}>
+        {icon === 'wallet' ? <AppText style={[styles.sourceActionMoneyIcon, { color: semantic.text }]}>€</AppText> : <MobileIcon name={icon} size={21} color={semantic.text} />}
+      </View>
+      <View style={styles.sourceActionCopy}>
+        <AppText style={styles.sourceActionTitle}>{title}</AppText>
+        <AppText style={[styles.sourceActionBody, { color: theme.color.muted }]} numberOfLines={2}>{body}</AppText>
+      </View>
+      <MobileIcon name="chevron-right" size={18} color={theme.color.muted} />
+    </Pressable>
+  );
 }
 
 function ReviewStep({ theme, postType, needSelection, offerSelection, selectedNeed, selectedOffer, expiryDays, previewTrade, previewCardCount, onChooseNeed, onChooseOffer, onExpiryChange }: { theme: ThemeTokens; postType: TradePostType | null; needSelection: TradeCreateSideSelection | null; offerSelection: TradeCreateSideSelection | null; selectedNeed: NeedItem | null; selectedOffer: OfferItem | null; expiryDays: number | null; previewTrade: TradeDeckItem; previewCardCount: number; onChooseNeed: () => void; onChooseOffer: () => void; onExpiryChange: (days: number | null) => void }) {
@@ -559,7 +635,20 @@ const styles = StyleSheet.create({
   postTypeCopy: { flex: 1, gap: 5 },
   postTypeTitle: { fontSize: 18, fontWeight: '900', letterSpacing: -0.25 },
   postTypeBody: { lineHeight: 20, fontWeight: '700' },
-  sideCard: { borderRadius: 28, borderWidth: 1, padding: 18, gap: 14 },
+  sideSourceStack: { gap: 12 },
+  sideSourceCard: { gap: 14 },
+  sourceIntro: { gap: 6 },
+  sourceIntroTitle: { fontSize: 20, lineHeight: 24, fontWeight: '900', letterSpacing: -0.3 },
+  sourceIntroBody: { lineHeight: 20, fontWeight: '700' },
+  sourceChoiceList: { gap: 10 },
+  sourceActionCard: { borderRadius: 20, borderWidth: 1, padding: 12, flexDirection: 'row', alignItems: 'center', gap: 11 },
+  sourceActionCardDashed: { borderStyle: 'dashed' },
+  sourceActionIcon: { width: 44, height: 44, borderRadius: 16, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
+  sourceActionMoneyIcon: { fontSize: 18, fontWeight: '900' },
+  sourceActionCopy: { flex: 1, minWidth: 0, gap: 3 },
+  sourceActionTitle: { fontSize: 15, fontWeight: '900' },
+  sourceActionBody: { fontSize: 12, lineHeight: 17, fontWeight: '700' },
+  sideCard: { borderRadius: 24, borderWidth: 1, padding: 16, gap: 12 },
   sideHeaderRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12 },
   sectionTitle: { flex: 1, fontSize: 18, fontWeight: '900' },
   changePill: { flexDirection: 'row', alignItems: 'center', gap: 2 },
diff --git a/apps/mobile/src/features/trade/TradeSidePickerScreen.tsx b/apps/mobile/src/features/trade/TradeSidePickerScreen.tsx
index 52a8968..a3d2fcb 100644
--- a/apps/mobile/src/features/trade/TradeSidePickerScreen.tsx
+++ b/apps/mobile/src/features/trade/TradeSidePickerScreen.tsx
@@ -43,7 +43,7 @@ export function TradeSidePickerScreen({ route, navigation }: Props) {
   const auth = useAuth();
   const side = route.params.side;
   const existing = route.params.selection;
-  const [sourceMode, setSourceMode] = useState<SourceMode>(null);
+  const [sourceMode, setSourceMode] = useState<SourceMode>(route.params.initialSourceMode ?? null);
   const [query, setQuery] = useState('');
   const [needs, setNeeds] = useState<NeedItem[]>([]);
   const [offers, setOffers] = useState<OfferItem[]>([]);
diff --git a/apps/mobile/src/features/trade/create/WizardProgress.tsx b/apps/mobile/src/features/trade/create/WizardProgress.tsx
index 0182d29..5d483d4 100644
--- a/apps/mobile/src/features/trade/create/WizardProgress.tsx
+++ b/apps/mobile/src/features/trade/create/WizardProgress.tsx
@@ -1,6 +1,6 @@
 import React from 'react';
 import { StyleSheet, View, type StyleProp, type ViewStyle } from 'react-native';
-import { getWizardProgressLabel, getWizardProgressPercent, getWizardStepStates, type WizardStepDefinition } from '@hellowhen/shared';
+import { getWizardProgressLabel, getWizardProgressPercent, type WizardStepDefinition } from '@hellowhen/shared';
 import { AppText } from '../../../components/AppText';
 import { useThemeTokens } from '../../../providers/ThemeProvider';
 
@@ -15,51 +15,22 @@ type WizardProgressProps<TStepId extends string> = {
 
 export function WizardProgress<TStepId extends string>({ steps, activeStepId, label, stepLabel, ofLabel, style }: WizardProgressProps<TStepId>) {
   const theme = useThemeTokens();
-  const states = getWizardStepStates(steps, activeStepId);
   const progressPercent = getWizardProgressPercent(steps, activeStepId);
   const progressLabel = label ?? getWizardProgressLabel(steps, activeStepId, { step: stepLabel, of: ofLabel });
 
   return (
-    <View style={[styles.wrap, style]}>
-      <View style={styles.labelRow}>
-        <AppText style={[styles.progressLabel, { color: theme.color.muted }]}>{progressLabel}</AppText>
-        <AppText style={[styles.progressLabel, { color: theme.color.muted }]}>{progressPercent}%</AppText>
-      </View>
+    <View style={[styles.wrap, style]} accessibilityLabel={progressLabel}>
+      <AppText style={[styles.progressLabel, { color: theme.color.muted }]}>{progressLabel}</AppText>
       <View style={[styles.track, { backgroundColor: theme.color.subtleSurface }]}>
         <View style={[styles.fill, { width: `${progressPercent}%`, backgroundColor: theme.semantic.proposal.bg }]} />
       </View>
-      <View style={styles.stepsRow}>
-        {states.map((step) => {
-          const isCurrent = step.status === 'current';
-          const isComplete = step.status === 'complete';
-          const isBlocked = step.status === 'blocked';
-          const borderColor = isCurrent || isComplete ? theme.semantic.proposal.border : theme.color.border;
-          const backgroundColor = isCurrent ? theme.semantic.proposal.softBg : isComplete ? theme.semantic.success.softBg : theme.color.surface;
-          const textColor = isCurrent ? theme.semantic.proposal.text : isComplete ? theme.semantic.success.text : isBlocked ? theme.color.muted : theme.color.muted;
-
-          return (
-            <View key={step.id} style={styles.stepItem}>
-              <View style={[styles.stepDot, { backgroundColor, borderColor }]}>
-                <AppText style={[styles.stepNumber, { color: textColor }]}>{step.number}</AppText>
-              </View>
-              <AppText style={[styles.stepTitle, { color: textColor }]} numberOfLines={1}>{step.title}</AppText>
-            </View>
-          );
-        })}
-      </View>
     </View>
   );
 }
 
 const styles = StyleSheet.create({
-  wrap: { gap: 10 },
-  labelRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 10 },
+  wrap: { gap: 7 },
   progressLabel: { fontSize: 12, fontWeight: '900', letterSpacing: 0.45, textTransform: 'uppercase' },
-  track: { height: 7, overflow: 'hidden', borderRadius: 999 },
+  track: { height: 5, overflow: 'hidden', borderRadius: 999 },
   fill: { height: '100%', borderRadius: 999 },
-  stepsRow: { flexDirection: 'row', gap: 8 },
-  stepItem: { flex: 1, minWidth: 0, alignItems: 'center', gap: 5 },
-  stepDot: { alignItems: 'center', justifyContent: 'center', width: 28, height: 28, borderRadius: 14, borderWidth: 1 },
-  stepNumber: { fontSize: 12, fontWeight: '900' },
-  stepTitle: { maxWidth: '100%', fontSize: 10, fontWeight: '900', letterSpacing: 0.2 },
 });
diff --git a/apps/mobile/src/features/trade/create/WizardShell.tsx b/apps/mobile/src/features/trade/create/WizardShell.tsx
index 7a9d130..5ca849a 100644
--- a/apps/mobile/src/features/trade/create/WizardShell.tsx
+++ b/apps/mobile/src/features/trade/create/WizardShell.tsx
@@ -69,12 +69,12 @@ export function WizardShell<TStepId extends string>({
 }
 
 const styles = StyleSheet.create({
-  headerStack: { gap: 12 },
-  subtitle: { marginTop: -4, fontSize: 13, fontWeight: '700', lineHeight: 19 },
+  headerStack: { gap: 9 },
+  subtitle: { marginTop: -5, fontSize: 13, fontWeight: '700', lineHeight: 18 },
   body: { gap: 0 },
-  content: { gap: 16, paddingBottom: 18 },
+  content: { gap: 14, paddingBottom: 18 },
   stepIntro: { gap: 5 },
-  stepTitle: { fontSize: 24, lineHeight: 29, fontWeight: '900', letterSpacing: -0.45 },
-  stepDescription: { fontSize: 14, lineHeight: 21, fontWeight: '700' },
-  footer: { paddingTop: 10, paddingBottom: 12 },
+  stepTitle: { fontSize: 23, lineHeight: 28, fontWeight: '900', letterSpacing: -0.45 },
+  stepDescription: { fontSize: 14, lineHeight: 20, fontWeight: '700' },
+  footer: { paddingTop: 8, paddingBottom: 10 },
 });
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index df1980f..ad092e0 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -3986,3 +3986,38 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .inventory-wizard-review { grid-template-columns: 150px minmax(0, 1fr); }
   .inventory-wizard-review__media { min-height: auto; }
 }
+
+/* WIZARD7: compact create wizard polish */
+.wizard-shell { gap: 14px; }
+.wizard-shell__header { gap: 10px; border-radius: 24px; padding: 12px 14px; box-shadow: 0 14px 32px color-mix(in srgb, #000 6%, transparent); }
+.wizard-shell__title-copy h1 { font-size: 22px; letter-spacing: -0.045em; }
+.wizard-shell__body { gap: 14px; }
+.wizard-shell__step-intro { gap: 4px; }
+.wizard-shell__step-intro h2 { font-size: 24px; }
+.wizard-shell__step-intro p { font-size: 13px; line-height: 1.45; }
+.wizard-shell__footer-wrap { margin-bottom: 0; }
+.wizard-progress--compact { gap: 7px; }
+.wizard-progress__label-row { justify-content: flex-start; }
+.wizard-progress__track { height: 5px; }
+.wizard-progress__steps { display: none; }
+.wizard-footer { gap: 8px; border-radius: 24px 24px 0 0; padding: 12px; }
+.wizard-footer__button { min-height: 48px; border-radius: 16px; }
+.trade-create-wizard { gap: 12px; }
+.trade-create-status-row { margin-top: -2px; }
+.trade-post-type-step { gap: 10px; }
+.trade-post-type-card { border-radius: 21px; padding: 12px; }
+.trade-post-type-card strong { font-size: 17px; }
+.trade-side-source-step--inline { gap: 10px; }
+.trade-side-source-grid--inline { gap: 9px; }
+.trade-side-source-inline-copy { margin: 0; color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.4; }
+.trade-side-source-card--dashed { border-style: dashed; }
+.trade-side-source-card--button { appearance: none; width: 100%; text-align: left; font: inherit; cursor: pointer; }
+.trade-side-source-card--button > span { font-size: 18px; font-weight: 950; }
+.trade-side-source-card--warning { border-color: var(--warning-border); background: color-mix(in srgb, var(--warning-bg) 28%, var(--app-surface)); }
+@media (max-width: 520px) {
+  .wizard-shell__header { border-radius: 22px; padding: 11px 12px; }
+  .wizard-shell__step-intro h2 { font-size: 23px; }
+  .wizard-shell__footer-wrap { bottom: calc(var(--app-tab-height) + env(safe-area-inset-bottom)); padding-top: 8px; }
+}
+.trade-side-source-back:is(button) { border: 0; background: transparent; padding: 0; cursor: pointer; }
+.trade-side-source-card--button { color: var(--app-text); }
diff --git a/apps/web/src/features/trade/TradeCreateClient.tsx b/apps/web/src/features/trade/TradeCreateClient.tsx
index ee979f3..59adb3d 100644
--- a/apps/web/src/features/trade/TradeCreateClient.tsx
+++ b/apps/web/src/features/trade/TradeCreateClient.tsx
@@ -161,6 +161,15 @@ function createSideChooseHref(side: 'need' | 'offer', values: TradeCreateValues)
   return `/trades/create/choose-${side}${query ? `?${query}` : ''}`;
 }
 
+function createSideNewHref(side: 'need' | 'offer', values: TradeCreateValues) {
+  const params = new URLSearchParams();
+  if (values.postType) params.set('postType', values.postType);
+  if (values.needId && values.postType !== 'open_offer') params.set('needId', values.needId);
+  if (values.offerId && values.postType !== 'open_need') params.set('offerId', values.offerId);
+  const query = params.toString();
+  return `/trades/create/choose-${side}/new${query ? `?${query}` : ''}`;
+}
+
 
 function tradeDisplayLabels(t: Translator): Partial<GeneratedTradeDisplayLabels> {
   return {
@@ -617,6 +626,8 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
 
   const chooseNeedHref = createSideChooseHref('need', values);
   const chooseOfferHref = createSideChooseHref('offer', values);
+  const newNeedHref = createSideNewHref('need', values);
+  const newOfferHref = createSideNewHref('offer', values);
   const currentCreateHref = createTradeHrefWithPostType({ postType: values.postType, needId: values.needId, offerId: values.offerId });
 
   const shouldShowNeedStep = values.postType !== 'open_offer';
@@ -715,7 +726,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
 
   const footerPrimaryLabel = activeStepId === 'review'
     ? visibleDuplicateTrade ? t('trade.create.duplicateResolution') : saving ? t('trade.create.publishing') : values.postType === 'open_need' ? t('trade.create.publishOpenNeed') : values.postType === 'open_offer' ? t('trade.create.publishOpenOffer') : t('trade.create.publishTrade')
-    : t('common.actions.next');
+    : t('common.actions.continue');
   const footerPrimaryDisabled = saving || !auth.isAuthenticated || (activeStepId === 'review' && !canSubmit);
 
   function handlePrimaryAction() {
@@ -729,10 +740,8 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
   return (
     <WizardShell
       title={t('trade.create.title')}
-      subtitle={values.postType ? postTypeLabel(values.postType, t) : t('trade.create.chooseKindBody')}
       backHref="/trades"
       backLabel={t('common.actions.back')}
-      rightSlot={<Link href={createTradeFullHrefWithPostType({ postType: values.postType, needId: values.needId, offerId: values.offerId })} className="button secondary">{t('inventory.wizard.fullFormShort')}</Link>}
       steps={wizardSteps}
       activeStepId={activeStepId}
       stepLabel={t('inventory.wizard.stepLabel')}
@@ -746,8 +755,8 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
           primaryLoadingLabel={t('trade.create.publishing')}
           secondaryLabel={t('common.actions.back')}
           onSecondary={goBack}
-          tertiaryLabel={t('common.actions.cancel')}
-          tertiaryHref="/trades"
+          tertiaryLabel={t('inventory.wizard.openFullForm')}
+          tertiaryHref={createTradeFullHrefWithPostType({ postType: values.postType, needId: values.needId, offerId: values.offerId })}
           helperText={activeStepId === 'review' ? t('trade.create.previewReuseBody') : undefined}
         />
       )}
@@ -777,11 +786,6 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
 
         {activeStepId === 'type' ? (
           <section className="mobile-card trade-post-type-step">
-            <div className="trade-post-type-step__header">
-              <span className="semantic-badge instruction">{t('trade.create.stepOneOfTwo')}</span>
-              <h3>{t('trade.create.publishQuestion')}</h3>
-              <p>{t('trade.create.chooseKindBody')}</p>
-            </div>
             <div className="trade-post-type-grid">
               {TRADE_POST_TYPE_OPTIONS.map((option) => (
                 <button key={option.value} type="button" className={`trade-post-type-card trade-post-type-card--button${values.postType === option.value ? ' is-active' : ''}`} onClick={() => choosePostType(option.value)}>
@@ -805,6 +809,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
               items={selectableNeeds}
               selectedId={values.needId}
               chooseHref={chooseNeedHref}
+              newHref={newNeedHref}
               emptyTitle={t('inventory.empty.createFirstNeed')}
               emptyBody={t('inventory.empty.needBody')}
               moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
@@ -830,6 +835,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
               items={selectableOffers}
               selectedId={values.offerId}
               chooseHref={chooseOfferHref}
+              newHref={newOfferHref}
               emptyTitle={t('inventory.empty.createFirstOffer')}
               emptyBody={t('inventory.empty.offerBody')}
               moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
@@ -865,6 +871,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
                   items={selectableNeeds}
                   selectedId={values.needId}
                   chooseHref={chooseNeedHref}
+                  newHref={newNeedHref}
                   emptyTitle={t('inventory.empty.createFirstNeed')}
                   emptyBody={t('inventory.empty.needBody')}
                   moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
@@ -880,6 +887,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
                   items={selectableOffers}
                   selectedId={values.offerId}
                   chooseHref={chooseOfferHref}
+                  newHref={newOfferHref}
                   emptyTitle={t('inventory.empty.createFirstOffer')}
                   emptyBody={t('inventory.empty.offerBody')}
                   moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
diff --git a/apps/web/src/features/trade/TradeCreateFullClient.tsx b/apps/web/src/features/trade/TradeCreateFullClient.tsx
index 7b8501a..8967baf 100644
--- a/apps/web/src/features/trade/TradeCreateFullClient.tsx
+++ b/apps/web/src/features/trade/TradeCreateFullClient.tsx
@@ -137,6 +137,16 @@ function createSideChooseHref(side: 'need' | 'offer', values: TradeCreateValues)
   return `/trades/create/choose-${side}${fullQuery ? `?${fullQuery}` : ''}`;
 }
 
+function createSideNewHref(side: 'need' | 'offer', values: TradeCreateValues) {
+  const params = new URLSearchParams();
+  if (values.postType) params.set('postType', values.postType);
+  if (values.needId && values.postType !== 'open_offer') params.set('needId', values.needId);
+  if (values.offerId && values.postType !== 'open_need') params.set('offerId', values.offerId);
+  params.set('returnTo', 'full');
+  const fullQuery = params.toString();
+  return `/trades/create/choose-${side}/new${fullQuery ? `?${fullQuery}` : ''}`;
+}
+
 
 function tradeDisplayLabels(t: Translator): Partial<GeneratedTradeDisplayLabels> {
   return {
@@ -551,6 +561,8 @@ export function TradeCreateFullClient({ initialNeedId = '', initialOfferId = '',
 
   const chooseNeedHref = createSideChooseHref('need', values);
   const chooseOfferHref = createSideChooseHref('offer', values);
+  const newNeedHref = createSideNewHref('need', values);
+  const newOfferHref = createSideNewHref('offer', values);
   const showPostTypeStep = !values.postType;
   const changePostTypeHref = createTradeHrefWithPostType({});
   const currentCreateHref = createTradeHrefWithPostType({ postType: values.postType, needId: values.needId, offerId: values.offerId });
@@ -623,6 +635,7 @@ export function TradeCreateFullClient({ initialNeedId = '', initialOfferId = '',
                   items={selectableNeeds}
                   selectedId={values.needId}
                   chooseHref={chooseNeedHref}
+                  newHref={newNeedHref}
                   emptyTitle={t('inventory.empty.createFirstNeed')}
                   emptyBody={t('inventory.empty.needBody')}
                   moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
@@ -638,6 +651,7 @@ export function TradeCreateFullClient({ initialNeedId = '', initialOfferId = '',
                   items={selectableOffers}
                   selectedId={values.offerId}
                   chooseHref={chooseOfferHref}
+                  newHref={newOfferHref}
                   emptyTitle={t('inventory.empty.createFirstOffer')}
                   emptyBody={t('inventory.empty.offerBody')}
                   moneyEnabled={values.postType === 'need_offer' && betaFeatures.moneyTradesEnabled}
diff --git a/apps/web/src/features/trade/TradeSidePicker.tsx b/apps/web/src/features/trade/TradeSidePicker.tsx
index f3d8448..1523cd3 100644
--- a/apps/web/src/features/trade/TradeSidePicker.tsx
+++ b/apps/web/src/features/trade/TradeSidePicker.tsx
@@ -18,12 +18,17 @@ type TradeSidePickerProps = {
   items: Inventory[];
   selectedId: string;
   chooseHref: string;
+  newHref: string;
   emptyTitle: string;
   emptyBody: string;
   moneyEnabled?: boolean;
   cashPromiseEnabled?: boolean;
 };
 
+function sourceHref(href: string, source: 'mine' | 'starter') {
+  return `${href}${href.includes('?') ? '&' : '?'}source=${source}`;
+}
+
 function InventoryPreview({ item, side }: { item: Inventory; side: Side }) {
   const image = item.media?.[0] ?? null;
   return (
@@ -40,12 +45,34 @@ function InventoryPreview({ item, side }: { item: Inventory; side: Side }) {
   );
 }
 
-export function TradeSidePicker({ label, side, mode, onModeChange, items, selectedId, chooseHref, emptyTitle, emptyBody, moneyEnabled = false, cashPromiseEnabled = false }: TradeSidePickerProps) {
+function SourceCard({ href, icon, title, body, dashed = false }: { href: string; icon: 'need' | 'offer' | 'trade' | 'add'; title: string; body: string; dashed?: boolean }) {
+  return (
+    <Link href={href} className={`trade-side-source-card${dashed ? ' trade-side-source-card--dashed' : ''}`}>
+      <span><WebIcon name={icon} size={22} decorative /></span>
+      <strong>{title}</strong>
+      <small>{body}</small>
+    </Link>
+  );
+}
+
+function SourceButton({ onClick, iconLabel, title, body, warning = false }: { onClick: () => void; iconLabel: string; title: string; body: string; warning?: boolean }) {
+  return (
+    <button type="button" className={`trade-side-source-card trade-side-source-card--button${warning ? ' trade-side-source-card--warning' : ''}`} onClick={onClick}>
+      <span>{iconLabel}</span>
+      <strong>{title}</strong>
+      <small>{body}</small>
+    </button>
+  );
+}
+
+export function TradeSidePicker({ label, side, mode, onModeChange, items, selectedId, chooseHref, newHref, emptyTitle, emptyBody, moneyEnabled = false, cashPromiseEnabled = false }: TradeSidePickerProps) {
   const { t } = useWebTranslation();
   const sideClass = side === 'need' ? 'need' : 'offer';
   const moneyText = side === 'need' ? t('trade.labels.iNeed') : t('trade.labels.iOffer');
   const savedText = side === 'need' ? t('inventory.labels.savedNeed') : t('inventory.labels.savedOffer');
   const chooseText = side === 'need' ? t('trade.sidePicker.searchSaved', { items: t('inventory.labels.needs').toLowerCase() }) : t('trade.sidePicker.searchSaved', { items: t('inventory.labels.offers').toLowerCase() });
+  const pluralLabel = side === 'need' ? t('inventory.labels.needs').toLowerCase() : t('inventory.labels.offers').toLowerCase();
+  const itemLabel = side === 'need' ? t('inventory.labels.need') : t('inventory.labels.offer');
   const selected = items.find((item) => item.id === selectedId) ?? null;
 
   return (
@@ -53,42 +80,41 @@ export function TradeSidePicker({ label, side, mode, onModeChange, items, select
       <div className="trade-side-picker__header">
         <div>
           <p className="eyebrow">{label}</p>
-          <h3>{mode === 'money' ? moneyText : mode === 'cash_promise' ? t('trade.cashPromise.title') : selected ? selected.title : chooseText}</h3>
+          <h3>{mode === 'money' ? moneyText : mode === 'cash_promise' ? t('trade.cashPromise.title') : selected ? selected.title : t('trade.sidePicker.chooseSourceTitle', { item: itemLabel.toLowerCase() })}</h3>
         </div>
         <span className={`semantic-badge ${sideClass}`}><WebIcon name={side === 'need' ? 'need' : 'offer'} size={14} decorative /> {label}</span>
       </div>
 
-      {moneyEnabled || cashPromiseEnabled ? (
-        <div className="trade-side-mode-toggle" role="group" aria-label={`${label} ${t('trade.labels.type')}`}>
-          <button type="button" className={mode === 'saved' ? 'is-active' : ''} onClick={() => onModeChange('saved')}>{savedText}</button>
-          {moneyEnabled ? <button type="button" className={mode === 'money' ? 'is-active' : ''} onClick={() => onModeChange('money')}>{t('account.walletMoney')}</button> : null}
-          {cashPromiseEnabled ? <button type="button" className={mode === 'cash_promise' ? 'is-active' : ''} onClick={() => onModeChange('cash_promise')}>{t('trade.cashPromise.title')}</button> : null}
-        </div>
-      ) : null}
-
       {mode === 'money' && moneyEnabled ? (
         <div className="trade-side-money-state">
           <strong>{moneyText}</strong>
           <span>{t('account.wallet.optionalWalletBody')}</span>
+          <button type="button" className="trade-side-source-back" onClick={() => onModeChange('saved')}>{savedText}</button>
         </div>
       ) : mode === 'cash_promise' && cashPromiseEnabled ? (
         <div className="trade-side-money-state trade-side-money-state--cash-promise">
           <strong>{t('trade.cashPromise.title')}</strong>
           <span>{t('trade.cashPromise.notProcessed')}</span>
+          <button type="button" className="trade-side-source-back" onClick={() => onModeChange('saved')}>{savedText}</button>
         </div>
       ) : selected ? (
         <div className="trade-side-choice-state">
-          <Link href={chooseHref} className="trade-side-choice-card" aria-label={t('trade.sidePicker.changeSource')}>
+          <Link href={sourceHref(chooseHref, 'mine')} className="trade-side-choice-card" aria-label={t('trade.sidePicker.changeSource')}>
             <InventoryPreview item={selected} side={side} />
           </Link>
-          <Link href={chooseHref} className="button secondary trade-side-change-button">{t('common.actions.edit')}</Link>
+          <Link href={sourceHref(chooseHref, 'mine')} className="button secondary trade-side-change-button">{t('common.actions.edit')}</Link>
         </div>
       ) : (
-        <Link href={chooseHref} className="trade-side-placeholder">
-          <span><WebIcon name={side === 'need' ? 'need' : 'offer'} size={22} decorative /></span>
-          <strong>{chooseText}</strong>
-          <small>{items.length ? t('inventory.messages.visibleItems', { count: items.length, items: side === 'need' ? t('inventory.labels.needs').toLowerCase() : t('inventory.labels.offers').toLowerCase() }) : emptyTitle || emptyBody}</small>
-        </Link>
+        <div className="trade-side-source-step trade-side-source-step--inline">
+          <p className="trade-side-source-inline-copy">{items.length ? t('inventory.messages.visibleItems', { count: items.length, items: pluralLabel }) : emptyTitle || emptyBody}</p>
+          <div className="trade-side-source-grid trade-side-source-grid--inline">
+            <SourceCard href={sourceHref(chooseHref, 'mine')} icon={side === 'need' ? 'need' : 'offer'} title={t('trade.sidePicker.useMine')} body={t('trade.sidePicker.useMineBody', { items: pluralLabel })} />
+            <SourceCard href={sourceHref(chooseHref, 'starter')} icon="trade" title={t('trade.sidePicker.useStarter')} body={t('trade.sidePicker.useStarterBody')} />
+            <SourceCard href={newHref} icon="add" title={t('trade.sidePicker.createNew', { item: itemLabel })} body={t('trade.sidePicker.createNewBody')} dashed />
+            {moneyEnabled ? <SourceButton iconLabel="€" title={t('account.walletMoney')} body={t('account.wallet.optionalWalletBody')} onClick={() => onModeChange('money')} /> : null}
+            {cashPromiseEnabled ? <SourceButton iconLabel="€" title={t('trade.cashPromise.title')} body={t('trade.cashPromise.notProcessed')} warning onClick={() => onModeChange('cash_promise')} /> : null}
+          </div>
+        </div>
       )}
     </section>
   );
diff --git a/apps/web/src/features/wizard/WizardProgress.tsx b/apps/web/src/features/wizard/WizardProgress.tsx
index 89c6ed7..7e43b96 100644
--- a/apps/web/src/features/wizard/WizardProgress.tsx
+++ b/apps/web/src/features/wizard/WizardProgress.tsx
@@ -1,6 +1,6 @@
 'use client';
 
-import { getWizardProgressLabel, getWizardProgressPercent, getWizardStepStates, type WizardStepDefinition } from '@hellowhen/shared';
+import { getWizardProgressLabel, getWizardProgressPercent, type WizardStepDefinition } from '@hellowhen/shared';
 
 type WizardProgressProps<TStepId extends string> = {
   steps: readonly WizardStepDefinition<TStepId>[];
@@ -11,27 +11,17 @@ type WizardProgressProps<TStepId extends string> = {
 };
 
 export function WizardProgress<TStepId extends string>({ steps, activeStepId, label, stepLabel, ofLabel }: WizardProgressProps<TStepId>) {
-  const states = getWizardStepStates(steps, activeStepId);
   const progressPercent = getWizardProgressPercent(steps, activeStepId);
   const progressLabel = label ?? getWizardProgressLabel(steps, activeStepId, { step: stepLabel, of: ofLabel });
 
   return (
-    <section className="wizard-progress" aria-label={progressLabel}>
+    <section className="wizard-progress wizard-progress--compact" aria-label={progressLabel}>
       <div className="wizard-progress__label-row">
         <span>{progressLabel}</span>
-        <span>{progressPercent}%</span>
       </div>
       <div className="wizard-progress__track" aria-hidden="true">
         <span className="wizard-progress__fill" style={{ width: `${progressPercent}%` }} />
       </div>
-      <ol className="wizard-progress__steps">
-        {states.map((step) => (
-          <li key={step.id} className={`wizard-progress__step wizard-progress__step--${step.status}`}>
-            <span className="wizard-progress__dot">{step.number}</span>
-            <span className="wizard-progress__step-title">{step.title}</span>
-          </li>
-        ))}
-      </ol>
     </section>
   );
 }
