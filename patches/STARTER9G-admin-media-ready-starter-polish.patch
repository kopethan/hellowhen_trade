diff --git a/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx b/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
index 656fe4a..5fd3d44 100644
--- a/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
+++ b/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
@@ -414,7 +414,13 @@ function TradeFeedIdeaCard({ ideaKey, onOpenIdea, inline = false }: { ideaKey: F
   const actionLabel = t(getFeedIdeaActionKey(ideaKey));
   const summary = getFeedIdeaAriaSummary(t, ideaKey);
   const media = getFeedTradeIdeaMedia(ideaKey);
+  const [imageFailed, setImageFailed] = useState(false);
+
+  useEffect(() => { setImageFailed(false); }, [ideaKey, media.imageUrl]);
+
   const isSingleSideIdea = idea.type !== 'trade';
+  const showMediaImage = Boolean(media.imageUrl && !imageFailed);
+  const mediaImageUrl = showMediaImage ? media.imageUrl : null;
   const visualPalette = feedIdeaVisualPalette[media.fallbackVisualKey];
 
   return (
@@ -431,9 +437,9 @@ function TradeFeedIdeaCard({ ideaKey, onOpenIdea, inline = false }: { ideaKey: F
 
       {isSingleSideIdea ? (
         <View pointerEvents="none" style={[styles.feedIdeaSingleVisual, { backgroundColor: visualPalette.background }]}>
-          {media.imageUrl ? (
+          {mediaImageUrl ? (
             <>
-              <Image source={{ uri: media.imageUrl }} resizeMode="cover" style={styles.feedIdeaSingleVisualImage} />
+              <Image source={{ uri: mediaImageUrl }} resizeMode="cover" style={styles.feedIdeaSingleVisualImage} onError={() => setImageFailed(true)} />
               <View style={styles.feedIdeaSingleVisualImageOverlay} />
             </>
           ) : (
diff --git a/apps/mobile/src/features/trade/tradeFeedIdeas.ts b/apps/mobile/src/features/trade/tradeFeedIdeas.ts
index defdd22..b04a468 100644
--- a/apps/mobile/src/features/trade/tradeFeedIdeas.ts
+++ b/apps/mobile/src/features/trade/tradeFeedIdeas.ts
@@ -31,10 +31,13 @@ export type FeedTradeIdeaKey = (typeof feedTradeIdeaKeys)[number];
 export type FeedTradeIdeaType = 'trade' | 'open_need' | 'open_offer';
 export type FeedTradeIdeaPostType = 'need_offer' | 'open_need' | 'open_offer';
 export type FeedTradeIdeaVisualKey = 'startup' | 'language' | 'local' | 'objects' | 'creative' | 'feedback' | 'social' | 'admin' | 'video' | 'remote';
+export type FeedTradeIdeaImageFocus = 'center' | 'top' | 'bottom' | 'left' | 'right';
 
 export type FeedTradeIdeaMedia = {
   /** Optional future media URL for admin-curated starter decks. Empty means use the deterministic fallback visual. */
   imageUrl?: string;
+  /** Crop anchor for future admin-curated images. Web uses it as object-position; mobile keeps center-cover for now. */
+  imageFocus?: FeedTradeIdeaImageFocus;
   fallbackVisualKey: FeedTradeIdeaVisualKey;
 };
 
@@ -200,10 +203,22 @@ export const feedTradeIdeas: Record<FeedTradeIdeaKey, FeedTradeIdeaTemplatePair>
 };
 
 
+export const feedTradeIdeaImageFocusPositions: Record<FeedTradeIdeaImageFocus, string> = {
+  center: 'center center',
+  top: 'center top',
+  bottom: 'center bottom',
+  left: 'left center',
+  right: 'right center',
+};
+
 export function getFeedTradeIdeaMedia(ideaKey: FeedTradeIdeaKey): FeedTradeIdeaMedia {
   return feedTradeIdeas[ideaKey].media;
 }
 
+export function getFeedTradeIdeaImageObjectPosition(media: FeedTradeIdeaMedia) {
+  return feedTradeIdeaImageFocusPositions[media.imageFocus ?? 'center'];
+}
+
 export function getFeedTradeIdeaPostType(idea: FeedTradeIdeaTemplatePair): FeedTradeIdeaPostType {
   return idea.type === 'trade' ? 'need_offer' : idea.type;
 }
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index c70a052..9c30a55 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -8502,6 +8502,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
   --starter-visual-b: var(--app-border);
   --starter-visual-c: var(--app-surface-strong);
   position: relative;
+  isolation: isolate;
   display: grid;
   place-items: center;
   min-height: 72px;
@@ -8513,6 +8514,10 @@ html[data-theme="dark"] .onboarding-guide-primary {
   box-shadow: inset 0 1px 0 color-mix(in srgb, #ffffff 44%, transparent);
 }
 
+.trade-feed-idea-card__single-visual--has-image {
+  background: color-mix(in srgb, var(--starter-visual-a) 12%, var(--app-surface-soft));
+}
+
 .trade-feed-idea-card__single-visual--startup { --starter-visual-a: #6366f1; --starter-visual-b: #dbeafe; --starter-visual-c: #fce7f3; }
 .trade-feed-idea-card__single-visual--language { --starter-visual-a: #14b8a6; --starter-visual-b: #ccfbf1; --starter-visual-c: #e0f2fe; }
 .trade-feed-idea-card__single-visual--local { --starter-visual-a: #f59e0b; --starter-visual-b: #fef3c7; --starter-visual-c: #dcfce7; }
@@ -8529,14 +8534,17 @@ html[data-theme="dark"] .onboarding-guide-primary {
   inset: 0;
   width: 100%;
   height: 100%;
+  z-index: 0;
   object-fit: cover;
+  transform: scale(1.012);
 }
 
 .trade-feed-idea-card__single-visual--has-image::after {
   content: "";
   position: absolute;
   inset: 0;
-  background: linear-gradient(180deg, transparent, color-mix(in srgb, #000000 16%, transparent));
+  z-index: 1;
+  background: linear-gradient(180deg, color-mix(in srgb, #ffffff 6%, transparent), color-mix(in srgb, #000000 16%, transparent));
 }
 
 .trade-feed-idea-card__single-visual span,
@@ -8547,6 +8555,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
   display: block;
   border-radius: 999px;
   background: color-mix(in srgb, #ffffff 64%, transparent);
+  z-index: 1;
   transform: rotate(-17deg);
 }
 
@@ -8555,6 +8564,16 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .trade-feed-idea-card__single-visual b { width: 44px; height: 44px; top: 32%; left: calc(50% - 22px); background: color-mix(in srgb, var(--starter-visual-a) 86%, #ffffff); opacity: .68; transform: none; }
 .trade-feed-idea-card__single-visual em { width: 34%; height: 8px; right: 8%; bottom: 18%; opacity: .38; }
 
+@media (prefers-reduced-motion: no-preference) {
+  .trade-feed-idea-card__single-visual img {
+    transition: transform 180ms ease;
+  }
+
+  .trade-feed-idea-card:hover .trade-feed-idea-card__single-visual img {
+    transform: scale(1.045);
+  }
+}
+
 .trade-feed-idea-card__divider {
   display: grid;
   grid-template-columns: minmax(64px, 1fr) auto minmax(64px, 1fr);
diff --git a/apps/web/src/features/trade/TradeFeedClient.tsx b/apps/web/src/features/trade/TradeFeedClient.tsx
index 4289adb..b2fda4a 100644
--- a/apps/web/src/features/trade/TradeFeedClient.tsx
+++ b/apps/web/src/features/trade/TradeFeedClient.tsx
@@ -14,7 +14,7 @@ import { useWebAuth } from '../../providers/WebAuthProvider';
 import { useWebTranslation } from '../../providers/WebI18nProvider';
 import { formatWebShortDate } from '../../lib/webFormat';
 import { TradeDeckGrid } from './TradeDeckGrid';
-import { createFeedIdeaTradeHref, emptyFeedStarterIdeaPlacement, feedTradeIdeaHasNeed, feedTradeIdeaHasOffer, feedTradeIdeas, getFeedStarterIdeaPlacement, getFeedTradeIdeaMedia, getInlineFeedIdeaKey, getRandomizedFeedIdeaKeys, type FeedTradeIdeaKey } from './tradeFeedIdeas';
+import { createFeedIdeaTradeHref, emptyFeedStarterIdeaPlacement, feedTradeIdeaHasNeed, feedTradeIdeaHasOffer, feedTradeIdeas, getFeedStarterIdeaPlacement, getFeedTradeIdeaImageObjectPosition, getFeedTradeIdeaMedia, getInlineFeedIdeaKey, getRandomizedFeedIdeaKeys, type FeedTradeIdeaKey } from './tradeFeedIdeas';
 import { getExchangeLabel, getStatusLabel, getTradeHeadline } from './tradePresentation';
 
 type FeedFilters = {
@@ -507,12 +507,17 @@ function TradeFeedIdeaCard({ ideaKey, createIdeaHref, inline = false }: { ideaKe
   const actionLabel = t(getFeedIdeaActionKey(ideaKey));
   const summary = getFeedIdeaAriaSummary(t, ideaKey);
   const media = getFeedTradeIdeaMedia(ideaKey);
+  const [imageFailed, setImageFailed] = useState(false);
+
+  useEffect(() => { setImageFailed(false); }, [ideaKey, media.imageUrl]);
 
   const isSingleSideIdea = idea.type !== 'trade';
+  const showMediaImage = Boolean(media.imageUrl && !imageFailed);
+  const mediaImageUrl = showMediaImage ? media.imageUrl : null;
   const visualClassName = [
     'trade-feed-idea-card__single-visual',
     `trade-feed-idea-card__single-visual--${media.fallbackVisualKey}`,
-    media.imageUrl ? 'trade-feed-idea-card__single-visual--has-image' : '',
+    showMediaImage ? 'trade-feed-idea-card__single-visual--has-image' : 'trade-feed-idea-card__single-visual--fallback',
   ].filter(Boolean).join(' ');
 
   return (
@@ -525,8 +530,8 @@ function TradeFeedIdeaCard({ ideaKey, createIdeaHref, inline = false }: { ideaKe
 
         {isSingleSideIdea ? (
           <div className={visualClassName} aria-hidden="true">
-            {media.imageUrl ? (
-              <img src={media.imageUrl} alt="" loading="lazy" decoding="async" />
+            {mediaImageUrl ? (
+              <img src={mediaImageUrl} alt="" loading="lazy" decoding="async" style={{ objectPosition: getFeedTradeIdeaImageObjectPosition(media) }} onError={() => setImageFailed(true)} />
             ) : (
               <>
                 <span />
diff --git a/apps/web/src/features/trade/tradeFeedIdeas.ts b/apps/web/src/features/trade/tradeFeedIdeas.ts
index defdd22..b04a468 100644
--- a/apps/web/src/features/trade/tradeFeedIdeas.ts
+++ b/apps/web/src/features/trade/tradeFeedIdeas.ts
@@ -31,10 +31,13 @@ export type FeedTradeIdeaKey = (typeof feedTradeIdeaKeys)[number];
 export type FeedTradeIdeaType = 'trade' | 'open_need' | 'open_offer';
 export type FeedTradeIdeaPostType = 'need_offer' | 'open_need' | 'open_offer';
 export type FeedTradeIdeaVisualKey = 'startup' | 'language' | 'local' | 'objects' | 'creative' | 'feedback' | 'social' | 'admin' | 'video' | 'remote';
+export type FeedTradeIdeaImageFocus = 'center' | 'top' | 'bottom' | 'left' | 'right';
 
 export type FeedTradeIdeaMedia = {
   /** Optional future media URL for admin-curated starter decks. Empty means use the deterministic fallback visual. */
   imageUrl?: string;
+  /** Crop anchor for future admin-curated images. Web uses it as object-position; mobile keeps center-cover for now. */
+  imageFocus?: FeedTradeIdeaImageFocus;
   fallbackVisualKey: FeedTradeIdeaVisualKey;
 };
 
@@ -200,10 +203,22 @@ export const feedTradeIdeas: Record<FeedTradeIdeaKey, FeedTradeIdeaTemplatePair>
 };
 
 
+export const feedTradeIdeaImageFocusPositions: Record<FeedTradeIdeaImageFocus, string> = {
+  center: 'center center',
+  top: 'center top',
+  bottom: 'center bottom',
+  left: 'left center',
+  right: 'right center',
+};
+
 export function getFeedTradeIdeaMedia(ideaKey: FeedTradeIdeaKey): FeedTradeIdeaMedia {
   return feedTradeIdeas[ideaKey].media;
 }
 
+export function getFeedTradeIdeaImageObjectPosition(media: FeedTradeIdeaMedia) {
+  return feedTradeIdeaImageFocusPositions[media.imageFocus ?? 'center'];
+}
+
 export function getFeedTradeIdeaPostType(idea: FeedTradeIdeaTemplatePair): FeedTradeIdeaPostType {
   return idea.type === 'trade' ? 'need_offer' : idea.type;
 }
diff --git a/docs/starter-idea-media.md b/docs/starter-idea-media.md
new file mode 100644
index 0000000..b6c9a12
--- /dev/null
+++ b/docs/starter-idea-media.md
@@ -0,0 +1,32 @@
+# Starter idea media guidelines
+
+Starter propositions are templates, not real trades. Media should make the cards feel polished without implying a real user, a bot post, or a messageable trade.
+
+## Supported metadata
+
+Starter idea entries can include media metadata in `tradeFeedIdeas.ts`:
+
+```ts
+media: {
+  imageUrl?: string;
+  imageFocus?: 'center' | 'top' | 'bottom' | 'left' | 'right';
+  fallbackVisualKey: 'startup' | 'language' | 'local' | 'objects' | 'creative' | 'feedback' | 'social' | 'admin' | 'video' | 'remote';
+}
+```
+
+Use `imageUrl` only for approved static/admin-curated images. When the image is missing or fails to load, the feed falls back to the deterministic visual selected by `fallbackVisualKey`.
+
+## Recommended image specs
+
+- Use simple editorial/object/access imagery, not fake user portraits.
+- Recommended source size: at least `1200 × 800`.
+- Safe crop area: keep the main subject near the center.
+- Use `imageFocus` only when the important part is clearly near an edge.
+- Avoid screenshots containing private text, phone numbers, emails, addresses, documents, or payment details.
+
+## Product rules
+
+- Do not use media to suggest that a starter idea is already a real trade.
+- Do not show fake users or bot-created activity.
+- Do not add conversation, proposal, or countdown imagery to starter ideas.
+- Keep the card copy clear: users are creating their own version before publishing.
