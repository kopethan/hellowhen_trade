diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index fba1c0d..85cc543 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -1531,8 +1531,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
               <View style={[styles.sourceSheet, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
                 <View style={styles.wizardStepRow}>
                   <View style={styles.timelineCopy}>
-                    <AppText style={styles.sectionTitle}>Place {detailPlaceIndex + 1} details</AppText>
-                    <AppText style={[styles.metaText, { color: theme.color.muted }]}>Edit this stop without overloading the main Plan screen.</AppText>
+                    <AppText style={styles.sectionTitle}>Place {detailPlaceIndex + 1}</AppText>
                   </View>
                   <Pressable accessibilityRole="button" accessibilityLabel="Close" onPress={() => setDetailPlaceIndex(null)} style={[styles.headerAction, { borderColor: theme.color.border }]}>
                     <MobileIcon name="close" color={theme.color.text} size={18} />
@@ -1540,16 +1539,16 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                 </View>
                 <ScrollView keyboardShouldPersistTaps="handled" style={styles.sourceListScroll} contentContainerStyle={styles.placeDetailSheetContent}>
                   <View style={styles.rowTop}>
-                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'} tone="place" size="sm" /> : <SemanticBadge label="Custom one-off stop" tone="place" size="sm" />}
+                    {detailPlace.sourcePlaceId ? <SemanticBadge label={detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'} tone="place" size="sm" /> : <SemanticBadge label="Custom" tone="place" size="sm" />}
                   </View>
                   {detailPlace.sourcePlaceId ? (
                     <View style={[styles.snapshotStrip, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}>
                       <AppText style={[styles.snapshotText, { color: theme.semantic.place.text }]} numberOfLines={3}>
-                        {detailPlace.sourcePlaceSource === 'my_place' ? 'Edit the saved My Place if this content should change everywhere you reuse it.' : 'Library Places are copied before editing so your changes stay private.'}
+                        {detailPlace.sourcePlaceSource === 'my_place' ? 'Updates your saved Place.' : 'Copied into My Places before editing.'}
                       </AppText>
-                      {detailPlace.sourcePlaceSource === 'my_place' ? <SecondaryButton label="Edit saved Place" onPress={() => editMyPlaceFromDetail(detailPlaceIndex)} /> : null}
-                      {detailPlace.sourcePlaceSource === 'hellowhen_library' ? <SecondaryButton label="Copy and edit" onPress={() => copyLibraryPlaceFromDetail(detailPlaceIndex)} /> : null}
-                      <SecondaryButton label="Use as custom" onPress={() => updateSelectedPlace(detailPlaceIndex, resetSelectedPlaceToCustom(detailPlace))} />
+                      {detailPlace.sourcePlaceSource === 'my_place' ? <SecondaryButton label="Edit saved" onPress={() => editMyPlaceFromDetail(detailPlaceIndex)} /> : null}
+                      {detailPlace.sourcePlaceSource === 'hellowhen_library' ? <SecondaryButton label="Copy to edit" onPress={() => copyLibraryPlaceFromDetail(detailPlaceIndex)} /> : null}
+                      <SecondaryButton label="Make custom" onPress={() => updateSelectedPlace(detailPlaceIndex, resetSelectedPlaceToCustom(detailPlace))} />
                     </View>
                   ) : (
                     <>
@@ -1585,8 +1584,7 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
             <View style={[styles.sourceSheet, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
               <View style={styles.wizardStepRow}>
                 <View style={styles.timelineCopy}>
-                  <AppText style={styles.sectionTitle}>{placePickerOpen ? (pickerTab === 'mine' ? 'Choose from My Places' : 'Choose from Hellowhen Library') : 'Choose place source'}</AppText>
-                  <AppText style={[styles.metaText, { color: theme.color.muted }]}>Add one Place without overloading the Plan screen.</AppText>
+                  <AppText style={styles.sectionTitle}>{placePickerOpen ? (pickerTab === 'mine' ? 'My Places' : 'Hellowhen Library') : 'Add place'}</AppText>
                 </View>
                 <Pressable accessibilityRole="button" accessibilityLabel="Close" onPress={closePlaceSourceSheet} style={[styles.headerAction, { borderColor: theme.color.border }]}>
                   <MobileIcon name="close" color={theme.color.text} size={18} />
@@ -1597,22 +1595,22 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                 <View style={styles.sourceOptionList}>
                   <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('mine')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }]}><MobileIcon name="save" color={theme.semantic.place.text} size={19} /></View>
-                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Use one of mine</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Choose from your private saved Places.</AppText></View>
+                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>My Places</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Saved places you created.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
                   <Pressable accessibilityRole="button" onPress={() => choosePlaceSource('library')} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="search" color={theme.semantic.place.text} size={19} /></View>
-                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Use Hellowhen Library</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Start from reusable Hellowhen Places.</AppText></View>
+                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Hellowhen Library</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Reusable place templates.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
                   <Pressable accessibilityRole="button" onPress={() => { closePlaceSourceSheet(); navigation.navigate('CreatePlace', { returnToCreatePlan: true, targetPlaceIndex: typeof placeSourceTarget === 'number' ? placeSourceTarget : undefined }); }} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="add" color={theme.semantic.place.text} size={19} /></View>
-                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Create a new Place</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Save it, then return with it selected.</AppText></View>
+                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>New Place</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Save and return here.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
                   <Pressable accessibilityRole="button" onPress={addCustomPlace} style={({ pressed }) => [styles.sourceOption, { backgroundColor: theme.color.surface, borderColor: theme.semantic.place.border }, pressed && styles.pressed]}>
                     <View style={[styles.sourceOptionIcon, { backgroundColor: theme.semantic.place.softBg, borderColor: theme.semantic.place.border }]}><MobileIcon name="more" color={theme.semantic.place.text} size={19} /></View>
-                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Custom one-off stop</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Use only inside this Plan.</AppText></View>
+                    <View style={styles.timelineCopy}><AppText style={styles.sourceOptionTitle}>Custom stop</AppText><AppText style={[styles.metaText, { color: theme.color.muted }]}>Only for this Plan.</AppText></View>
                     <MobileIcon name="chevron-right" color={theme.color.muted} size={18} />
                   </Pressable>
                 </View>
@@ -1623,13 +1621,13 @@ export function CreatePlanScreen({ navigation, route }: SimpleScreenProps<'Creat
                     <PillButton label="Hellowhen Library" active={pickerTab === 'library'} onPress={() => choosePlaceSource('library')} />
                     <PillButton label="Refresh" onPress={() => { void loadReusablePlaces(); }} />
                   </View>
-                  <TextField label="Search Places" value={placeQuery} onChangeText={setPlaceQuery} placeholder="Search title, category, area..." />
+                  <TextField label="Search Places" value={placeQuery} onChangeText={setPlaceQuery} placeholder="Search Places" />
                   {loadingPlaces ? <View style={styles.inlineSmallLoading}><ActivityIndicator /><AppText style={[styles.loadingText, { color: theme.color.muted }]}>Loading Places...</AppText></View> : null}
-                  {!loadingPlaces && activeList.length === 0 ? <EmptyBlock title="No matching Places" body={pickerTab === 'mine' ? 'Create a My Place from the source menu or use a custom stop.' : 'No matching Hellowhen Library Places yet.'} /> : null}
+                  {!loadingPlaces && activeList.length === 0 ? <EmptyBlock title="No matching Places" body={pickerTab === 'mine' ? 'Create a Place or use a custom stop.' : 'No matching Library Places yet.'} /> : null}
                   <ScrollView style={styles.sourceListScroll} keyboardShouldPersistTaps="handled">
                     {activeList.map((place) => <PlaceChoiceCard key={place.id} place={place} onAdd={() => addReusablePlace(place)} />)}
                   </ScrollView>
-                  <SecondaryButton label="Back to sources" onPress={() => setPlacePickerOpen(false)} />
+                  <SecondaryButton label="Sources" onPress={() => setPlacePickerOpen(false)} />
                 </View>
               )}
             </View>
@@ -1785,16 +1783,16 @@ const styles = StyleSheet.create({
   placeTimeLineItem: { paddingBottom: 12 },
   planTimelinePlaceAction: { paddingVertical: 18 },
   placeTimelineRow: { borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 16, flexDirection: 'row', alignItems: 'center', gap: 12 },
-  placeDetailSheetContent: { gap: 12, paddingBottom: 12 },
-  placePickerPanel: { borderRadius: 24, borderWidth: 1, padding: 14, gap: 12 },
+  placeDetailSheetContent: { gap: 10, paddingBottom: 12 },
+  placePickerPanel: { gap: 10 },
   sourceSheetOverlay: { flex: 1, justifyContent: 'flex-end' },
   sourceSheetScrim: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(0,0,0,0.38)' },
-  sourceSheet: { maxHeight: '86%', borderTopLeftRadius: 30, borderTopRightRadius: 30, borderWidth: 1, paddingHorizontal: 16, paddingTop: 16, paddingBottom: 26, gap: 14 },
-  sourceOptionList: { gap: 10 },
-  sourceOption: { minHeight: 74, borderRadius: 20, borderWidth: 1, padding: 12, flexDirection: 'row', alignItems: 'center', gap: 12 },
-  sourceOptionIcon: { width: 38, height: 38, borderRadius: 19, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
-  sourceOptionTitle: { fontSize: 16, fontWeight: '900' },
-  sourceListScroll: { maxHeight: 360 },
+  sourceSheet: { maxHeight: '86%', borderTopLeftRadius: 26, borderTopRightRadius: 26, borderWidth: 1, paddingHorizontal: 16, paddingTop: 14, paddingBottom: 24, gap: 10 },
+  sourceOptionList: { gap: 0 },
+  sourceOption: { minHeight: 62, borderRadius: 0, borderWidth: 0, borderTopWidth: StyleSheet.hairlineWidth, paddingHorizontal: 0, paddingVertical: 11, flexDirection: 'row', alignItems: 'center', gap: 12 },
+  sourceOptionIcon: { width: 34, height: 34, borderRadius: 17, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
+  sourceOptionTitle: { fontSize: 15, fontWeight: '900' },
+  sourceListScroll: { maxHeight: 340 },
   deckFeedContent: { gap: 20, paddingTop: 2, paddingBottom: 34 },
   deckSection: { gap: 10 },
   deckSectionHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12, paddingHorizontal: 2 },
diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 56bfff5..c8e26a1 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -2785,7 +2785,8 @@ html[data-theme="dark"] .inventory-empty-state__plus {
 .plan-feed-card__glass .eyebrow { color: white; font-size: 11px; font-weight: 950; letter-spacing: 0.08em; text-transform: uppercase; }
 .plan-feed-card__glass .semantic-badge { color: white; background: rgba(15, 23, 42, 0.48); border-color: rgba(255, 255, 255, 0.18); }
 .plan-detail-page { gap: 14px; }
-.plan-detail-topbar { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
+.plan-detail-topbar { display: flex; align-items: center; justify-content: space-between; gap: 10px; padding-bottom: 8px; border-bottom: 1px solid var(--app-divider); }
+.plan-detail-topbar h3 { margin: 0; font-size: 20px; letter-spacing: -0.02em; }
 .plan-detail-hero--polished { gap: 14px; overflow: hidden; }
 .plan-detail-hero__copy { display: grid; gap: 8px; }
 .plan-detail-hero__copy h1 { margin: 0; font-size: clamp(31px, 7vw, 54px); line-height: 0.95; letter-spacing: -0.075em; }
@@ -9059,17 +9060,17 @@ html[data-theme="dark"] .onboarding-guide-primary {
 
 /* PLAN-PROD6 — Web Create Plan reusable Place picker */
 .plan-create-intro p { max-width: 620px; }
-.plan-source-place-strip { display: grid; grid-template-columns: auto minmax(0, 1fr) auto; align-items: center; gap: 8px; border: 1px solid var(--app-border); border-radius: 18px; background: var(--app-surface); padding: 8px; }
+.plan-source-place-strip { display: grid; grid-template-columns: minmax(0, 1fr); align-items: start; gap: 8px; border-top: 1px solid var(--app-divider); border-bottom: 1px solid var(--app-divider); background: transparent; padding: 12px 0; }
 .plan-source-place-strip p { margin: 0; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--app-text); font-size: 13px; font-weight: 900; }
 .plan-source-place-strip .button { min-height: 34px; padding: 0 11px; }
-.plan-place-picker-panel { display: grid; gap: 12px; }
+.plan-place-picker-panel { display: grid; gap: 10px; }
 .plan-place-picker-panel label, .plan-create-place-box label { display: grid; gap: 7px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; }
 .plan-place-picker-panel input, .plan-place-picker-panel textarea { width: 100%; border: 1px solid var(--app-border-strong); border-radius: 18px; background: var(--app-surface-soft); color: var(--app-text); padding: 12px 13px; font: inherit; font-weight: 750; outline: none; }
 .plan-place-picker-panel textarea { min-height: 94px; resize: vertical; line-height: 1.35; }
-.plan-place-picker-list { display: grid; gap: 8px; max-height: 420px; overflow: auto; padding-right: 2px; }
-.plan-place-picker-card { display: grid; grid-template-columns: 58px minmax(0, 1fr) auto; align-items: center; gap: 10px; width: 100%; border: 1px solid color-mix(in srgb, var(--place-border) 38%, var(--app-border)); border-radius: 20px; background: color-mix(in srgb, var(--place-bg) 22%, var(--app-surface)); color: inherit; padding: 8px; text-align: left; cursor: pointer; }
-.plan-place-picker-card:hover { border-color: var(--place-border); background: color-mix(in srgb, var(--place-bg) 34%, var(--app-surface)); }
-.plan-place-picker-card__media { display: grid; place-items: center; overflow: hidden; width: 58px; height: 58px; border-radius: 16px; background: var(--place-bg); color: var(--place-text); font-size: 22px; font-weight: 950; }
+.plan-place-picker-list { display: grid; gap: 0; max-height: 420px; overflow: auto; padding-right: 2px; border-top: 1px solid var(--app-divider); }
+.plan-place-picker-card { display: grid; grid-template-columns: 48px minmax(0, 1fr) auto; align-items: center; gap: 10px; width: 100%; border: 0; border-bottom: 1px solid var(--app-divider); border-radius: 0; background: transparent; color: inherit; padding: 10px 0; text-align: left; cursor: pointer; }
+.plan-place-picker-card:hover { background: color-mix(in srgb, var(--place-bg) 20%, transparent); }
+.plan-place-picker-card__media { display: grid; place-items: center; overflow: hidden; width: 48px; height: 48px; border-radius: 16px; background: var(--place-bg); color: var(--place-text); font-size: 20px; font-weight: 950; }
 .plan-place-picker-card__media img { width: 100%; height: 100%; object-fit: cover; }
 .plan-place-picker-card__body { display: grid; gap: 3px; min-width: 0; }
 .plan-place-picker-card__body strong, .plan-place-picker-card__body small, .plan-place-picker-card__body span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
@@ -9078,15 +9079,15 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-place-picker-card__body span { color: var(--app-text-muted); font-size: 12px; font-weight: 800; }
 .plan-place-source-overlay { position: fixed; inset: 0; z-index: 80; display: grid; align-items: end; justify-items: stretch; padding: 16px; }
 .plan-place-source-backdrop { position: absolute; inset: 0; border: 0; background: rgba(9, 9, 11, 0.48); cursor: pointer; }
-.plan-place-source-sheet { position: relative; z-index: 1; display: grid; gap: 14px; width: min(100%, 620px); max-height: min(86vh, 760px); overflow: auto; border: 1px solid var(--app-border); border-radius: 30px 30px 24px 24px; background: var(--app-surface); box-shadow: 0 24px 80px rgba(15, 23, 42, 0.28); padding: 16px; }
-.plan-place-source-grid { display: grid; gap: 10px; }
-.plan-place-source-option { display: grid; grid-template-columns: 42px minmax(0, 1fr); align-items: center; gap: 12px; width: 100%; min-height: 78px; border: 1px solid var(--app-border); border-radius: 22px; background: var(--app-surface-soft); color: inherit; padding: 12px; text-align: left; cursor: pointer; }
-.plan-place-source-option:hover { border-color: var(--app-border-strong); background: var(--app-surface); }
-.plan-place-source-option--primary { border-color: color-mix(in srgb, var(--place-border) 72%, var(--app-border)); background: color-mix(in srgb, var(--place-bg) 46%, var(--app-surface)); }
-.plan-place-source-option__icon { display: grid; place-items: center; width: 42px; height: 42px; border: 1px solid var(--place-border); border-radius: 999px; background: var(--place-bg); color: var(--place-text); font-size: 18px; font-weight: 950; }
+.plan-place-source-sheet { position: relative; z-index: 1; display: grid; gap: 10px; width: min(100%, 560px); max-height: min(86vh, 720px); overflow: auto; border: 1px solid var(--app-border); border-radius: 26px 26px 22px 22px; background: var(--app-surface); box-shadow: 0 24px 80px rgba(15, 23, 42, 0.28); padding: 14px; }
+.plan-place-source-grid { display: grid; gap: 0; border-top: 1px solid var(--app-divider); }
+.plan-place-source-option { display: grid; grid-template-columns: 36px minmax(0, 1fr); align-items: center; gap: 12px; width: 100%; min-height: 62px; border: 0; border-bottom: 1px solid var(--app-divider); border-radius: 0; background: transparent; color: inherit; padding: 10px 0; text-align: left; cursor: pointer; }
+.plan-place-source-option:hover { background: color-mix(in srgb, var(--place-bg) 18%, transparent); }
+.plan-place-source-option--primary { background: transparent; }
+.plan-place-source-option__icon { display: grid; place-items: center; width: 36px; height: 36px; border: 1px solid var(--place-border); border-radius: 999px; background: var(--place-bg); color: var(--place-text); font-size: 16px; font-weight: 950; }
 .plan-place-source-option span:last-child { display: grid; gap: 3px; min-width: 0; }
 .plan-place-source-option strong { color: var(--app-text); font-size: 15px; font-weight: 950; }
-.plan-place-source-option small { color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
+.plan-place-source-option small { color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.28; }
 .plan-place-picker-actions { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; }
 @media (min-width: 721px) {
   .plan-place-source-overlay { align-items: center; justify-items: center; }
@@ -9095,8 +9096,9 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-create-place-box { display: grid; gap: 11px; border: 1px solid var(--app-border); border-radius: 22px; background: var(--app-surface-soft); padding: 12px; }
 .plan-create-place-box h4 { margin: 0; letter-spacing: -0.02em; }
 @media (max-width: 520px) {
-  .plan-source-place-strip, .plan-place-picker-card { grid-template-columns: 1fr; }
-  .plan-place-picker-card__media { width: 100%; height: 130px; }
+  .plan-source-place-strip { grid-template-columns: 1fr; }
+  .plan-place-picker-card { grid-template-columns: 48px minmax(0, 1fr); }
+  .plan-place-picker-card__media { width: 48px; height: 48px; }
 }
 
 /* PLAN-WIZ4 — Order/time Create Plan wizard polish */
@@ -9158,8 +9160,9 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .plan-stop-note summary { width: fit-content; cursor: pointer; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: 0.04em; text-transform: uppercase; }
 .plan-stop-note[open] { gap: 10px; }
 .plan-place-detail-sheet { max-width: 680px; }
-.plan-place-detail-actions { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; border-top: 1px solid var(--app-border); padding-top: 12px; }
+.plan-place-detail-actions { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; border-top: 1px solid var(--app-divider); padding-top: 12px; }
 .plan-source-place-strip span { min-width: 0; color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
+.plan-source-place-strip .cta-row { justify-content: flex-start; }
 .plan-preview-stage { display: grid; gap: 18px; border-top: 1px solid var(--app-divider); padding-top: 14px; }
 .plan-preview-confirm-hero { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: start; padding-bottom: 16px; border-bottom: 1px solid var(--app-divider); }
 .plan-preview-confirm-hero__copy { display: grid; gap: 7px; min-width: 0; }
diff --git a/apps/web/src/features/plans/PlanCreateClient.tsx b/apps/web/src/features/plans/PlanCreateClient.tsx
index 4048ba8..be63b08 100644
--- a/apps/web/src/features/plans/PlanCreateClient.tsx
+++ b/apps/web/src/features/plans/PlanCreateClient.tsx
@@ -712,7 +712,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
   }
 
   const pickerIsOpen = pickerTarget !== null;
-  const pickerTitle = pickerTarget === 'new' ? 'Choose place source' : `Choose Place ${typeof pickerTarget === 'number' ? pickerTarget + 1 : ''}`;
+  const pickerTitle = pickerTarget === 'new' ? 'Add place' : `Place ${typeof pickerTarget === 'number' ? pickerTarget + 1 : ''}`;
   const detailPlace = detailPlaceIndex !== null ? places[detailPlaceIndex] : null;
 
   return (
@@ -722,7 +722,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
           <div>
             <PlansInternalBadge plansVisible={plansVisible} />
             <h2>Create Plan</h2>
-            <p>Add places, set times, preview.</p>
+            <p>Set time, add places, preview.</p>
           </div>
         </section>
 
@@ -903,14 +903,13 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
             <section className="plan-place-source-sheet plan-place-detail-sheet" role="dialog" aria-modal="true" aria-label={`Place ${detailPlaceIndex + 1} details`}>
               <div className="plan-detail-topbar">
                 <div>
-                  <h3>Place {detailPlaceIndex + 1} details</h3>
-                  <p className="meta">Edit only this stop. Reusable Place descriptions stay on the saved Place.</p>
+                  <h3>Place {detailPlaceIndex + 1}</h3>
                 </div>
                 <button type="button" className="plans-feed-icon-button" onClick={() => setDetailPlaceIndex(null)} aria-label="Close Place details">×</button>
               </div>
               <div className="plan-place-picker-panel">
                 <div className="plan-timeline-row__heading">
-                  {detailPlace.sourcePlaceId ? <span className="semantic-badge place">{detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library snapshot' : 'My Place snapshot'}</span> : <span className="semantic-badge place">Custom one-off stop</span>}
+                  {detailPlace.sourcePlaceId ? <span className="semantic-badge place">{detailPlace.sourcePlaceSource === 'hellowhen_library' ? 'Library' : 'My Place'}</span> : <span className="semantic-badge place">Custom</span>}
                 </div>
                 {!detailPlace.sourcePlaceId ? (
                   <>
@@ -940,15 +939,15 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                 ) : (
                   <div className="plan-source-place-strip">
                     <strong>{detailPlace.sourcePlaceTitle || detailPlace.title}</strong>
-                    <span>{detailPlace.sourcePlaceSource === 'my_place' ? 'Edit the saved My Place if this content should change everywhere you reuse it.' : 'Library Places are copied before editing so your changes stay private.'}</span>
+                    <span>{detailPlace.sourcePlaceSource === 'my_place' ? 'Updates your saved Place.' : 'Copied into My Places before editing.'}</span>
                     <div className="cta-row">
                       {detailPlace.sourcePlaceSource === 'my_place' ? (
-                        <button type="button" className="button secondary" onClick={() => openEditMyPlaceFromDetail(detailPlaceIndex)}>Edit saved Place</button>
+                        <button type="button" className="button secondary" onClick={() => openEditMyPlaceFromDetail(detailPlaceIndex)}>Edit saved</button>
                       ) : null}
                       {detailPlace.sourcePlaceSource === 'hellowhen_library' ? (
-                        <button type="button" className="button secondary" onClick={() => openCopyLibraryPlaceFromDetail(detailPlaceIndex)}>Copy and edit</button>
+                        <button type="button" className="button secondary" onClick={() => openCopyLibraryPlaceFromDetail(detailPlaceIndex)}>Copy to edit</button>
                       ) : null}
-                      <button type="button" className="button secondary" onClick={() => updatePlace(detailPlaceIndex, resetToCustomPatch())}>Use as custom</button>
+                      <button type="button" className="button secondary" onClick={() => updatePlace(detailPlaceIndex, resetToCustomPatch())}>Make custom</button>
                     </div>
                   </div>
                 )}
@@ -975,8 +974,7 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
             <section className="plan-place-source-sheet" role="dialog" aria-modal="true" aria-label={pickerTitle}>
               <div className="plan-detail-topbar">
                 <div>
-                  <h3>{pickerView === 'source' ? 'Choose place source' : pickerTab === 'mine' ? 'Choose from My Places' : 'Choose from Hellowhen Library'}</h3>
-                  <p className="meta">Saved Places are copied as snapshots into this Plan.</p>
+                  <h3>{pickerView === 'source' ? 'Add place' : pickerTab === 'mine' ? 'My Places' : 'Hellowhen Library'}</h3>
                 </div>
                 <button type="button" className="plans-feed-icon-button" onClick={closePlacePicker} aria-label="Close Place picker">×</button>
               </div>
@@ -985,19 +983,19 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                 <div className="plan-place-source-grid">
                   <button type="button" className="plan-place-source-option plan-place-source-option--primary" onClick={() => openPickerList('mine')}>
                     <span className="plan-place-source-option__icon">⌖</span>
-                    <span><strong>Use one of mine</strong><small>Choose from your private saved Places.</small></span>
+                    <span><strong>My Places</strong><small>Saved places you created.</small></span>
                   </button>
                   <button type="button" className="plan-place-source-option" onClick={() => openPickerList('library')}>
                     <span className="plan-place-source-option__icon">✦</span>
-                    <span><strong>Use Hellowhen Library</strong><small>Start from reusable Hellowhen Places.</small></span>
+                    <span><strong>Hellowhen Library</strong><small>Reusable place templates.</small></span>
                   </button>
                   <button type="button" className="plan-place-source-option" onClick={openCreatePlaceFromPicker}>
                     <span className="plan-place-source-option__icon">＋</span>
-                    <span><strong>Create a new Place</strong><small>Save it, then return with it selected.</small></span>
+                    <span><strong>New Place</strong><small>Save and return here.</small></span>
                   </button>
                   <button type="button" className="plan-place-source-option" onClick={useCustomPlaceFromPicker}>
                     <span className="plan-place-source-option__icon">•••</span>
-                    <span><strong>Custom one-off stop</strong><small>Use only inside this Plan.</small></span>
+                    <span><strong>Custom stop</strong><small>Only for this Plan.</small></span>
                   </button>
                 </div>
               ) : (
@@ -1008,16 +1006,16 @@ export function PlanCreateClient({ plansEnabled, plansVisible }: PlanCreateClien
                   </div>
                   <label>
                     <span>Search Places</span>
-                    <input value={placeQuery} onChange={(event) => setPlaceQuery(event.target.value)} placeholder="Search title, category, area..." />
+                    <input value={placeQuery} onChange={(event) => setPlaceQuery(event.target.value)} placeholder="Search Places" />
                   </label>
                   {pickerTab === 'mine' ? (
-                    <PlacePickerList places={filteredMyPlaces} emptyLabel="No matching My Places yet. Create one from the source menu or use a custom stop." onChoose={chooseReusablePlace} />
+                    <PlacePickerList places={filteredMyPlaces} emptyLabel="No matching My Places yet." onChoose={chooseReusablePlace} />
                   ) : (
                     <PlacePickerList places={filteredLibraryPlaces} emptyLabel="No matching Hellowhen Library Places yet." onChoose={chooseReusablePlace} />
                   )}
                   <div className="plan-place-picker-actions">
-                    <button type="button" className="button secondary" onClick={() => setPickerView('source')}>Back to sources</button>
-                    <button type="button" className="button secondary" onClick={openCreatePlaceFromPicker}>Create new Place</button>
+                    <button type="button" className="button secondary" onClick={() => setPickerView('source')}>Sources</button>
+                    <button type="button" className="button secondary" onClick={openCreatePlaceFromPicker}>New Place</button>
                   </div>
                 </div>
               )}
