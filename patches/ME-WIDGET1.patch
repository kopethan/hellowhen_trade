diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 876f694..abeb5e0 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -10010,41 +10010,81 @@ html[data-theme="dark"] .onboarding-guide-primary {
   .app-filter-options { grid-template-columns: minmax(0, 1fr); }
 }
 
-/* ME-HUB-WEB1: make Account behave like the normal Me hub in the Plans · Me · Trade shell. */
+/* ME-WIDGET1: visual foundation for the Me hub widget system. */
 .web-app-page--me {
   --web-app-page-max: min(1120px, 100%);
+  --me-widget-radius: 30px;
+  --me-widget-radius-large: 38px;
+  --me-widget-surface: color-mix(in srgb, var(--app-surface) 94%, transparent);
+  --me-widget-surface-strong: color-mix(in srgb, var(--app-surface) 98%, var(--app-surface-soft));
+  --me-widget-surface-soft: color-mix(in srgb, var(--app-surface-soft) 72%, var(--app-surface));
+  --me-widget-border: color-mix(in srgb, var(--app-border) 78%, transparent);
+  --me-widget-border-strong: color-mix(in srgb, var(--app-border-strong) 78%, var(--app-border));
+  --me-widget-shadow: 0 18px 48px rgba(15, 23, 42, 0.09);
+  --me-widget-shadow-soft: 0 12px 30px rgba(15, 23, 42, 0.065);
   display: grid;
-  gap: 18px;
+  gap: 22px;
+  padding-bottom: calc(20px + env(safe-area-inset-bottom));
+}
+
+:root:not([data-theme="light"]) .web-app-page--me,
+html[data-theme="dark"] .web-app-page--me {
+  --me-widget-surface: color-mix(in srgb, var(--app-surface) 88%, #243047 12%);
+  --me-widget-surface-strong: color-mix(in srgb, var(--app-surface) 82%, #2C3952 18%);
+  --me-widget-surface-soft: color-mix(in srgb, var(--app-surface-soft) 64%, #243047 36%);
+  --me-widget-border: color-mix(in srgb, var(--app-border-strong) 72%, transparent);
+  --me-widget-border-strong: color-mix(in srgb, var(--app-border-strong) 86%, #94A3B8 14%);
+  --me-widget-shadow: 0 18px 54px rgba(0, 0, 0, 0.24);
+  --me-widget-shadow-soft: 0 12px 34px rgba(0, 0, 0, 0.18);
 }
 
 .me-hub-page {
   display: grid;
-  gap: 18px;
+  gap: 22px;
 }
 
 .me-hub-hero {
+  position: relative;
   display: grid;
-  gap: 16px;
-  border: 1px solid color-mix(in srgb, var(--instruction-border) 42%, var(--app-border));
-  border-radius: 34px;
+  gap: 18px;
+  overflow: hidden;
+  border: 1px solid color-mix(in srgb, var(--instruction-border) 46%, var(--me-widget-border));
+  border-radius: var(--me-widget-radius-large);
   background:
-    radial-gradient(circle at 0 0, color-mix(in srgb, var(--instruction-bg) 72%, transparent), transparent 34%),
-    linear-gradient(135deg, color-mix(in srgb, var(--app-surface) 88%, var(--instruction-bg)), var(--app-surface));
-  padding: 18px;
-  box-shadow: var(--app-card-shadow-strong);
+    radial-gradient(circle at 8% 12%, color-mix(in srgb, var(--instruction-bg) 78%, transparent), transparent 32%),
+    radial-gradient(circle at 92% 10%, color-mix(in srgb, var(--proposal-bg) 32%, transparent), transparent 26%),
+    linear-gradient(135deg, color-mix(in srgb, var(--me-widget-surface-strong) 88%, var(--instruction-bg)), var(--me-widget-surface));
+  padding: 22px;
+  box-shadow: var(--me-widget-shadow);
+}
+
+.me-hub-hero::after {
+  content: "";
+  position: absolute;
+  right: -68px;
+  bottom: -82px;
+  width: 210px;
+  height: 210px;
+  border: 1px solid color-mix(in srgb, var(--instruction-border) 24%, transparent);
+  border-radius: 999px;
+  background: radial-gradient(circle, color-mix(in srgb, var(--instruction-bg) 34%, transparent), transparent 66%);
+  pointer-events: none;
 }
 
 .me-hub-hero--signed-in {
-  border-color: color-mix(in srgb, var(--success-border) 28%, var(--app-border));
+  border-color: color-mix(in srgb, var(--success-border) 32%, var(--me-widget-border));
   background:
-    radial-gradient(circle at 0 0, color-mix(in srgb, var(--success-bg) 48%, transparent), transparent 34%),
-    linear-gradient(135deg, color-mix(in srgb, var(--app-surface) 90%, var(--success-bg)), var(--app-surface));
+    radial-gradient(circle at 8% 12%, color-mix(in srgb, var(--success-bg) 54%, transparent), transparent 32%),
+    radial-gradient(circle at 92% 8%, color-mix(in srgb, var(--instruction-bg) 34%, transparent), transparent 28%),
+    linear-gradient(135deg, color-mix(in srgb, var(--me-widget-surface-strong) 90%, var(--success-bg)), var(--me-widget-surface));
 }
 
 .me-hub-hero__identity {
+  position: relative;
+  z-index: 1;
   display: grid;
   grid-template-columns: auto minmax(0, 1fr);
-  gap: 14px;
+  gap: 16px;
   align-items: center;
 }
 
@@ -10053,7 +10093,11 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 
 .me-hub-hero__copy h1 {
-  margin: 8px 0 6px;
+  margin: 8px 0 7px;
+  color: var(--app-text);
+  font-size: clamp(25px, 4.4vw, 36px);
+  letter-spacing: -0.055em;
+  line-height: 1.02;
   overflow-wrap: anywhere;
 }
 
@@ -10061,12 +10105,14 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .me-hub-hero__loading {
   margin: 0;
   color: var(--app-text-muted);
-  font-size: 13px;
-  font-weight: 800;
-  line-height: 1.4;
+  font-size: 14px;
+  font-weight: 820;
+  line-height: 1.5;
 }
 
 .me-hub-hero__actions {
+  position: relative;
+  z-index: 1;
   display: flex;
   flex-wrap: wrap;
   gap: 10px;
@@ -10075,64 +10121,69 @@ html[data-theme="dark"] .onboarding-guide-primary {
 .me-hub-hero__actions .button,
 .me-hub-hero__actions button {
   flex: 1 1 150px;
-  min-height: 42px;
+  min-height: 46px;
 }
 
 .me-hub-stat-grid {
   display: grid;
   grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: 10px;
+  gap: 12px;
 }
 
 .me-hub-stat-card {
   display: grid;
   grid-template-columns: auto minmax(0, 1fr);
-  gap: 10px;
+  gap: 12px;
   align-items: center;
-  min-height: 82px;
-  border: 1px solid var(--app-border);
-  border-radius: 24px;
-  background: var(--app-surface);
-  padding: 13px;
-  box-shadow: var(--app-control-shadow);
+  min-height: 94px;
+  border: 1px solid var(--me-widget-border);
+  border-radius: 28px;
+  background: linear-gradient(145deg, var(--me-widget-surface-strong), var(--me-widget-surface-soft));
+  padding: 15px;
+  box-shadow: var(--me-widget-shadow-soft);
   transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
 }
 
-.me-hub-stat-card:hover {
-  border-color: var(--app-border-strong);
+.me-hub-stat-card:hover,
+.me-hub-stat-card:focus-visible {
+  border-color: var(--me-widget-border-strong);
+  box-shadow: var(--me-widget-shadow);
   text-decoration: none;
   transform: translateY(-1px);
 }
 
-.me-hub-stat-card__icon {
+.me-hub-stat-card__icon,
+.me-hub-link-card .mobile-link-card__icon {
   display: grid;
   place-items: center;
-  width: 38px;
-  height: 38px;
-  border: 1px solid var(--app-border);
-  border-radius: 15px;
-  background: var(--app-surface-soft);
+  flex: 0 0 auto;
+  width: 44px;
+  height: 44px;
+  border: 1px solid var(--me-widget-border);
+  border-radius: 17px;
+  background: var(--me-widget-surface-soft);
   color: var(--app-text);
+  box-shadow: none;
 }
 
 .me-hub-stat-card span:not(.web-icon) {
   display: grid;
-  gap: 2px;
+  gap: 4px;
   min-width: 0;
 }
 
 .me-hub-stat-card strong {
   color: var(--app-text);
-  font-size: 24px;
+  font-size: clamp(26px, 4.5vw, 32px);
   font-weight: 950;
-  letter-spacing: -0.05em;
-  line-height: 1;
+  letter-spacing: -0.06em;
+  line-height: 0.95;
 }
 
 .me-hub-stat-card small {
   overflow: hidden;
   color: var(--app-text-muted);
-  font-size: 11px;
+  font-size: 13px;
   font-weight: 900;
   line-height: 1.25;
   text-overflow: ellipsis;
@@ -10140,99 +10191,159 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 
 .me-hub-stat-card--trade .me-hub-stat-card__icon,
-.me-hub-link-card--trade .mobile-link-card__icon { border-color: color-mix(in srgb, var(--trade-border) 54%, var(--app-border)); background: var(--trade-bg); color: var(--trade-text); }
+.me-hub-link-card--trade .mobile-link-card__icon { border-color: color-mix(in srgb, var(--trade-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--trade-bg) 78%, var(--me-widget-surface)); color: var(--trade-text); }
 .me-hub-stat-card--need .me-hub-stat-card__icon,
-.me-hub-link-card--need .mobile-link-card__icon { border-color: color-mix(in srgb, var(--need-border) 54%, var(--app-border)); background: var(--need-bg); color: var(--need-text); }
+.me-hub-link-card--need .mobile-link-card__icon { border-color: color-mix(in srgb, var(--need-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--need-bg) 78%, var(--me-widget-surface)); color: var(--need-text); }
 .me-hub-stat-card--offer .me-hub-stat-card__icon,
-.me-hub-link-card--offer .mobile-link-card__icon { border-color: color-mix(in srgb, var(--offer-border) 54%, var(--app-border)); background: var(--offer-bg); color: var(--offer-text); }
+.me-hub-link-card--offer .mobile-link-card__icon { border-color: color-mix(in srgb, var(--offer-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--offer-bg) 78%, var(--me-widget-surface)); color: var(--offer-text); }
 .me-hub-stat-card--plan .me-hub-stat-card__icon,
-.me-hub-link-card--plan .mobile-link-card__icon { border-color: color-mix(in srgb, var(--plan-border) 54%, var(--app-border)); background: var(--plan-bg); color: var(--plan-text); }
+.me-hub-link-card--plan .mobile-link-card__icon { border-color: color-mix(in srgb, var(--plan-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--plan-bg) 78%, var(--me-widget-surface)); color: var(--plan-text); }
 .me-hub-stat-card--proposal .me-hub-stat-card__icon,
-.me-hub-link-card--proposal .mobile-link-card__icon { border-color: color-mix(in srgb, var(--proposal-border) 54%, var(--app-border)); background: var(--proposal-bg); color: var(--proposal-text); }
+.me-hub-link-card--proposal .mobile-link-card__icon { border-color: color-mix(in srgb, var(--proposal-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--proposal-bg) 78%, var(--me-widget-surface)); color: var(--proposal-text); }
 .me-hub-stat-card--danger .me-hub-stat-card__icon,
-.me-hub-link-card--danger .mobile-link-card__icon { border-color: color-mix(in srgb, var(--danger-border) 54%, var(--app-border)); background: var(--danger-bg); color: var(--danger-text); }
+.me-hub-link-card--danger .mobile-link-card__icon { border-color: color-mix(in srgb, var(--danger-border) 58%, var(--me-widget-border)); background: color-mix(in srgb, var(--danger-bg) 78%, var(--me-widget-surface)); color: var(--danger-text); }
 
 .me-hub-section {
-  gap: 12px;
-  border: 1px solid color-mix(in srgb, var(--app-border) 76%, transparent);
-  border-radius: 30px;
-  background: color-mix(in srgb, var(--app-surface) 96%, transparent);
-  padding: 14px;
-  box-shadow: var(--app-card-shadow);
+  gap: 16px;
+  border: 1px solid var(--me-widget-border);
+  border-radius: var(--me-widget-radius);
+  background: var(--me-widget-surface);
+  padding: 18px;
+  box-shadow: var(--me-widget-shadow-soft);
 }
 
 .me-hub-section--quick {
-  border-color: color-mix(in srgb, var(--trade-border) 26%, var(--app-border));
+  border-color: color-mix(in srgb, var(--trade-border) 30%, var(--me-widget-border));
+  background:
+    radial-gradient(circle at 5% 0, color-mix(in srgb, var(--trade-bg) 34%, transparent), transparent 28%),
+    var(--me-widget-surface);
 }
 
 .me-hub-section__header {
   align-items: flex-start;
+  gap: 16px;
 }
 
 .me-hub-section__header h2 {
-  margin: 4px 0 0;
-  font-size: 17px;
-  letter-spacing: -0.03em;
+  margin: 5px 0 0;
+  color: var(--app-text);
+  font-size: 21px;
+  font-weight: 950;
+  letter-spacing: -0.045em;
+  line-height: 1.12;
 }
 
 .me-hub-section__header p {
-  max-width: 320px;
+  max-width: 360px;
+  color: var(--app-text-muted);
+  font-size: 13px;
+  font-weight: 820;
+  line-height: 1.45;
   text-align: right;
 }
 
 .me-hub-list {
-  gap: 8px;
+  gap: 10px;
 }
 
 .me-hub-link-card {
   align-items: center;
-  border-radius: 22px;
-  background: color-mix(in srgb, var(--app-surface-soft) 60%, var(--app-surface));
+  min-height: 76px;
+  border-color: color-mix(in srgb, var(--me-widget-border) 86%, transparent);
+  border-radius: 24px;
+  background: color-mix(in srgb, var(--me-widget-surface-soft) 78%, var(--me-widget-surface));
+  padding: 15px;
+  box-shadow: none;
 }
 
-.me-hub-link-card:hover {
+.me-hub-link-card:hover,
+.me-hub-link-card:focus-visible {
+  border-color: var(--me-widget-border-strong);
+  background: var(--me-widget-surface-strong);
+  box-shadow: var(--me-widget-shadow-soft);
+  text-decoration: none;
   transform: translateY(-1px);
 }
 
+.me-hub-link-card .mobile-link-card__title-row strong {
+  color: var(--app-text);
+  font-size: 15px;
+  font-weight: 950;
+  letter-spacing: -0.025em;
+  line-height: 1.2;
+}
+
 .me-hub-link-card__body-text {
   display: block;
-  margin-top: 4px;
+  margin-top: 5px;
   color: var(--app-text-muted);
+  font-size: 13px;
+  font-weight: 780;
+  line-height: 1.4;
+}
+
+.me-hub-link-card .mobile-link-card__arrow {
+  color: var(--app-text-soft);
+}
+
+.me-hub-link-card .mobile-link-card__meta-badge {
+  min-width: 30px;
+  justify-content: center;
   font-size: 12px;
-  font-weight: 800;
-  line-height: 1.35;
+}
+
+.account-quick-action-card.me-hub-link-card {
+  align-content: start;
+  align-items: start;
+  min-height: 132px;
+  padding: 16px;
+}
+
+.account-quick-action-card.me-hub-link-card .mobile-link-card__icon {
+  width: 46px;
+  height: 46px;
+  border-radius: 18px;
+}
+
+.account-quick-action-card.me-hub-link-card .mobile-link-card__title-row {
+  margin-top: 10px;
+}
+
+.account-quick-action-card.me-hub-link-card .me-hub-link-card__body-text {
+  max-width: 17rem;
 }
 
 .me-hub-layout {
   display: grid;
-  gap: 16px;
+  gap: 18px;
 }
 
 .me-hub-layout__main,
 .me-hub-layout__side {
   display: grid;
   align-content: start;
-  gap: 16px;
+  gap: 18px;
 }
 
 @media (min-width: 760px) {
   .web-app-page--me {
-    padding-bottom: 16px;
+    padding-bottom: calc(var(--app-dock-height) + 58px + env(safe-area-inset-bottom));
   }
 
   .me-hub-page {
-    gap: 20px;
+    gap: 24px;
   }
 
   .me-hub-hero {
     grid-template-columns: minmax(0, 1fr) auto;
     align-items: center;
-    padding: 22px;
+    min-height: 158px;
+    padding: 28px;
   }
 
   .me-hub-hero__actions {
     justify-content: flex-end;
-    min-width: 250px;
+    min-width: 260px;
   }
 
   .me-hub-hero__actions .button,
@@ -10242,19 +10353,52 @@ html[data-theme="dark"] .onboarding-guide-primary {
 
   .me-hub-stat-grid {
     grid-template-columns: repeat(4, minmax(0, 1fr));
+    gap: 14px;
   }
 
   .me-hub-layout {
-    grid-template-columns: minmax(0, 1.18fr) minmax(320px, 0.82fr);
+    grid-template-columns: minmax(0, 1.12fr) minmax(340px, 0.88fr);
     align-items: start;
+    gap: 20px;
   }
 
   .me-hub-quick-grid {
     grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 12px;
+  }
+}
+
+@media (min-width: 1180px) {
+  .me-hub-quick-grid {
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+  }
+}
+
+@media (max-width: 900px) and (min-width: 760px) {
+  .me-hub-layout {
+    grid-template-columns: minmax(0, 1fr);
+  }
+
+  .me-hub-quick-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
   }
 }
 
 @media (max-width: 560px) {
+  .web-app-page--me {
+    gap: 18px;
+    padding-bottom: calc(var(--app-tab-height) + 34px + env(safe-area-inset-bottom));
+  }
+
+  .me-hub-page {
+    gap: 18px;
+  }
+
+  .me-hub-hero {
+    border-radius: 30px;
+    padding: 20px;
+  }
+
   .me-hub-hero__identity {
     grid-template-columns: 1fr;
   }
@@ -10268,6 +10412,15 @@ html[data-theme="dark"] .onboarding-guide-primary {
     grid-template-columns: 1fr;
   }
 
+  .me-hub-stat-card {
+    min-height: 88px;
+  }
+
+  .me-hub-section {
+    border-radius: 28px;
+    padding: 16px;
+  }
+
   .me-hub-section__header {
     display: grid;
   }
@@ -10276,4 +10429,8 @@ html[data-theme="dark"] .onboarding-guide-primary {
     max-width: none;
     text-align: left;
   }
+
+  .account-quick-action-card.me-hub-link-card {
+    min-height: 82px;
+  }
 }
