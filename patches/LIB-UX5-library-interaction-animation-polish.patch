diff --git a/apps/mobile/src/components/library/LibraryActiveFilterChips.tsx b/apps/mobile/src/components/library/LibraryActiveFilterChips.tsx
index fa443bf..ccb8d03 100644
--- a/apps/mobile/src/components/library/LibraryActiveFilterChips.tsx
+++ b/apps/mobile/src/components/library/LibraryActiveFilterChips.tsx
@@ -1,5 +1,5 @@
 import React from 'react';
-import { Pressable, StyleSheet, View } from 'react-native';
+import { Pressable, ScrollView, StyleSheet } from 'react-native';
 import type { SemanticColorName } from '@hellowhen/theme';
 import { useThemeTokens } from '../../providers/ThemeProvider';
 import { AppText } from '../AppText';
@@ -24,12 +24,18 @@ export function LibraryActiveFilterChips({ filters, tone }: LibraryActiveFilterC
   if (filters.length === 0) return null;
 
   return (
-    <View style={styles.row}>
+    <ScrollView
+      horizontal
+      keyboardShouldPersistTaps="handled"
+      showsHorizontalScrollIndicator={false}
+      contentContainerStyle={styles.row}
+    >
       {filters.map((filter) => (
         <Pressable
           key={filter.key}
           accessibilityRole="button"
           accessibilityLabel={filter.accessibilityLabel ?? filter.label}
+          hitSlop={2}
           onPress={filter.onRemove}
           style={({ pressed }) => [
             styles.chip,
@@ -41,22 +47,21 @@ export function LibraryActiveFilterChips({ filters, tone }: LibraryActiveFilterC
           <MobileIcon name="close" size={14} color={semantic.text} decorative />
         </Pressable>
       ))}
-    </View>
+    </ScrollView>
   );
 }
 
 const styles = StyleSheet.create({
   row: {
-    flexDirection: 'row',
-    flexWrap: 'wrap',
     gap: 8,
+    paddingRight: 2,
   },
   chip: {
-    minHeight: 36,
-    maxWidth: '100%',
-    borderRadius: 18,
+    minHeight: 40,
+    maxWidth: 240,
+    borderRadius: 20,
     borderWidth: 1,
-    paddingHorizontal: 11,
+    paddingHorizontal: 12,
     flexDirection: 'row',
     alignItems: 'center',
     gap: 6,
@@ -69,5 +74,6 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.72,
+    transform: [{ scale: 0.98 }],
   },
 });
diff --git a/apps/mobile/src/components/library/LibraryFilterControls.tsx b/apps/mobile/src/components/library/LibraryFilterControls.tsx
index 06de01e..e407c97 100644
--- a/apps/mobile/src/components/library/LibraryFilterControls.tsx
+++ b/apps/mobile/src/components/library/LibraryFilterControls.tsx
@@ -20,7 +20,7 @@ type LibraryFilterOptionProps = {
 export function LibraryFilterGroup({ title, children }: LibraryFilterGroupProps) {
   return (
     <View style={styles.group}>
-      <AppText style={styles.groupTitle}>{title}</AppText>
+      <AppText accessibilityRole="header" style={styles.groupTitle}>{title}</AppText>
       <View style={styles.options}>{children}</View>
     </View>
   );
@@ -51,6 +51,7 @@ export function LibraryFilterOption({
         pressed && styles.pressed,
       ]}
     >
+      {selected ? <View style={[styles.selectedDot, { backgroundColor: semantic.onBg }]} /> : null}
       <AppText style={[styles.optionText, { color: selected ? semantic.onBg : theme.color.text }]}>{label}</AppText>
     </Pressable>
   );
@@ -58,7 +59,7 @@ export function LibraryFilterOption({
 
 const styles = StyleSheet.create({
   group: {
-    gap: 9,
+    gap: 10,
   },
   groupTitle: {
     fontSize: 13,
@@ -71,13 +72,20 @@ const styles = StyleSheet.create({
     gap: 8,
   },
   option: {
-    minHeight: 40,
-    borderRadius: 20,
+    minHeight: 44,
+    borderRadius: 22,
     borderWidth: 1,
     paddingHorizontal: 13,
     paddingVertical: 9,
+    flexDirection: 'row',
     alignItems: 'center',
     justifyContent: 'center',
+    gap: 7,
+  },
+  selectedDot: {
+    width: 6,
+    height: 6,
+    borderRadius: 3,
   },
   optionText: {
     fontSize: 12,
@@ -86,6 +94,6 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.76,
-    transform: [{ scale: 0.99 }],
+    transform: [{ scale: 0.98 }],
   },
 });
diff --git a/apps/mobile/src/components/library/LibraryFilterSheet.tsx b/apps/mobile/src/components/library/LibraryFilterSheet.tsx
index 9f8668d..4f64e24 100644
--- a/apps/mobile/src/components/library/LibraryFilterSheet.tsx
+++ b/apps/mobile/src/components/library/LibraryFilterSheet.tsx
@@ -1,7 +1,8 @@
-import React from 'react';
-import { Modal, Platform, Pressable, ScrollView, StyleSheet, View } from 'react-native';
+import React, { useEffect, useRef, useState } from 'react';
+import { Animated, Easing, Modal, Platform, Pressable, ScrollView, StyleSheet, View } from 'react-native';
 import type { SemanticColorName } from '@hellowhen/theme';
 import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useReducedMotion } from '../../hooks/useReducedMotion';
 import { useThemeTokens } from '../../providers/ThemeProvider';
 import { AppText } from '../AppText';
 import { MobileIcon } from '../MobileIcon';
@@ -39,98 +40,190 @@ export function LibraryFilterSheet({
 }: LibraryFilterSheetProps) {
   const theme = useThemeTokens();
   const insets = useSafeAreaInsets();
+  const reducedMotion = useReducedMotion();
   const semantic = tone ? theme.semantic[tone] : theme.semantic.info;
   const bottomInset = Platform.OS === 'android' ? Math.max(0, insets.bottom) : Math.max(8, insets.bottom);
+  const [rendered, setRendered] = useState(visible);
+  const progress = useRef(new Animated.Value(visible ? 1 : 0)).current;
+  const enteredRef = useRef(visible);
+
+  useEffect(() => {
+    progress.stopAnimation();
+
+    if (visible && !rendered) {
+      enteredRef.current = false;
+      setRendered(true);
+      return;
+    }
+
+    if (visible) {
+      if (enteredRef.current) return;
+      enteredRef.current = true;
+
+      if (reducedMotion) {
+        progress.setValue(1);
+        return;
+      }
+
+      progress.setValue(0);
+      const animation = Animated.timing(progress, {
+        toValue: 1,
+        duration: 220,
+        easing: Easing.out(Easing.cubic),
+        useNativeDriver: true,
+      });
+      animation.start();
+      return () => animation.stop();
+    }
+
+    if (!rendered) return;
+    enteredRef.current = false;
+
+    if (reducedMotion) {
+      progress.setValue(0);
+      setRendered(false);
+      return;
+    }
+
+    const animation = Animated.timing(progress, {
+      toValue: 0,
+      duration: 160,
+      easing: Easing.in(Easing.cubic),
+      useNativeDriver: true,
+    });
+    animation.start(({ finished }) => {
+      if (finished) setRendered(false);
+    });
+    return () => animation.stop();
+  }, [progress, reducedMotion, rendered, visible]);
+
+  if (!rendered) return null;
+
+  const translateY = progress.interpolate({
+    inputRange: [0, 1],
+    outputRange: [34, 0],
+  });
+  const scale = progress.interpolate({
+    inputRange: [0, 1],
+    outputRange: [0.985, 1],
+  });
 
   return (
-    <Modal animationType="fade" onRequestClose={onClose} transparent visible={visible}>
-      <Pressable accessible={false} style={[styles.backdrop, { paddingBottom: 14 + bottomInset }]} onPress={onClose}>
-        <Pressable
-          accessible={false}
-          onPress={(event) => event.stopPropagation()}
-          style={[styles.sheet, { backgroundColor: theme.color.elevated, borderColor: theme.color.border }]}
-        >
-          <View style={styles.headerRow}>
-            <View style={styles.headerCopy}>
-              <AppText style={styles.title}>{title}</AppText>
-              {body ? <AppText style={[styles.body, { color: theme.color.muted }]}>{body}</AppText> : null}
-            </View>
+    <Modal animationType="none" onRequestClose={onClose} transparent visible={rendered}>
+      <View style={[styles.modalRoot, { paddingBottom: 14 + bottomInset }]}>
+        <Animated.View
+          pointerEvents="none"
+          style={[StyleSheet.absoluteFillObject, styles.backdropVisual, { opacity: progress }]}
+        />
+        <Pressable accessible={false} style={styles.dismissArea} onPress={onClose}>
+          <Animated.View style={[styles.sheetMotion, { opacity: progress, transform: [{ translateY }, { scale }] }]}>
             <Pressable
-              accessibilityRole="button"
-              accessibilityLabel={closeAccessibilityLabel}
-              onPress={onClose}
-              style={({ pressed }) => [styles.closeButton, { borderColor: theme.color.border }, pressed && styles.pressed]}
+              accessibilityViewIsModal
+              accessible={false}
+              onPress={(event) => event.stopPropagation()}
+              style={[styles.sheet, { backgroundColor: theme.color.elevated, borderColor: theme.color.border }]}
             >
-              <MobileIcon name="close" size={18} color={theme.color.text} decorative />
-            </Pressable>
-          </View>
+              <View style={[styles.handle, { backgroundColor: theme.color.border }]} />
 
-          <ScrollView
-            contentContainerStyle={styles.content}
-            keyboardShouldPersistTaps="handled"
-            showsVerticalScrollIndicator={false}
-          >
-            {children}
-          </ScrollView>
+              <View style={styles.headerRow}>
+                <View style={styles.headerCopy}>
+                  <AppText accessibilityRole="header" style={styles.title}>{title}</AppText>
+                  {body ? <AppText style={[styles.body, { color: theme.color.muted }]}>{body}</AppText> : null}
+                </View>
+                <Pressable
+                  accessibilityRole="button"
+                  accessibilityLabel={closeAccessibilityLabel}
+                  onPress={onClose}
+                  style={({ pressed }) => [styles.closeButton, { borderColor: theme.color.border }, pressed && styles.pressed]}
+                >
+                  <MobileIcon name="close" size={18} color={theme.color.text} decorative />
+                </Pressable>
+              </View>
 
-          <View style={[styles.footer, { borderTopColor: theme.color.border }]}>
-            <Pressable
-              accessibilityRole="button"
-              accessibilityState={{ disabled: resetDisabled }}
-              disabled={resetDisabled}
-              onPress={onReset}
-              style={({ pressed }) => [
-                styles.footerButton,
-                styles.resetButton,
-                { borderColor: theme.color.border },
-                pressed && !resetDisabled && styles.pressed,
-                resetDisabled && styles.disabled,
-              ]}
-            >
-              <AppText style={[styles.resetText, { color: theme.color.text }]}>{resetLabel}</AppText>
-            </Pressable>
-            <Pressable
-              accessibilityRole="button"
-              accessibilityState={{ disabled: applyDisabled }}
-              disabled={applyDisabled}
-              onPress={onApply}
-              style={({ pressed }) => [
-                styles.footerButton,
-                styles.applyButton,
-                { backgroundColor: semantic.bg, borderColor: semantic.bg },
-                pressed && !applyDisabled && styles.pressed,
-                applyDisabled && styles.disabled,
-              ]}
-            >
-              <AppText style={[styles.applyText, { color: semantic.onBg }]}>{applyLabel}</AppText>
+              <ScrollView
+                contentContainerStyle={styles.content}
+                keyboardShouldPersistTaps="handled"
+                showsVerticalScrollIndicator={false}
+              >
+                {children}
+              </ScrollView>
+
+              <View style={[styles.footer, { borderTopColor: theme.color.border }]}>
+                <Pressable
+                  accessibilityRole="button"
+                  accessibilityState={{ disabled: resetDisabled }}
+                  disabled={resetDisabled}
+                  onPress={onReset}
+                  style={({ pressed }) => [
+                    styles.footerButton,
+                    styles.resetButton,
+                    { borderColor: theme.color.border },
+                    pressed && !resetDisabled && styles.pressed,
+                    resetDisabled && styles.disabled,
+                  ]}
+                >
+                  <AppText style={[styles.resetText, { color: theme.color.text }]}>{resetLabel}</AppText>
+                </Pressable>
+                <Pressable
+                  accessibilityRole="button"
+                  accessibilityState={{ disabled: applyDisabled }}
+                  disabled={applyDisabled}
+                  onPress={onApply}
+                  style={({ pressed }) => [
+                    styles.footerButton,
+                    styles.applyButton,
+                    { backgroundColor: semantic.bg, borderColor: semantic.bg },
+                    pressed && !applyDisabled && styles.pressed,
+                    applyDisabled && styles.disabled,
+                  ]}
+                >
+                  <AppText style={[styles.applyText, { color: semantic.onBg }]}>{applyLabel}</AppText>
+                </Pressable>
+              </View>
             </Pressable>
-          </View>
+          </Animated.View>
         </Pressable>
-      </Pressable>
+      </View>
     </Modal>
   );
 }
 
 const styles = StyleSheet.create({
-  backdrop: {
+  modalRoot: {
     flex: 1,
-    justifyContent: 'flex-end',
-    backgroundColor: 'rgba(2, 6, 23, 0.56)',
     paddingHorizontal: 14,
     paddingTop: 28,
   },
-  sheet: {
+  backdropVisual: {
+    backgroundColor: 'rgba(2, 6, 23, 0.56)',
+  },
+  dismissArea: {
+    flex: 1,
+    justifyContent: 'flex-end',
+  },
+  sheetMotion: {
     maxHeight: '88%',
+  },
+  sheet: {
+    maxHeight: '100%',
     borderWidth: 1,
     borderRadius: 28,
     overflow: 'hidden',
   },
+  handle: {
+    width: 38,
+    height: 4,
+    borderRadius: 2,
+    alignSelf: 'center',
+    marginTop: 10,
+    opacity: 0.9,
+  },
   headerRow: {
     flexDirection: 'row',
     alignItems: 'flex-start',
     gap: 12,
     paddingHorizontal: 18,
-    paddingTop: 18,
+    paddingTop: 12,
     paddingBottom: 12,
   },
   headerCopy: {
@@ -150,9 +243,9 @@ const styles = StyleSheet.create({
     fontWeight: '600',
   },
   closeButton: {
-    width: 40,
-    height: 40,
-    borderRadius: 20,
+    width: 44,
+    height: 44,
+    borderRadius: 22,
     borderWidth: 1,
     alignItems: 'center',
     justifyContent: 'center',
@@ -160,7 +253,7 @@ const styles = StyleSheet.create({
   content: {
     paddingHorizontal: 18,
     paddingVertical: 8,
-    gap: 18,
+    gap: 20,
   },
   footer: {
     borderTopWidth: 1,
@@ -169,7 +262,7 @@ const styles = StyleSheet.create({
     gap: 10,
   },
   footerButton: {
-    minHeight: 48,
+    minHeight: 50,
     borderRadius: 18,
     borderWidth: 1,
     alignItems: 'center',
@@ -192,7 +285,7 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.76,
-    transform: [{ scale: 0.99 }],
+    transform: [{ scale: 0.98 }],
   },
   disabled: {
     opacity: 0.45,
diff --git a/apps/mobile/src/components/library/LibrarySearchFilterRow.tsx b/apps/mobile/src/components/library/LibrarySearchFilterRow.tsx
index 50b97c4..13b3921 100644
--- a/apps/mobile/src/components/library/LibrarySearchFilterRow.tsx
+++ b/apps/mobile/src/components/library/LibrarySearchFilterRow.tsx
@@ -1,5 +1,5 @@
 import React from 'react';
-import { Pressable, StyleSheet, TextInput, View } from 'react-native';
+import { Keyboard, Pressable, StyleSheet, TextInput, View } from 'react-native';
 import type { SemanticColorName } from '@hellowhen/theme';
 import { useThemeTokens } from '../../providers/ThemeProvider';
 import { AppText } from '../AppText';
@@ -33,6 +33,11 @@ export function LibrarySearchFilterRow({
   const theme = useThemeTokens();
   const semantic = tone ? theme.semantic[tone] : theme.semantic.info;
 
+  function openFilters() {
+    Keyboard.dismiss();
+    onOpenFilters();
+  }
+
   return (
     <View style={styles.row}>
       <View style={[styles.searchBox, { backgroundColor: theme.color.surface, borderColor: theme.color.border }]}>
@@ -41,10 +46,12 @@ export function LibrarySearchFilterRow({
           accessibilityLabel={searchAccessibilityLabel ?? searchPlaceholder}
           value={query}
           onChangeText={onQueryChange}
+          onSubmitEditing={() => Keyboard.dismiss()}
           placeholder={searchPlaceholder}
           placeholderTextColor={theme.color.muted}
           autoCapitalize="none"
           autoCorrect={false}
+          maxLength={120}
           returnKeyType="search"
           style={[styles.searchInput, { color: theme.color.text }]}
         />
@@ -65,7 +72,7 @@ export function LibrarySearchFilterRow({
         accessibilityRole="button"
         accessibilityLabel={filterAccessibilityLabel}
         accessibilityState={{ expanded: filtersExpanded }}
-        onPress={onOpenFilters}
+        onPress={openFilters}
         style={({ pressed }) => [
           styles.filterButton,
           {
@@ -77,7 +84,7 @@ export function LibrarySearchFilterRow({
       >
         <MobileIcon name="filter" size={19} color={filterCount > 0 ? semantic.text : theme.color.text} decorative />
         {filterCount > 0 ? (
-          <View style={[styles.countBadge, { backgroundColor: semantic.bg }]}>
+          <View accessibilityLiveRegion="polite" style={[styles.countBadge, { backgroundColor: semantic.bg }]}>
             <AppText style={[styles.countText, { color: semantic.onBg }]}>{filterCount > 99 ? '99+' : filterCount}</AppText>
           </View>
         ) : null}
@@ -145,6 +152,6 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.76,
-    transform: [{ scale: 0.98 }],
+    transform: [{ scale: 0.97 }],
   },
 });
diff --git a/apps/mobile/src/components/library/SlidingSegmentedControl.tsx b/apps/mobile/src/components/library/SlidingSegmentedControl.tsx
index 2caf694..0587b01 100644
--- a/apps/mobile/src/components/library/SlidingSegmentedControl.tsx
+++ b/apps/mobile/src/components/library/SlidingSegmentedControl.tsx
@@ -1,5 +1,5 @@
 import React, { useEffect, useMemo, useRef, useState } from 'react';
-import { Animated, Pressable, StyleSheet, View, type LayoutChangeEvent, type StyleProp, type ViewStyle } from 'react-native';
+import { Animated, Easing, Pressable, StyleSheet, View, type LayoutChangeEvent, type StyleProp, type ViewStyle } from 'react-native';
 import type { SemanticColorName } from '@hellowhen/theme';
 import { useReducedMotion } from '../../hooks/useReducedMotion';
 import { useThemeTokens } from '../../providers/ThemeProvider';
@@ -39,16 +39,20 @@ function SegmentLabel({
   const progress = useRef(new Animated.Value(active ? 1 : 0)).current;
 
   useEffect(() => {
+    progress.stopAnimation();
     if (reducedMotion) {
       progress.setValue(active ? 1 : 0);
       return;
     }
 
-    Animated.timing(progress, {
+    const animation = Animated.timing(progress, {
       toValue: active ? 1 : 0,
-      duration: 150,
+      duration: 140,
+      easing: Easing.out(Easing.cubic),
       useNativeDriver: false,
-    }).start();
+    });
+    animation.start();
+    return () => animation.stop();
   }, [active, progress, reducedMotion]);
 
   const color = progress.interpolate({
@@ -68,7 +72,7 @@ export function SlidingSegmentedControl<T extends string>({
   options,
   onChange,
   tone,
-  animationDuration = 240,
+  animationDuration = 250,
   style,
 }: SlidingSegmentedControlProps<T>) {
   const theme = useThemeTokens();
@@ -84,20 +88,25 @@ export function SlidingSegmentedControl<T extends string>({
   }, [containerWidth, options.length]);
 
   useEffect(() => {
+    animatedIndex.stopAnimation();
     if (reducedMotion) {
       animatedIndex.setValue(activeIndex);
       return;
     }
 
-    Animated.timing(animatedIndex, {
+    const animation = Animated.timing(animatedIndex, {
       toValue: activeIndex,
       duration: animationDuration,
+      easing: Easing.out(Easing.cubic),
       useNativeDriver: true,
-    }).start();
+    });
+    animation.start();
+    return () => animation.stop();
   }, [activeIndex, animatedIndex, animationDuration, reducedMotion]);
 
   function handleLayout(event: LayoutChangeEvent) {
-    setContainerWidth(event.nativeEvent.layout.width);
+    const nextWidth = event.nativeEvent.layout.width;
+    setContainerWidth((current) => Math.abs(current - nextWidth) < 0.5 ? current : nextWidth);
   }
 
   const translateX = animatedIndex.interpolate({
@@ -141,7 +150,9 @@ export function SlidingSegmentedControl<T extends string>({
             accessibilityLabel={option.accessibilityLabel ?? option.label}
             accessibilityState={{ selected: active, disabled: Boolean(option.disabled) }}
             disabled={option.disabled}
-            onPress={() => onChange(option.value)}
+            onPress={() => {
+              if (!active) onChange(option.value);
+            }}
             style={({ pressed }) => [
               styles.segment,
               pressed && !option.disabled && styles.pressed,
@@ -201,6 +212,7 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.72,
+    transform: [{ scale: 0.985 }],
   },
   disabled: {
     opacity: 0.45,
diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 9646460..896c402 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -2463,6 +2463,7 @@ function PlaceList({ scope, navigation, scrollProps, headerContent }: { scope: P
         contentContainerStyle={[scrollProps?.contentInsetStyle, styles.listContent]}
         showsVerticalScrollIndicator={false}
         keyboardShouldPersistTaps="handled"
+        keyboardDismissMode={Platform.OS === 'ios' ? 'interactive' : 'on-drag'}
         refreshControl={<RefreshControl refreshing={refreshing} onRefresh={() => { void load({ refresh: true }); }} />}
       >
         {libraryControls}
diff --git a/apps/mobile/src/features/trade/MyNeedsScreen.tsx b/apps/mobile/src/features/trade/MyNeedsScreen.tsx
index 07b869a..28ee134 100644
--- a/apps/mobile/src/features/trade/MyNeedsScreen.tsx
+++ b/apps/mobile/src/features/trade/MyNeedsScreen.tsx
@@ -1,5 +1,5 @@
 import React, { useCallback, useMemo, useState } from 'react';
-import { Pressable, RefreshControl, ScrollView, StyleSheet, View } from 'react-native';
+import { Platform, Pressable, RefreshControl, ScrollView, StyleSheet, View } from 'react-native';
 import { useFocusEffect, useNavigation } from '@react-navigation/native';
 import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
 import type { InventoryTemplateDto } from '@hellowhen/contracts';
@@ -136,7 +136,7 @@ export function MyNeedsScreen() {
   return (
     <AppSmartHeaderScreen header={header} resetKey={sourceTab}>
       {(scrollProps) => (
-        <ScrollView {...scrollProps.scrollViewProps} contentContainerStyle={[scrollProps.contentInsetStyle, styles.content]} showsVerticalScrollIndicator={false} refreshControl={<RefreshControl refreshing={activeLoading} onRefresh={() => { void loadItems(); void loadTemplates(); setFolderRefreshKey((key) => key + 1); }} />}>
+        <ScrollView {...scrollProps.scrollViewProps} contentContainerStyle={[scrollProps.contentInsetStyle, styles.content]} showsVerticalScrollIndicator={false} keyboardShouldPersistTaps="handled" keyboardDismissMode={Platform.OS === 'ios' ? 'interactive' : 'on-drag'} refreshControl={<RefreshControl refreshing={activeLoading} onRefresh={() => { void loadItems(); void loadTemplates(); setFolderRefreshKey((key) => key + 1); }} />}>
           <SourceTabs value={sourceTab} onChange={(nextTab) => { setSourceTab(nextTab); setNotice(null); setCreatedNeed(null); }} />
           {sourceTab === 'mine' ? <AppText style={[styles.subtitle, { color: theme.color.muted }]}>{t('inventory.empty.needNativeBody')}</AppText> : null}
           {notice ? <InfoNotice tone="success" title={t('inventory.messages.starterSaved')} body={notice} /> : null}
diff --git a/apps/mobile/src/features/trade/MyOffersScreen.tsx b/apps/mobile/src/features/trade/MyOffersScreen.tsx
index 1870530..47dec6b 100644
--- a/apps/mobile/src/features/trade/MyOffersScreen.tsx
+++ b/apps/mobile/src/features/trade/MyOffersScreen.tsx
@@ -1,5 +1,5 @@
 import React, { useCallback, useMemo, useState } from 'react';
-import { Pressable, RefreshControl, ScrollView, StyleSheet, View } from 'react-native';
+import { Platform, Pressable, RefreshControl, ScrollView, StyleSheet, View } from 'react-native';
 import { useFocusEffect, useNavigation } from '@react-navigation/native';
 import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
 import type { InventoryTemplateDto } from '@hellowhen/contracts';
@@ -136,7 +136,7 @@ export function MyOffersScreen() {
   return (
     <AppSmartHeaderScreen header={header} resetKey={sourceTab}>
       {(scrollProps) => (
-        <ScrollView {...scrollProps.scrollViewProps} contentContainerStyle={[scrollProps.contentInsetStyle, styles.content]} showsVerticalScrollIndicator={false} refreshControl={<RefreshControl refreshing={activeLoading} onRefresh={() => { void loadItems(); void loadTemplates(); setFolderRefreshKey((key) => key + 1); }} />}>
+        <ScrollView {...scrollProps.scrollViewProps} contentContainerStyle={[scrollProps.contentInsetStyle, styles.content]} showsVerticalScrollIndicator={false} keyboardShouldPersistTaps="handled" keyboardDismissMode={Platform.OS === 'ios' ? 'interactive' : 'on-drag'} refreshControl={<RefreshControl refreshing={activeLoading} onRefresh={() => { void loadItems(); void loadTemplates(); setFolderRefreshKey((key) => key + 1); }} />}>
           <SourceTabs value={sourceTab} onChange={(nextTab) => { setSourceTab(nextTab); setNotice(null); setCreatedOffer(null); }} />
           {sourceTab === 'mine' ? <AppText style={[styles.subtitle, { color: theme.color.muted }]}>{t('inventory.empty.offerNativeBody')}</AppText> : null}
           {notice ? <InfoNotice tone="success" title={t('inventory.messages.starterSaved')} body={notice} /> : null}
diff --git a/apps/mobile/src/features/trade/components/ManagedInventoryLibraryFilters.tsx b/apps/mobile/src/features/trade/components/ManagedInventoryLibraryFilters.tsx
index b57d05f..fd55050 100644
--- a/apps/mobile/src/features/trade/components/ManagedInventoryLibraryFilters.tsx
+++ b/apps/mobile/src/features/trade/components/ManagedInventoryLibraryFilters.tsx
@@ -363,7 +363,7 @@ const styles = StyleSheet.create({
     fontWeight: '700',
   },
   clearButton: {
-    minHeight: 42,
+    minHeight: 44,
     marginTop: 3,
     borderRadius: 16,
     borderWidth: 1,
@@ -377,5 +377,6 @@ const styles = StyleSheet.create({
   },
   pressed: {
     opacity: 0.78,
+    transform: [{ scale: 0.985 }],
   },
 });
diff --git a/apps/mobile/src/features/trade/components/StarterInventoryLibrary.tsx b/apps/mobile/src/features/trade/components/StarterInventoryLibrary.tsx
index 279de47..52d1ae0 100644
--- a/apps/mobile/src/features/trade/components/StarterInventoryLibrary.tsx
+++ b/apps/mobile/src/features/trade/components/StarterInventoryLibrary.tsx
@@ -252,6 +252,11 @@ export function StarterInventoryLibrary({
   };
   const closeFilters = () => setFilterSheetVisible(false);
   const resetDraftFilters = () => setDraftFilters({ ...defaultStarterFilters });
+  const clearAll = () => {
+    setQuery('');
+    setFilters({ ...defaultStarterFilters });
+    setDraftFilters({ ...defaultStarterFilters });
+  };
   const applyDraftFilters = () => {
     setFilters(draftFilters);
     setFilterSheetVisible(false);
@@ -343,6 +348,19 @@ export function StarterInventoryLibrary({
           <SemanticBadge label={t('inventory.labels.starterLibrary')} tone="instruction" />
           <AppText style={styles.emptyTitle}>{emptyTitle ?? t('inventory.empty.noStarterFound', { items: plural })}</AppText>
           <AppText style={[styles.emptyBody, { color: theme.color.muted }]}>{emptyBody ?? t('inventory.empty.tryAnotherSearch')}</AppText>
+          {needle || activeFilterCount > 0 ? (
+            <Pressable
+              accessibilityRole="button"
+              onPress={clearAll}
+              style={({ pressed }) => [
+                styles.emptyAction,
+                { backgroundColor: theme.semantic[tone].softBg, borderColor: theme.semantic[tone].border },
+                pressed && styles.pressed,
+              ]}
+            >
+              <AppText style={[styles.emptyActionText, { color: theme.semantic[tone].text }]}>{t('inventory.libraryFilters.clearAll')}</AppText>
+            </Pressable>
+          ) : null}
         </AppCard>
       ) : null}
 
@@ -455,6 +473,8 @@ const styles = StyleSheet.create({
   emptyCard: { gap: 10 },
   emptyTitle: { fontSize: 20, lineHeight: 24, fontWeight: '900', letterSpacing: -0.25 },
   emptyBody: { lineHeight: 20, fontWeight: '700' },
+  emptyAction: { minHeight: 44, alignSelf: 'flex-start', borderRadius: 16, borderWidth: 1, paddingHorizontal: 14, alignItems: 'center', justifyContent: 'center' },
+  emptyActionText: { fontSize: 13, fontWeight: '900' },
   disabled: { opacity: 0.55 },
-  pressed: { opacity: 0.78, transform: [{ scale: 0.99 }] },
+  pressed: { opacity: 0.78, transform: [{ scale: 0.985 }] },
 });
diff --git a/apps/mobile/src/hooks/useReducedMotion.ts b/apps/mobile/src/hooks/useReducedMotion.ts
index 0309c86..5871b94 100644
--- a/apps/mobile/src/hooks/useReducedMotion.ts
+++ b/apps/mobile/src/hooks/useReducedMotion.ts
@@ -2,11 +2,12 @@ import { useEffect, useState } from 'react';
 import { AccessibilityInfo } from 'react-native';
 
 /**
- * Mirrors the platform Reduce Motion preference so shared motion primitives
- * can fall back to an immediate state change when animation is not desired.
+ * Mirrors the platform Reduce Motion preference. Motion is treated as reduced
+ * until the async platform preference is known, so users who disable motion do
+ * not see a brief animation during initial mount.
  */
 export function useReducedMotion() {
-  const [reducedMotion, setReducedMotion] = useState(false);
+  const [reducedMotion, setReducedMotion] = useState(true);
 
   useEffect(() => {
     let mounted = true;
