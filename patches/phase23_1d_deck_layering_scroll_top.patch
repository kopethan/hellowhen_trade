diff -ruN old/apps/web/src/app/globals.css new/apps/web/src/app/globals.css
--- apps/web/src/app/globals.css	2026-05-10 18:17:09.994861557 +0000
+++ apps/web/src/app/globals.css	2026-05-10 18:17:10.011821520 +0000
@@ -111,21 +111,24 @@
 
 .web-app-viewport { min-height: 100dvh; display: grid; place-items: center; padding: 0; }
 .web-app-shell { position: relative; width: min(100vw, var(--app-shell-width)); height: 100dvh; min-height: 100dvh; overflow: hidden; background: var(--app-bg); box-shadow: var(--app-shadow); }
-.web-top-header { position: fixed; z-index: 20; top: 0; left: 50%; width: min(100vw, var(--app-shell-width)); min-height: var(--app-header-height); display: flex; align-items: flex-end; gap: 12px; padding: calc(12px + env(safe-area-inset-top)) 18px 14px; transform: translateX(-50%); background: color-mix(in srgb, var(--app-bg) 90%, transparent); border-bottom: 1px solid color-mix(in srgb, var(--app-border) 80%, transparent); backdrop-filter: blur(18px); }
+.web-top-header { position: fixed; z-index: 80; top: 0; left: 50%; width: min(100vw, var(--app-shell-width)); min-height: var(--app-header-height); display: flex; align-items: flex-end; gap: 12px; padding: calc(12px + env(safe-area-inset-top)) 18px 14px; transform: translateX(-50%); background: color-mix(in srgb, var(--app-bg) 90%, transparent); border-bottom: 1px solid color-mix(in srgb, var(--app-border) 80%, transparent); backdrop-filter: blur(18px); }
 .web-top-header--root { justify-content: space-between; }
 .web-top-header--nested { align-items: center; padding-bottom: 12px; }
 .web-top-header--nested h1 { font-size: 22px; letter-spacing: -0.04em; }
 .web-header-beta-badge { position: absolute; top: calc(10px + env(safe-area-inset-top)); left: 50%; transform: translateX(-50%); display: inline-flex; align-items: center; justify-content: center; min-height: 24px; padding: 0 11px; border: 1px solid var(--instruction-border); border-radius: 999px; background: color-mix(in srgb, var(--instruction-bg) 82%, var(--app-surface)); color: var(--instruction-text); font-size: 10px; font-weight: 950; letter-spacing: 0.16em; line-height: 1; box-shadow: var(--app-control-shadow); }
 .web-kicker { margin: 0 0 4px; color: var(--app-text-soft); font-size: 11px; font-weight: 900; letter-spacing: 0.12em; line-height: 1; text-transform: uppercase; }
 .web-back-button { display: inline-grid; place-items: center; flex: 0 0 auto; width: 42px; height: 42px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface); color: var(--app-text); font-size: 32px; font-weight: 600; line-height: 1; box-shadow: var(--app-control-shadow); }
-.web-scroll-area { height: 100%; overflow-y: auto; padding: calc(var(--app-header-height) + 16px) 16px calc(var(--app-tab-height) + 22px + env(safe-area-inset-bottom)); scrollbar-width: none; }
+.web-scroll-area { position: relative; z-index: 1; height: 100%; overflow-y: auto; padding: calc(var(--app-header-height) + 16px) 16px calc(var(--app-tab-height) + 22px + env(safe-area-inset-bottom)); scrollbar-width: none; }
 .web-scroll-area::-webkit-scrollbar { display: none; }
-.web-bottom-tabs { position: fixed; z-index: 30; bottom: 0; left: 50%; width: min(100vw, var(--app-shell-width)); min-height: calc(var(--app-tab-height) + env(safe-area-inset-bottom)); display: grid; grid-template-columns: repeat(4, 1fr); gap: 4px; padding: 8px 10px calc(8px + env(safe-area-inset-bottom)); transform: translateX(-50%); background: color-mix(in srgb, var(--app-bg) 92%, transparent); border-top: 1px solid color-mix(in srgb, var(--app-border) 85%, transparent); backdrop-filter: blur(18px); }
+.web-bottom-tabs { position: fixed; z-index: 90; bottom: 0; left: 50%; width: min(100vw, var(--app-shell-width)); min-height: calc(var(--app-tab-height) + env(safe-area-inset-bottom)); display: grid; grid-template-columns: repeat(4, 1fr); gap: 4px; padding: 8px 10px calc(8px + env(safe-area-inset-bottom)); transform: translateX(-50%); background: color-mix(in srgb, var(--app-bg) 92%, transparent); border-top: 1px solid color-mix(in srgb, var(--app-border) 85%, transparent); backdrop-filter: blur(18px); }
 .web-icon { display: inline-block; flex: 0 0 auto; width: var(--web-icon-size, 20px); height: var(--web-icon-size, 20px); background-color: currentColor; -webkit-mask: var(--web-icon-url) center / contain no-repeat; mask: var(--web-icon-url) center / contain no-repeat; }
 .web-tab { display: grid; place-items: center; gap: 3px; min-width: 0; border-radius: 18px; color: var(--app-text-muted); font-size: 11px; font-weight: 900; }
 .web-tab__icon { display: grid; place-items: center; width: 24px; height: 24px; border-radius: 999px; color: inherit; line-height: 1; }
 .web-tab--active { background: var(--app-surface); color: var(--app-text); box-shadow: var(--app-control-shadow); }
 
+.web-scroll-top-button { position: fixed; z-index: 70; right: max(18px, calc((100vw - var(--app-shell-width)) / 2 + 18px)); bottom: calc(var(--app-tab-height) + env(safe-area-inset-bottom) + 18px); display: grid; place-items: center; width: 44px; height: 44px; border: 1px solid color-mix(in srgb, var(--app-border-strong) 78%, transparent); border-radius: 999px; background: color-mix(in srgb, var(--app-surface) 94%, transparent); color: var(--app-text); padding: 0; font-size: 22px; font-weight: 950; line-height: 1; box-shadow: var(--app-control-shadow); opacity: 0; pointer-events: none; transform: translateY(10px) scale(0.96); transition: opacity 160ms ease, transform 160ms ease; backdrop-filter: blur(18px); }
+.web-scroll-top-button.is-visible { opacity: 1; pointer-events: auto; transform: translateY(0) scale(1); }
+
 .utility-shell { width: min(1120px, calc(100vw - 32px)); margin: 0 auto; padding: 24px 0 48px; }
 .mobile-page { display: grid; gap: 16px; }
 .page-intro { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }
@@ -441,7 +444,7 @@
 
 .trade-deck-grid { display: grid; grid-template-columns: minmax(0, 1fr); gap: 22px; align-items: start; }
 
-.square-stack-deck { position: relative; width: 100%; padding: 0 28px 34px 0; --deck-visible-back-layers: 0; --deck-max-back-layers: 3; --deck-advance-control-outset: 6px; }
+.square-stack-deck { position: relative; z-index: 0; isolation: isolate; width: 100%; padding: 0 28px 34px 0; --deck-visible-back-layers: 0; --deck-max-back-layers: 3; --deck-advance-control-outset: 6px; }
 .square-stack-deck__surface { position: relative; width: 100%; aspect-ratio: 1 / 1; outline: none; cursor: pointer; touch-action: pan-y; user-select: none; }
 .square-stack-deck__surface:focus-visible { outline: 3px solid var(--proposal-border); outline-offset: 8px; border-radius: 34px; }
 .square-stack-deck__layer { position: absolute; inset: 0; overflow: hidden; border: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent); border-radius: 34px; background: var(--app-surface); color: var(--app-text); box-shadow: var(--app-card-shadow-strong); transition: transform 210ms cubic-bezier(0.2, 0.9, 0.2, 1), opacity 210ms ease; will-change: transform, opacity; }
@@ -490,7 +493,7 @@
   body { background: radial-gradient(circle at top left, color-mix(in srgb, var(--app-surface) 70%, transparent), transparent 34rem), var(--app-bg-muted); }
   .web-app-viewport { display: block; min-height: 100dvh; padding: 0; }
   .web-app-shell { width: 100%; height: auto; min-height: 100dvh; overflow: visible; background: var(--app-bg-muted); box-shadow: none; }
-  .web-top-header { position: sticky; top: 0; left: auto; width: 100%; min-height: 82px; transform: none; display: flex; align-items: center; justify-content: space-between; padding: 16px max(32px, calc((100vw - 1440px) / 2 + 32px)); background: color-mix(in srgb, var(--app-bg-muted) 88%, transparent); border-bottom: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent); }
+  .web-top-header { position: sticky; z-index: 80; top: 0; left: auto; width: 100%; min-height: 82px; transform: none; display: flex; align-items: center; justify-content: space-between; padding: 16px max(32px, calc((100vw - 1440px) / 2 + 32px)); background: color-mix(in srgb, var(--app-bg-muted) 88%, transparent); border-bottom: 1px solid color-mix(in srgb, var(--app-border) 70%, transparent); }
   .web-top-header--nested { align-items: center; }
   .web-top-header--nested h1 { font-size: 26px; }
   .web-kicker { margin-bottom: 6px; }
@@ -499,8 +502,9 @@
   .web-desktop-nav__link { display: inline-flex; align-items: center; justify-content: center; gap: 7px; min-height: 42px; border: 1px solid transparent; border-radius: 999px; color: var(--app-text-muted); padding: 0 16px; font-size: 14px; font-weight: 900; }
   .web-desktop-nav__link:hover { color: var(--app-text); background: color-mix(in srgb, var(--app-surface) 72%, transparent); }
   .web-desktop-nav__link.is-active { border-color: var(--app-border); background: var(--app-surface); color: var(--app-text); box-shadow: var(--app-control-shadow); }
-  .web-scroll-area { height: auto; overflow: visible; max-width: var(--web-content-width); margin: 0 auto; padding: 28px 32px 72px; scrollbar-width: auto; }
+  .web-scroll-area { position: relative; z-index: 1; height: auto; overflow: visible; max-width: var(--web-content-width); margin: 0 auto; padding: 28px 32px 72px; scrollbar-width: auto; }
   .web-bottom-tabs { display: none; }
+  .web-scroll-top-button { display: none; }
   .mobile-page:not(.trade-feed-page), .wallet-page, .trade-detail-page { max-width: 760px; margin-right: auto; margin-left: auto; }
   .support-mobile-page { max-width: min(1120px, 100%); margin-right: auto; margin-left: auto; grid-template-columns: minmax(320px, 0.9fr) minmax(420px, 1.1fr); align-items: start; }
   .support-mobile-page > .notice-box { grid-column: 1 / -1; }
@@ -520,7 +524,7 @@
 }
 
 @media (max-width: 759px) {
-  .square-stack-deck { padding-right: 24px; padding-bottom: 30px; }
+  .square-stack-deck { padding: 10px 24px 30px 0; }
   .square-stack-deck__control { display: none !important; width: 42px; height: 42px; }
   .square-stack-deck__control--prev { top: -13px; left: -13px; }
   .square-stack-deck--native-mobile .square-stack-deck__surface { touch-action: none; overscroll-behavior: contain; }
diff -ruN old/apps/web/src/components/WebMobileShell.tsx new/apps/web/src/components/WebMobileShell.tsx
--- apps/web/src/components/WebMobileShell.tsx	2026-05-10 18:17:09.964008643 +0000
+++ apps/web/src/components/WebMobileShell.tsx	2026-05-10 18:17:09.979610511 +0000
@@ -1,6 +1,6 @@
 'use client';
 
-import type { ReactNode } from 'react';
+import { type ReactNode, useEffect, useRef, useState } from 'react';
 import { usePathname } from 'next/navigation';
 import { isUtilityRoute } from '../lib/webRoutes';
 import { WebBottomTabs } from './WebBottomTabs';
@@ -9,6 +9,28 @@
 export function WebMobileShell({ children }: { children: ReactNode }) {
   const pathname = usePathname() || '/';
   const utility = isUtilityRoute(pathname);
+  const scrollAreaRef = useRef<HTMLDivElement | null>(null);
+  const [showScrollTop, setShowScrollTop] = useState(false);
+
+  useEffect(() => {
+    if (utility) return;
+    const scrollArea = scrollAreaRef.current;
+    if (!scrollArea) return;
+
+    const updateScrollTopVisibility = () => {
+      setShowScrollTop(scrollArea.scrollTop > 420);
+    };
+
+    updateScrollTopVisibility();
+    scrollArea.addEventListener('scroll', updateScrollTopVisibility, { passive: true });
+    return () => scrollArea.removeEventListener('scroll', updateScrollTopVisibility);
+  }, [pathname, utility]);
+
+  function scrollToTop() {
+    const scrollArea = scrollAreaRef.current;
+    if (!scrollArea) return;
+    scrollArea.scrollTo({ top: 0, behavior: 'smooth' });
+  }
 
   if (utility) {
     return <main className="utility-shell">{children}</main>;
@@ -18,7 +40,15 @@
     <main className="web-app-viewport">
       <section className="web-app-shell" aria-label="Hellowhen Trade web app">
         <WebTopHeader />
-        <div className="web-scroll-area">{children}</div>
+        <div ref={scrollAreaRef} className="web-scroll-area">{children}</div>
+        <button
+          type="button"
+          className={showScrollTop ? 'web-scroll-top-button is-visible' : 'web-scroll-top-button'}
+          onClick={scrollToTop}
+          aria-label="Back to top"
+        >
+          ↑
+        </button>
         <WebBottomTabs />
       </section>
     </main>
