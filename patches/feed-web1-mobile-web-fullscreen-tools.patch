diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index ee5b538..46d6fd5 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -487,6 +487,8 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-filter-pill { cursor: pointer; }
 .trade-feed-controls .trade-filter-panel { position: relative; z-index: 185; grid-column: 1 / -1; display: grid; gap: 12px; border: 1px solid var(--app-border); border-radius: 24px; background: color-mix(in srgb, var(--app-surface) 96%, transparent); padding: 14px; box-shadow: var(--app-card-shadow); backdrop-filter: blur(18px); }
 .trade-filter-panel__header, .trade-filter-panel__search { display: none; }
+.trade-filter-panel--filter .trade-filter-panel__header { display: flex; align-items: center; justify-content: space-between; gap: 14px; }
+.trade-filter-panel--filter .trade-filter-panel__search { display: grid; gap: 8px; }
 .trade-filter-panel label { display: grid; gap: 7px; color: var(--app-text); font-size: 13px; font-weight: 900; }
 .trade-filter-panel__search-input { position: relative; display: block; }
 .trade-filter-panel__search-input input { min-height: 48px; border-radius: 18px; padding-left: 42px; }
@@ -499,6 +501,54 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-search-suggestion span { min-width: 0; overflow: hidden; font-size: 14px; font-weight: 900; text-overflow: ellipsis; white-space: nowrap; }
 .trade-search-suggestion small { flex: 0 0 auto; color: var(--app-text-muted); font-size: 11px; font-weight: 900; text-transform: uppercase; }
 .trade-filter-actions { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
+.trade-feed-controls .trade-filter-panel--filter {
+  position: fixed;
+  inset: 0;
+  z-index: 220;
+  grid-column: auto;
+  grid-template-columns: minmax(0, 680px);
+  justify-content: center;
+  align-content: start;
+  gap: 14px;
+  overflow-y: auto;
+  border: 0;
+  border-radius: 0;
+  background: var(--app-bg);
+  padding: calc(18px + env(safe-area-inset-top)) clamp(16px, 5vw, 32px) calc(18px + env(safe-area-inset-bottom));
+  box-shadow: none;
+  backdrop-filter: none;
+  overscroll-behavior: contain;
+}
+.trade-feed-controls .trade-filter-panel--filter > * { min-width: 0; }
+.trade-filter-panel--filter .trade-filter-panel__header h2,
+.trade-activity-panel__header h2 { margin: 3px 0 0; color: var(--app-text); font-size: clamp(24px, 5vw, 34px); line-height: 1; letter-spacing: -0.05em; }
+.trade-filter-panel__close { display: grid; place-items: center; width: 44px; height: 44px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text); padding: 0; font-size: 28px; line-height: 1; box-shadow: none; cursor: pointer; }
+.trade-filter-panel--filter .trade-filter-panel__search > span { color: var(--app-text); font-size: 13px; font-weight: 950; }
+.trade-filter-panel--filter .trade-search-suggestions { position: static; margin-top: 8px; box-shadow: var(--app-control-shadow); }
+.trade-activity-panel {
+  position: fixed;
+  inset: 0;
+  z-index: 220;
+  display: grid;
+  grid-template-rows: auto auto minmax(0, 1fr);
+  gap: 14px;
+  width: auto;
+  max-height: none;
+  overflow: hidden;
+  border: 0;
+  border-radius: 0;
+  background: var(--app-bg);
+  padding: calc(18px + env(safe-area-inset-top)) clamp(16px, 5vw, 32px) calc(18px + env(safe-area-inset-bottom));
+  box-shadow: none;
+  backdrop-filter: none;
+  overscroll-behavior: contain;
+}
+.trade-activity-panel > * { width: min(960px, 100%); min-width: 0; margin-inline: auto; }
+.trade-activity-panel__header { background: var(--app-bg); }
+.trade-activity-panel__body { min-height: 0; overflow-y: auto; overscroll-behavior: contain; padding-right: 2px; }
+.trade-activity-panel .trade-mine-panel { width: 100%; }
+.trade-tools-backdrop { display: none; }
+
 .feed-status-row { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
 .feed-status-row p { margin: 0; font-size: 13px; font-weight: 800; }
 .feed-status-actions { display: inline-flex; align-items: center; justify-content: flex-end; gap: 8px; flex-wrap: wrap; }
@@ -807,13 +857,16 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 html.trade-tools-open, html.trade-tools-open body { overflow: hidden; }
 
 @media (max-width: 759px) {
-  .trade-feed-controls { grid-template-columns: auto auto auto; align-items: center; justify-content: end; }
+  .trade-feed-controls { grid-template-columns: minmax(0, 1fr) 44px 44px; align-items: center; justify-content: stretch; gap: 10px; width: 100%; }
+  .trade-filter-pill { width: 100%; min-width: 0; justify-content: center; padding: 0 14px; }
+  .trade-filter-pill span { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+  .trade-activity-pill, .trade-create-pill { flex: 0 0 44px; width: 44px; height: 44px; padding: 0; }
   .trade-search-open-pill { display: inline-flex; min-width: 0; justify-content: flex-start; }
   .trade-search-open-pill span { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
   .trade-search-combobox--desktop { display: none; }
   .trade-search-field--desktop { display: none; }
-  .trade-tools-backdrop { z-index: 170; background: color-mix(in srgb, var(--app-bg) 78%, rgba(15, 23, 42, 0.20)); backdrop-filter: blur(6px); }
-  .trade-feed-controls .trade-filter-panel { position: fixed; inset: 0; z-index: 180; display: grid; grid-template-rows: auto auto auto auto auto 1fr auto; align-content: start; gap: 14px; overflow-y: auto; border: 0; border-radius: 0; background: var(--app-bg); padding: calc(14px + env(safe-area-inset-top)) 14px calc(16px + env(safe-area-inset-bottom)); box-shadow: none; overscroll-behavior: contain; }
+  .trade-tools-backdrop { display: none; }
+  .trade-feed-controls .trade-filter-panel--filter { grid-template-columns: minmax(0, 1fr); padding: calc(14px + env(safe-area-inset-top)) 14px calc(16px + env(safe-area-inset-bottom)); }
   .trade-filter-panel__header { display: flex; align-items: center; justify-content: space-between; gap: 14px; padding-bottom: 2px; }
   .trade-filter-panel__header h2 { margin: 3px 0 0; color: var(--app-text); font-size: 25px; line-height: 1; letter-spacing: -0.05em; }
   .trade-filter-panel__close { display: grid; place-items: center; width: 42px; height: 42px; border: 1px solid var(--app-border); border-radius: 999px; background: var(--app-surface-soft); color: var(--app-text); padding: 0; font-size: 28px; line-height: 1; box-shadow: none; }
@@ -1450,9 +1503,9 @@ html[data-theme="dark"] .trade-poster-card.has-fallback h2 {
   .trade-create-pill { width: auto; min-width: 44px; padding: 0 14px; }
   .trade-create-pill span { display: inline; }
   .inventory-controls { width: 100%; margin-right: auto; margin-left: auto; padding: 0 0 4px; background: transparent; }
-  .trade-feed-controls .trade-filter-panel { position: absolute; top: calc(100% + 10px); right: 0; width: min(390px, calc(100vw - 32px)); grid-column: auto; border-radius: 28px; }
-  .trade-activity-panel { position: fixed; inset: calc(var(--app-header-height) + 18px) max(24px, calc((100vw - var(--web-content-width)) / 2 + 24px)) 24px auto; z-index: 190; display: grid; grid-template-rows: auto auto minmax(0, 1fr); width: min(460px, calc(100vw - 48px)); max-height: none; overflow: hidden; border: 1px solid var(--app-border); border-radius: 30px; background: color-mix(in srgb, var(--app-surface) 98%, transparent); padding: 16px; box-shadow: var(--app-card-shadow-strong); backdrop-filter: blur(20px); }
-  .trade-activity-panel__body { overflow-y: auto; padding-right: 2px; overscroll-behavior: contain; }
+  .trade-feed-controls .trade-filter-panel--filter { grid-template-columns: minmax(0, 720px); padding: calc(26px + env(safe-area-inset-top)) 32px calc(30px + env(safe-area-inset-bottom)); }
+  .trade-activity-panel { padding: calc(26px + env(safe-area-inset-top)) 32px calc(30px + env(safe-area-inset-bottom)); }
+  .trade-activity-panel > * { width: min(920px, 100%); }
   .feed-status-row { width: min(880px, 100%); margin: 0 auto; }
   .trade-deck-grid { --trade-deck-max: 360px; --trade-deck-pad-left: 16px; --trade-deck-pad-right: 36px; --trade-deck-col: calc(var(--trade-deck-max) + var(--trade-deck-pad-left) + var(--trade-deck-pad-right)); display: grid; grid-template-columns: repeat(auto-fit, minmax(min(100%, var(--trade-deck-col)), var(--trade-deck-col))); justify-content: center; align-items: start; column-gap: clamp(22px, 3vw, 48px); row-gap: clamp(26px, 4vw, 56px); width: 100%; margin: 2px auto 0; padding: 6px 0 24px; }
   .trade-stack-deck { width: 100%; max-width: var(--trade-deck-col); padding: 16px 36px 38px 16px; }
