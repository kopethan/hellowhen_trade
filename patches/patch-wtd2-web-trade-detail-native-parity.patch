diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index f58bf80..c572957 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -548,7 +548,7 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-section-heading h2, .trade-section-heading h3 { margin: 0; }
 .trade-detail-page--social .trade-social-section > p:not(.meta):not(.trade-how-it-works) { color: var(--app-text); font-size: 15px; font-weight: 700; line-height: 1.62; }
 .trade-details-modern-section { gap: 15px; }
-.trade-next-step-panel { display: grid; gap: 10px; border-top: 1px solid color-mix(in srgb, var(--app-border) 72%, transparent); padding-top: 14px; }
+.trade-next-step-panel { display: grid; gap: 10px; border-top: 1px solid color-mix(in srgb, var(--app-border) 72%, transparent); margin-top: 2px; padding-top: 15px; }
 .trade-next-step-panel h3 { margin: 0; font-size: 18px; letter-spacing: -0.035em; }
 .trade-next-step-panel p { margin: 0; }
 .icon-heading { display: inline-flex; align-items: center; gap: 8px; }
@@ -571,6 +571,17 @@ button.warning { background: var(--warning-bg); color: var(--warning-text); bord
 .trade-detail-list--chips dt, .trade-detail-list--chips dd { display: block; width: 100%; text-align: left; }
 .trade-detail-list--chips dt { font-size: 11px; letter-spacing: 0.04em; text-transform: uppercase; }
 .trade-detail-list--chips dd { font-size: 14px; line-height: 1.25; }
+.trade-next-step-copy { margin: 0; color: var(--app-text); font-size: 16px; font-weight: 850; line-height: 1.45; }
+.trade-detail-list--native { overflow: hidden; border: 1px solid color-mix(in srgb, var(--app-border) 86%, transparent); border-radius: 22px; background: color-mix(in srgb, var(--app-surface) 74%, transparent); }
+.trade-detail-list--native div, .trade-detail-list--native div:first-child { display: flex; align-items: center; justify-content: space-between; gap: 18px; min-width: 0; border-top: 1px solid color-mix(in srgb, var(--app-border) 60%, transparent); padding: 14px 16px; }
+.trade-detail-list--native div:first-child { border-top: 0; }
+.trade-detail-list--native dt, .trade-detail-list--native dd { display: block; width: auto; }
+.trade-detail-list--native dt { flex: 0 1 auto; color: var(--app-text-muted); font-size: 13px; font-weight: 850; letter-spacing: 0; text-transform: none; }
+.trade-detail-list--native dd { flex: 1 1 auto; margin: 0; color: var(--app-text); font-size: 14px; font-weight: 950; line-height: 1.25; text-align: right; text-transform: none; overflow-wrap: anywhere; }
+.trade-detail-people-row { display: grid; gap: 8px; }
+.trade-detail-person-pill { display: flex; align-items: center; justify-content: space-between; gap: 12px; min-width: 0; border: 1px solid color-mix(in srgb, var(--app-border) 86%, transparent); border-radius: 20px; background: color-mix(in srgb, var(--app-surface-soft) 78%, transparent); padding: 12px 14px; }
+.trade-detail-person-pill > .eyebrow { margin: 0; flex: 0 0 auto; }
+.trade-detail-person-pill .user-identity-link { max-width: min(68%, 260px); }
 
 @media (max-width: 560px) {
   .trade-detail-toolbar { margin-right: -8px; margin-left: -8px; padding-right: 8px; padding-left: 8px; }
diff --git a/apps/web/src/features/trade/TradeDetailClient.tsx b/apps/web/src/features/trade/TradeDetailClient.tsx
index 7600400..c36dbba 100644
--- a/apps/web/src/features/trade/TradeDetailClient.tsx
+++ b/apps/web/src/features/trade/TradeDetailClient.tsx
@@ -17,7 +17,7 @@ import { mockTrades } from '../../lib/mockData';
 import { TradeImageGrid } from './TradeImageGrid';
 import { UserIdentityLink } from '../users/UserIdentityLink';
 import { useWebTranslation } from '../../providers/WebI18nProvider';
-import { formatDateLabel, formatRelativeExpiry, getExchangeLabel, getModeLabel, getNeedSide, getOfferSide, getStatusLabel, getTradeHeadline, getTradeHowItWorks, getTradeMode, getTradePostType, getTradeProposalCopy, type TradeI18n, type TradeSide } from './tradePresentation';
+import { formatDateLabel, formatRelativeExpiry, getExchangeLabel, getNeedSide, getOfferSide, getStatusLabel, getTradeHeadline, getTradeHowItWorks, getTradePostType, getTradeProposalCopy, type TradeI18n, type TradeSide } from './tradePresentation';
 
 function normalizeTradeResponse(value: unknown): TradeDto | null {
   if (!value || typeof value !== 'object') return null;
@@ -302,8 +302,7 @@ export function TradeDetailClient({ tradeId, initialTrade }: { tradeId: string;
   const headline = getTradeHeadline(currentTrade, i18n);
   const howItWorks = getTradeHowItWorks(currentTrade, i18n);
   const postType = getTradePostType(currentTrade);
-  const proposalCopy = getTradeProposalCopy(currentTrade, i18n);
-  const mode = getTradeMode(currentTrade);
+  const typeLabel = postType === 'open_need' ? t('trade.labels.openNeed') : postType === 'open_offer' ? t('trade.labels.openOffer') : t('trade.labels.needOffer');
 
   return (
     <article className="trade-detail-page trade-detail-page--social">
@@ -357,16 +356,42 @@ export function TradeDetailClient({ tradeId, initialTrade }: { tradeId: string;
           </div>
           {actionLoading ? <span className="semantic-badge instruction">{t('trade.detail.updated')}</span> : null}
         </div>
-        <p className="trade-how-it-works">{howItWorks}</p>
-        <dl className="trade-detail-list trade-detail-list--chips">
+        <p className="trade-next-step-copy">{howItWorks}</p>
+        <dl className="trade-detail-list trade-detail-list--native">
+          <div><dt>{t('trade.labels.type')}</dt><dd>{typeLabel}</dd></div>
           <div><dt>{t('trade.labels.status')}</dt><dd>{getStatusLabel(currentTrade.status, i18n)}</dd></div>
+          <div><dt>{t('trade.labels.expiry')}</dt><dd>{formatRelativeExpiry(currentTrade.expiresAt, i18n)}</dd></div>
           <div><dt>{t('trade.labels.exchange')}</dt><dd>{exchange}</dd></div>
-          <div><dt>{t('trade.labels.responseNeeded')}</dt><dd>{proposalCopy.responseNeeded}</dd></div>
-          <div><dt>{t('trade.labels.mode')}</dt><dd>{getModeLabel(mode, i18n) ?? t('trade.labels.unavailable')}</dd></div>
           <div><dt>{t('trade.labels.created')}</dt><dd>{formatDateLabel(currentTrade.createdAt, i18n)}</dd></div>
-          <div><dt>{t('trade.labels.expires')}</dt><dd>{formatRelativeExpiry(currentTrade.expiresAt, i18n)}</dd></div>
         </dl>
 
+        <div className="trade-detail-people-row">
+          <div className="trade-detail-person-pill">
+            <span className="eyebrow">{t('trade.labels.owner')}</span>
+            <UserIdentityLink
+              user={currentTrade.owner}
+              userId={currentTrade.ownerId}
+              variant="chip"
+              avatarSize="sm"
+              statusText={t('trade.labels.creator')}
+              showHandle={false}
+            />
+          </div>
+          {currentTrade.provider ? (
+            <div className="trade-detail-person-pill">
+              <span className="eyebrow">{t('trade.labels.provider')}</span>
+              <UserIdentityLink
+                user={currentTrade.provider}
+                userId={currentTrade.providerId}
+                variant="chip"
+                avatarSize="sm"
+                statusText={t('trade.labels.acceptedTrader')}
+                showHandle={false}
+              />
+            </div>
+          ) : null}
+        </div>
+
         <div className="trade-next-step-panel">
           <div>
             <p className="eyebrow">{t('trade.labels.confirmation')}</p>
