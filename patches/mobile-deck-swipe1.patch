diff --git a/apps/mobile/src/features/plans/PlansScreens.tsx b/apps/mobile/src/features/plans/PlansScreens.tsx
index 663b020..9346769 100644
--- a/apps/mobile/src/features/plans/PlansScreens.tsx
+++ b/apps/mobile/src/features/plans/PlansScreens.tsx
@@ -1210,7 +1210,9 @@ function PlanList({ scope, navigation, filters = [], searchQuery = '' }: { scope
       showsVerticalScrollIndicator={false}
       refreshControl={<RefreshControl refreshing={refreshing} onRefresh={() => { void load({ refresh: true }); }} />}
       ListHeaderComponent={listHeader}
-      removeClippedSubviews={Platform.OS === 'android'}
+      // Deck cards intentionally travel diagonally left during swipe;
+      // clipped FlatList cells create the visible "invisible wall" on Android.
+      removeClippedSubviews={false}
       initialNumToRender={3}
       maxToRenderPerBatch={3}
       windowSize={5}
diff --git a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
index 37d5602..e4df0ad 100644
--- a/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
+++ b/apps/mobile/src/features/plans/components/PlanSquareDeck.tsx
@@ -249,6 +249,9 @@ const styles = StyleSheet.create({
   container: {
     alignItems: 'center',
     justifyContent: 'center',
+    overflow: 'visible',
+    zIndex: 2,
+    elevation: 2,
   },
   card: {
     flex: 1,
diff --git a/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx b/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
index 65e3154..fc6c3d1 100644
--- a/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
+++ b/apps/mobile/src/features/trade/TradeDeckFeedScreen.tsx
@@ -374,7 +374,9 @@ export function TradeDeckFeedScreen() {
             refreshControl={<RefreshControl refreshing={loading} onRefresh={refreshDiscoveryOrder} />}
             ListHeaderComponent={listHeader}
             ListEmptyComponent={!error ? <EmptyTradesState loading={loading} hasTrades={hasTrades} hasFilters={hasFilters} onCreate={createTrade} onRefresh={refreshDiscoveryOrder} onClear={clearFilters} /> : null}
-            removeClippedSubviews={Platform.OS === 'android'}
+            // Deck cards intentionally travel diagonally left during swipe;
+            // clipped FlatList cells create the visible "invisible wall" on Android.
+            removeClippedSubviews={false}
             initialNumToRender={3}
             maxToRenderPerBatch={3}
             windowSize={5}
diff --git a/apps/mobile/src/features/trade/components/TradeSquareDeck.tsx b/apps/mobile/src/features/trade/components/TradeSquareDeck.tsx
index cd62b9a..a69f314 100644
--- a/apps/mobile/src/features/trade/components/TradeSquareDeck.tsx
+++ b/apps/mobile/src/features/trade/components/TradeSquareDeck.tsx
@@ -38,5 +38,8 @@ const styles = StyleSheet.create({
   container: {
     alignItems: 'center',
     justifyContent: 'center',
+    overflow: 'visible',
+    zIndex: 2,
+    elevation: 2,
   },
 });
diff --git a/apps/mobile/src/features/trade/deck/components/ContinuousSquareStackDeck.tsx b/apps/mobile/src/features/trade/deck/components/ContinuousSquareStackDeck.tsx
index 971aa0e..da5f7f7 100644
--- a/apps/mobile/src/features/trade/deck/components/ContinuousSquareStackDeck.tsx
+++ b/apps/mobile/src/features/trade/deck/components/ContinuousSquareStackDeck.tsx
@@ -205,13 +205,18 @@ const styles = StyleSheet.create({
     position: 'relative',
     alignItems: 'center',
     justifyContent: 'center',
+    overflow: 'visible',
+    zIndex: 2,
+    elevation: 2,
   },
   layer: {
     position: 'absolute',
     alignItems: 'center',
     justifyContent: 'center',
+    overflow: 'visible',
   },
   shadowLayer: {
+    overflow: 'visible',
     shadowColor: '#000000',
     shadowOffset: { width: 0, height: 10 },
   },
