diff --git a/apps/mobile/src/features/trade/CreateNeedFullScreen.tsx b/apps/mobile/src/features/trade/CreateNeedFullScreen.tsx
index a75b039..a6864df 100644
--- a/apps/mobile/src/features/trade/CreateNeedFullScreen.tsx
+++ b/apps/mobile/src/features/trade/CreateNeedFullScreen.tsx
@@ -99,12 +99,13 @@ export function CreateNeedFullScreen({ route, navigation }: Props) {
   useEffect(() => {
     const initialTemplateKey = route.params?.initialTemplateKey;
     if (!initialTemplateKey || starterPrefillApplied) return;
+    const starterTemplateKey = initialTemplateKey;
     let mounted = true;
     async function loadStarterTemplate() {
       try {
         const templatesResponse = await api.inventoryTemplates.list({ sourceType: 'hellowhen', language, take: 100 });
         if (!mounted) return;
-        const candidates = getLocalizedTemplateKeyCandidates(initialTemplateKey);
+        const candidates = getLocalizedTemplateKeyCandidates(starterTemplateKey);
         const template = normalizeTemplateResponse(templatesResponse).find((item) => item.kind === 'need' && candidates.includes(item.key));
         if (!template) throw new Error('starter_template_missing');
         setTitle(template.title);
diff --git a/apps/mobile/src/features/trade/CreateOfferFullScreen.tsx b/apps/mobile/src/features/trade/CreateOfferFullScreen.tsx
index 6b2fd96..d400b4b 100644
--- a/apps/mobile/src/features/trade/CreateOfferFullScreen.tsx
+++ b/apps/mobile/src/features/trade/CreateOfferFullScreen.tsx
@@ -99,12 +99,13 @@ export function CreateOfferFullScreen({ route, navigation }: Props) {
   useEffect(() => {
     const initialTemplateKey = route.params?.initialTemplateKey;
     if (!initialTemplateKey || starterPrefillApplied) return;
+    const starterTemplateKey = initialTemplateKey;
     let mounted = true;
     async function loadStarterTemplate() {
       try {
         const templatesResponse = await api.inventoryTemplates.list({ sourceType: 'hellowhen', language, take: 100 });
         if (!mounted) return;
-        const candidates = getLocalizedTemplateKeyCandidates(initialTemplateKey);
+        const candidates = getLocalizedTemplateKeyCandidates(starterTemplateKey);
         const template = normalizeTemplateResponse(templatesResponse).find((item) => item.kind === 'offer' && candidates.includes(item.key));
         if (!template) throw new Error('starter_template_missing');
         setTitle(template.title);
diff --git a/apps/mobile/src/features/trade/CreateTradeScreen.tsx b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
index 7c9e4de..f69f832 100644
--- a/apps/mobile/src/features/trade/CreateTradeScreen.tsx
+++ b/apps/mobile/src/features/trade/CreateTradeScreen.tsx
@@ -180,7 +180,7 @@ function buildPreviewTrade({ postType, needSelection, offerSelection, need, offe
 
 export function CreateTradeScreen({ route, navigation }: Props) {
   const theme = useThemeTokens();
-  const { t } = useTranslation();
+  const { t, language } = useTranslation();
   const auth = useAuth();
   const [activeStepId, setActiveStepId] = useState<TradeWizardStepId>('type');
   const [postType, setPostType] = useState<TradePostType | null>(route.params?.initialPostType ?? null);
diff --git a/apps/mobile/src/features/trade/TradeIdeaDetailScreen.tsx b/apps/mobile/src/features/trade/TradeIdeaDetailScreen.tsx
index f721692..17628a0 100644
--- a/apps/mobile/src/features/trade/TradeIdeaDetailScreen.tsx
+++ b/apps/mobile/src/features/trade/TradeIdeaDetailScreen.tsx
@@ -244,7 +244,7 @@ const styles = StyleSheet.create({
   notFoundButton: { minHeight: 50, borderRadius: 18, borderWidth: 1, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 12 },
   heroCard: { borderWidth: 1, borderRadius: 28, padding: 18, gap: 12 },
   heroTitle: { fontSize: 31, lineHeight: 32, fontWeight: '900', letterSpacing: -1.25 },
-  heroBody: { fontSize: 14, lineHeight: 21, fontWeight: '750' },
+  heroBody: { fontSize: 14, lineHeight: 21, fontWeight: '700' },
   sideCard: { borderWidth: 1, borderRadius: 26, padding: 16, gap: 12 },
   sideHeader: { flexDirection: 'row', alignItems: 'flex-start', justifyContent: 'space-between', gap: 12 },
   sideCopy: { flex: 1, minWidth: 0, gap: 4 },
@@ -266,7 +266,7 @@ const styles = StyleSheet.create({
   expiryHeader: { flexDirection: 'row', alignItems: 'flex-start', justifyContent: 'space-between', gap: 12 },
   expiryCopy: { flex: 1, minWidth: 0, gap: 4 },
   expiryTitle: { fontSize: 21, lineHeight: 24, fontWeight: '900', letterSpacing: -0.6 },
-  expiryBody: { fontSize: 13, lineHeight: 19, fontWeight: '750' },
+  expiryBody: { fontSize: 13, lineHeight: 19, fontWeight: '700' },
   expiryGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
   expiryOption: { minWidth: '30%', flexGrow: 1, borderWidth: 1, borderRadius: 16, paddingHorizontal: 12, paddingVertical: 10, gap: 3 },
   expiryOptionText: { fontSize: 12, lineHeight: 15, fontWeight: '900' },
@@ -277,7 +277,7 @@ const styles = StyleSheet.create({
   moreIdeaButton: { borderWidth: 1, borderRadius: 16, padding: 12, gap: 7 },
   moreIdeaCopy: { gap: 4 },
   moreIdeaPack: { fontSize: 10, lineHeight: 12, fontWeight: '900', letterSpacing: 0.55, textTransform: 'uppercase' },
-  moreIdeaTitle: { fontSize: 13, lineHeight: 18, fontWeight: '850' },
+  moreIdeaTitle: { fontSize: 13, lineHeight: 18, fontWeight: '800' },
   moreIdeaAction: { fontSize: 11, lineHeight: 14, fontWeight: '900' },
   actionBar: { position: 'absolute', left: -18, right: -18, bottom: 0, flexDirection: 'row', flexWrap: 'wrap', gap: 10, borderTopWidth: StyleSheet.hairlineWidth, paddingHorizontal: 18, paddingTop: 12, paddingBottom: 16 },
   tertiaryButton: { width: '100%', minHeight: 42, borderRadius: 16, borderWidth: 1, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 12 },
diff --git a/apps/web/src/features/inventory/InventoryCreateWizardClient.tsx b/apps/web/src/features/inventory/InventoryCreateWizardClient.tsx
index fff9ea7..490424f 100644
--- a/apps/web/src/features/inventory/InventoryCreateWizardClient.tsx
+++ b/apps/web/src/features/inventory/InventoryCreateWizardClient.tsx
@@ -356,12 +356,13 @@ export function InventoryCreateWizardClient({ kind, cancelHref, afterCreateRedir
 
   useEffect(() => {
     if (!initialTemplateKey || starterPrefillApplied) return;
+    const starterTemplateKey = initialTemplateKey;
     let mounted = true;
     async function loadStarterTemplate() {
       try {
         const templatesResponse = await api.inventoryTemplates.list({ sourceType: 'hellowhen', language, take: 100 });
         if (!mounted) return;
-        const candidates = getLocalizedTemplateKeyCandidates(initialTemplateKey);
+        const candidates = getLocalizedTemplateKeyCandidates(starterTemplateKey);
         const template = normalizeTemplateResponse(templatesResponse).find((item) => item.kind === kind && candidates.includes(item.key));
         if (!template) throw new Error('starter_template_missing');
         const nextValues = templateToInventoryFormValues(template, kind, language);
