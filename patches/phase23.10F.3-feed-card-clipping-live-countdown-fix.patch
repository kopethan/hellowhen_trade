--- a/apps/web/src/features/trade/TradeStackDeck.tsx	2026-05-11 19:39:46.246920072 +0000
+++ b/apps/web/src/features/trade/TradeStackDeck.tsx	2026-05-11 19:50:08.809013127 +0000
@@ -1,6 +1,6 @@
 'use client';
 
-import { useMemo, useState } from 'react';
+import { useEffect, useMemo, useState } from 'react';
 import { useRouter } from 'next/navigation';
 import type { TradeDto } from '@hellowhen/contracts';
 import { WebIcon } from '../../components/WebIcon';
@@ -45,13 +45,15 @@
     .join(' · ');
 }
 
-function formatExpiryCountdown(value?: string | null) {
+function formatExpiryCountdown(value?: string | null, now = Date.now()) {
   if (!value) return null;
   const expiresMs = new Date(value).getTime();
   if (!Number.isFinite(expiresMs)) return null;
 
-  const secondsLeft = Math.max(0, Math.floor((expiresMs - Date.now()) / 1000));
-  if (secondsLeft <= 0) return null;
+  const secondsLeft = Math.floor((expiresMs - now) / 1000);
+  if (secondsLeft <= 0) {
+    return { value: 'EXPIRED', tone: 'expired' as const };
+  }
 
   const days = Math.floor(secondsLeft / 86400);
   const hours = Math.floor((secondsLeft % 86400) / 3600);
@@ -65,6 +67,51 @@
   };
 }
 
+function TradeCountdown({ expiresAt }: { expiresAt?: string | null }) {
+  const [now, setNow] = useState(() => Date.now());
+
+  useEffect(() => {
+    if (!expiresAt) return undefined;
+
+    const interval = window.setInterval(() => {
+      setNow(Date.now());
+    }, 1000);
+
+    return () => window.clearInterval(interval);
+  }, [expiresAt]);
+
+  if (!expiresAt) {
+    return (
+      <span className="trade-stack-card__mobile-expiry is-none" aria-label="No expiry date set">
+        NO EXPIRY
+      </span>
+    );
+  }
+
+  const countdown = formatExpiryCountdown(expiresAt, now);
+  if (!countdown) {
+    return (
+      <span className="trade-stack-card__mobile-expiry is-none" aria-label="No expiry date set">
+        NO EXPIRY
+      </span>
+    );
+  }
+
+  if (countdown.tone === 'expired') {
+    return (
+      <span className="trade-stack-card__mobile-expiry is-expired" aria-label="Trade expired">
+        EXPIRED
+      </span>
+    );
+  }
+
+  return (
+    <span className={`trade-stack-card__mobile-countdown is-${countdown.tone}`} aria-label={`Expires in ${countdown.value}`}>
+      <strong>{countdown.value}</strong>
+    </span>
+  );
+}
+
 function compactJoin(values: Array<string | null | undefined>) {
   return values.filter((value): value is string => Boolean(value && value.trim())).join(' · ');
 }
@@ -115,8 +162,7 @@
 
   const needCardMeta = compactCardMeta(need.metadata, need.description);
   const offerCardMeta = compactCardMeta(offer.metadata, offer.description);
-  const countdown = formatExpiryCountdown(trade.expiresAt);
-  const showTradeFooter = Boolean(countdown || exchange !== 'Need + Offer exchange');
+  const showTradeFooter = true;
 
   const summaryContent = postType === 'need_offer' ? (
     <div className="trade-stack-card trade-stack-card--summary trade-stack-card--mobile-parity">
@@ -146,11 +192,7 @@
       {showTradeFooter ? (
         <div className="trade-stack-card__mobile-footer">
           {exchange !== 'Need + Offer exchange' ? <span className="trade-stack-card__mobile-money">{exchange}</span> : null}
-          {countdown ? (
-            <span className={`trade-stack-card__mobile-countdown is-${countdown.tone}`} aria-label={`Expires in ${countdown.value}`}>
-              <strong>{countdown.value}</strong>
-            </span>
-          ) : null}
+          <TradeCountdown expiresAt={trade.expiresAt} />
         </div>
       ) : null}
     </div>
--- a/apps/web/src/app/globals.css	2026-05-11 19:43:38.860249608 +0000
+++ b/apps/web/src/app/globals.css	2026-05-11 19:49:42.578629783 +0000
@@ -763,13 +763,14 @@
 
 .trade-poster-card h2 {
   display: -webkit-box;
-  margin: 0;
+  margin: 0 0 -0.06em;
+  padding-bottom: 0.1em;
   overflow: hidden;
   color: #fff;
-  font-size: clamp(22px, 6.2vw, 29px);
+  font-size: clamp(20px, 5.6vw, 26px);
   font-weight: 950;
-  letter-spacing: -0.058em;
-  line-height: 1.05;
+  letter-spacing: -0.052em;
+  line-height: 1.16;
   text-shadow: 0 2px 18px rgba(0, 0, 0, 0.36);
   -webkit-box-orient: vertical;
   -webkit-line-clamp: 2;
@@ -778,9 +779,9 @@
 .trade-poster-card__subtitle {
   display: -webkit-box;
   overflow: hidden;
-  font-size: 12px;
+  font-size: 11px;
   font-weight: 760;
-  line-height: 1.28;
+  line-height: 1.34;
   -webkit-box-orient: vertical;
   -webkit-line-clamp: 2;
 }
@@ -1011,22 +1012,25 @@
 .trade-stack-card--mobile-parity { grid-template-rows: auto minmax(0, 1fr) auto minmax(0, 1fr) auto; gap: 0; padding: clamp(22px, 7vw, 32px); text-align: center; }
 .trade-stack-card__mobile-top { display: flex; align-items: center; justify-content: space-between; gap: 12px; color: var(--app-text-muted); font-size: 12px; font-weight: 950; letter-spacing: .12em; text-transform: uppercase; }
 .trade-stack-card__mobile-top strong { color: var(--app-text); font-size: 12px; font-weight: 950; }
-.trade-stack-card__mobile-section { display: grid; align-content: center; justify-items: center; gap: 6px; min-height: 0; overflow: hidden; padding-block: 2px; }
+.trade-stack-card__mobile-section { display: grid; align-content: center; justify-items: center; gap: 5px; min-height: 0; overflow: visible; padding-block: 5px; }
 .trade-stack-card__mobile-section p { margin: 0; font-size: 13px; font-weight: 950; letter-spacing: -0.02em; }
 .trade-stack-card__mobile-section--need p { color: color-mix(in srgb, #2563eb 76%, var(--app-text)); }
 .trade-stack-card__mobile-section--offer p { color: color-mix(in srgb, #059669 78%, var(--app-text)); }
-.trade-stack-card__mobile-section h2 { display: -webkit-box; width: 100%; max-width: 100%; margin: 0; overflow: hidden; overflow-wrap: anywhere; color: var(--app-text); font-size: clamp(19px, 5.3vw, 25px); line-height: 1.16; letter-spacing: -0.048em; text-overflow: ellipsis; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
-.trade-stack-card__mobile-section span { display: block; max-width: min(90%, 280px); overflow: hidden; color: var(--app-text-muted); font-size: 11px; font-weight: 850; line-height: 1.25; text-overflow: ellipsis; white-space: nowrap; }
+.trade-stack-card__mobile-section h2 { display: -webkit-box; width: 100%; max-width: 100%; margin: 0 0 -0.08em; padding-bottom: 0.12em; overflow: hidden; overflow-wrap: anywhere; color: var(--app-text); font-size: clamp(18px, 5vw, 23px); line-height: 1.24; letter-spacing: -0.044em; text-overflow: ellipsis; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }
+.trade-stack-card__mobile-section span { display: block; max-width: min(90%, 280px); overflow: hidden; color: var(--app-text-muted); font-size: 10.5px; font-weight: 850; line-height: 1.35; text-overflow: ellipsis; white-space: nowrap; }
 .trade-stack-card__mobile-divider { display: grid; grid-template-columns: minmax(64px, 1fr) auto minmax(64px, 1fr); align-items: center; gap: 12px; min-height: 34px; color: var(--app-text-muted); }
 .trade-stack-card__mobile-divider i { display: block; height: 2px; border-radius: 999px; background: color-mix(in srgb, var(--app-border-strong) 62%, transparent); }
 .trade-stack-card__mobile-divider > span { display: grid; place-items: center; width: 24px; height: 24px; color: color-mix(in srgb, var(--app-text-muted) 82%, var(--app-text)); }
 .trade-stack-card__mobile-footer { display: flex; align-items: flex-end; justify-content: center; gap: 10px; min-height: 22px; }
 .trade-stack-card__mobile-money { display: inline-flex; align-items: center; min-height: 28px; border: 1px solid var(--money-border); border-radius: 999px; background: var(--money-bg); color: var(--money-text); padding: 0 12px; font-size: 12px; font-weight: 950; }
-.trade-stack-card__mobile-countdown { display: inline-flex; align-items: center; justify-content: center; color: #ef4444; font-weight: 950; letter-spacing: .04em; line-height: 1.1; }
-.trade-stack-card__mobile-countdown strong { font-variant-numeric: tabular-nums; font-size: 13px; letter-spacing: .06em; }
+.trade-stack-card__mobile-countdown,
+.trade-stack-card__mobile-expiry { display: inline-flex; align-items: center; justify-content: center; font-weight: 950; letter-spacing: .04em; line-height: 1.15; }
+.trade-stack-card__mobile-countdown strong { color: #ef4444; font-variant-numeric: tabular-nums; font-size: 12.5px; letter-spacing: .06em; }
 .trade-stack-card__mobile-countdown.is-normal strong { color: #ef4444; }
 .trade-stack-card__mobile-countdown.is-soon strong { color: #dc2626; }
 .trade-stack-card__mobile-countdown.is-urgent strong { color: #b91c1c; }
+.trade-stack-card__mobile-expiry { color: var(--app-text-muted); font-size: 11px; letter-spacing: .12em; }
+.trade-stack-card__mobile-expiry.is-expired { color: #b91c1c; }
 
 /* Phase 22.4: web dark-mode surface polish */
 html[data-theme="dark"] .web-top-header,
@@ -1076,13 +1080,13 @@
 
 @media (min-width: 760px) {
   .trade-stack-card--mobile-parity { padding: 30px; }
-  .trade-stack-card__mobile-section h2 { font-size: clamp(20px, 1.55vw, 25px); }
-  .trade-stack-card__mobile-section span { font-size: 12px; }
+  .trade-stack-card__mobile-section h2 { font-size: clamp(18px, 1.42vw, 23px); }
+  .trade-stack-card__mobile-section span { font-size: 11px; }
 }
 
 @media (max-width: 420px) {
   .trade-stack-card--mobile-parity { padding: 22px; }
-  .trade-stack-card__mobile-section h2 { font-size: 20px; }
+  .trade-stack-card__mobile-section h2 { font-size: 18px; }
   .trade-stack-card__mobile-countdown strong { font-size: 12px; }
 }
 
