diff --git a/apps/web/src/app/globals.css b/apps/web/src/app/globals.css
index 8b2f1db..2c58717 100644
--- a/apps/web/src/app/globals.css
+++ b/apps/web/src/app/globals.css
@@ -4187,19 +4187,12 @@ html[data-theme="dark"] .onboarding-guide-primary {
   }
 }
 
-/* WIZARD21: web source picker modal / mobile sheet */
-.trade-side-choice-card--button {
-  appearance: none;
-  width: 100%;
-  border: 0;
-  background: transparent;
-  color: inherit;
-  padding: 0;
-  text-align: left;
-  cursor: pointer;
+/* WIZARD26: source picker route links */
+.trade-side-choice-card--link {
+  display: block;
+  text-decoration: none;
 }
 .trade-side-source-trigger {
-  appearance: none;
   display: grid;
   grid-template-columns: 46px minmax(0, 1fr);
   gap: 10px;
@@ -4211,7 +4204,7 @@ html[data-theme="dark"] .onboarding-guide-primary {
   color: var(--app-text);
   padding: 12px;
   text-align: left;
-  cursor: pointer;
+  text-decoration: none;
 }
 .trade-side-source-trigger > span {
   grid-row: span 2;
@@ -4225,77 +4218,5 @@ html[data-theme="dark"] .onboarding-guide-primary {
 }
 .trade-side-source-trigger strong { align-self: end; }
 .trade-side-source-trigger small { align-self: start; color: var(--app-text-muted); font-size: 12px; font-weight: 800; line-height: 1.35; }
-.trade-source-modal {
-  position: fixed;
-  inset: 0;
-  z-index: 80;
-  display: grid;
-  place-items: center;
-  padding: 18px;
-  background: color-mix(in srgb, #000 42%, transparent);
-  backdrop-filter: blur(10px);
-}
-.trade-source-modal__sheet {
-  display: grid;
-  gap: 14px;
-  width: min(100%, 430px);
-  max-height: min(720px, calc(100dvh - 36px));
-  overflow: auto;
-  border: 1px solid var(--app-border);
-  border-radius: 28px;
-  background: var(--app-surface);
-  box-shadow: 0 28px 80px color-mix(in srgb, #000 28%, transparent);
-  padding: 16px;
-}
-.trade-source-modal__handle { display: none; }
-.trade-source-modal__header {
-  display: grid;
-  grid-template-columns: minmax(0, 1fr) 36px;
-  align-items: start;
-  gap: 12px;
-}
-.trade-source-modal__header h3,
-.trade-source-modal__header p { margin: 0; }
-.trade-source-modal__header p:not(.eyebrow) { color: var(--app-text-muted); font-size: 13px; font-weight: 760; line-height: 1.45; }
-.trade-source-modal__close {
-  appearance: none;
-  display: grid;
-  place-items: center;
-  width: 36px;
-  height: 36px;
-  border: 1px solid var(--app-border);
-  border-radius: 999px;
-  background: var(--app-surface-soft);
-  color: var(--app-text);
-  font: inherit;
-  font-size: 22px;
-  line-height: 1;
-  cursor: pointer;
-}
-.trade-side-source-grid--modal { gap: 10px; }
-.trade-side-choice-card--button:focus-visible,
-.trade-side-source-trigger:focus-visible,
-.trade-source-modal__close:focus-visible { outline: 3px solid color-mix(in srgb, var(--proposal-border) 72%, transparent); outline-offset: 3px; }
-@media (max-width: 640px) {
-  .trade-source-modal {
-    place-items: end stretch;
-    padding: 0;
-  }
-  .trade-source-modal__sheet {
-    width: 100%;
-    max-height: min(78dvh, 620px);
-    border-right: 0;
-    border-bottom: 0;
-    border-left: 0;
-    border-radius: 28px 28px 0 0;
-    padding: 10px 14px calc(14px + env(safe-area-inset-bottom));
-  }
-  .trade-source-modal__handle {
-    display: block;
-    justify-self: center;
-    width: 44px;
-    height: 5px;
-    border-radius: 999px;
-    background: var(--app-border-strong);
-  }
-}
+.trade-side-choice-card--link:focus-visible,
+.trade-side-source-trigger:focus-visible { outline: 3px solid color-mix(in srgb, var(--proposal-border) 72%, transparent); outline-offset: 3px; }
diff --git a/apps/web/src/app/trades/create/choose-need-source/page.tsx b/apps/web/src/app/trades/create/choose-need-source/page.tsx
new file mode 100644
index 0000000..bcdbd5c
--- /dev/null
+++ b/apps/web/src/app/trades/create/choose-need-source/page.tsx
@@ -0,0 +1,23 @@
+import { TradeSideChoosePage } from '../../../../features/trade/TradeSideChoosePage';
+
+type ChooseNeedSourcePageProps = {
+  searchParams?: Promise<{ needId?: string; offerId?: string; postType?: string; returnTo?: string }>;
+};
+
+export default async function ChooseNeedSourcePage({ searchParams }: ChooseNeedSourcePageProps) {
+  const resolvedSearchParams = searchParams ? await searchParams : {};
+  const postType = resolvedSearchParams.postType === 'need_offer' || resolvedSearchParams.postType === 'open_need' || resolvedSearchParams.postType === 'open_offer'
+    ? resolvedSearchParams.postType
+    : 'need_offer';
+
+  return (
+    <TradeSideChoosePage
+      side="need"
+      currentNeedId={resolvedSearchParams.needId}
+      currentOfferId={resolvedSearchParams.offerId}
+      initialSource=""
+      postType={postType}
+      returnTo={resolvedSearchParams.returnTo === 'full' ? 'full' : ''}
+    />
+  );
+}
diff --git a/apps/web/src/app/trades/create/choose-offer-source/page.tsx b/apps/web/src/app/trades/create/choose-offer-source/page.tsx
new file mode 100644
index 0000000..3094485
--- /dev/null
+++ b/apps/web/src/app/trades/create/choose-offer-source/page.tsx
@@ -0,0 +1,23 @@
+import { TradeSideChoosePage } from '../../../../features/trade/TradeSideChoosePage';
+
+type ChooseOfferSourcePageProps = {
+  searchParams?: Promise<{ needId?: string; offerId?: string; postType?: string; returnTo?: string }>;
+};
+
+export default async function ChooseOfferSourcePage({ searchParams }: ChooseOfferSourcePageProps) {
+  const resolvedSearchParams = searchParams ? await searchParams : {};
+  const postType = resolvedSearchParams.postType === 'need_offer' || resolvedSearchParams.postType === 'open_need' || resolvedSearchParams.postType === 'open_offer'
+    ? resolvedSearchParams.postType
+    : 'need_offer';
+
+  return (
+    <TradeSideChoosePage
+      side="offer"
+      currentNeedId={resolvedSearchParams.needId}
+      currentOfferId={resolvedSearchParams.offerId}
+      initialSource=""
+      postType={postType}
+      returnTo={resolvedSearchParams.returnTo === 'full' ? 'full' : ''}
+    />
+  );
+}
diff --git a/apps/web/src/features/trade/TradeCreateClient.tsx b/apps/web/src/features/trade/TradeCreateClient.tsx
index c0b3abe..3ea247a 100644
--- a/apps/web/src/features/trade/TradeCreateClient.tsx
+++ b/apps/web/src/features/trade/TradeCreateClient.tsx
@@ -164,6 +164,15 @@ function createSideChooseHref(side: 'need' | 'offer', values: TradeCreateValues)
   return `/trades/create/choose-${side}${query ? `?${query}` : ''}`;
 }
 
+function createSideSourceHref(side: 'need' | 'offer', values: TradeCreateValues) {
+  const params = new URLSearchParams();
+  if (values.postType) params.set('postType', values.postType);
+  if (values.needId && values.postType !== 'open_offer') params.set('needId', values.needId);
+  if (values.offerId && values.postType !== 'open_need') params.set('offerId', values.offerId);
+  const query = params.toString();
+  return `/trades/create/choose-${side}-source${query ? `?${query}` : ''}`;
+}
+
 function createSideNewHref(side: 'need' | 'offer', values: TradeCreateValues) {
   const params = new URLSearchParams();
   if (values.postType) params.set('postType', values.postType);
@@ -632,6 +641,8 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
 
   const chooseNeedHref = createSideChooseHref('need', values);
   const chooseOfferHref = createSideChooseHref('offer', values);
+  const needSourceHref = createSideSourceHref('need', values);
+  const offerSourceHref = createSideSourceHref('offer', values);
   const newNeedHref = createSideNewHref('need', values);
   const newOfferHref = createSideNewHref('offer', values);
   const currentCreateHref = createTradeHrefWithPostType({ postType: values.postType, needId: values.needId, offerId: values.offerId });
@@ -894,6 +905,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
                   items={selectableNeeds}
                   selectedId={values.needId}
                   chooseHref={chooseNeedHref}
+                  sourceChoiceHref={needSourceHref}
                   newHref={newNeedHref}
                   emptyTitle={t('inventory.empty.createFirstNeed')}
                   emptyBody={t('inventory.empty.needBody')}
@@ -910,6 +922,7 @@ export function TradeCreateClient({ initialNeedId = '', initialOfferId = '', ini
                   items={selectableOffers}
                   selectedId={values.offerId}
                   chooseHref={chooseOfferHref}
+                  sourceChoiceHref={offerSourceHref}
                   newHref={newOfferHref}
                   emptyTitle={t('inventory.empty.createFirstOffer')}
                   emptyBody={t('inventory.empty.offerBody')}
diff --git a/apps/web/src/features/trade/TradeSideChoosePage.tsx b/apps/web/src/features/trade/TradeSideChoosePage.tsx
index 63c49bc..fb71fa1 100644
--- a/apps/web/src/features/trade/TradeSideChoosePage.tsx
+++ b/apps/web/src/features/trade/TradeSideChoosePage.tsx
@@ -90,6 +90,17 @@ function choosePageHref(side: Side, next: { postType?: TradePostType | ''; needI
   return `/trades/create/choose-${side}${query ? `?${query}` : ''}`;
 }
 
+function chooseSourcePageHref(side: Side, next: { postType?: TradePostType | ''; needId?: string; offerId?: string }, context?: { mode?: 'createTrade' | 'proposal'; tradeId?: string; returnTo?: 'full' | '' }) {
+  if (context?.mode === 'proposal') return choosePageHref(side, next, undefined, context);
+  const params = new URLSearchParams();
+  if (next.postType) params.set('postType', next.postType);
+  if (next.needId && next.postType !== 'open_offer') params.set('needId', next.needId);
+  if (next.offerId && next.postType !== 'open_need') params.set('offerId', next.offerId);
+  if (context?.returnTo === 'full') params.set('returnTo', 'full');
+  const query = params.toString();
+  return `/trades/create/choose-${side}-source${query ? `?${query}` : ''}`;
+}
+
 function newItemHref(side: Side, next: { postType?: TradePostType | ''; needId?: string; offerId?: string }, context?: { mode?: 'createTrade' | 'proposal'; tradeId?: string; returnTo?: 'full' | '' }) {
   const params = new URLSearchParams();
   if (context?.mode === 'proposal' && context.tradeId) {
@@ -254,7 +265,7 @@ export function TradeSideChoosePage({ side, currentNeedId = '', currentOfferId =
   const selectedId = selectedIdForSide(side, currentNeedId, currentOfferId) ?? '';
   const context = { mode, tradeId, returnTo };
   const backHref = mode === 'proposal' && tradeId ? tradeProposalHref(tradeId, { needId: currentNeedId, offerId: currentOfferId }) : createTradeHref({ postType, needId: currentNeedId, offerId: currentOfferId }, returnTo === 'full');
-  const sourceChoiceHref = choosePageHref(side, { postType, needId: currentNeedId, offerId: currentOfferId }, undefined, context);
+  const sourceChoiceHref = chooseSourcePageHref(side, { postType, needId: currentNeedId, offerId: currentOfferId }, context);
   const createHref = newItemHref(side, { postType, needId: currentNeedId, offerId: currentOfferId }, context);
 
   useEffect(() => {
@@ -398,6 +409,11 @@ export function TradeSideChoosePage({ side, currentNeedId = '', currentOfferId =
               <strong>{t('trade.sidePicker.useStarter')}</strong>
               <small>{t('trade.sidePicker.useStarterBody')}</small>
             </Link>
+            <Link href={createHref} className="trade-side-source-card trade-side-source-card--dashed">
+              <span><WebIcon name="add" size={22} decorative /></span>
+              <strong>{t('trade.sidePicker.createNew', { item: label })}</strong>
+              <small>{t('trade.sidePicker.createNewBody')}</small>
+            </Link>
           </div>
         </section>
       ) : null}
diff --git a/apps/web/src/features/trade/TradeSidePicker.tsx b/apps/web/src/features/trade/TradeSidePicker.tsx
index adba342..cb50657 100644
--- a/apps/web/src/features/trade/TradeSidePicker.tsx
+++ b/apps/web/src/features/trade/TradeSidePicker.tsx
@@ -1,7 +1,6 @@
 'use client';
 
 import Link from 'next/link';
-import { useEffect, useId, useState } from 'react';
 import type { NeedDto, OfferDto } from '@hellowhen/contracts';
 import { WebIcon } from '../../components/WebIcon';
 import { getInventoryMetadata, mediaSrc } from '../inventory/inventoryPresentation';
@@ -19,6 +18,7 @@ type TradeSidePickerProps = {
   items: Inventory[];
   selectedId: string;
   chooseHref: string;
+  sourceChoiceHref?: string;
   newHref: string;
   emptyTitle: string;
   emptyBody: string;
@@ -46,26 +46,6 @@ function InventoryPreview({ item, side }: { item: Inventory; side: Side }) {
   );
 }
 
-function SourceCard({ href, icon, title, body, dashed = false, onNavigate }: { href: string; icon: 'need' | 'offer' | 'trade' | 'add'; title: string; body: string; dashed?: boolean; onNavigate?: () => void }) {
-  return (
-    <Link href={href} className={`trade-side-source-card${dashed ? ' trade-side-source-card--dashed' : ''}`} onClick={onNavigate}>
-      <span><WebIcon name={icon} size={22} decorative /></span>
-      <strong>{title}</strong>
-      <small>{body}</small>
-    </Link>
-  );
-}
-
-function SourceButton({ onClick, iconLabel, title, body, warning = false }: { onClick: () => void; iconLabel: string; title: string; body: string; warning?: boolean }) {
-  return (
-    <button type="button" className={`trade-side-source-card trade-side-source-card--button${warning ? ' trade-side-source-card--warning' : ''}`} onClick={onClick}>
-      <span>{iconLabel}</span>
-      <strong>{title}</strong>
-      <small>{body}</small>
-    </button>
-  );
-}
-
 function SourceActionLink({ href, label, ariaLabel, tone = 'default' }: { href: string; label: string; ariaLabel: string; tone?: 'default' | 'starter' | 'create' }) {
   return (
     <Link href={href} aria-label={ariaLabel} className={`trade-side-inline-action trade-side-inline-action--${tone}`}>
@@ -74,11 +54,8 @@ function SourceActionLink({ href, label, ariaLabel, tone = 'default' }: { href:
   );
 }
 
-export function TradeSidePicker({ label, side, mode, onModeChange, items, selectedId, chooseHref, newHref, emptyTitle, emptyBody, moneyEnabled = false, cashPromiseEnabled = false }: TradeSidePickerProps) {
+export function TradeSidePicker({ label, side, mode, onModeChange, items, selectedId, chooseHref, sourceChoiceHref, newHref, emptyTitle, emptyBody, moneyEnabled = false, cashPromiseEnabled = false }: TradeSidePickerProps) {
   const { t } = useWebTranslation();
-  const [sourcePickerOpen, setSourcePickerOpen] = useState(false);
-  const dialogTitleId = useId();
-  const dialogBodyId = useId();
   const sideClass = side === 'need' ? 'need' : 'offer';
   const moneyText = side === 'need' ? t('trade.labels.iNeed') : t('trade.labels.iOffer');
   const savedText = side === 'need' ? t('inventory.labels.savedNeed') : t('inventory.labels.savedOffer');
@@ -87,15 +64,7 @@ export function TradeSidePicker({ label, side, mode, onModeChange, items, select
   const selected = items.find((item) => item.id === selectedId) ?? null;
   const sourceTitle = t('trade.sidePicker.chooseSourceTitle', { item: itemLabel.toLowerCase() });
   const sourceBody = t('trade.sidePicker.chooseSourceBody', { items: pluralLabel, item: itemLabel.toLowerCase() });
-
-  useEffect(() => {
-    if (!sourcePickerOpen) return undefined;
-    function onKeyDown(event: KeyboardEvent) {
-      if (event.key === 'Escape') setSourcePickerOpen(false);
-    }
-    window.addEventListener('keydown', onKeyDown);
-    return () => window.removeEventListener('keydown', onKeyDown);
-  }, [sourcePickerOpen]);
+  const sourceRouteHref = sourceChoiceHref ?? chooseHref;
 
   return (
     <section className="mobile-card trade-side-picker">
@@ -121,9 +90,9 @@ export function TradeSidePicker({ label, side, mode, onModeChange, items, select
         </div>
       ) : selected ? (
         <div className="trade-side-choice-state">
-          <button type="button" className="trade-side-choice-card trade-side-choice-card--button" aria-label={t('trade.sidePicker.changeSource')} onClick={() => setSourcePickerOpen(true)}>
+          <Link href={sourceRouteHref} className="trade-side-choice-card trade-side-choice-card--link" aria-label={t('trade.sidePicker.changeSource')}>
             <InventoryPreview item={selected} side={side} />
-          </button>
+          </Link>
           <div className="trade-side-inline-actions" role="group" aria-label={t('trade.sidePicker.changeSource')}>
             <SourceActionLink
               href={sourceHref(chooseHref, 'mine')}
@@ -147,45 +116,13 @@ export function TradeSidePicker({ label, side, mode, onModeChange, items, select
       ) : (
         <div className="trade-side-source-step trade-side-source-step--inline">
           <p className="trade-side-source-inline-copy">{items.length ? t('inventory.messages.visibleItems', { count: items.length, items: pluralLabel }) : emptyTitle || emptyBody}</p>
-          <button type="button" className="trade-side-source-trigger" onClick={() => setSourcePickerOpen(true)}>
+          <Link href={sourceRouteHref} className="trade-side-source-trigger">
             <span><WebIcon name={side === 'need' ? 'need' : 'offer'} size={20} decorative /></span>
             <strong>{sourceTitle}</strong>
             <small>{sourceBody}</small>
-          </button>
+          </Link>
         </div>
       )}
-
-      {sourcePickerOpen ? (
-        <div className="trade-source-modal" role="presentation" onMouseDown={(event) => {
-          if (event.target === event.currentTarget) setSourcePickerOpen(false);
-        }}>
-          <section
-            className="trade-source-modal__sheet"
-            role="dialog"
-            aria-modal="true"
-            aria-labelledby={dialogTitleId}
-            aria-describedby={dialogBodyId}
-          >
-            <div className="trade-source-modal__handle" aria-hidden="true" />
-            <div className="trade-source-modal__header">
-              <div>
-                <p className="eyebrow">{label}</p>
-                <h3 id={dialogTitleId}>{sourceTitle}</h3>
-                <p id={dialogBodyId}>{sourceBody}</p>
-              </div>
-              <button type="button" className="trade-source-modal__close" onClick={() => setSourcePickerOpen(false)} aria-label={t('common.actions.close')}>×</button>
-            </div>
-
-            <div className="trade-side-source-grid trade-side-source-grid--modal">
-              <SourceCard href={sourceHref(chooseHref, 'mine')} icon={side === 'need' ? 'need' : 'offer'} title={t('trade.sidePicker.useMine')} body={t('trade.sidePicker.useMineBody', { items: pluralLabel })} onNavigate={() => setSourcePickerOpen(false)} />
-              <SourceCard href={sourceHref(chooseHref, 'starter')} icon="trade" title={t('trade.sidePicker.useStarter')} body={t('trade.sidePicker.useStarterBody')} onNavigate={() => setSourcePickerOpen(false)} />
-              <SourceCard href={newHref} icon="add" title={t('trade.sidePicker.createNew', { item: itemLabel })} body={t('trade.sidePicker.createNewBody')} dashed onNavigate={() => setSourcePickerOpen(false)} />
-              {moneyEnabled ? <SourceButton iconLabel="€" title={t('account.walletMoney')} body={t('account.wallet.optionalWalletBody')} onClick={() => { onModeChange('money'); setSourcePickerOpen(false); }} /> : null}
-              {cashPromiseEnabled ? <SourceButton iconLabel="€" title={t('trade.cashPromise.title')} body={t('trade.cashPromise.notProcessed')} warning onClick={() => { onModeChange('cash_promise'); setSourcePickerOpen(false); }} /> : null}
-            </div>
-          </section>
-        </div>
-      ) : null}
     </section>
   );
 }
