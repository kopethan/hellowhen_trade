diff --git a/apps/mobile/src/components/MobileIcon.tsx b/apps/mobile/src/components/MobileIcon.tsx
index 7efb8e0..52b6c41 100644
--- a/apps/mobile/src/components/MobileIcon.tsx
+++ b/apps/mobile/src/components/MobileIcon.tsx
@@ -13,2 +13,3 @@ export type MobileIconName =
   | 'calendar'
+  | 'compass'
   | 'chevron-right'
@@ -139,1 +140,3 @@ function IconPaths({ name, color }: { name: MobileIconName; color: string }) {
+    case 'compass':
+      return <><Circle cx={12} cy={12} r={8.25} stroke={color} strokeWidth={1.7} fill="none" /><Path d="M15.75 8.25L13.35 13.35L8.25 15.75L10.65 10.65L15.75 8.25Z" strokeWidth={1.7} {...strokeProps} /><Circle cx={12} cy={12} r={1.05} fill={color} /></>;
     case 'search':
diff --git a/apps/mobile/src/features/explore/ExploreScreen.tsx b/apps/mobile/src/features/explore/ExploreScreen.tsx
index 823115b..733027e 100644
--- a/apps/mobile/src/features/explore/ExploreScreen.tsx
+++ b/apps/mobile/src/features/explore/ExploreScreen.tsx
@@ -1,3 +1,3 @@
-import React, { useMemo, useState } from 'react';
-import { Pressable, ScrollView, StyleSheet, View, useWindowDimensions } from 'react-native';
+import React, { useMemo } from 'react';
+import { FlatList, StyleSheet, View } from 'react-native';
 import { useNavigation } from '@react-navigation/native';
@@ -10,3 +10,2 @@ import { AppText } from '../../components/AppText';
 import { betaFeatures } from '../../lib/betaFeatures';
-import { MobileIcon } from '../../components/MobileIcon';
 import type { RootStackParamList } from '../../navigation/RootNavigator';
@@ -17,3 +16,3 @@ import { TradeFeedIdeaCard } from '../trade/TradeDeckFeedScreen';
 import { feedTradeIdeaKeys, feedTradeIdeas, type FeedTradeIdeaKey } from '../trade/tradeFeedIdeas';
-import { getMobileTradeDeckCardSize } from '../trade/components/tradeDeckGeometry';
+import { MOBILE_DECK_FEED_GAP } from '../trade/components/tradeDeckGeometry';
 
@@ -21,2 +20,7 @@ const tradeExploreIdeaKeys = feedTradeIdeaKeys.filter((ideaKey) => feedTradeIdea
 
+type ExploreFeedItem =
+  | { kind: 'section'; key: string; section: 'trade' | 'plan'; count: number }
+  | { kind: 'trade'; key: string; ideaKey: FeedTradeIdeaKey; index: number; total: number }
+  | { kind: 'plan'; key: string; ideaKey: (typeof starterPlanIdeaKeys)[number]; idea: StarterPlanIdea; preview: PlanDto; index: number; total: number };
+
 function planIdeaPreviewPlan(idea: StarterPlanIdea): PlanDto {
@@ -66,26 +70,34 @@ export function ExploreScreen() {
   const { t } = useTranslation();
-  const { width } = useWindowDimensions();
-  const [activeIdeaIndex, setActiveIdeaIndex] = useState(0);
-  const [activePlanIdeaIndex, setActivePlanIdeaIndex] = useState(0);
-  const cardSize = getMobileTradeDeckCardSize(width);
-  const ideaKeys = useMemo(() => tradeExploreIdeaKeys, []);
-  const activeIdeaKey = ideaKeys[activeIdeaIndex] ?? ideaKeys[0];
-  const planIdeaKeys = useMemo(() => starterPlanIdeaKeys, []);
-  const activePlanIdeaKey = planIdeaKeys[activePlanIdeaIndex] ?? planIdeaKeys[0];
-  const activePlanIdea = activePlanIdeaKey ? starterPlanIdeas[activePlanIdeaKey] : null;
-  const activePlanPreview = useMemo(() => activePlanIdea ? planIdeaPreviewPlan(activePlanIdea) : null, [activePlanIdea]);
 
-  function moveIdea(direction: -1 | 1) {
-    setActiveIdeaIndex((current) => {
-      if (!ideaKeys.length) return 0;
-      return (current + direction + ideaKeys.length) % ideaKeys.length;
-    });
-  }
+  const feedItems = useMemo<ExploreFeedItem[]>(() => {
+    const items: ExploreFeedItem[] = [
+      { kind: 'section', key: 'trade-ideas-section', section: 'trade', count: tradeExploreIdeaKeys.length },
+      ...tradeExploreIdeaKeys.map((ideaKey, index) => ({
+        kind: 'trade' as const,
+        key: `trade-idea-${ideaKey}`,
+        ideaKey,
+        index,
+        total: tradeExploreIdeaKeys.length,
+      })),
+    ];
+
+    if (betaFeatures.plansEnabled && betaFeatures.plansVisible) {
+      items.push({ kind: 'section', key: 'plan-ideas-section', section: 'plan', count: starterPlanIdeaKeys.length });
+      starterPlanIdeaKeys.forEach((ideaKey, index) => {
+        const idea = starterPlanIdeas[ideaKey];
+        if (!idea) return;
+        items.push({
+          kind: 'plan',
+          key: `plan-idea-${ideaKey}`,
+          ideaKey,
+          idea,
+          preview: planIdeaPreviewPlan(idea),
+          index,
+          total: starterPlanIdeaKeys.length,
+        });
+      });
+    }
 
-  function movePlanIdea(direction: -1 | 1) {
-    setActivePlanIdeaIndex((current) => {
-      if (!planIdeaKeys.length) return 0;
-      return (current + direction + planIdeaKeys.length) % planIdeaKeys.length;
-    });
-  }
+    return items;
+  }, []);
 
@@ -100,103 +112,50 @@ export function ExploreScreen() {
     <AppFixedHeaderScreen header={header}>
-      <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
-        <View style={styles.section}>
-          <View style={styles.sectionHeader}>
-            <AppText accessibilityRole="header" style={styles.sectionTitle}>{t('trade.feedIdeas.badge')}</AppText>
-            <AppText style={[styles.sectionCount, { color: theme.color.muted }]}>{ideaKeys.length}</AppText>
-          </View>
-
-          {activeIdeaKey ? (
-            <>
-              <View style={[styles.deckStage, { width: cardSize, height: cardSize + 20 }]}>
-                <View
-                  pointerEvents="none"
-                  style={[
-                    styles.deckBackCard,
-                    styles.deckBackCardFar,
-                    { backgroundColor: theme.color.subtleSurface, borderColor: theme.color.border },
-                  ]}
-                />
-                <View
-                  pointerEvents="none"
-                  style={[
-                    styles.deckBackCard,
-                    styles.deckBackCardNear,
-                    { backgroundColor: theme.color.surface, borderColor: theme.color.border },
-                  ]}
-                />
-                <View style={styles.deckFrontCard}>
-                  <TradeFeedIdeaCard
-                    ideaKey={activeIdeaKey}
-                    onOpenIdea={(ideaKey) => navigation.navigate('TradeIdeaDetail', { ideaId: ideaKey })}
-                  />
-                </View>
+      <FlatList
+        data={feedItems}
+        keyExtractor={(item) => item.key}
+        contentContainerStyle={styles.content}
+        showsVerticalScrollIndicator={false}
+        initialNumToRender={4}
+        maxToRenderPerBatch={4}
+        windowSize={5}
+        removeClippedSubviews={false}
+        renderItem={({ item }) => {
+          if (item.kind === 'section') {
+            const isPlanSection = item.section === 'plan';
+            return (
+              <View style={[styles.sectionHeader, isPlanSection && styles.followingSectionHeader]}>
+                <AppText accessibilityRole="header" style={styles.sectionTitle}>
+                  {isPlanSection ? t('plans.workspace.planIdeas.title') : t('trade.feedIdeas.badge')}
+                </AppText>
+                <AppText style={[styles.sectionCount, { color: theme.color.muted }]}>{item.count}</AppText>
               </View>
+            );
+          }
 
-              <View style={[styles.deckControls, { width: cardSize }]}>
-                <Pressable
-                  accessibilityRole="button"
-                  accessibilityLabel={t('common.actions.previousCard')}
-                  hitSlop={8}
-                  onPress={() => moveIdea(-1)}
-                  style={({ pressed }) => [styles.deckButton, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}
-                >
-                  <MobileIcon name="deck-back" size={18} color={theme.color.text} decorative />
-                </Pressable>
-                <AppText accessibilityLiveRegion="polite" style={[styles.deckPosition, { color: theme.color.muted }]}>{activeIdeaIndex + 1} / {ideaKeys.length}</AppText>
-                <Pressable
-                  accessibilityRole="button"
-                  accessibilityLabel={t('common.actions.nextCard')}
-                  hitSlop={8}
-                  onPress={() => moveIdea(1)}
-                  style={({ pressed }) => [styles.deckButton, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}
-                >
-                  <MobileIcon name="deck-advance" size={18} color={theme.color.text} decorative />
-                </Pressable>
+          if (item.kind === 'trade') {
+            return (
+              <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+                <TradeFeedIdeaCard
+                  ideaKey={item.ideaKey}
+                  onOpenIdea={(ideaKey) => navigation.navigate('TradeIdeaDetail', { ideaId: ideaKey })}
+                />
               </View>
-            </>
-          ) : null}
-        </View>
-
-        {betaFeatures.plansEnabled && betaFeatures.plansVisible && activePlanIdea && activePlanPreview ? (
-          <View style={[styles.section, styles.followingSection]}>
-            <View style={styles.sectionHeader}>
-              <AppText accessibilityRole="header" style={styles.sectionTitle}>{t('plans.workspace.planIdeas.title')}</AppText>
-              <AppText style={[styles.sectionCount, { color: theme.color.muted }]}>{planIdeaKeys.length}</AppText>
-            </View>
-
-            <PlanSquareDeck
-              key={activePlanIdeaKey}
-              plan={activePlanPreview}
-              index={activePlanIdeaIndex}
-              total={planIdeaKeys.length}
-              onOpen={() => navigation.navigate('PlanIdeaDetail', { ideaId: activePlanIdeaKey })}
-              topBadgeLabel={t('plans.deck.ideaBadge', { pack: activePlanIdea.pack })}
-              topBadgeTone="plan"
-              showModeBadge={false}
-            />
+            );
+          }
 
-            <View style={styles.planDeckControls}>
-              <Pressable
-                accessibilityRole="button"
-                accessibilityLabel={t('common.actions.previousCard')}
-                hitSlop={8}
-                onPress={() => movePlanIdea(-1)}
-                style={({ pressed }) => [styles.deckButton, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}
-              >
-                <MobileIcon name="deck-back" size={18} color={theme.color.text} decorative />
-              </Pressable>
-              <AppText accessibilityLiveRegion="polite" style={[styles.deckPosition, { color: theme.color.muted }]}>{activePlanIdeaIndex + 1} / {planIdeaKeys.length}</AppText>
-              <Pressable
-                accessibilityRole="button"
-                accessibilityLabel={t('common.actions.nextCard')}
-                hitSlop={8}
-                onPress={() => movePlanIdea(1)}
-                style={({ pressed }) => [styles.deckButton, { backgroundColor: theme.color.surface, borderColor: theme.color.border }, pressed && styles.pressed]}
-              >
-                <MobileIcon name="deck-advance" size={18} color={theme.color.text} decorative />
-              </Pressable>
+          return (
+            <View style={[styles.conceptDeck, item.index === 0 ? styles.firstConceptDeck : styles.nextConceptDeck]}>
+              <PlanSquareDeck
+                plan={item.preview}
+                index={item.index}
+                total={item.total}
+                onOpen={() => navigation.navigate('PlanIdeaDetail', { ideaId: item.ideaKey })}
+                topBadgeLabel={t('plans.deck.ideaBadge', { pack: item.idea.pack })}
+                topBadgeTone="plan"
+                showModeBadge={false}
+              />
             </View>
-          </View>
-        ) : null}
-      </ScrollView>
+          );
+        }}
+      />
     </AppFixedHeaderScreen>
@@ -209,17 +168,9 @@ const styles = StyleSheet.create({
   content: { paddingTop: 8, paddingBottom: 44 },
-  section: { gap: 14 },
-  followingSection: { marginTop: 36 },
   sectionHeader: { flexDirection: 'row', alignItems: 'baseline', justifyContent: 'space-between', gap: 12 },
+  followingSectionHeader: { marginTop: 36 },
   sectionTitle: { fontSize: 18, lineHeight: 23, fontWeight: '900', letterSpacing: -0.25 },
   sectionCount: { fontSize: 12, lineHeight: 16, fontWeight: '800' },
-  deckStage: { alignSelf: 'center', position: 'relative' },
-  deckBackCard: { position: 'absolute', top: 0, left: 0, width: '100%', aspectRatio: 1, borderRadius: 28, borderWidth: StyleSheet.hairlineWidth },
-  deckBackCardFar: { transform: [{ translateY: 16 }, { scale: 0.94 }], opacity: 0.62 },
-  deckBackCardNear: { transform: [{ translateY: 8 }, { scale: 0.97 }], opacity: 0.86 },
-  deckFrontCard: { position: 'absolute', top: 0, left: 0 },
-  deckControls: { alignSelf: 'center', minHeight: 48, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 4 },
-  deckButton: { width: 48, height: 48, borderRadius: 24, borderWidth: 1, alignItems: 'center', justifyContent: 'center' },
-  deckPosition: { fontSize: 12, lineHeight: 16, fontWeight: '800', fontVariant: ['tabular-nums'] },
-  planDeckControls: { alignSelf: 'center', width: '100%', maxWidth: 348, minHeight: 48, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 4 },
-  pressed: { opacity: 0.76, transform: [{ scale: 0.98 }] },
+  conceptDeck: { alignItems: 'center', justifyContent: 'center', overflow: 'visible' },
+  firstConceptDeck: { marginTop: 14 },
+  nextConceptDeck: { marginTop: MOBILE_DECK_FEED_GAP },
 });
diff --git a/packages/shared/src/appNavigation.ts b/packages/shared/src/appNavigation.ts
index 298fe1d..3bc257a 100644
--- a/packages/shared/src/appNavigation.ts
+++ b/packages/shared/src/appNavigation.ts
@@ -3,3 +3,3 @@ export const normalMobileAppNavItemIds = ['plans', 'explore', 'trade'] as const;
 export type NormalMobileAppNavItemId = typeof normalMobileAppNavItemIds[number];
-export type NormalMobileAppNavIcon = NormalAppNavIcon | 'search';
+export type NormalMobileAppNavIcon = NormalAppNavIcon | 'search' | 'compass';
 export type NormalMobileAppTabName = 'PlanTab' | 'ExploreTab' | 'TradeTab';
@@ -18,3 +18,3 @@ export const normalMobileAppNavItems = [
     routeTitleKey: 'navigation.routes.explore',
-    icon: 'search',
+    icon: 'compass',
     mobileTabName: 'ExploreTab',
diff --git a/scripts/mobile-app-review-smoke.mjs b/scripts/mobile-app-review-smoke.mjs
index 54041ef..30e614f 100644
--- a/scripts/mobile-app-review-smoke.mjs
+++ b/scripts/mobile-app-review-smoke.mjs
@@ -8,5 +8,11 @@
   assertNotContains('apps/mobile/src/components/AccountHeaderActionButton.tsx', 'if (active) setUnreadCount(0);', 'A transient unread-count refresh failure must not erase the last known Account badge.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'hitSlop={8}', 'Explore deck navigation must keep enlarged touch targets.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'accessibilityLiveRegion="polite"', 'Explore deck position changes must remain announced to assistive technology.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<FlatList', 'The submitted Explore surface must render independent concepts as a virtualized vertical feed.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'trade'", 'The submitted Explore surface must keep Trade ideas as independent concepts.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'plan'", 'The submitted Explore surface must keep Plan ideas as independent concepts.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'moveIdea(', 'The submitted Explore surface must not use global previous/next Trade idea controls.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'movePlanIdea(', 'The submitted Explore surface must not use global previous/next Plan idea controls.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'deckControls', 'The submitted Explore surface must not restore global concept navigation buttons.');
   assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AppText accessibilityRole="header" style={styles.sectionTitle}>', 'Explore discovery sections must remain semantic headings.');
+  assertContains('packages/shared/src/appNavigation.ts', "icon: 'compass',\n    mobileTabName: 'ExploreTab'", 'The submitted Explore tab must use the dedicated compass icon.');
+  assertContains('apps/mobile/src/components/MobileIcon.tsx', "case 'compass':", 'The submitted binary must keep the dedicated Explore compass glyph.');
   assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AccountHeaderActionButton', 'Explore must use the shared primary Account control.');
diff --git a/scripts/mobile-launch-smoke.mjs b/scripts/mobile-launch-smoke.mjs
index c3990ce..89ad961 100644
--- a/scripts/mobile-launch-smoke.mjs
+++ b/scripts/mobile-launch-smoke.mjs
@@ -9,5 +9,12 @@
   assertNotContains('apps/mobile/src/components/AccountHeaderActionButton.tsx', 'if (active) setUnreadCount(0);', 'A transient unread-count refresh failure must not erase the last known Account badge.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'hitSlop={8}', 'Explore deck navigation must keep enlarged touch targets.');
-  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'accessibilityLiveRegion="polite"', 'Explore deck position changes must remain announced to assistive technology.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<FlatList', 'Explore concepts must use a virtualized vertical feed instead of one global idea deck.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'trade'", 'Explore must model each Trade idea as its own feed concept.');
+  assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', "kind: 'plan'", 'Explore must model each Plan idea as its own feed concept.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'moveIdea(', 'Explore must not use global previous/next controls to switch Trade concepts.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'movePlanIdea(', 'Explore must not use global previous/next controls to switch Plan concepts.');
+  assertNotContains('apps/mobile/src/features/explore/ExploreScreen.tsx', 'deckControls', 'Explore must not reintroduce global concept navigation buttons.');
   assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AppText accessibilityRole="header" style={styles.sectionTitle}>', 'Explore discovery sections must remain semantic headings.');
+  assertContains('packages/shared/src/appNavigation.ts', "NormalMobileAppNavIcon = NormalAppNavIcon | 'search' | 'compass'", 'The mobile navigation contract must support the Explore compass icon without changing web navigation.');
+  assertContains('packages/shared/src/appNavigation.ts', "icon: 'compass',\n    mobileTabName: 'ExploreTab'", 'Explore must use the compass icon rather than the search icon.');
+  assertContains('apps/mobile/src/components/MobileIcon.tsx', "case 'compass':", 'The native icon system must provide a dedicated compass glyph for Explore.');
   assertContains('apps/mobile/src/features/explore/ExploreScreen.tsx', '<AccountHeaderActionButton', 'Explore must use the shared Account/profile header action.');
